# 🧠 The Brain: Sozialer Navigator (Project Intelligence)

> **Status**: Active Development (Kostenloser Antrags-Flow & Render Backend Stabilisierung)
> **Last Updated**: 2026-08-02 (dev-loop iteration 44 — fixed a live cold-start/fallback-divergence bug: the calculator's live-API abort timeout (15s) was half of what its own UI message promised (30s), routinely forcing a silent fallback to a client-side Wohngeld engine whose constants diverge from the audited backend)
> **Focus**: Kostenloser Antragsversand, Rendert-Backend Stabilität, Multi-KI Handoff

## 1. 🎯 Top Priority Missions
What we are working on *right now*.
- [x] **Bugfix: live-calculator API-request abort timeout (15s) was half of the 30s cold-start window promised by the app's own UI message, routinely forcing a silent fallback to a divergent client-side calculation engine (2026-08-02, dev-loop iteration 44)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. `src/components/smart-calculator.jsx`'s `handleSubmit` shows a loading message after 3s explicitly warning "Beim ersten Aufruf kann der Serverstart (Render.com Cold Start) bis zu 30 Sekunden dauern" (the live backend, `sozialer-navigator-api.onrender.com`, is on a Render tier that spins down when idle) — but the `AbortController` gating the actual `fetch()` (`signal: controller.signal`) was set to abort after only `15000`ms, half of the window the app itself just told the user to expect. Real-world impact: any user who is first to hit the calculator after a period of inactivity — precisely the scenario the app's own message anticipates — routinely got their live request aborted mid-cold-start, well before the promised 30s grace period elapsed. On abort, the `catch` block silently falls back to `evaluateAllBenefits()` (`src/logic/benefit-engine.js` → `src/logic/calculator-2026.js`), a second, independent implementation of the WoGG §19 Wohngeld formula with its own rent-cap table (`RENT_LIMITS`) and coefficients (`WOGG_COEFFS`) — confirmed via direct comparison that these do **not** match `backend/rules/wohngeld.json`'s `rent_caps`/`coefficients` (e.g. 1 person/Mietstufe 1: backend cap `359` vs. client-fallback cap `491`; backend `a` coefficient `0.063` vs. client `a` `0.040`), the audited table `backend/engine.py` uses. So a routine, non-error cold-start timeout silently served a divergent, unaudited Wohngeld estimate on the site's core value proposition — compounded by the fallback banner's only recovery action being a full `location.reload()`, discarding the user's already-filled wizard state (confirmed via grep: only a post-submit `sessionStorage` write exists per iteration 42's fix, no read-back of in-progress wizard state). Fixed with the minimal correct change: raised the abort timeout from `15000` to `35000`ms, so a real cold start now has a fair chance to complete before the code gives up and falls back — bringing enforcement in line with (slightly exceeding) the 30s already promised to users. Deliberately left unfixed as a separate, larger follow-up (flagged by both the finding agent and the reviewer): `calculator-2026.js`'s `RENT_LIMITS`/`WOGG_COEFFS` tables still materially diverge from `backend/rules/wohngeld.json`'s audited values with no shared source of truth, so *if* the fallback still triggers (rate-limiting, real outage, cold start >35s), it can still produce a different number than the live backend — reconciling two independently-built Wohngeld constant tables needs a dedicated data-verification pass against the official 2026 WoGG tables, not a single-line timeout fix, and picking the wrong one without that verification risks introducing a new, less-audited error. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the diff is exactly this one line via its own `git diff HEAD`, independently re-verified the 30s UI message text and the `signal: controller.signal` wiring by reading the file directly rather than trusting the description, checked for and ruled out any other timer/UI state that would regress from a longer wait (no competing timeout, no spinner assuming fast resolution, `clearTimeout` correctly still fires on both the success and catch paths), ran its own independent `npm install && npm run build`, confirmed no scope creep via `git diff --stat`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Reviewer's advisory note, not acted on**: 35s leaves only ~5s margin over the stated 30s worst case; if cold-start overruns beyond 35s are observed in the field, a larger buffer (e.g. 40-45s) may be worth a future follow-up.
- [x] **Hygiene: closed both low-priority follow-ups iteration 42 flagged — 2 dead files removed, 1 stale year label fixed (2026-08-02, dev-loop iteration 43)**: no other actionable open item existed besides the two long-standing deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts, out of scope), so this pass first verified and closed iteration 42's own two flagged follow-ups, then did a quick fresh scan for one more candidate. (1) `backend/rules/2025.json` — confirmed genuinely dead: `backend/engine.py` (the only rules-file loader) opens exactly 5 hardcoded filenames (`sgb2.json`/`wohngeld.json`/`kindergeld.json`/`kinderzuschlag.json`/`elterngeld.json`), no glob/dynamic loading exists anywhere in `backend/`, and a full-repo grep for "2025" in any `.py` file returned zero hits. Content-wise it structurally duplicated `sgb2.json`'s Regelbedarfsstufen and `wohngeld.json`'s WoGG rent-cap coefficients with nothing unique — deleted. (2) `src/components/result-roadmap.astro` — confirmed genuinely dead: `authority-app.jsx`'s `import ResultRoadmap from './result-roadmap'` resolves to the sibling `result-roadmap.jsx` (a React file can't import a `.astro` file as a JSX component), and a repo-wide grep found zero other references to the `.astro` file; it was an orphaned earlier Astro-native version superseded by the `.jsx` rewrite, same "confusing dead duplicate" shape as iteration 31's `getAllAuthoritiesForCity()` finding — deleted. (3) `src/content/lexikon/buergergeld.md:19` cited its Regelsatz example as "563 € für Alleinstehende in 2024" — cross-checked 563€ against `backend/rules/sgb2.json`'s `rbs_1` and `src/components/chat-widget.jsx`'s explicitly-labeled "Regelsätze 2026" copy, confirmed the figure itself is still correct for 2026, only the trailing year label was stale — fixed to "in 2026". A quick fresh scan for one more candidate bug (checked `chat.ts`, `lead-modal.astro`, `leads.ts`, `erfolg.astro`, `partner-cta.astro`) turned up nothing new and actionable this pass. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-ran the same greps rather than trusting the description, independently diffed the deleted `2025.json`'s content against `sgb2.json`/`wohngeld.json` to confirm zero unique data was lost, independently confirmed `result-roadmap.jsx` is a real, complete, actually-rendered component and that the deleted `.astro` file's only imports (`AuthoritySearch`/`PartnerCTA`) are separate untouched files with no shared dependency now missing, independently cross-checked the 563€/2026 figure, confirmed via `git diff --stat`/`git status` that only the 3 intended changes were made with no scope creep, confirmed `package-lock.json` was left clean/untouched, ran its own independent `npm run build`) before committing — the implementer did not self-approve. This closes both of iteration 42's flagged follow-ups.
- [x] **UX/functional fix: `/pdf-paket` page's advertised auto-fill/cover-page feature never fired for any real user — the `sessionStorage` key it reads was never written anywhere (2026-08-02, dev-loop iteration 42)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones — this pass was explicitly pointed at areas not yet deeply audited in 41 prior iterations (non-`api/**` pages/components, lexikon content accuracy, backend calculation branches not yet covered). `src/components/trust-pdf-generator.jsx` — the entire body of the live `/pdf-paket` route (`src/pages/pdf-paket.astro`) — reads `sessionStorage.getItem('sozialerNavigatorState')` on mount and, if present, uses it to auto-fill the PLZ/city field, show a "Daten sicher übernommen!" trust panel (Miete/Einkommen/Haushalt summary), and insert a pre-filled cover page into the generated official PDF (Brutto-Einkommen, Warmmiete, Haushaltsgröße, Berufl. Status) — explicitly advertised in the UI copy as "Wir heften diese Werte automatisch als Deckblatt an deinen Antrag, damit du sie nicht erneut ausfüllen musst." Confirmed via repo-wide grep that `sessionStorage.getItem('sozialerNavigatorState')` (2 call sites, both reads, both in this one file) was the *only* reference to that key anywhere in `src/` — nothing ever called `.setItem` for it. `src/components/results-section.astro`'s "Formlosen Antrag generieren ➔" CTA buttons (the live results screen, the main free-value-prop hand-off point, same flow iteration 31 called "the core free value proposition of the entire site") link to `/pdf-paket?type=...` via a plain `<a href>` full-page navigation — and neither `results-section.astro` nor `smart-calculator.jsx` (the wizard that already computes all the needed data into its `profileInput` object) ever persisted it before that navigation. Real-world impact: every real user completing the calculator and clicking through to generate their free Antrag-PDF got `savedState === null` unconditionally — the city/PLZ field was never auto-filled, the reassuring "your data carried over" panel never showed (silently replaced by generic fallback copy), and the promised pre-filled Behörde cover page silently never got attached, contradicting the page's own on-screen promise for 100% of real traffic through this path. Same "advertised feature is structurally dead because its data hand-off was never wired" bug shape as iteration 18's 7 dead wizard fields, but at a different hand-off point (calculator → `/pdf-paket`) not previously audited. Fixed with the minimal correct change: in `smart-calculator.jsx`'s `handleSubmit`, right after `profileInput` is fully constructed (so both the live-API-success and local-fallback result paths that follow both see it), added `sessionStorage.setItem('sozialerNavigatorState', JSON.stringify({ input: { city: selectedCity, rent: profileInput.rent, income: profileInput.income, persons: profileInput.persons, kids: profileInput.kids, status: profileInput.status } }))` wrapped in try/catch (mirrors the existing defensive try/catch already on the read side in `trust-pdf-generator.jsx`, guards against Safari-private-mode/quota-exceeded `setItem` throws). Field names/shapes were verified field-for-field against exactly what `trust-pdf-generator.jsx` reads (`savedState.input.city.plz`/`.city.stadt`/`.rent`/`.income`/`.persons`/`.kids`/`.status`), and `selectedCity` was confirmed (via `wohngeld-data.js`/`cities_2026.json`) to already carry `.plz`/`.stadt`/`.mietstufe` in the exact shape expected. `setItem` fully overwrites on every submit, so re-running the calculator with different inputs in the same tab correctly refreshes the persisted data — no stale-merge risk. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed via its own repo-wide grep that the bug was real and this was the only pre-existing reference to the key, independently re-verified every field name/shape match by reading the actual current file contents rather than trusting the description, confirmed the write's placement is unconditionally before both the live-API and local-fallback branches so both see it, confirmed no other code reads/writes this same sessionStorage key that could now conflict, confirmed the try/catch is an appropriate (not over/under-engineered) defensive choice matching the existing read-side pattern, confirmed no XSS/security concern (same-origin, user's own data, only ever consumed as PDF text-field values or plain-text JSX interpolation — never `dangerouslySetInnerHTML`), confirmed via `git diff --stat` that only this one file/16 lines were touched with no scope creep, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New lower-priority items noted by the scanning agent, not pursued this pass**: `backend/rules/2025.json` appears to be a dead/orphaned duplicate of `sgb2.json`+`wohngeld.json`'s values with no loader referencing it anywhere in `backend/` (worth a dedicated hygiene look); `src/content/lexikon/buergergeld.md:19` cites its Regelsatz example as "in 2024" even though the figure itself (563€) is still the correct current value used everywhere else — a stale year label, not a wrong number, purely cosmetic.
- [x] **Cosmetic fix: closed iteration 40's own flagged follow-up — homepage hero mockup still showed the pre-fix stale 292€ Kinderzuschlag figure (2026-08-02, dev-loop iteration 41)**: iteration 40 fixed the live backend's real Kinderzuschlag max-amount-per-child rate (250€/292€ → 259€/297€) but flagged, as a separate non-blocking cosmetic follow-up, that `src/pages/index.astro`'s static homepage hero — a purely decorative "engine.py: check_eligibility()"-styled mockup card, not wired to any real calculation, confirmed via direct inspection to be hardcoded text nodes with no `{}` expressions — still displayed the old `"292,00 €"` Kinderzuschlag sample figure alongside `"380,00 €"` Wohngeld summing to `"672,00 €"`, which could read as inconsistent to a careful visitor comparing it against the real (now-correct) 297€ figure used consistently everywhere else in the repo (`backend/rules/kinderzuschlag.json`, `src/logic/benefit-engine.js`, public FAQ copy). This iteration closed that exact follow-up: updated the mockup's Kinderzuschlag figure to `"297,00 €"` and recomputed the summed total to `"677,00 €"` (380 + 297), touching only these two hardcoded strings (plus one incidental trailing-whitespace cleanup on an adjacent blank line) in `src/pages/index.astro`. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-verified the diff via its own `git diff`, independently confirmed the arithmetic (380,00 + 297,00 = 677,00 €, no rounding issue), independently confirmed via direct file inspection that these are static hardcoded text nodes with zero JS/Astro logic computing them (not a real calculation path), independently cross-checked `backend/rules/kinderzuschlag.json` and `src/logic/benefit-engine.js` to confirm 297€ is indeed the correct, consistent current value used everywhere else in the repo and this edit removes the last remaining stale-292€ reference rather than introducing a new inconsistency, confirmed via `git diff --stat` that only `src/pages/index.astro` was touched with no scope creep and no `package-lock.json` diff left behind, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This closes iteration 40's flagged follow-up completely — no other actionable open item found this pass besides the two long-standing deliberately-parked ones below (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope).
- [x] **Data-correctness fix: live-backend Kindergeld/Kinderzuschlag amounts were stale by one legislative update cycle, understating both benefits on the primary calculation path (2026-08-02, dev-loop iteration 40)**: found via a fresh targeted scan (delegated to a research subagent, since brain.md had no actionable open item left besides the two deliberately-parked ones), specifically cross-checking `backend/rules/*.json` against the JS fallback engine and the site's own public copy for numeric inconsistencies. `backend/rules/kindergeld.json`'s `amount_per_child` was `250.00` and `backend/rules/kinderzuschlag.json`'s `max_amount_per_child` was `292.00` — both read directly into the live eligibility/amount math in `backend/engine.py` (`calculate_kindergeld` line ~283, `calculate_kinderzuschlag` line ~345, the primary `/api/v4/analyze` calculation path). But every other source of truth in the repo already agreed on higher, current 2026 values: `src/logic/benefit-engine.js` (`259 * kidsCount` for Kindergeld with an explicit "Stand 2026" comment; `297 * kidsCount` for Kinderzuschlag max), `src/logic/calculator-2026.js` (259 for Kindergeld-as-child-income), and the site's own public-facing FAQ/marketing copy on `src/pages/kindergeld/index.astro`, `src/components/results-section.astro`, `src/components/chat-widget.jsx`, and `src/data/target-groups.js` — all consistently stating "259 € pro Kind" (Kindergeld) and "bis zu 297 € pro Kind" (Kinderzuschlag). Kindergeld rose 250€→255€ (Jan 2025)→259€ (Jan 2026) and Kinderzuschlag's max rose 250€→292€ (2024)→297€ (2025, unchanged into 2026) — so the backend JSON was stuck at old rates from before both of those updates. Real-world impact: every household with children calculated via the live backend (the primary path — the local JS fallback only runs on network failure/timeout) got Kindergeld understated by 9€/child/month and, for households whose Kinderzuschlag amount is capped by the per-child maximum (the common case in the eligible income band), Kinderzuschlag understated by 5€/child/month — a live calculator result a user could directly notice contradicts the site's own FAQ text on the same page. Same "live Python backend stale/wrong, everything else already correct" bug shape as iterations 33/34/38, just not previously found in these two rules files' amount fields (iteration 38 only touched this same `kinderzuschlag.json` file's *threshold* fields, not its amount field). Fixed with the minimal correct change: updated `kindergeld.json`'s `amount_per_child` to `259.00` and `kinderzuschlag.json`'s `max_amount_per_child` to `297.00` — no `engine.py` logic changes needed, matching the pattern of prior data-only fixes in this series. Also updated the three `backend/verify_engine.py` expected values that depended on the old constants (Test 1 Kindergeld 250.0→259.0 for 1 child; Test 2 Kindergeld 500.0→518.0 for 2 children; Test 2 Kinderzuschlag 584.0→594.0, hand-verified against the test household's actual calculation path — gross income clears the couple minimum-income gate, net income falls short of parent_need, so the `"possible"` branch returns `max_kiz` directly = 2×297). Verified via `python3 -m py_compile` (clean, fresh venv since system Python lacks pydantic) + the updated `backend/verify_engine.py` assertion suite (all 12 checks pass, including the 3 updated ones, zero regression elsewhere) + `npm run build` (clean, Python-only change with no frontend impact) + independent subagent review (PASS — independently re-confirmed via its own repo-wide grep that 259€/297€ are the consistent values everywhere else and only these two backend JSON files were the outlier, independently confirmed both JSON keys are read into live (not dead) calculation code, independently hand-verified the arithmetic behind all three updated `verify_engine.py` expectations by tracing the actual branch taken in `calculate_kinderzuschlag()` for Test 2's household composition, ran its own independent venv install + `py_compile` + `verify_engine.py` (exit 0) + `npm run build`, confirmed via `git diff --stat` that only the 3 intended files/10 lines changed with no scope creep, confirmed no `package-lock.json` diff was left behind) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New open item found by the reviewer, not fixed this pass (cosmetic, non-blocking)**: `src/pages/index.astro:274` has a static homepage hero mockup (styled as an "engine.py: check_eligibility()" illustrative example, not wired to any real calculation) displaying `"292,00 €"` as a sample Kinderzuschlag figure alongside a `"380,00 €"` Wohngeld figure summing to `"672,00 €"` — this decorative example happens to still show the old stale 292€ rate. Harmless (not read by any code, purely illustrative marketing copy) but could read as inconsistent with the real 2026 figures and the fresh fix above if a careful visitor compares them; worth a follow-up cosmetic pass to bump the mockup number to 297€ (and adjust the summed total accordingly) for internal consistency, not urgent enough to bundle into this data-correctness fix.
- [x] **Data-correctness/legal fix: Kinderzuschlag minimum-income gate compared net income against a legally gross ("brutto") threshold, closing iteration 38's own flagged follow-up (2026-08-02, dev-loop iteration 39)**: iteration 38 fixed the swapped 600€/900€ single-vs-couple threshold values but flagged, as a separate, unfixed, informational open item, that `calculate_kinderzuschlag()`'s minimum-income gate (`backend/engine.py`) summed `inc.amount_net` (net) to compare against those thresholds — but §6a BKGG's Mindesteinkommensgrenze, and the site's own public-facing text (`src/pages/kindergeld/index.astro:241`, "Mindesteinkommen von 900 Euro **brutto** für Paare bzw. 600 Euro **brutto** für Alleinerziehende"), define it as a gross threshold, meaning the net-based check made the real-world bar higher than the law requires — a user could have gross income clearing the legal gate but still get wrongly blocked because their net income (necessarily lower) fell short of the same number. Fixed with the minimal correct change iteration 38 already sketched: added a second accumulator, `parent_earned_income_gross` (summed from `inc.amount_brutto`), used only for the minimum-income gate check and its reason string (now explicitly says "brutto"); left the pre-existing `parent_earned_income` (net) accumulator completely untouched for the downstream parent-need/50%-taper Bedarf calculation a few lines below, which is a legally distinct net-income-based test that must not be conflated with the gross gate — exactly the care iteration 38 called out as needed. Confirmed the fix's precondition actually holds before relying on it: `src/components/smart-calculator.jsx` already collects and sends **both** `amount_brutto` and `amount_net` as two independent, separately-required wizard fields ("Monatliches Brutto-Einkommen" / "Monatliches Netto-Einkommen"), so real gross data was already reaching the backend and simply going unused for this specific check — no frontend change was needed. Verified via `python3 -m py_compile` (clean, fresh venv since system Python lacks pydantic) + the existing `backend/verify_engine.py` assertion suite (all checks still pass, zero regression to the 3 existing household test cases) + a targeted repro script constructing a single parent with gross ≥ 600€/net < 600€ (confirmed no longer wrongly blocked at the gate) and a couple with net ≥ 900€/gross < 900€ (confirmed now correctly blocked at the gate — this is the actual real-world bug the old net-based check missed) + `npm run build` (clean, Python-only change with no frontend impact) + independent subagent review (PASS — independently re-read the diff and confirmed the gate now uses gross while the downstream taper calculation still correctly uses only net with no variable conflation, independently confirmed `IncomeSource.amount_brutto` is a real always-present non-Optional field so the sum can't silently break, independently traced the frontend to confirm gross income is real independently-collected user data and not a duplicate/faked value, ran its own independent regression suite + a freshly-written independent repro script reproducing both boundary cases, confirmed via `git diff --stat` that only this one file/10 lines changed with no scope creep) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This closes iteration 38's flagged follow-up completely — no other actionable open item found this pass besides the two long-standing deliberately-parked ones below.
- [x] **Data-correctness/legal fix: Kinderzuschlag minimum-parental-income thresholds were transposed in the live backend's rules data, silently applying each household type's threshold to the wrong household type (2026-08-02, dev-loop iteration 38)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed at `backend/rules/*.json`, since brain.md had no actionable open item left besides the two deliberately-parked ones and that area had never been audited in any of the 37 prior iterations). `backend/rules/kinderzuschlag.json` had `"min_income_single": 900.00` and `"min_income_couple": 600.00` — but under §6a BKGG, the real Mindesteinkommensgrenze is **600€ for single parents (Alleinerziehende)** and **900€ for couples**, i.e. the two values were exactly swapped. `backend/engine.py:327`'s lookup logic itself (`min_income_couple if has_couple else min_income_single`) was already correct — only the underlying data was wrong — so the live Kinderzuschlag eligibility gate (`/api/v4/analyze`, the primary calculation path called from `smart-calculator.jsx`) was actually enforcing 600€ for couples and 900€ for single parents, backwards. Real-world impact: a single parent with parental income in the 600-899€ band was wrongly told they don't meet the minimum-income requirement and pushed toward Bürgergeld instead of being evaluated for Kinderzuschlag (worth up to 292€/child/month per the same rules file) they were legally entitled to have checked; a couple with income in that same band incorrectly cleared a gate the law says should block them, and could be shown a Kinderzuschlag "possible"/"eligible" result they don't legally qualify for. Same "live Python backend wrong, JS fallback engine already correct" bug shape as the Mietstufe fix (iteration 33) and the asylum-seeker exclusion (iteration 34) — confirmed via independent corroboration already present in the repo: `src/logic/benefit-engine.js:118` (`const minGross = parentsCount === 1 ? 600 : 900;`) and the site's own public-facing text at `src/pages/kindergeld/index.astro:241` ("Mindesteinkommen von 900 Euro brutto für Paare bzw. 600 Euro brutto für Alleinerziehende") both already had the correct 600/single, 900/couple values — only the live-backend JSON was wrong, since the original scaffold commit. Fixed with the minimal correct change: swapped the two values back (`min_income_single: 600.00`, `min_income_couple: 900.00`), touching only this one JSON file — `engine.py`'s lookup code needed no change. Verified via `python3 -m py_compile` (clean) + the existing `backend/verify_engine.py` assertion suite (all checks still pass, zero regression to the 3 existing household test cases, none of which happen to sit in the 600-899€ boundary band) + a targeted repro script constructing single-parent and couple households with net parental income in that exact boundary band, confirming the gate now resolves correctly for both (single parent clears the gate at 700€; couple is correctly blocked at 700€ — both inverted from the pre-fix behavior) + `npm run build` (clean, Python-only change with no frontend impact expected or found) + independent subagent review (PASS — independently re-confirmed the lookup logic and post-fix resolution by reading the actual current file contents, independently found the same two corroborating sources (JS engine + kindergeld page text) plus its own knowledge of §6a BKGG, confirmed via `git diff --stat` that only this one file/2 lines changed, ran its own independent `py_compile` + `verify_engine.py` + a fresh independently-written repro script including a before/after sanity check by temporarily reverting to the pre-fix JSON and confirming the bug reproduces exactly as described, then restoring the fix) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New open item found this pass, not fixed (flagged by both the implementer and the independent reviewer, informational/orthogonal to the swap fix)**: `calculate_kinderzuschlag()`'s minimum-income gate (`engine.py:325`, `parent_earned_income += inc.amount_net`) compares **net** income against the threshold, but §6a BKGG's Mindesteinkommensgrenze — and the site's own text quoted above — explicitly define it as a **brutto** (gross) threshold. Reusing the net-income accumulator for this specific legal test likely makes the real-world bar higher than the law intends (a user needs more gross income than the law requires to clear a net-income-based check against a brutto-denominated threshold), a plausible additional pre-existing bug — but distinct from and independent of today's swap fix, not verified/quantified this pass, and left unfixed to keep this iteration's change minimal and cleanly reviewable. Worth a dedicated future iteration: likely fix is accumulating `amount_brutto` instead of `amount_net` for this specific gate check (the later `parent_need`/excess-income calculation a few lines down may still legitimately want net income — needs care to not conflate the two legally-distinct income concepts).
- [x] **Bugfix: WBA-reminder signup accepted already-past Bescheid end dates, silently guaranteeing the user never gets reminded (2026-08-02, dev-loop iteration 37)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. `src/components/wba-reminder-modal.astro`'s year `<select>` had hardcoded options `2024`/`2025`/`2026`/`2027` — with today being 2026-08-02, two of those four options (2024, 2025) were already fully in the past. `src/pages/api/wba-reminder.ts` only validated `month` is 1-12 and `year` is 2000-2100 — no check that the resulting Bescheid end date is actually in the future. The cron job that sends the reminder emails (`src/pages/api/cron/send-wba-reminders.ts`) matches `reminder_date_1`/`reminder_date_2` against today's date via an exact-equality `.eq()` query — so any reminder date already in the past can never equal "today" again and will never fire, silently, forever. Real-world impact: a user who mis-clicked a past year (or any past month within the current year — the month dropdown has no date-awareness either) saw the "Service aktiviert!" success message and a Supabase row got written, but they would never receive either the 6-week or 2-week reminder email — the worst failure mode for a service whose entire promise is "we'll remind you before your benefits lapse." Fixed at both layers: (1) the modal's year select is now populated dynamically at runtime via script (`currentYear` through `currentYear+3`) instead of hardcoded values, so stale past years stop appearing as the years roll forward; (2) the API now rejects (400, German error message) if the computed `endDate.getTime() <= Date.now()`, checked before the reminder dates are calculated and before the Supabase insert, closing the root cause regardless of what the dropdown offers (defense in depth — also covers picking a past month within the current year). Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the diff via its own `git diff`, verified the date math (`new Date(yearNum, monthNum, 0)` last-day-of-month logic) is untouched and correct, confirmed the future-date check is correctly placed before both the reminder-date calculation and the Supabase insert so no past-dated row can leak through, confirmed the dynamically-generated year list preserves the `required` attribute and empty-placeholder default-selection behavior, confirmed the month dropdown and existing 1-12/2000-2100 range validation are untouched, noted one edge case as expected-not-a-bug — submitting on the last day of the current month for that same month is correctly rejected since both reminder windows would already be in the past too — ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: resolved iteration 35's own flagged follow-up — audited the ~27-33 per-file `plz`/`amt_adresse` mismatches, found and fixed 6 more genuine bugs (2026-08-02, dev-loop iteration 36)**: iteration 35 found 5 copy-pasted-`plz` bugs via cross-city PLZ duplicate detection, but its second reviewer also flagged a broader, unverified pattern — dozens of entries per file where `plz` differs from the postal code embedded in that same record's own `amt_adresse` field, hypothesized as intentional (many big/mid cities genuinely have multiple real postal codes: a representative city-wide PLZ vs. the specific street address of the Sozialamt/Jobcenter office) rather than confirmed bugs, since a same-shape mismatch with no cross-city collision can't be told apart from a real error just by pattern-matching. This iteration did the "dedicated pass" iteration 35 recommended: extracted all mismatches in both files (27 in `wohngeld-data.js`, 34 in `cities_2026.json` — via a small script matching each object's `plz` against the 5-digit PLZ embedded in its own `amt_adresse`), and checked each one individually rather than assuming the whole set was safe. The overwhelming majority (Berlin, Hamburg, München, Köln, Düsseldorf, Dortmund, Essen, Hannover, Wiesbaden, etc.) confirmed genuinely intentional — these are real multi-PLZ cities, left untouched. Found 6 that were not: (1) **4 missing-leading-zero PLZ in `cities_2026.json` only** — Leipzig (`"4103"`), Dresden (`"1067"`), Chemnitz (`"9111"`), Jena (`"7743"`) were each missing their leading zero (real PLZ: `04103`/`01067`/`09111`/`07743`), confirmed via that record's own `amt_adresse` field AND the sibling `wohngeld-data.js` entry for the same city, which already had the correct zero-padded value in every case — meaning this was a `cities_2026.json`-only regression, not present in the sibling file. Since `src/utils/get-authority.ts`'s Behördensuche lookup does an *exact* string match (`c.plz === plz`), any user typing their real, correctly-zero-padded postal code for these 4 cities could not find their city at all. (2) **1 corrupted non-numeric `plz` in `cities_2026.json`** — a second, duplicate Ludwigshafen record (`slug: "ludwigshafen-am-rhein"`, distinct from the primary `slug: "ludwigshafen"` record) had `plz: " Ludwigshafen"` — literally the city name as a string, not a postal code — fixed to `"67059"` per its own `amt_adresse` (and matching the primary Ludwigshafen record's real PLZ; the two rows being a same-city duplicate, like the pre-existing Bayreuth/Pforzheim duplicates, is expected and out of scope to deduplicate here). (3) **1 cross-city PLZ error in `wohngeld-data.js` only** — Recklinghausen had `plz: "45879"` while its own `amt_adresse` said `"Rathausplatz 3, 45657 Recklinghausen"`; the equivalent `cities_2026.json` Recklinghausen entry already correctly had `"45657"`. This one didn't get caught by iteration 35's cross-city-duplicate detector because no other city's `plz` field in the dataset happened to also be exactly `"45879"` — but the review agent found independent in-repo corroboration that `45879` genuinely belongs to Gelsenkirchen, not Recklinghausen: `src/components/smart-calculator.jsx`'s own hardcoded `MAJOR_CITY_PLZ_RANGES` table has `{ stadt: "Gelsenkirchen", min: 45879, max: 45899 }`, and traced the bug to its origin in the original source file, `src/data/master_daten_2026_improved.csv.txt:85`, which already had this exact self-contradiction (`45879,Recklinghausen,...,"Rathausplatz 3, 45657 Recklinghausen"`) — a pre-existing source-data bug, not introduced by any prior dev-loop iteration. Fixed to `"45657"`. Verified via `npm run build` (clean) + JSON-validity check (`cities_2026.json` still parses, still 109 entries) + a post-fix collision re-scan (confirmed none of the 6 fixes introduced a new cross-city PLZ collision; the two same-value cases that exist post-fix, Ludwigshafen's two rows and the pre-existing Bayreuth/Pforzheim ones, are same-city and expected) + independent subagent review (PASS on all 6 — independently re-verified each fix against its own `amt_adresse` field and the sibling file by reading the actual current file contents rather than trusting the description, specifically scrutinized the highest-risk claim (Recklinghausen/Gelsenkirchen, the one relying on real-world PLZ geography rather than an in-repo duplicate) and found the independent `smart-calculator.jsx` corroboration plus the source-CSV origin described above, confirmed via `git diff --stat` that only these two files/6 lines were touched, ran its own independent `npm run build` and JSON-parse check) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This closes iteration 35's flagged open item completely — every mismatch in both files has now been individually checked, not just pattern-matched.
- [x] **Data-correctness fix: 5 cities had a `plz` (postal code) field copy-pasted from a different city's record, breaking PLZ-based city lookup (2026-08-02, dev-loop iteration 35)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. Both city datasets — `src/data/wohngeld-data.js` (95 entries, used by the homepage calculator's `smart-calculator.jsx` "PLZ ONLY" search, `c.plz.startsWith(inputStr)`) and `src/data/cities_2026.json` (109 entries, used by the Behördensuche feature via `src/utils/get-authority.ts`/`src/components/authority-search.astro`/`authority-app.jsx`, several call sites doing an *exact* `c.plz === plz` match) — had 5 records where the `plz` field was an exact copy of a *different* city's real postal code, contradicted by that same record's own `amt_adresse` (office street address) field a few lines below, which contains the city's actual correct postal code: Wolfsburg (`"38100"`, Braunschweig's real PLZ → fixed to `"38440"`), Witten (`"58313"`, Herdecke's → fixed to `"58452"`), Marburg (`"35576"`, Wetzlar's → fixed to `"35037"`), Marl (`"45657"`, Recklinghausen's → fixed to `"45768"`), Gelsenkirchen (`"48143"`, Münster's → fixed to `"45881"`). Real-world impact: users typing their real, correct postal code for Wolfsburg/Witten/Marburg/Marl/Gelsenkirchen into either the homepage calculator's city search or the Behördensuche could not find their city at all (no fallback path existed either — none of these 5 are in `smart-calculator.jsx`'s `MAJOR_CITY_PLZ_RANGES` list), while users typing the *donor* city's real PLZ (Braunschweig/Herdecke/Wetzlar/Recklinghausen/Münster) saw a bogus, factually-wrong duplicate suggestion for the unrelated victim city. Not every city needed fixing in both files — Wolfsburg's `cities_2026.json` entry and Gelsenkirchen's `wohngeld-data.js` entry were already correct, only their sibling file had the bug; all 5 were wrong in at least one of the two files. **First review pass caught a real gap before commit**: the initial fix touched Marl only in `cities_2026.json` and missed the identical bug in the equivalent `wohngeld-data.js` record (still `plz: "45657"` there) — found and fixed in a follow-up edit within the same iteration, then re-verified by a second independent review pass. Verified via `npm run build` (clean, both before and after the Marl follow-up fix) + two sequential independent subagent reviews (first PASS-with-one-gap-found — independently re-derived each city's real PLZ from its own `amt_adresse` field rather than trusting the description, confirmed the pre-fix values genuinely matched a different city's real PLZ in both files, checked for and correctly distinguished 2 unrelated legitimate same-city duplicate-PLZ pairs — Bayreuth's two offices and a duplicate Pforzheim row — from the actual bug class, confirmed via its own `git diff --stat` that only the 2 data files were touched, confirmed no new PLZ collision was introduced, ran its own build; second pass re-confirmed the Marl fix specifically, re-scanned both full files — 95 and 109 entries respectively — for any remaining cross-city `plz` duplicate and found none, confirmed the other 4 fixes were untouched/still present, ran its own clean build) before committing — the implementer did not self-approve at any point. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New open item found by the second review pass, not fixed this pass**: both files have ~27-33 entries (out of ~95-109) where `plz` differs from the postal code embedded in that same record's own `amt_adresse` field, but is *not* duplicated with another city's `plz` (e.g. Berlin's `plz: "10115"` vs. its `amt_adresse`'s `10178`) — this looks like an intentional pattern (a representative/central PLZ for the city's Mietstufe-zone vs. the specific street address of its Sozialamt/Jobcenter office, common for multi-PLZ big cities) rather than a bug, since it's the same shape as this iteration's own fix target only where the "mismatch" has no cross-city collision to prove it's wrong data rather than a deliberate choice — but it wasn't verified city-by-city this pass, since doing so exhaustively is a bigger data-audit task than a single dev-loop iteration and the two reviewers explicitly flagged it as "out of scope per the task's exact definition" rather than confirmed-safe. Worth a dedicated pass to spot-check a sample of these for correctness before assuming they're all intentional.
- [x] **Data-correctness/legal fix: live-backend calculation had zero awareness of asylum-seeker status — AsylbLG-eligible users were told they qualify for Bürgergeld/Wohngeld/Lastenzuschuss/Kinderzuschlag instead (2026-08-02, dev-loop iteration 34)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. The wizard's `status` select offers `asylum_seeker` (`src/components/smart-calculator.jsx`), and the local JS fallback engine (`src/logic/benefit-engine.js`) already correctly excludes `status === "asylum_seeker"` from `KinderzuschlagModule`/`WohngeldModule`/`LastenzuschussModule`/`BuergergeldModule` (an "JS-Engine-Audit" fix from an earlier, undated iteration) — but that fix only ever touched the local fallback engine. The live Render backend (`backend/engine.py`, the primary calculation path; the local engine only runs on network failure/timeout) had **zero** references to asylum/Asyl anywhere (confirmed via full-file grep) and no field in `backend/models.py`'s `Person` model to even carry the status. Real-world impact: under German law, asylum seekers during their asylum procedure receive AsylbLG (Asylbewerberleistungsgesetz) instead of Bürgergeld/Wohngeld/Kinderzuschlag — the systems are mutually exclusive — so any user selecting "Asylbewerber(in)" and completing the wizard (an unblocked, unvalidated flow — `status` isn't part of `isStepValid()`) got a normal, legally-incorrect SGB II/Wohngeld/Kinderzuschlag calculation from the primary path with no error or indicator anything was wrong, for one of the calculator's more vulnerable user groups. Fixed with the same scope as the JS engine's own exclusion: added `is_asylum_seeker: bool = False` to `Person` (`backend/models.py`), a `_is_asylum_seeker()` helper plus early-return `ineligible` guards (pointing users to the separate AsylbLG/Sozialamt process) added to exactly `calculate_sgb2()`, `calculate_wohngeld()` (also covers the frontend's derived Lastenzuschuss display, which reads directly off the Wohngeld result when `isOwner`), and `calculate_kinderzuschlag()` in `backend/engine.py` — deliberately leaving `calculate_kindergeld()`/`calculate_elterngeld()` untouched, matching the JS engine's own scope (Kindergeld/Elterngeld are not part of the AsylbLG/SGB-II mutual-exclusion system). Wired `is_asylum_seeker: status === "asylum_seeker"` into the live-backend payload's `main` member object in `smart-calculator.jsx`, alongside the existing `is_retired`/`is_student` fields. Verified via `python3 -m py_compile` (clean), the existing `backend/verify_engine.py` assertion suite (all checks still pass, zero regression to the 3 existing household test cases), `npm run build` (clean) + independent subagent review (PASS — independently re-traced the field-name/value matching frontend-to-Pydantic end-to-end rather than trusting the description, independently confirmed every one of the three guarded functions' existing return paths share the same `status`/`amount`/`reason`/`application_link` key shape as the new early-return dicts — relevant because `backend/main.py`'s `/api/v4/analyze` endpoint does `res["application_link"]` unconditionally per result, a KeyError/500 risk if a key were missing, confirmed none is, independently confirmed via its own grep of `benefit-engine.js` that the 3 guarded Python functions exactly match the JS engine's own AsylbLG-exclusion scope and that Kindergeld/Elterngeld are correctly left untouched, ran its own independent venv install + `verify_engine.py` + a fresh repro script proving both the asylum-seeker exclusion and a same-shape control household's unaffected normal eligibility, confirmed via `git diff --stat` that only the 3 intended files were touched) before committing — the implementer did not self-approve. **One non-blocking nitpick from the review, not changed**: the new `ASYLBLG_APPLICATION_LINK` points to the bare BAMF homepage (`https://www.bamf.de/`) while the accompanying reason text correctly says the actual venue is "beim Sozialamt" (BAMF runs the asylum status procedure, not the AsylbLG benefit payments, which are administered locally) — a slight authority/link mismatch, but the same style already exists elsewhere in this file (the pre-existing retiree→Grundsicherung-im-Alter redirect links to a `deutsche-rentenversicherung.de` page for a benefit that's also actually administered by the local Sozialamt), so left as consistent with established precedent rather than a new problem introduced here; worth a dedicated look if this file's application-link accuracy gets a future pass. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: live-backend Wohngeld/Lastenzuschuss calculation silently used Mietstufe 1 for every user, regardless of their real city (2026-08-02, dev-loop iteration 33)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. `src/components/smart-calculator.jsx`'s `handleSubmit` builds a `payload` object (lines ~387-395) POSTed to the live Render backend (`/api/v4/analyze`, the primary calculation path — the local fallback engine only runs on network failure/timeout) but never included `city_tier` (the Mietstufe, 1-7), even though the component already holds it on `selectedCity.mietstufe` (displayed to the user at line 861, sourced from `src/data/wohngeld-data.js`). `backend/models.py:43` declares `city_tier: Optional[int] = None` — its own inline comment (`# HIER WAR DER FEHLER: Dieses Feld muss existieren!`) shows a prior pass added the field to the schema but never wired the frontend to populate it. `backend/engine.py:203`: `mietstufe = request.city_tier if request.city_tier in range(1, 8) else 1` — since the frontend always sent `city_tier` as absent/undefined, this always evaluated to the `else` branch, silently defaulting every live-API user to Mietstufe 1 (the lowest rent-cap tier) regardless of their actual selected city. Per iteration 25's own persona-testing note, Mietstufe varies Wohngeld by "17€-230€" for identical income across tiers 1-7 — so any user in a city with mietstufe 2-7 (the majority of the dataset) got a systematically understated, silently-wrong Wohngeld/Lastenzuschuss estimate from the primary calculation path, with no error and no visual indicator anything was off. Fixed with the minimal correct change: added `city_tier: selectedCity ? selectedCity.mietstufe : null` to the payload object. The `null` fallback is defensive/effectively unreachable in the real flow — confirmed `isStepValid(1)` requires `selectedCity !== null` before the wizard lets the user advance past step 1, and there's no UI path to null it back out afterward. Confirmed the local fallback engine (`src/logic/benefit-engine.js`/`calculator-2026.js`) was never affected by this bug — it already reads `input.selectedCity.mietstufe` correctly (with its own sensible default of 4 when no city is set), so this fix is scoped purely to the live-backend payload path. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed `selectedCity`/`.mietstufe` is the correct in-scope variable/property by tracing the file itself rather than trusting the description, independently re-traced `backend/engine.py`'s fallback-to-1 behavior in Python semantics (`None`/out-of-range values in `range(1,8)` evaluate `False`), confirmed the `null`-fallback path is safe and can't be reached in normal use via the step-validation gate, confirmed via `git diff` that this is exactly the one added line with nothing else touched, confirmed its own clean `npm run build`, and confirmed the already-correct local fallback engine was correctly left untouched) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **UX/SEO fix: dead-end internal links to retired `/wohngeld/`, `/buergergeld/`, `/buergergeld-grundsicherung/` routes on `/ratgeber/` and the sitewide lexikon sidebar (2026-08-02, dev-loop iteration 32)**: found via a fresh targeted scan (delegated to a research subagent) of areas not yet deeply audited in prior iterations — non-admin/non-api pages, `src/data/**`, `src/utils/**` beyond the two files already checked, and config files (`vercel.json`, `astro.config.mjs`). `src/pages/ratgeber/index.astro`'s two headline "guide" cards ("Wohngeld 2026", "Bürgergeld & Regelsätze") linked to `/wohngeld/` and `/buergergeld/`, and `src/pages/lexikon/[...slug].astro`'s "Kontext" sidebar widget — rendered on every one of the 18 lexikon article pages — picked one of `/wohngeld/`, `/buergergeld/`, or `/buergergeld-grundsicherung/` as its "Zum {Thema}-Ratgeber" CTA. None of these three paths are real Astro routes: confirmed via `vercel.json`, which has explicit permanent (301) redirects sending all three straight to the bare homepage (`/`) — these were retired pages, and `astro.config.mjs`'s sitemap `serialize()` still has stale priority rules referencing them too (left untouched, cosmetic/out of scope). Real-world impact: any user clicking these links — on the ratgeber hub or on any lexikon article's sidebar — was silently bounced to the homepage with zero context, a bait-and-switch broken-link bug (same class as the `/#wohngeldrechner` anchor bug fixed in iteration 3). Fixed by repointing all three to real, live, on-topic destinations instead of a bare homepage bounce: `/wohngeld/` → `/lexikon/wohngeld/` (a real, existing lexikon deep-dive article, `src/content/lexikon/wohngeld.md`), `/buergergeld/` → `/lexikon/buergergeld/` (`src/content/lexikon/buergergeld.md`), `/buergergeld-grundsicherung/` → `/grundsicherung-im-alter/` (a real, separately-live landing page, already independently linked from `src/pages/kindergeld/index.astro:250`, confirming it's a genuine route and not itself orphaned). **Regression caught by independent review and fixed before commit**: the lexikon sidebar widget's title/slug matching logic meant that viewing `/lexikon/wohngeld/` or `/lexikon/buergergeld/` directly would now show the widget linking to itself (a self-referential CTA) — strictly better than the prior dead-homepage-bounce for those same two pages, but still worth closing cleanly. Fixed with a one-line guard: the widget doesn't render at all when its computed `targetLink` equals the current article's own `/lexikon/{slug}/` URL. Verified via `npm run build` (clean, both before and after the self-link guard) + independent subagent review (PASS — independently confirmed both lexikon target articles exist and resolve to the correct URLs by cross-checking `getStaticPaths`/content-collection slug derivation and `astro.config.mjs`'s `trailingSlash: 'always'` setting, confirmed `/grundsicherung-im-alter/` is genuinely live, confirmed via its own repo-wide grep that no other reference to the three dead URLs remains anywhere in `src/`, confirmed `vercel.json`'s redirects were correctly left untouched since they still serve legitimate legacy/external inbound links, confirmed no scope creep, ran its own independent `npm install && npm run build`, and directly inspected the built HTML output to confirm the self-link guard actually suppresses the widget on the two affected pages while leaving it intact and correctly pointed everywhere else) before committing — the implementer did not self-approve. **Noted, not fixed this pass (non-blocking, flagged by the reviewer)**: the sidebar widget's `isSgbXii` branch (matching on `t.includes('sgb xii') || t.includes('grundsicherung im alter') || t.includes('renten') || s.includes('sgb-xii')`) currently matches zero of the 18 existing lexikon entries' titles/slugs, so the new `/grundsicherung-im-alter/` target is technically unreachable dead code today — harmless (no live effect, no broken link since it's never rendered), just future-proofing for lexikon entries that don't exist yet; not worth a dedicated pass on its own. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Critical bugfix: `src/lib/pdf-generator.ts` was missing its `pdf-lib` import, so `generateApplicationPdf()` threw `ReferenceError: PDFDocument is not defined` on every single call — breaking both live PDF-generation flows (2026-08-02, dev-loop iteration 31)**: found via a fresh targeted scan of the three candidates iteration 30 flagged as not-yet-deeply-audited (`src/utils/get-authority.ts`, `src/utils/districtUtils.ts`, `src/lib/pdf-generator.ts`). `src/lib/pdf-generator.ts`'s `generateApplicationPdf()` function body uses `PDFDocument`, `StandardFonts`, and `rgb` (all from the `pdf-lib` package, already in `package.json`) but the file had **no import statement at all** — a plain repo read confirmed zero `import` lines anywhere in the file. Traced via `git log --follow` / `git show`: the import was present and correct in the file's earlier history (under its pre-rename name `src/lib/pdfGenerator.ts`) but got silently deleted in commit `e7b5fae` ("feat: final launch version...") while the file was being rewritten — a later pure rename (`e193ecb`, 100% content match) carried the already-broken file forward to its current path/name unchanged, so the bug has been live since `e7b5fae`. This function is called by **both** of this site's real PDF-producing flows: `src/pages/api/send-application.ts` (the free "formloser Antrag" flow — the core free value proposition of the entire site) and `src/pages/api/webhooks/stripe.ts`'s `handleCheckoutCompleted()` (the paid Stripe checkout flow, generating the PDF sent after a successful payment) — meaning every real user request to either flow has been silently throwing and failing since that commit, not just a latent/unreachable defect. Confirmed live and not just a hypothesis by writing and running an actual reproduction script (`npx tsx`, calling `generateApplicationPdf` with a minimal valid payload) against the unfixed code first — confirmed it threw `ReferenceError: PDFDocument is not defined` — before making any change. Fixed with the minimal correct change: restored the single dropped import line, `import { PDFDocument, rgb, StandardFonts } from 'pdf-lib';`, at the top of the file. Nothing else in the file was touched. Re-ran the same reproduction script against the fixed code and confirmed it now resolves successfully and returns a valid PDF `Buffer`. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed the diff is exactly this 2-line addition via its own `git diff`, independently re-traced the `git show`/rename history rather than trusting the description, confirmed `PDFDocument`/`StandardFonts`/`rgb` are pdf-lib's correct named exports by checking `node_modules/pdf-lib`'s own type declarations, ran its own independent `npm run build`, wrote and ran its own separate repro script proving the exact before/after behavior — including temporarily `git stash`ing the fix to reconfirm the old code throws, then `git stash pop`-restoring the fix, leaving the repo in the fixed state — and confirmed both call sites (`send-application.ts`, `webhooks/stripe.ts`) pass data shapes matching the function's signature) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This was the single highest-value finding available this pass — a total, unconditional break of both live PDF-generation paths is far more severe than any of the info-disclosure/hygiene items fixed in recent iterations — so no other candidate area was scanned further once this was found and confirmed. **Noted for a future pass, not fixed here (low priority, no live impact)**: while scanning `src/utils/get-authority.ts` on the way to this finding, spotted that its exported `getAllAuthoritiesForCity()` has zero callers anywhere in the repo (confirmed via repo-wide `git grep`) — genuinely dead code — and its own internal logic has a latent bug even if it were called (`c.plz === cityName`, comparing a PLZ field against the function's `cityName` argument, which the function doesn't even take a separate `plz` parameter for). Since it's unreachable, this has no live effect; a future hygiene pass should either remove it or fix-and-wire it, not both fixed and left dead. `src/utils/districtUtils.ts` was also checked this pass and found clean, no issue.
- [x] **Security/hygiene fix: raw internal `error.message` leaked to the client on 3 public API routes (2026-08-02, dev-loop iteration 30)**: found via a fresh targeted scan across `src/components/**`, `src/utils/**`, and a repo-wide grep for `error.message` in `src/pages/api/**`, prompted by there being no actionable open item left in brain.md besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). Also checked several other candidate areas this pass that turned out to be non-issues, worth recording so a future scan doesn't re-tread them: `chat-widget.jsx`'s `dangerouslySetInnerHTML={{ __html: renderText(msg.text) }}` is safe — `renderText()` HTML-escapes `&`/`<`/`>` before applying its own limited markdown (`**bold**`, newlines); the several `innerHTML` template-literal sites in `authority-search.astro` and `results-section.astro` only interpolate static, non-user-controlled data (city names from the static `cities_2026.json` dataset selected via dropdown, not free text; benefit/opportunity titles hardcoded in `backend/main.py`) — confirmed by tracing `input.city` back to `selectedCity`, which is only ever set via `handleCitySelect(city)` from the static autocomplete list, never from the raw free-text search field; `src/pages/api/webhooks/stripe.ts`'s `STRIPE_WEBHOOK_SECRET` check already fails closed (early-returns if unset), so it's not another instance of the fail-open bug class fixed in iterations 27-29. The actual finding: `src/pages/api/checkout.ts`, `src/pages/api/leads.ts`, and `src/pages/api/send-application.ts` (three public, unauthenticated POST routes — Stripe checkout creation, Supabase lead insert, real email+PDF send respectively) each caught their internal error and echoed the raw `error.message` string straight into the client-facing JSON 500 response body (`{ error: error.message }` / `{ error: error.message || '...' }` / `{ error: 'Interner Server Fehler: ' + error.message }`). That leaks internal implementation details to any anonymous caller — Stripe SDK internals/config hints, Supabase error text (which can include table/column/constraint names), or Resend/pdf-lib internals — a minor information-disclosure smell, not a severe vuln on its own, but unnecessary and inconsistent with how the rest of the codebase already treats untrusted-facing surfaces carefully. Fixed by replacing each of the three response bodies with a fixed, generic German-language error string, while leaving every `console.error(...)` call immediately above untouched so full error detail is still captured server-side for debugging — this is not error-swallowing, only not re-exposing internals to the client. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed via its own `git diff` that exactly these 3 single-line swaps were made with no other logic touched, confirmed the `console.error` calls still log the complete original error object, grepped the frontend callers in `src/components/**` that hit `/api/leads` and `/api/send-application` and confirmed neither does string-matching control flow on `.error` content — both just display it generically in an `alert()` with a fallback, confirmed no frontend caller of `/api/checkout` exists in `src/components` to depend on its error text either, confirmed its own clean `npm run build`, confirmed no unrelated files touched) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No new actionable open item found this pass** besides the two long-standing deliberately-parked ones below — a future iteration should do another fresh targeted scan (candidates not yet deeply audited: `src/utils/get-authority.ts`'s district/fallback lookup logic, `src/utils/districtUtils.ts`, `src/lib/pdf-generator.ts`).
- [x] **Security fix: fail-open `CRON_SECRET` check in `src/pages/api/cron/send-wba-reminders.ts` (2026-08-02, dev-loop iteration 29)**: found via a fresh targeted scan of `src/pages/api/**`, prompted by there being no actionable open item left in brain.md besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). This is a public POST route (`/api/cron/send-wba-reminders`, registered in `vercel.json`'s `crons` block, invoked daily by Vercel Cron) with no access control besides its own `Authorization: Bearer <CRON_SECRET>` header check — but that check was `if (CRON_SECRET && authHeader !== ...)`, which **fails open**: if `CRON_SECRET` is unset in the deployment environment, the condition is false and the handler runs with zero authentication, letting anyone on the internet POST to this URL and trigger a real batch send of WBA reminder emails (via Resend, from the site's verified domain, to real leads pulled from Supabase) on demand, repeatable at will with no rate limiting. Exact same anti-pattern already found and fixed for `ADMIN_PASSWORD` in `admin/feedback.astro` (iteration 27) and its precedent, `dashboard.astro` (iteration 28) — a secret-gated check that silently degrades to "no gate at all" when the secret is unconfigured, instead of failing closed. Fixed by inverting the condition to `if (!CRON_SECRET || authHeader !== ...)`, so an unconfigured `CRON_SECRET` now safely disables the endpoint (401 on every request) rather than leaving it wide open — deploying/using this feature requires an operator to set `CRON_SECRET` in Vercel's env config (same operational requirement iteration 27 already established for `ADMIN_PASSWORD`), matching how Vercel's native Cron Jobs feature auto-injects that header when the env var is configured, so a correctly configured deployment sees no behavior change at all. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-verified the diff is exactly this 2-line change via its own `git diff`, hand-checked all 4 truth-table cases (secret unset/set × header matching/mismatched) produce the intended pass/401 outcome, confirmed via repo-wide grep that no other code calls this endpoint expecting the old unauthenticated-fallback behavior, confirmed via its own independent `npm install && npm run build` run, confirmed no `.env.example`/docs conflict) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed by either the implementer or the reviewer. **Not addressed this pass, out of scope**: this endpoint's Supabase client uses `PUBLIC_SUPABASE_ANON_KEY` (not the service-role key used elsewhere for admin operations) to read/update the `wba_reminders` table, meaning the security of reads/writes to that table also depends on Supabase Row-Level-Security policies configured outside this repo — pre-existing since the file's original commit, not a regression introduced here, and out of scope for a single-file auth-gate fix; worth a dedicated look if the RLS policy for that table is ever in question.
- [x] **Security fix: `src/pages/admin/dashboard.astro` had zero authentication, leaking real lead PII to any site visitor (2026-08-02, dev-loop iteration 28)**: found via a fresh targeted scan of `src/pages/admin/**`, prompted by there being no actionable open item left in brain.md besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). Unlike `src/pages/admin/feedback.astro` (password-gated as of iteration 27), `dashboard.astro` had **no auth check at all** — only a code comment saying "In einer Produktionsumgebung würdest du hier eine Authentifizierung vorschalten" that was never acted on. Any visitor loading `/admin/dashboard` got a page that ran client-side JS querying the Supabase table `master_claims_2026` (real leads: `full_name`, `city`, `income`, `total_claim_value`, `avgs_interest`) via a Supabase `anon` key hardcoded in the page source, with the results rendered into the DOM via raw `innerHTML` string concatenation — a stored-XSS vector on top of the PII leak if `full_name`/`city` ever contained HTML (no code in this repo currently writes to that table, so the write path is external/unverified, but the read side was live and exploitable regardless of who populates it). Fixed by porting the exact server-side auth pattern already shipped in `feedback.astro` in the same directory: `ADMIN_PASSWORD`-gated login form, httpOnly/`sameSite: 'strict'`/`secure`-in-prod `admin_auth` cookie, and the data fetch moved server-side using `SUPABASE_SERVICE_ROLE_KEY` (never shipped to the client) instead of the client-side anon-key query. Rendering now goes through Astro's normal auto-escaping JSX templating instead of `innerHTML`, closing the XSS angle as a side effect. Also dropped the dead "Aktion"/"Details" column (a button with no `onclick` handler that never did anything) and changed the "Aktualisieren" control from a client JS call to a plain page-reload link, both now-unnecessary once the page is server-rendered per request. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed the auth gate has no bypass, confirmed zero leftover client-side Supabase/anon-key code via its own grep, confirmed all lead fields render via auto-escaping `{}` JSX with no `set:html`/innerHTML anywhere, confirmed the dropped dead button and reload-link change are harmless given nothing else in the repo referenced `loadLeads`/`window.loadLeads`/`master_claims_2026`, confirmed cookie name/path/flags match `feedback.astro` exactly so logging into either admin page authenticates both — consistent with existing behavior, not a new gap) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Not addressed this pass, worth a future look**: the shared `admin_auth` cookie means both admin pages trust the same single shared password with no per-user accounts/audit trail — pre-existing since `feedback.astro`'s iteration-27 fix, not a regression introduced here, but worth revisiting if the admin surface grows.
- [x] **Security fix: hardcoded admin password in `src/pages/admin/feedback.astro`, plus a formData double-read bug in the same file (2026-08-01, dev-loop iteration 27)**: found via a fresh targeted scan of `src/pages/**` for new bugs/TODOs, prompted by there being no actionable open item left in this file besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). `src/pages/admin/feedback.astro` — the admin panel that reads/deletes real user feedback (PII: contact info, free-text messages, submitted URLs) from Supabase via the `SUPABASE_SERVICE_ROLE_KEY` — had `const ADMIN_PASSWORD = "navigator-secret"` hardcoded and committed in plaintext in this **public** repo: anyone reading the source on GitHub could read the password and log in as admin to view or delete any user's feedback data. Fixed by moving it to `import.meta.env.ADMIN_PASSWORD` (an env var, never committed — `.env`/`.env.production` are already gitignored), matching how `SUPABASE_SERVICE_ROLE_KEY`/`PUBLIC_SUPABASE_URL` are already read in the same file. Failed closed rather than falling back to any default: if `ADMIN_PASSWORD` is unset, every auth check is gated on `ADMIN_PASSWORD &&` so login is fully disabled with a clear inline error ("ADMIN_PASSWORD fehlt in .env — Login ist deaktiviert."), not silently bypassable via an empty-string comparison. Also hardened the `admin_auth` cookie (previously had none of these): added `httpOnly: true` (blocks reading the cookie via JS/XSS), `sameSite: 'strict'` (blocks the cookie being sent on cross-site requests, closing a latent CSRF gap on the delete/toggle POST actions, which had no CSRF token), and `secure: isProd` (only sent over HTTPS in production, `isProd` derived the same way `webhooks/stripe.ts`/`send-application.ts` already detect prod via `process.env.VERCEL_ENV`). **Second, unrelated bug found and fixed in the same file while reviewing the auth flow**: the original code called `Astro.request.formData()` up to 3 times per POST request across 3 separate if-blocks (login-check, a near-duplicate login block, and the delete/toggle action handler) — a `Request` body stream can only be consumed once per the Fetch spec, so the second call would throw ("Body is unusable" under Node/undici, which Astro's Vercel Node serverless functions use) on essentially every real POST to this page, meaning the login flow was likely already broken in production independent of the password issue. Fixed by parsing `formData` exactly once at the top of the frontmatter (`null` for non-POST requests) and reusing that single object in both the login check and the action handler, removing the now-redundant duplicate login block entirely. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed the password string is gone repo-wide via its own grep, traced all 6 auth-flow scenarios by hand including the empty-string-`ADMIN_PASSWORD` bypass case, confirmed the formData single-read fix is correct and complete, confirmed no leftover references to the removed `data` variable, confirmed the env-var read pattern matches the file's own existing style, confirmed no scope creep) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Not fixed this pass, out of scope**: `item.url` (a user-submitted URL) is rendered as a raw `href` in the admin table without scheme validation — a `javascript:`-scheme URL could theoretically execute in the admin's browser context if clicked, but only after the admin (not an anonymous attacker) is already authenticated and manually clicks a suspicious link in their own feedback table; lower severity than the credential leak, worth a dedicated look in a future pass. No `.env.example`/README env-var docs exist in this repo to update (checked, none found) — deploying this fix requires the operator to set `ADMIN_PASSWORD` in Vercel's env config, otherwise the admin panel is (safely) locked out.
- [x] **Security fix: added rate limiting to `/api/checkout.ts`, the last unrate-limited cost-vector flagged in iteration 25 (2026-08-01, dev-loop iteration 26)**: `src/pages/api/checkout.ts` creates a real, unauthenticated Stripe Checkout session via the Stripe API on every request, with zero rate limiting — the "reasonable next candidate" iteration 25 explicitly flagged but left unfixed after covering `send-application.ts`/`leads.ts`/`wba-reminder.ts`/`chat.ts`. Wired the same `checkRateLimit`/`rateLimitResponse` helper from `src/lib/rate-limit.ts` in as the first statement of the `POST` handler, before both the `STRIPE_SECRET_KEY` env check and the `stripe.checkout.sessions.create()` call, using a distinct `scope: 'checkout'` bucket (5 requests/60s per IP — checked against no collision with the four existing scopes). Chose 5/min as proportional to this route's actual risk shape: unlike `send-application.ts` (real email + PDF generation per request, tightest limit) it only creates a Stripe API session object (no real payment or email side effect until a customer completes checkout), so it's closer in cost-shape to `chat.ts`/`leads.ts` than to the email-sending routes, while still meaningfully bounding Stripe-API-call-volume abuse and fake-session creation. Did not add additional input validation (email format, required fields) in this pass — out of scope, and the injection-relevant half of this concern (unescaped Stripe metadata reaching outbound HTML emails) was already closed in iteration 12's fix to the downstream `webhooks/stripe.ts` handler, confirmed still in place (`stripControlChars`/`EMAIL_REGEX` applied when metadata is read back before use). Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed rate-limit check is the literal first statement with a real early return, confirmed scope string doesn't collide with the other 4 in-use scopes, confirmed the 5/min limit is a defensible proportional choice, confirmed diff is minimal with no regression to the existing Stripe session logic) before committing.
- [x] **Security fix: added IP-based rate limiting to public unauthenticated endpoints (2026-08-01, dev-loop iteration 25)**: resolved a residual risk flagged repeatedly (iterations 8, 9, 10) but never fixed — `src/pages/api/leads.ts`, `send-application.ts`, and `wba-reminder.ts` are all public, unauthenticated POST routes with zero rate limiting, each capable of triggering a real Supabase write and/or a real outbound email per request. `send-application.ts` is the highest-value target: it sends a real email (with a PDF attachment) to a real government authority on the user's behalf at no cost and no barrier — an unrate-limited spam/abuse vector against third parties riding the site's domain reputation. Added `src/lib/rate-limit.ts`: a small in-memory fixed-window counter keyed by `scope:clientIP` (IP from `x-forwarded-for`/`x-real-ip`), with periodic sweeping of expired buckets to bound memory. Wired into all three flagged routes, plus a **new finding** from this pass: `src/pages/api/chat.ts` forwards to OpenAI/Gemini using a real API key when one is configured (`OPENAI_API_KEY`/`GEMINI_API_KEY`) and had zero request limiting — an unbounded real-money-cost vector the moment either key is set, even though no key is currently configured in this repo's tracked env. Limits chosen proportional to cost/sensitivity: `send-application.ts` 3/10min (real email + PDF gen), `leads.ts`/`wba-reminder.ts` 10/min (DB write only), `chat.ts` 20/min (interactive UX, cost risk only if a key is later added). **Known limitation, documented in the file's own comment, not fixed this pass**: state is a module-level `Map`, so it only limits requests handled by the same warm Vercel serverless instance — it does not coordinate across concurrent instances under real distributed-traffic abuse. Confirmed via `astro.config.mjs` (`adapter: vercel()`, no `edge` flag) that this project uses Node.js Serverless Functions, which do keep warm module state across invocations in the same container, so the approach works as intended for its stated (partial) scope; a shared store (e.g. Upstash Redis) would close the multi-instance gap but needs a new dependency + env var, out of scope here. IP-derivation trusts Vercel to set (not let a client override) `x-forwarded-for`/`x-real-ip` for direct connections — documented inline, reasonably well-supported per available evidence but not independently verified against a live deployment in this pass. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed algorithm has no off-by-one and never blocks a legitimate first request, confirmed the check is the literal first statement in all 4 `POST` handlers with a real `return` on block (no fall-through to the expensive work below), confirmed import paths, confirmed Vercel serverless compatibility, confirmed no scope creep) before committing; the reviewer's one requested polish (documenting the IP-trust assumption inline) was applied before commit. **New open item from this pass, not fixed**: `src/pages/api/checkout.ts` (creates real unauthenticated Stripe Checkout sessions) has the same unrate-limited-cost-vector shape as `chat.ts` but wasn't in this pass's scope — reasonable next candidate.
- [x] **Security fix: `/api/leads` forwarded raw, unsanitized data to the external Make.com webhook despite sanitizing the same fields for Supabase (2026-08-01, dev-loop iteration 24)**: found via a fresh targeted scan of `src/pages/api/**`, prompted by there being no other actionable open item in this file besides the two deliberately-parked ones. Iteration 10 added `stripControlChars`/`EMAIL_REGEX` sanitization to `src/pages/api/leads.ts` for the Supabase insert and its own brain.md note claimed this sanitization applied "before they reach Supabase **or the Make.com webhook forward**" — but re-reading the actual code (confirmed via `git log`/`git show` on the original commit, `cf772e5`, which never touched the webhook block) showed that claim was inaccurate: the webhook-forward call at the bottom of the file spread the raw `data` object (`...data`) verbatim into the JSON body POSTed to the hardcoded external `MAKE_WEBHOOK_URL`, completely bypassing the sanitization done two lines above for the Supabase insert. Same "sanitize for one sink, forward raw to another" bug class already found and fixed multiple times elsewhere in this repo (Stripe webhook HTML-injection in iteration 12, WBA-reminder emails in iteration 9) — here an attacker's CR/LF or other control characters in `first_name`/`email`/`interest`/`city`/`modal_type`/`partner_vertical` would reach the external Make.com automation completely unsanitized even though the Supabase copy was clean, and Make.com scenarios like this typically fan out into downstream emails/CRM actions where such injection matters. Also noticed and fixed a smaller latent gap in the same file while there: `phone` was never run through `stripControlChars` for the Supabase insert either (unlike every sibling field) — added the same sanitization. Fixed by reusing the already-sanitized local variables (`first_name`, `last_name`, `email`, `phone`, `interest`, `city`, `modal_type`, `partner_vertical`) as overrides spread after `...data` in the webhook body, so the sanitized values win while other pass-through fields (`consent_partner_transfer`, etc.) are preserved unchanged. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed override order correct so sanitized values win, confirmed `lead-modal.astro` (the sole caller) has no dependency on the raw phone value being echoed, confirmed no other unsanitized free-text field still reaches the webhook, confirmed diff is minimal/focused) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Security fix: removed two dead, unauthenticated, live-reachable Flask endpoints from `backend/main.py` (2026-08-01, dev-loop iteration 23)**: found via a fresh targeted scan of `backend/**` prompted by iteration 22's JS-engine-audit note about a missing `source_type` on child/partner income — that specific lead turned out to be a non-issue on closer inspection (the wizard's "Einkommen der Kinder (z.B. Unterhalt, Waisenrente)" field is legally non-Erwerbseinkommen, so `source_type` defaulting to Pydantic's `IncomeType.NONE` already produces the legally-correct Versicherungspauschale treatment — identical to what `IncomeType.MAINTENANCE` would get, since neither is in `ERWERBSEINKOMMEN_TYPES`; and the wizard collects no partner income at all, so that half of the note was moot too — no code change needed there). Continuing the scan turned up a real, actionable finding instead: `backend/main.py` (the Flask app deployed on Render via `Procfile`'s `gunicorn ... main:app` — confirmed live, since `smart-calculator.jsx` calls this same service's `/api/v4/analyze`) exposed two more routes with zero auth, zero rate limiting, and zero use by any current frontend code (confirmed via repo-wide grep for `create-checkout-session`/`v4/leads`/`LeadRequest`/`leads.jsonl` — zero hits outside `backend/main.py`/`models.py` themselves): (1) `POST /api/v4/create-checkout-session` created a real Stripe Checkout session from fully attacker-controlled `email`/`firstName`/`lastName`/`authority` request-body fields with no validation at all (not even the `.includes('@')` check other now-fixed endpoints originally had) — an unauthenticated Stripe-session-creation oracle; (2) `POST /api/v4/leads` appended unvalidated request JSON verbatim to a local `leads.jsonl` file with no field checks. Both were dead duplicates of the real, actively-hardened live paths (`src/pages/api/checkout.ts` for Stripe, `src/pages/api/leads.ts` for Supabase leads — both untouched, both already have proper validation from iterations 8-12) — same "orphaned but publicly reachable" bug class already found and fixed for `send-pdf.ts` in iterations 10-11. `git log --oneline -- backend/main.py` shows these routes are untouched placeholder/test code from the original single scaffold commit (`sk_test_Platzhalter_Bitte_Ersetzen` fallback key, an explicit `# TODO: Setze deinen ECHTEN Secret Key` comment) — never wired to real production Stripe credentials, never called by the frontend. Removed both routes plus the now-unused `stripe` import/config, the now-unused `LeadRequest` Pydantic model (`backend/models.py`), the now-unused `json`/`datetime` imports, and the `stripe` line from `backend/requirements.txt`. Verified via: `python3 -m py_compile main.py models.py` (clean), a full backend test-client smoke test in a fresh venv confirming `/` and `/api/v4/analyze` still work exactly as before and the two removed routes now correctly 404, the existing `backend/verify_engine.py` assertion suite (all 12 checks still pass, zero regression), `npm run build` (clean, this diff is backend-Python-only so no frontend impact expected or found), and an independent subagent review (PASS — independently re-confirmed zero live references to the removed routes/model anywhere in the repo, confirmed `/api/v4/analyze` and its Pydantic models (`HouseholdRequest`/`Person`/`IncomeSource`/`IncomeType`/`TerminationReason`) are byte-for-byte untouched, confirmed no dangling imports, confirmed via git history that these routes were never the production-intended payment path) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Vermögensfreibetrag wizard field: built the missing UI + fixed the flat-threshold bug, closing out the seven dead fields flagged in iteration 18 (2026-08-01, dev-loop iteration 22)**: resolved `hasHighAssets` (Step 4), the last of the seven fields — deliberately deferred in iteration 21 because a naive boolean checkbox couldn't represent that Kinderzuschlag/Wohngeld/Lastenzuschuss/Bürgergeld/Sozialhilfe/Grundsicherung-im-Alter each apply a *different* real Schonvermögen threshold. Added a numeric "Vermögen / Ersparnisse (Haushalt)" input to Step 4 (state `assets`, wired into `profileInput.assets`), then in `src/logic/benefit-engine.js` replaced each module's blanket `if (input.hasHighAssets)` gate with a real numeric comparison against that module's own already-documented threshold (Kinderzuschlag/Bürgergeld: 40k; Wohngeld/Lastenzuschuss: 60k; Sozialhilfe: 10k; Grundsicherung im Alter already had a numeric `>10000` check, now finally fed real data since `input.assets` had no UI before). **First review pass caught a real correctness bug before commit**: all five thresholds were flat household totals, but German Schonvermögen law scales with household size — SGB II Karenzzeit (Kinderzuschlag/Bürgergeld) is 40k for the first person **+15k per additional household member**; WoGG Wohngeld-Plus (Wohngeld/Lastenzuschuss) is 60k **+30k per additional member**; and the SGB XII modules' *own pre-existing reasoning text* already said "10.000 € pro Person" while the code compared a flat total, contradicting itself. Since this diff is what makes these previously-100%-dead gates start actually firing for real users, shipping the flat version would have wrongly told multi-person households (e.g. a family of 4 with 70k savings — legally fine under the real 85k SGB-II-Karenzzeit limit) that they don't qualify for benefits they actually do. Fixed by adding two small helpers (`getAssetLimit`/`exceedsAssetLimit`: base + per-additional-member increment, used for the 4 SGB-II/WoGG-style modules) and a flat per-person multiplication for the 2 SGB-XII modules (matching their own stated "pro Person" text) — verified by hand for 1-person (reduces to the original single-person figures) and 4-person households (85k/150k/40k respectively, no double-counting). Updated the Wohngeld/Lastenzuschuss/Bürgergeld reasoning strings and the wizard's `InfoTooltip` copy to reflect the per-household-size scaling. **Deliberately did not touch the live backend** (`backend/engine.py` has no assets/Vermögen logic at all, confirmed via grep — same reasoning as Pflegegrad/Bestattungsvorsorge in iterations 20-21). Verified via `npm run build` (clean) + **two sequential independent subagent reviews** (first found the flat-threshold bug described above and specified the exact fix; a second, fresh review pass re-verified the corrected math by hand for 1- and 4-person households, confirmed no variable-shadowing regression in the Grundsicherung-im-Alter module, confirmed the `hasHighAssets` fallback survived at all 6 call sites, and confirmed the tooltip copy still matches) before committing — the implementer did not self-approve at any point. This closes iteration 18's original list of 7 dead wizard fields to fully resolved (7/7), each built one at a time across iterations 19-22 with independent review, rather than batched through in one unattended pass as iteration 18 explicitly warned against.
- [x] **JS-Engine-Audit + 4 Korrekturen (2026-08-01)**: Follow-up zum Python-Backend-Audit — auf Nutzerwunsch die separate, im Frontend genutzte JS-Engine (`src/logic/calculator-2026.js` + `src/logic/benefit-engine.js`, importiert von `smart-calculator.jsx` als Fallback, wenn die Live-API nicht erreichbar ist) mit einem Node-Testharness (diverse Personas + gezielte A/B-Tests) geprüft. Die JS-Engine implementierte die 5 Python-Fixes strukturell bereits (Mietstufe, Rentner/Studenten-Ausschluss, Freibetrag nach Einkommensart, Kinderzuschlag/Bürgergeld-Vorrang) — aber vier konkrete Bugs verhinderten, dass das korrekt griff:
  1. **Live-Payload sendete `is_retired`/`is_student`/Einkommens-`source_type` nie** (`smart-calculator.jsx`) — dadurch waren die frisch gepushten Python-Backend-Fixes über den echten API-Pfad nur teilweise wirksam, und mit dem neuen Freibetrag-Fix hätten alle echten Nutzer (auch normale Angestellte) fälschlich nur die 30€-Pauschale statt des korrekten gestaffelten Freibetrags bekommen — ein durch den Python-Fix neu entstehendes Regressionsrisiko, hier verhindert.
  2. **String-Mismatch**: `calculator-2026.js` prüfte 3x `status === 'self-employed'` (Bindestrich), das Formular liefert aber `'self_employed'` (Unterstrich) — Selbstständige liefen immer durch die generische Angestellten-Logik statt der dedizierten Selbstständigen-Behandlung.
  3. **Asylbewerber wurden nicht von Bürgergeld/Wohngeld/Lastenzuschuss/Kinderzuschlag ausgeschlossen** (`benefit-engine.js`) — konnten gleichzeitig mit AsylbLG angezeigt werden, obwohl beide Systeme sich während des Asylverfahrens ausschließen. Fix ergänzt in allen vier betroffenen Modulen (der erste Fix erzeugte laut Review-Pass eine Lücke bei den übrigen drei, sofort mitbehoben).
  Verifiziert über: Node-Testharness mit 19 diversen Personas (0 Fehler/Auffälligkeiten), gezielte A/B-Tests (Mietstufe 1-7 zeigt echte Varianz; self_employed vs. employee zeigt jetzt unterschiedliche, korrekte Beträge; Asylbewerber zeigt nur noch AsylbLG statt AsylbLG+Bürgergeld), `npm run build` (grün), unabhängiger Review-Subagent (fand die unvollständige Asylbewerber-Ausschluss-Lücke, sofort behoben) vor dem finalen Commit. **Nicht behoben, nur notiert**: Partner- und Kindes-Einkommen im Live-Payload haben ebenfalls kein `source_type` (fällt auf Pydantic-Default `"none"` zurück, also Pauschale statt Freibetrag) — laut Review vorbestehend, nicht durch diesen Pass verursacht, aber gleiche Fehlerklasse; Kandidat für eine künftige Iteration.
- [x] **100-Personas-Audit + 5 Engine-Korrekturen (2026-08-01)**: Auf Nutzerwunsch `backend/engine.py` direkt (nicht über die Live-API) mit 100 diversen Haushalts-Personas durchgerechnet (Singles/Paare/Alleinerziehende mit 1-3 Kindern, alle 7 Mietstufen, Einkommen 0-6000€, Schwangerschaft, Behinderung, Rentner, Studierende, Eigentümer, Sanktionsfälle). Fünf systematische Lücken gefunden und behoben, in dieser Reihenfolge:
  1. **Wohngeld nutzte `city_tier` (Mietstufe) gar nicht** — A/B-Test bewies identischen Betrag über alle 7 Mietstufen hinweg. `calculate_wohngeld()` implementiert jetzt die amtliche WoGG-Rasterformel (§19 WoGG: `WG = 1,15 * (M - (a+b*M+c*Y)*Y)`) mit den bereits vorhandenen, aber ungenutzten Mietobergrenzen/Koeffizienten aus `rules/wohngeld.json`, inkl. Bagatellgrenze (<10€ → ineligible statt auf 10€ hochgerundet).
  2. **Rentner wurden als Bürgergeld-berechtigt angezeigt** — `is_retired`/Alter wurden nie geprüft. Neue Vorprüfung in `calculate_sgb2()`: Hauptperson ab `REGELALTERSGRENZE=67` oder `is_retired=True` → Redirect auf Grundsicherung im Alter (SGB XII) statt Bürgergeld-Betrag.
  3. **Erwerbstätigenfreibetrag wurde auf alle Einkommensarten angewendet** (auch Rente/Unterhalt) statt nur auf Erwerbseinkommen. Neue Konstante `ERWERBSEINKOMMEN_TYPES` (employment/minijob/self_employed) erhält den gestaffelten Freibetrag; alle anderen Einkommensarten nur die neue `versicherungspauschale` (30€, in `rules/sgb2.json`).
  4. **Studierende wurden ohne BAföG-Ausschlussprüfung als Bürgergeld-berechtigt angezeigt** (§27 SGB II). Neue Vorprüfung: Hauptperson `is_student=True` und nicht gleichzeitig `is_single_parent`/`is_pregnant` (bekannte Ausnahmen) → ineligible mit Erklärung.
  5. **Kein Vorrang zwischen Kinderzuschlag und Bürgergeld** — beide konnten gleichzeitig als unabhängige Ansprüche angezeigt werden. `evaluate_all()` prüft jetzt das Nachrangprinzip (§12a SGB II): nur ein **bestätigtes** (`status=="eligible"`) Kinderzuschlag-Ergebnis kann Bürgergeld auf ineligible setzen, wenn Wohngeld+Kinderzuschlag die Lücke decken; ein unsicheres `"possible"`-Ergebnis hebt den Anspruch nicht auf, sondern ergänzt nur einen Hinweistext (Korrektur aus dem Review-Pass, siehe unten).
  Verifiziert über: erneuter 100-Personas-Lauf (0 Fehler, alte Widersprüche aufgelöst oder transparent erklärt), gezielte A/B-Tests (Mietstufe 1-7 bei identischem Einkommen zeigt jetzt echte Varianz 17€-230€ statt konstant 36€; Erwerbseinkommen vs. Rente bei identischem Netto zeigt jetzt unterschiedliche Bürgergeld-Beträge), bestehende `verify_engine.py`-Suite (weiterhin grün, keine Regression), `npm run build` (grün). Unabhängiger Review-Subagent fand einen echten Fix-Bedarf (Nachrang-Umschaltung reagierte auf unsicheren "possible"-Status statt nur auf "eligible") — behoben und erneut vollständig verifiziert vor dem Commit. Hinweis: `is_cared_for` (Pflegegrad) bleibt weiterhin unbenutzt in `backend/engine.py` — deckt sich mit dem unabhängigen Fund aus Iteration 18 unten; wurde in diesem Pass bewusst nicht behoben (kein Backend-Modul für Pflege existiert bislang, siehe dortige Begründung).
- [x] **Bestattungsvorsorge/Hinterbliebenenrente wizard field: built the missing UI, third of the seven dead fields flagged in iteration 18 (2026-08-01, dev-loop iteration 21)**: resolved `isBereaved` (Step 4, declared in `smart-calculator.jsx`, default `false`) — already read into `profileInput` (the local-fallback-engine payload) and already gates one module in `src/logic/benefit-engine.js` (~line 869, `Hinterbliebenenrente`: `isRelevant: (input) => !!input.isBereaved`, fixed 450€ average estimate, softest `eligible: "possible"` tier) but had zero rendered UI, same dead-state pattern as disability/Pflegegrad before iterations 19-20. Added a single checkbox ("Ehepartner oder Elternteil verstorben?") to Step 4, styled identically to the sibling GdB checkbox block directly above it (same card/checkbox/label/`InfoTooltip` pattern) — no conditional sub-fields needed since the engine module itself is a flat yes/no gate with a fixed estimate, unlike GdB/Pflegegrad which have officially-scaled sub-values. Kept the `InfoTooltip` copy explicitly hedged ("kann... auslösen, sofern die verstorbene Person rentenversichert war") to match the engine's own soft "possible" framing and avoid implying guaranteed money for a sensitive, high-stakes life event. **Deliberately did not touch the live backend** (`backend/engine.py`): confirmed via case-insensitive grep for bereave/witwe/hinterbliebe/waise — zero matches, so (same reasoning as Pflegegrad in iteration 20) there is no backend-side logic to wire this checkbox to yet; the local fallback engine, which most users hit, now fully works for this field. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed JSX/state wiring, copy accuracy/hedging appropriate for a bereavement question, no stale dependents on the old always-false behavior, backend genuinely has nothing to wire, no scope creep) before committing. **Remaining open, not touched this pass** (1 of the original 7 state variables): Vermögensfreibetrag (`hasHighAssets`, Step 4) still has zero rendered UI — **deliberately deferred, not just skipped for time**: unlike the other 6 fields, this one is NOT a clean single-purpose gate. `hasHighAssets` is checked as a blanket boolean across *six* different benefit modules in `benefit-engine.js` (Kinderzuschlag ~120, Wohngeld ~165, Lastenzuschuss ~201, Bürgergeld ~301, Sozialhilfe ~378) each with **legally different real asset-freibetrag thresholds** (SGB XII Grundsicherung: 10.000€ per person; Bürgergeld: 40.000€ for the first household member; Wohngeld/Lastenzuschuss: 60.000€) that the engine has already collapsed into one undifferentiated flag — plus a separate, currently-dead `input.assets` numeric field only consulted by the Grundsicherung-im-Alter module (`parseFloat(input.assets) > 10000 || input.hasHighAssets`, line ~339) that has no UI either and no wizard input feeds it. A single "Verfügen Sie über Vermögen oberhalb der Freibeträge?" checkbox would be actively misleading: a household with e.g. 15.000€ savings is fine for Bürgergeld (40k) and Wohngeld (60k) but excluded from Sozialhilfe/Grundsicherung (10k) — ticking one box can't represent that correctly, and a wrong answer here means telling someone they qualify for zero of five major benefits (or wrongly telling them they qualify for none when they'd actually clear some thresholds). Doing this properly needs either (a) a numeric "Vermögen/Ersparnisse" input plus per-module threshold comparisons replacing each module's blanket `input.hasHighAssets` check with `input.assets > <module-specific threshold>`, or (b) a deliberate product decision on how to phrase a single simplified question if per-module numeric precision is out of scope for the wizard's UX — either way this is calculation-logic work across 5 files, not a one-field UI-only fix like the other 6, so it needs a dedicated iteration (or human product/legal sign-off on the simplification), not a rushed pass. This closes out iteration 18's list of 7 dead fields to "6 resolved, 1 deliberately deferred with a documented reason," not "7 done."
- [x] **Pflegegrad wizard field: built the missing UI, second of the seven dead fields flagged in iteration 18 (2026-08-01, dev-loop iteration 20)**: resolved the highest-value remaining candidate recommended at the end of iteration 19 — `hasCareDependent`/`careDependentGrad`/`careOrganization` (declared under the "Step 2 States" comment in `smart-calculator.jsx`, default `false`/`"PG 2"`/`"private"`) were already read into `profileInput` (the local-fallback-engine payload, lines ~312-314) and already consumed by real, tested calculation logic in `src/logic/benefit-engine.js` (six Pflege modules: Pflegegeld, Pflegesachleistung, Pflege-Entlastungsbetrag, Landespflegegeld, Zuschuss barrierefreier Umbau, Pflegeunterstützungsgeld — lines ~696-824) and `src/logic/calculator-2026.js` (a Wohngeld GdB/Pflegegrad-adjacent allowance, lines ~273-292) — but had zero rendered UI anywhere in the component, exactly like `hasDisability` before iteration 19. Added a "Pflegegrad amtlich festgestellt?" checkbox to Step 2 (Haushalt & Wohnen — matches where the state is declared), with two conditional selects when checked: Pflegegrad (PG 1–5, string values verified to exactly match what both engine files check via `.includes()`/array-membership) and "Wer übernimmt die Pflege überwiegend?" (private/Angehörige vs. service/Pflegedienst, matching the `careOrganization` fork between the Pflegegeld and Pflegesachleistung modules). Followed the same card/checkbox/`InfoTooltip`/conditional-reveal pattern as the sibling disability block. **Deliberately did not wire the live-backend payload this time** (unlike the disability field's `is_disabled`): `backend/models.py` has an `is_cared_for` field on `Person`, but `backend/engine.py` never reads it anywhere (confirmed via case-insensitive grep for "care"/"pflege" — zero matches) — wiring it would be the same kind of inert, cosmetic-only fix iteration 18 correctly discarded for disability before the UI existed; here the backend-side logic itself doesn't exist yet, so there's nothing to wire it *to*. Worth a future backend pass if the live API should also apply Pflege-related surcharges/allowances, tracked as a new implicit gap (not a regression — the local fallback engine, which is what most users hit, now fully works). Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed state wiring, exact string-value matches against both engine files' expectations, design-system consistency, step-2 placement correctness matching where state is declared, and copy/legal accuracy of the Pflegegrad 1 "no cash benefit" case) before committing.
- [x] **Disability (GdB) wizard field: built the missing UI, one field of the seven flagged in iteration 18 (2026-08-01, dev-loop iteration 19)**: resolved the highest-value candidate from iteration 18's finding (below) — `hasDisability`/`disabilityGdb` had real, already-tested calc logic on both the local fallback engine (`calculator-2026.js`: 17% Bürgergeld Mehrbedarf, Wohngeld GdB-threshold allowance) and the live backend (`backend/engine.py:90`, 35% SGB2 Mehrbedarf) but no rendered control. Added a "Grad der Behinderung (GdB) amtlich festgestellt?" checkbox to Step 4 (Einkommen & Vermögen), with a GdB `<select>` (20/30/.../100 in steps of 10 — matches the official §152 SGB IX scale — default "50 (Schwerbehinderung)") shown when checked, following the existing checkbox+conditional-select pattern already used for "Alleinerziehend" (Step 3) and the pregnancy-status select (also Step 3): same `labelClass`/`inputClass`/`inputDarkClass` via `cn()`, same `InfoTooltip`, same `animate-in slide-in-from-top-2` reveal. Also wired `is_disabled: hasDisability` into the live-backend payload's `members[0]` object (previously never sent — `backend/engine.py:90`/`backend/models.py:34` already read/accept it; this is the one-line fix iteration 18 correctly discarded as inert until the UI existed, now legitimate). Confirmed the local Wohngeld path (`benefit-engine.js:168-181`/`218-231`) already forwards `disabilityGdb` correctly to `calculateExactWohngeld` — the review agent flagged that the separate, unused `calculateBestOption` export in `calculator-2026.js` does *not* forward `disabilityGdb`, but `grep -rn "calculateBestOption" src/` confirms it's dead code (zero callers), so this doesn't affect the live feature; left alone as out-of-scope repo hygiene, not a live bug. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed JSX structure/pattern match, state wiring, backend field match, and GdB copy/scale accuracy; no scope creep) before committing. **Remaining open, not touched this pass** (6 of the original 7 fields, see iteration 18 below for full detail): Pflegegrad/care-dependent (`hasCareDependent`/`careDependentGrad`/`careOrganization`, Step 2), Vermögensfreibetrag (`hasHighAssets`, Step 4), Bestattungsvorsorge (`isBereaved`, Step 4) still have zero rendered UI — each needs the same one-field-at-a-time treatment (build UI, verify calc wiring, careful legal copy) in a future iteration; recommend Pflegegrad next since it also gates the Wohngeld GdB-adjacent allowance path (`calculateExactWohngeld`'s `isCareDependent`/`careDependentGrad` branches) and has legal specificity (Pflegegrad 1-5) similar to what was just handled for disability.
- [x] **Dead wizard state: 7 collected fields never settable by any UI control (found 2026-08-01, dev-loop iteration 18)**: started this iteration to fix what looked like the same bug class as iteration 13 (`expects_child`/`owns_property` missing from the live-backend payload) — `src/components/smart-calculator.jsx` builds a `members[0].is_disabled` field for the live backend payload sourced from `hasDisability` state, and `backend/engine.py`'s `calculate_sgb2()` genuinely applies a 35% Mehrbedarf-Behinderung surcharge when `is_disabled` is true (verified: rbs_1 563€ → 760.05€ for a single disabled adult). Drafted the one-line payload fix (`is_disabled: hasDisability`), but an independent review subagent caught that the premise was wrong: `hasDisability` (and `disabilityGdb`, plus `hasCareDependent`/`careDependentGrad`/`careOrganization` from Step 2, and `hasHighAssets`/`isBereaved` from Step 4) are declared `useState` variables that get read into **both** the live-backend payload/`profileInput` (used by the local fallback engine, `src/logic/benefit-engine.js`/`calculator-2026.js`) **and** are consumed downstream by real calculation logic in both engines (e.g. `calculator-2026.js:279-281` uses `hasDisability`+`disabilityGdb` for a Wohngeld room-size/GdB rule; `benefit-engine.js:885-887` similarly) — but there is **no checkbox, select, radio, or any other rendered input anywhere in the 1298-line component** that ever calls `setHasDisability`, `setDisabilityGdb`, `setHasCareDependent`, `setCareDependentGrad`, `setCareOrganization`, `setHasHighAssets`, or `setIsBereaved`. Confirmed independently (not just trusting the reviewer) via `grep -n "setHasDisability\|disabilityGdb\|hasDisability"` and a scan of Step 2/Step 4 JSX render blocks — zero matches outside the `useState` declarations and the payload-building code. These 7 fields are permanently stuck at their defaults (`false`/`"50"`/`"PG 2"`/`"private"`) for every real user, on **both** the live and offline calculation paths — meaning the disability Mehrbedarf, Pflegegrad/care-dependent Wohngeld adjustment, Vermögensfreibetrag (assets), and Bestattungsvorsorge (bereaved) logic are all currently unreachable dead code from a user's perspective, regardless of backend correctness. **Discarded the inert one-line payload fix rather than committing it** — wiring a field that can never be non-default would be a cosmetic, misleading "fix" with zero live effect; nothing to build on until the UI exists. **Deliberately not fixed this pass**: building 7 legally-nuanced form fields (Schwerbehinderung/GdB, Pflegegrad, Vermögensfreibetrag threshold, Bestattungsvorsorge) that must (a) fit the existing "Law Firm Premium" design system, (b) get the legal framing/wording right per benefit type, and (c) be QA'd end-to-end per field, is feature-scale work — not a bounded bug fix suitable for a single unattended pass. Also a real risk if rushed: presenting a legally-loaded question (e.g. Grad der Behinderung) with imprecise wording could itself misinform users, so this needs either a deliberate product/design pass or at minimum a slower, one-field-at-a-time iteration with careful legal-copy review, not five-plus fields batched through in one autonomous cycle. Recommend a future iteration (or the human owner) pick ONE of these fields (disability is the highest-value candidate — it's the only one with a real, verified, non-trivial live-backend financial impact already coded and tested) and build just its UI end-to-end before attempting the rest. **Resolved for the disability field in iteration 19 above; Pflegegrad/Vermögensfreibetrag/Bestattungsvorsorge still open.**
- [x] **Jobcenter Search Integration**: Parsing `master_daten_2026_improved.csv.txt` (used `cities_2026.json`) and building the lookup UI.
- [x] **Wohngeldbehörde Search Integration**: Implemented via `AuthoritySearch.astro` (handles both Jobcenter & Wohngeld).
- [x] **Partner Interface / API**: Implemented schema & integration (PartnerCTA).
- [x] **Lead Nurturing Webhook**: Implemented secure API (`/api/leads`) to send user data to the automation workflow.
- [x] **AVGS Monetization**: Refined funnel with "Recht statt Almosen" messaging & integrated into Results.
- [x] **Test hardening: `backend/verify_engine.py` now actually asserts instead of just printing (2026-08-01, dev-loop iteration 17)**: resolved the follow-up flagged at the end of iteration 16 — the script's 3 household test cases previously only printed computed amounts/statuses with no pass/fail check, so a real regression (like the RBS1/RBS2 Regelsatz mixup iteration 16 had to find via a fresh manual scan) could silently print a wrong number while the script still declared "[SUCCESS] Verification Tests Completed Successfully!" and exited 0. Added a `check(label, actual, expected, tolerance)` helper (numeric comparison with a small tolerance, exact match for strings/status fields) called after each test case against the current, verified-correct engine output — 12 assertions total covering SGB II amount/status, Kindergeld, Wohngeld, Kinderzuschlag, and Elterngeld across the 3 cases. Failures accumulate in a `FAILURES` list (so one bad case doesn't hide the rest) and the script now `sys.exit(1)`s with a `[FAILURE]` summary if any check fails, `[SUCCESS]`/exit 0 otherwise. Sanity-checked the regression guard actually works by re-introducing the exact iteration-16 bug pattern (RBS keyed off member count instead of partner presence) in a throwaway copy of `engine.py` and confirming the new Test 1 SGB2-amount check correctly fails (1845.68 vs expected 1902.68) and the script exits 1 — then discarded the throwaway file. Still no CI wired to run this script automatically (noted again as a possible future item, not addressed here — out of scope, this pass only fixes the script's own signal quality for whoever/whatever does run it). Verified via `npm run build` (clean; change is Python-only, no frontend impact) + independent subagent review (PASS — confirmed assertions correct, expected values match a live run, no scope creep beyond this one file, exit-code behavior verified) before committing.
- [x] **Data-correctness fix: single-parent households got the wrong SGB II Regelsatz (2026-08-01, dev-loop iteration 16)**: found via a fresh targeted scan of `backend/**` (brain.md had no other actionable open item besides the two deliberately-parked ones — iteration 15 explicitly flagged that the next iteration should scan for new bugs). `backend/engine.py`'s `calculate_sgb2()` determined the "main" household member's base Regelsatz via `rate = rbs_1 if len(request.members) == 1 else rbs_2` — keying off raw member count instead of partner presence. Per SGB II Regelbedarfsstufen, RBS 1 (563€) applies to a single adult **or a single parent with only minor children** (no partner); RBS 2 (506€, the couple rate) applies only when a partner is present. Any single-parent household (main + child(ren), no partner — `len(members) > 1` but no partner role) was silently downgraded to the couple rate, a 57€/month underpayment on one of the calculator's most common real-world cases. The exact same buggy pattern existed a second time, independently, in the Sperrzeit-sanction calculation a few lines below (`main_regelsatz = rbs_1 if len(request.members) == 1 else rbs_2`), understating the 30% self-termination sanction base the same way (151.80€ instead of the correct 168.90€ for a single parent). Fixed both sites by introducing `has_partner = any(m.role == "partner" for m in request.members)` (mirroring the pattern already used correctly elsewhere in the same file for Kinderzuschlag, e.g. `has_couple` at the existing line ~220) and using it instead of the member-count check; solo-adult and couple households are unaffected (verified numerically: single-parent trace went from 1065.68€ → 1122.68€ total Bürgergeld, exactly the 57€ RBS delta; couple-household sanction unchanged at 151.80€). This is reachable from production — `src/components/smart-calculator.jsx` calls the live Render backend (`/api/v4/analyze`) that runs this code. Found and fixed via two sequential independent subagent passes (initial targeted scan found the first instance; a first review pass caught that the identical bug pattern was still present unfixed at the sanction-calculation call site a few lines down; a second, final review pass confirmed both fixes correct with live numeric runs and no regressions) before committing — the implementer did not self-approve at any point. No `npm run build` impact (Python-only backend file, no frontend import). **Follow-up noted, not fixed this pass**: `backend/verify_engine.py`'s existing single-parent test case only prints its result rather than asserting against an expected value, which is why this bug went uncaught — worth adding real assertions in a future pass, low priority since there's no CI wired to run this script anyway.
- [x] **Content Hygiene Fix (2026-07-29)**: Found & fixed a leaked `// TODO: Faktencheck...` comment that was embedded inside a live, user-facing FAQ answer string on `/kindergeld/` (rendered on-page AND in FAQPage JSON-LD schema). Trimmed to the fact-checked, deadline/portal-name-free part of the sentence. Also removed a stale `// TODO: Verify logo path` comment in `src/components/seo/schema.astro` after confirming `public/logo.png` exists and the schema URL is correct. Verified via `npm run build` + independent subagent review.

## 2. 🗺️ Strategic Roadmap
### Phase 1: Core Foundation (Current)
- [x] **Smart Calculator**: Logic for Wohngeld & Bürgergeld 2026 implemented.
- [x] **Premium UI**: "Pro-Card" design system, Apple-inspired aesthetics.
- [x] **Local SEO Architecture**: `[citySlug]` structure ready.
- [x] **Search Data**: Robust Jobcenter & Wohngeldbehörde database integration (via `AuthoritySearch`).

### Phase 2: Authority & Trust (GEO Focused)
- [x] **GEO-First Content**: Rewrite `/lexikon` intros as direct Q&A for AI.
- [x] **Schema.org Optimization**: [CRITICAL] Implement `GovernmentService` and `FAQPage` schema.
- [x] **Social Proof (Trust Badges)**: `<TrustBadges />` (factual claims: kostenlos, DSGVO, Made in Germany, SSL) is now rendered on `index.astro` between the mission section and FAQ.
- [ ] **Social Proof (Testimonials)**: `src/components/testimonials.astro` exists but is intentionally **not** wired in — it contains fabricated named customer quotes (Thomas W., Sabine K., Michael R.) with no real source. Owner decision (2026-07-29): do not publish as-is; needs either real customer testimonials or anonymized/generic placeholders before going live (risk: irreführende Werbung / UWG).
- [x] **Repo hygiene follow-up (2026-07-31)**: removed the remaining stray root-level dev-scratch scripts flagged on 2026-07-29 — `test_bat.bat`, `test_lead.js`, `verify_all_cities.js`, `verify_fix.js` — plus two more of the identical category found during this pass, `test_stripe.js` and `test_fictitious_person.js` (manual localhost-only smoke scripts, unreferenced by app code/build/docs). Added `*.bat`, `/test_*.js`, `/verify_*.js` to `.gitignore` to prevent recurrence (root-anchored, doesn't touch `src/`). Verified via `git grep` (no references anywhere outside brain.md's own note) + `npm run build` + independent subagent review. Not in scope for this pass (deliberately left alone, different risk profile): `backend/` (Python service + `__pycache__`), root `.cjs`/PDF-extraction utility scripts, and generated `*.pdf`/`*.json` sample files — these look like they could still be in active use and warrant a dedicated look rather than a drive-by deletion.
- [x] **Repo hygiene follow-up 2 (2026-07-31, dev-loop iteration 2)**: did the deferred "dedicated look" at the root-level `.cjs`/PDF-extraction scripts and generated sample files. Confirmed dead (zero references in `package.json`, `src/`, `public/`, `vercel.json`, `backend/`, or any config — checked via repo-wide `git grep`) and removed: `add_fields.js`, `add_missing_cities.cjs`, `audit_cities.cjs`, `download_real_pdfs.js`, `extract_missing_cities.cjs`, `generate_plz_lookup.cjs`, `generate_test_pdf.js`, `get-pdf-fields.ts`, `get_real_fields.js`, `read_pdf_fields.cjs` (one-off PDF-field/city-data utilities, several hardcoding the original developer's personal Windows path `c:/Users/Jan-r/OneDrive/...`; `generate_plz_lookup.cjs`'s intended output `src/data/plz-lookup.js` was never even committed, confirming it was never adopted). Also removed the empty (0-byte) duplicate root `engine.py` — **not** the real `backend/engine.py` (15KB, live Render service), which was left untouched and verified present after the change. Also removed `full_status.json` / `temp_nlm_status.json` (stale status blobs from an unrelated external notebook/AI tool) and the generated sample PDFs `Erika_Mustermann_Bürgergeld_Antrag.pdf`, `Erika_Mustermann_Wohngeld_Antrag.pdf`, `Filled_TEST_Buergergeld.pdf` (test output, not the live `public/forms/*.pdf` templates, which were confirmed untouched and still referenced by `authority-app.jsx`/`trust-pdf-generator.jsx`). Added `/*_status.json` to `.gitignore` (root-anchored). Verified via `npm run build` (clean, dist output complete) + independent subagent review (PASS, no issues) before committing. Backend and `public/forms/` explicitly out of scope and confirmed untouched.
- [x] **Repo hygiene follow-up 3 (2026-07-31, dev-loop iteration 5)**: deleted the two dead calculator files flagged as a follow-up in iteration 4 — `src/logic/client-calculator.js` (585-line unused alternate calculator/UI-state module, same "log request URL before fetch" pattern noted in the iteration-4 privacy fix, but never imported anywhere) and `src/logic/calculator.ts` (0-byte empty file). Re-verified independently via repo-wide `git grep` (not just `src/`) before deleting: zero imports/references in `src/`, `backend/`, `package.json`, or any config — the only other place either filename appeared was `PROJECT_CONTEXT_FOR_NOTEBOOKLM.md`, which described `client-calculator.js` as "UI Input" in the architecture docs; updated that doc's file-tree listing and workflow section to drop the reference, and removed the equivalent stale "UI Input" line from this file's Tech Stack section. `src/logic/` now contains only the two live files: `benefit-engine.js` and `calculator-2026.js`. Verified via `npm run build` (clean) + independent subagent review (PASS, no issues, confirmed dead via its own repo-wide grep) before committing. (`npm install` in this session's fresh container regenerated `package-lock.json` with cosmetic `"peer": true` diffs from a newer local npm version — unrelated to this change, reverted rather than committed.)
- [x] **Repo hygiene follow-up 4 (2026-07-31, dev-loop iteration 6)**: removed 17 orphaned dead files under `src/` found via a fresh sweep (`brain.md` had no other open, actionable `- [ ]` items left besides the two deliberately-parked ones below). Confirmed zero references via repo-wide `git grep` for each: `capsule-nav.astro` (unused "kelvin-*" themed nav from an earlier design iteration, superseded by the current brand-navy/gold system — not the same component as the live design), `application-pdf-template.jsx` (superseded by live `trust-pdf-generator.jsx`), `benefit-recommendations.astro`, `bestandskunden-section.astro`, `city-map.jsx` (superseded by `authority-search.astro`/`authority-app.jsx`), `city-schemas.astro` (distinct from the live `seo/schema.astro` & `seo/seo-schema.astro`), `lead-magnet.astro` (distinct from the live `lead-modal.astro`), `notice-check-quiz.tsx`, `risk-teaser.astro`, `seo/local-business-schema.astro`, `step-calculator.jsx` (superseded by the live `smart-calculator.jsx`), `step-card.astro`, `welcome.astro`+`welcome.jsx` (unmodified Astro-starter-template pair, never wired into any page — the two files only reference each other), `layouts/dashboard-layout.astro` (the live `admin/dashboard.astro` actually uses `layouts/layout.astro`), and `assets/astro.svg`+`background.svg` (only ever used by the deleted `welcome.astro`). Verified via `npm run build` (clean, no missing-module errors) + independent subagent review (PASS) before committing. Not touched (deliberately out of scope, generic boilerplate not specific to this project): the root `README.md` still describes the default Astro-starter folder structure (`Welcome.astro`, `astro.svg`) rather than the real project layout — cosmetic doc staleness, not a functional issue, worth a dedicated pass if the team wants a project-specific README. (`npm install` in this session's fresh container again regenerated `package-lock.json` with cosmetic npm-version diffs, same as iteration 5 — reverted rather than committed.)
- [x] **Security fix: unauthenticated HTML-injection / open mail-relay in `/api/send-application` (2026-07-31, dev-loop iteration 8)**: this is the public, unauthenticated, free "formloser Antrag" endpoint (`src/pages/api/send-application.ts`) — no payment/auth barrier at all. It accepted fully client-controlled JSON (`email`, `firstName`, `lastName`, `street`, `zipCity`, `benefitLabel`, `authority`, `authorityEmail`) and (a) validated the recipient address only via `.includes('@')`, and (b) interpolated every one of those fields directly into three real HTML emails (to the authority, to the applicant, and to the admin audit inbox) sent via Resend from the site's verified domain, with zero escaping anywhere in the call chain. An attacker could set `authorityEmail`/`email` to any third-party address and inject arbitrary HTML/links (e.g. a fake "Bescheid bestätigen" phishing link) into a legitimate-looking, real email sent from `info@sozialer-navigator.de` — turning the endpoint into an open relay riding on the site's mail reputation, at no cost and with no rate limit. Fixed: added `src/lib/html-escape.ts` (`escapeHtml`) and wrapped every interpolated field in all three HTML email bodies; replaced the `.includes('@')` check with a real `EMAIL_REGEX` (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`) applied to both `email` and `authorityEmail`, which also blocks whitespace/CRLF in those two fields; added a `stripControlChars` pass on all free-text fields (`firstName`, `lastName`, `street`, `zipCity`, `benefitLabel`, `authority`, plus the two email fields) right after parsing the request body, closing a residual gap the independent reviewer flagged — `firstName`/`lastName`/`benefitLabel` are interpolated raw into plain-text email *subject* lines (correctly left un-HTML-escaped there, since HTML-escaping a subject would show literal `&amp;` to a human), so without this they could still carry a `\r\n` into the subject with unverified consequences depending on Resend's internal header handling. Left the PDF generator untouched (it has its own separate `sanitizeForPdf`, a different context — WinAnsi font encoding, not HTML/header injection). Verified via `npm run build` (clean) + independent subagent review (PASS on the HTML-injection fix; flagged the subject-line control-character gap, which was then fixed in a follow-up edit within the same iteration) before committing. Found via a targeted scan of `src/pages/api/**` email-sending handlers, prompted by there being no other small, actionable open `- [ ]` item in this file besides the two deliberately-parked ones below. **Backup candidates noted, not fixed this pass** (lower severity, out of scope): `src/pages/api/leads.ts` and `src/pages/api/wba-reminder.ts` insert client JSON into Supabase with only presence checks on a few fields, no email-format validation, no rate limiting (DB-spam risk, not outbound-email/HTML-injection risk); `src/pages/api/chat.ts` forwards user-supplied calculator context into an LLM prompt with no range validation, but doesn't cross a trust boundary in a harmful way (the user's own data going to the user's own request).
- [x] **Validation fix: unvalidated public leads/PDF-relay endpoints (2026-08-01, dev-loop iteration 10)**: fixed the two "backup candidate" endpoints flagged (but deliberately left) in iterations 8-9. Ran as two parallel worktree-isolated agents (no file overlap), each independently reviewed by a fresh subagent before merge — the review caught a real regression, described below. (1) `src/pages/api/leads.ts`: the public, unauthenticated Supabase-insert endpoint accepted any JSON with zero validation (no email-format check, no required-field check) — explicitly flagged twice in brain.md as "worth a validation pass." Added the same `EMAIL_REGEX`/`stripControlChars` pattern from `send-application.ts`: required-field check on `first_name`/`email`/`interest`/`modal_type`, email-format validation, control-char stripping on all string fields before they reach Supabase or the Make.com webhook forward. **Regression caught by independent review and fixed before merge**: the initial implementation also required `last_name`, but `src/components/lead-modal.astro`'s single free-text "Ihr Name" field splits on whitespace and produces `lastName = ''` for any single-word name — that would have silently 400'd every mononym/nickname submission across all `openLeadModal(...)` flows (PDF checklist, AVGS modal, Kinderzuschlag, Elterngeld, etc.), surfaced to the user as an indistinguishable generic "Fehler beim Senden" alert. Fixed by leaving `last_name` optional (mirrors how `phone`/`city` are already handled), matching its actual pre-existing behavior. (2) `src/pages/api/send-pdf.ts`: a public, unauthenticated endpoint that sends a real branded email with a PDF attachment via Resend to `formData.get('email')`, previously validated only via truthiness — any string would trigger a real send, making it a free, unrate-limited relay ridable for spam/harassment against arbitrary third parties using the site's domain reputation. Added the same `EMAIL_REGEX` check plus CRLF-stripping and trimming. Note: this endpoint has no live frontend caller found (the site's "PDF-Checklist" CTAs all route through `/api/leads` instead, per the pattern above) — only referenced by `debug_infrastructure.js` — so it may be dead/orphaned; fixed defensively rather than removed since it's still a reachable public route, but worth a dedicated look in a future repo-hygiene pass to confirm dead and decide removal vs. keep. Both verified via `npm run build` (clean) + the independent subagent review described above before committing/merging. Not touched (out of scope): rate limiting on either endpoint — still a residual DB-spam/abuse-relay risk lower than the fixed issues, worth a future pass if abuse is observed.
- [x] **Security fix: HTML-injection in WBA-reminder emails (2026-08-01, dev-loop iteration 9)**: the line above (iteration 8) assessed `wba-reminder.ts` as "DB-spam risk, not outbound-email/HTML-injection risk" — that assessment was incomplete: it missed that the public, unauthenticated `wba-reminder.ts` endpoint (only presence-checked `name`/`email`/`month`/`year`, no email-format validation) feeds a Supabase table (`wba_reminders`) that a separate cron job, `src/pages/api/cron/send-wba-reminders.ts`, later reads to send two real HTML reminder emails per lead via Resend from the site's verified domain (`service@sozialer-navigator.de`) — and that cron job interpolated `lead.name` directly into both HTML bodies with **zero escaping**. Same bug class as the `send-application.ts` open-relay/HTML-injection fix from iteration 8: an attacker could POST an arbitrary `name` (e.g. containing an `<a href="...">` phishing link) and any target `email`, wait for the scheduled reminder date, and have that HTML blasted out in a real email from the company's domain to an arbitrary recipient. Fixed: `wba-reminder.ts` now validates `email` against the same `EMAIL_REGEX` used in `send-application.ts`, strips control characters (`\r\n`) from `name`/`email` via `stripControlChars`, and validates `month`/`year` are sane integers (1-12 / 2000-2100) before constructing the `Date` (previously a non-numeric month/year would silently produce an `Invalid Date` and later throw inside `toISOString()`); `send-wba-reminders.ts` now imports and applies `escapeHtml()` (from `src/lib/html-escape.ts`, added in iteration 8) to `lead.name` at both interpolation sites (reminder 1 and reminder 2 templates). `lead.email` is only used in the `to:` array (not interpolated into HTML), so no other field needed escaping. Verified via `npm run build` (clean) + independent subagent review (PASS, confirmed both templates fixed, no other unescaped fields, no regressions to month/year date-math or the existing required-field validation) before committing. **Backup candidate noted, still not fixed** (lower severity, distinct risk, out of scope for this pass): `src/pages/api/leads.ts` still has no email-format validation and no rate limiting on its public Supabase insert endpoint — but unlike `wba-reminder.ts`, nothing currently reads the `leads` table back into an HTML email template, so its residual risk is DB-spam/data-quality, not HTML-injection. Worth a validation pass in a future iteration regardless.
- [x] **Data-correctness fix: live-backend calculator silently dropped `expects_child`/`owns_property` (2026-08-01, dev-loop iteration 13)**: found via a targeted scan of `src/components/**` and `backend/**` for logic not covered by iterations 1-12 (which focused on `src/pages/api/**` email-injection bugs). `src/components/smart-calculator.jsx`'s `handleSubmit` builds a `payload` POSTed to the live Python backend (`/api/v4/analyze`) but never included two fields the frontend already computes: (1) `isPregnantOrNewborn` (the wizard checkbox, tooltip explicitly "für Elterngeld") was never sent as `expects_child` — `backend/engine.py`'s `calculate_elterngeld()` gates eligibility on `has_baby OR expects_child`, so any user who checked "Schwangerschaft" always got told they're ineligible for Elterngeld from the live API (no error thrown, so the client's offline fallback engine never kicked in to correct it — silently wrong, not a crash). (2) `isOwner` (`housingType === "Eigentum"`, already computed locally to build `rentCold`/`rentUtility`) was never sent as `owns_property` — `backend/engine.py`'s `calculate_sgb2()` uses this flag to apply a different, owner-specific accommodation-cost formula instead of the default renter formula, so property-owning users always got the renter calculation regardless of what they selected in the wizard. Fixed by adding both keys to the `payload` object. **Deliberately left out of scope**: the backend also has a separate per-member `is_pregnant` field (a different SGB2 "Mehrbedarf Schwangerschaft" surcharge) — the frontend's single checkbox conflates "still pregnant" and "already has a newborn under 14 months", and the surcharge should legally only apply while still pregnant, so wiring it up risks over-crediting need for households with an already-born child; needs a UI split into two distinct states first, tracked as a new open item below rather than guessed at. Ran as two parallel worktree-isolated agents (no file overlap) alongside the pdf_generator.py removal below, each independently reviewed by a fresh subagent (both CONFIRMED) before merge. Verified via `npm run build` (clean) + both independent reviews before committing.
- [x] **Repo hygiene: removed dead `backend/pdf_generator.py` (2026-08-01, dev-loop iteration 13)**: a `class PDFService` using `reportlab`, completely orphaned — zero references anywhere in `backend/` (confirmed via repo-wide grep by both the implementer and independent reviewer), and `reportlab` isn't even in `backend/requirements.txt` (only flask/flask-cors/gunicorn/pydantic/stripe), so it would have raised `ModuleNotFoundError` if ever invoked. The live PDF path is the unrelated TS module `src/lib/pdf-generator.ts`. Also removed the now-orphaned compiled bytecode cache `backend/__pycache__/pdf_generator.cpython-312.pyc` (was tracked in git) and added `__pycache__/`/`*.pyc` to `.gitignore` to stop Python bytecode artifacts from being committed going forward. Verified via `npm run build` (clean) + independent subagent review (CONFIRMED) before committing. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diffs (same npm-version artifact as every prior iteration) were reverted, not committed.
- [x] **Repo hygiene follow-up 6 (2026-08-01, dev-loop iteration 15)**: resolved the "noted but not fixed" item from iteration 13. Deleted `backend/Antrag_V4.pdf` and `backend/Antrag_Vorbereitung.pdf` — leftover reportlab-generated sample output from when the now-removed `pdf_generator.py` was still manually invoked, first added in the original `049a07a` backend commit and untouched since. Confirmed dead via repo-wide grep (both filenames appeared nowhere except this file's own prior note) by both the implementer and an independent subagent reviewer, which also spot-checked that the actually-live PDF templates (`public/forms/Hauptantrag_Buergergeld.pdf`, `public/forms/TEST_Buergergeld.pdf`, `public/forms/TEST_Wohngeld.pdf`, `public/downloads/wohngeld-notfallplan.pdf`) are unrelated and untouched. Verified via `npm run build` (clean) + independent subagent review (CONFIRMED safe) before committing. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. No other small, actionable open item remains from prior "noted but not fixed" flags besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional "User Accounts" feature, out of scope for a hygiene/bugfix pass) — next iteration should look for new bugs/TODOs via a fresh targeted scan.
- [x] **Elterngeld "Mehrbedarf Schwangerschaft" (SGB2 surcharge) wired to the frontend (2026-08-01, dev-loop iteration 14)**: resolved the item flagged in iteration 13. `src/components/smart-calculator.jsx`'s single boolean checkbox ("Schwangerschaft oder Kind unter 14 Monate alt?") conflated two distinct legal states — still pregnant vs. already has a newborn — and the SGB2 `is_pregnant` Mehrbedarf surcharge should legally only apply while still pregnant. Replaced the checkbox with a three-way `<select>` (`pregnancyStatus`: "none" | "pregnant" | "newborn"). `isPregnantOrNewborn` is now derived (`pregnancyStatus !== "none"`) and keeps its existing behavior unchanged for both "pregnant" and "newborn" — `expects_child` in the backend payload and the local-fallback engine's Elterngeld-relevance flag (`src/logic/benefit-engine.js`) still fire for either state, so Elterngeld eligibility gating is untouched. The new `is_pregnant: pregnancyStatus === "pregnant"` is sent on the "main" household member only, so the SGB2 surcharge now applies exclusively to the still-pregnant case, closing the over-crediting risk noted in iteration 13. **Known limitation, not fixed this pass**: `is_pregnant` is only ever set on the "main" applicant member — if the pregnant person in the household is the partner rather than the main applicant, the surcharge still won't apply; the wizard has no UI concept of "who" is pregnant beyond the single household-level flag, so fixing this would need a separate product decision (e.g. a person-picker) and is out of scope here. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed correctness of both derived flags, no regressions via repo-wide grep, no leftover references to the old checkbox/setter, JSX matches existing `<select>` patterns in the file) before committing.
- [x] **Security fix: HTML-injection in Stripe webhook emails (2026-08-01, dev-loop iteration 12)**: found via a targeted scan of `src/pages/api/**` for the same bug class already fixed in iterations 8/9 elsewhere (unescaped user-controlled fields interpolated into outbound HTML emails). `src/pages/api/webhooks/stripe.ts`'s `handleCheckoutCompleted()` sends 3 real HTML emails (customer confirmation, authority application, admin audit) built from `session.metadata` — set verbatim from client-controlled JSON in `checkout.ts` with zero server-side validation (`firstName`, `lastName`, `authority`, `authorityEmail`, `street`, `zipCity`, `benefitLabel`). None of these were HTML-escaped: an attacker completing a real (or cent-level) Stripe checkout with e.g. `firstName: <a href="https://evil.example">Bescheid bestätigen</a>` could get that HTML rendered in legitimate emails sent from the site's verified domain — to the paying customer, to an attacker-chosen "authority" address (also attacker-controlled), and to the internal admin inbox. Iteration 7 had already fixed this file's separate signature-verification bypass but missed this HTML-injection issue. Fixed by wrapping every interpolation site across all three templates (plus the error-path `sendAdminAlert()` email, which `JSON.stringify()`'d the raw session/metadata into HTML unescaped — flagged by the independent reviewer as the same bug class, outside the initial diff, fixed in the same pass) in the existing `escapeHtml()` helper from `src/lib/html-escape.ts`. Also added `stripControlChars` at the point metadata is extracted (closing a CR/LF header-injection gap in the two email subject lines that use `firstName`/`lastName`/`benefitLabel`, same residual-gap pattern as iteration 8's `send-application.ts` fix) and tightened the authority-email send gate from a bare `.includes('@')` check to the same `EMAIL_REGEX` used in the sibling files. Verified via `npm run build` (clean) + independent subagent review (PASS; confirmed all interpolation sites covered, subject-line CR/LF protection intact, `EMAIL_REGEX` tightening doesn't break the `TEST_AUTHORITY_EMAIL`/local-dev-redirect override paths, control-char stripping harmless for the non-HTML uses of the same fields) before committing. `npm install` in this session again regenerated cosmetic `package-lock.json` `"peer": true` diffs (same npm-version artifact as prior iterations) — reverted, not committed.
- [x] **Repo hygiene follow-up 5 + docs (2026-08-01, dev-loop iteration 11)**: resolved two independent open items, run as parallel worktree-isolated agents (no file overlap), each independently reviewed by a fresh subagent before merge. (1) `src/pages/api/send-pdf.ts`: the "dedicated look to confirm dead and decide removal vs. keep" flagged in iteration 10. The first worktree agent's own check produced a false "still referenced" alarm — its worktree had branched from a commit *before* `558ff49` (the earlier hygiene commit that removed `debug_infrastructure.js`, `send-pdf.ts`'s only historical caller), so its stale checkout still had that file tracked. Re-verified directly on the actual branch tip in the orchestrating session: repo-wide `git grep` for `send-pdf`/`sendpdf`/`send_pdf`/`debug_infrastructure` found zero live references anywhere (confirmed again independently by the review subagent). Deleted the file; `src/lib/html-escape.ts` and `src/lib/email.ts` (shared helpers it used) were confirmed still present and still used by `send-application.ts`/`webhooks/stripe.ts`/`cron/send-wba-reminders.ts`, so nothing else broke. This removes a previously-hardened but ultimately unreachable public mail-relay attack surface entirely, rather than just defending it. (2) `README.md`: replaced the unmodified default Astro-starter-kit boilerplate (talked about `Welcome.astro`, generic `astro.svg`) with a project-accurate README — description, real tech stack (Astro 5/Vercel, React islands, Tailwind v4, Supabase, Stripe, Resend, pdf-lib, Python `backend/`), real project structure (spot-checked against the actual tree), commands matching `package.json` scripts, and a pointer to `brain.md` for deeper context. Review caught one stale leftover (`src/assets/` listed but doesn't exist — from copying structure notes before the `assets/` folder was removed in an earlier hygiene pass) and it was fixed before commit. Both changes verified via `npm run build` (clean) + independent subagent review (PASS on both, one minor README fix applied) before committing. `npm install`-generated cosmetic `package-lock.json` diffs (same npm-version `"peer": true` pattern as prior iterations) were reverted, not committed.
- [x] **Security fix: Stripe webhook signature bypass (2026-07-31, dev-loop iteration 7)**: `src/pages/api/webhooks/stripe.ts` decided whether to skip Stripe signature verification via `request.url.includes('localhost')` — a substring match against the *entire* incoming URL, including the query string. In production, a request like `POST /api/webhooks/stripe?x=localhost` would satisfy that check, causing the handler to `JSON.parse` the raw body directly as a trusted `Stripe.Event` instead of verifying it via `stripe.webhooks.constructEvent`. Since `handleCheckoutCompleted` trusts `session.metadata` (firstName/lastName/authority/authorityEmail/street/zipCity) verbatim, an unauthenticated attacker could forge a `checkout.session.completed` event and trigger real side effects: PDF generation plus an email sent to a real government authority (with an attacker-chosen address in CC), all without ever paying. Fixed to check the actual parsed hostname (`new URL(request.url).hostname`), which is immune to query-string manipulation, and to accept both `localhost` and `127.0.0.1` for local-dev robustness (the latter is what some `stripe listen --forward-to` setups use by default). Verified via `npm run build` (clean) + independent subagent review (confirmed exploit was real and the fix closes it; suggested the `127.0.0.1` addition, which was incorporated) before committing. Found via a targeted security-focused scan of `src/pages/api/**` webhook/payment handlers (brain.md had no other open, actionable `- [ ]` items besides the two deliberately-parked ones below). Not touched (out of scope): the separate `!process.env.VERCEL_ENV` dev/prod check further down the same file (used to redirect the authority email to admin) — looked plausible for this project's actual Vercel deployment (confirmed via `npm run build` using `@astrojs/vercel`), no evidence of a bug there.
- [x] **Bugfix pass 3 (2026-07-31, dev-loop iteration 3)**: fixed two small independent bugs found via a targeted scan (broken internal links + stray TODOs). (1) `src/pages/404.astro`: the "Zum Rechner" button linked to `/#wohngeldrechner`, an anchor id that doesn't exist anywhere in the codebase — it now scrolled to nothing. Fixed to `/#calculator`, the actual id used on the homepage's calculator section. (2) `brain.md` Quick Links: removed a dead `- [ ]` link pointing to `../.gemini/antigravity/brain/.../geo_strategy.md`, a path that doesn't exist anywhere on disk — a stale local-session artifact from a different AI tool, not a repo file, so there was nothing to point it to instead. Left the `/kinderzuschlag/` TODO in `src/pages/kindergeld/index.astro:251` alone — it's a placeholder comment for a page that hasn't been built yet, not a bug; building that page would be a new feature, out of scope for a bugfix pass. Verified via `npm run build` (clean) + independent subagent review (PASS, no issues) before committing.
- [x] **Privacy fix: debug console.log leaking user PII (2026-07-31, dev-loop iteration 4)**: found via a targeted repo scan that `src/components/smart-calculator.jsx` (the homepage calculator, mounted `client:load`) and `src/components/results-section.astro` were logging live user data to the browser console in production. Removed: the fetch payload/response logs in `smart-calculator.jsx` (income, rent, household composition sent to the backend, plus the full API response), and three logs in `results-section.astro` (script init, `showView()` calls, and the `benefit-calculation-completed` event handler logging the full result object incl. calculated benefit amounts). Purely diagnostic — no variables were declared only for the log calls, no side effects removed, confirmed no runtime/behavior change. Verified via `npm run build` (clean) + independent subagent review (PASS, no issues) before committing. **Follow-up noted, not fixed this pass**: `src/logic/client-calculator.js` has the same "log request URL before fetch" pattern at line ~436, but a repo-wide `git grep` found zero imports/references to that file anywhere in `src/` — it appears to be dead code (an unused alternate calculator path), so the log isn't reaching production; left alone rather than editing dead code, but worth deleting the whole file in a future repo-hygiene pass alongside the also-confirmed-empty `src/logic/calculator.ts` (0 references, empty file).

### Phase 3: Monetization Ecosystem
- [x] **Partner API**: Secure lead transmission to legal/coaching partners.
- [ ] **User Accounts**: (Optional) Save calculations for later.

## 3. 🏗️ Tech Stack & Architecture
- **Framework**: [Astro 5](https://astro.build) (Hybrid Rendering)
  - *Why*: SEO performance + React Islands for interactivity.
- **Styling**: [Tailwind CSS v4](https://tailwindcss.com)
  - *Config*: `tailwind.config.mjs`
  - *Key Colors*: `brand-blue` (#0071e3), `brand-slate` (#1d1d1f)
- **State Management**: React State (Local) + URL Params (Shareability).
- **Backend Logic**:
  - Calculation: Client-side (`src/logic/calculator-2026.js`).
  - Data: CSV/JSON imports (`src/data/`).

## 4. 🧠 Key Knowledge & Rules
### Design Philosophy
- **"Law Firm Premium"**: Authoritative, trustworthy, and sophisticated.
- **Visuals**: Deep Navy (`#0a1628`) & Gold (`#c5a67c`). Marble & Glass textures.
- **AntiGravity System**: Centralized design tokens for consistency.
- **Narrative**: **"Recht statt Almosen"** (Right, not Charity). Frame benefits as an investment in the user's future.

### Data & Logic
- **Wohngeld 2026**: Strict adherence to the official WoGG formula.
- **Mietstufen**: 1-7 scale determining rent caps. Crucial for accuracy.
- **Files**:
  - Logic: `src/logic/calculator-2026.js`
  - Data: `src/data/wohngeldData.js` (Agencies & Rent Levels)
  - Content: `src/pages/*.astro`

## 5. 📂 Quick Links
- **[Context for AI](PROJECT_CONTEXT_FOR_NOTEBOOKLM.md)**: Deep dive into the project's purpose.
- **[Schema Strategy](SCHEMA_STRATEGY.md)**: How we talk to Google.
- **[Verification](VERIFICATION_ARTIFACT.md)**: Testing protocols.

---
*This file is the central nervous system of the project. Keep it updated.*
