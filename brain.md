# 🧠 The Brain: Sozialer Navigator (Project Intelligence)

> **Status**: Active Development (Kostenloser Antrags-Flow & Render Backend Stabilisierung)
> **Last Updated**: 2026-08-06 (dev-loop iteration 86 — fixed drifted Bürgergeld-Freibetrag tiers in `chat-widget.jsx`'s local quick-estimate)
> **Focus**: Kostenloser Antragsversand, Rendert-Backend Stabilität, Multi-KI Handoff

## 1. 🎯 Top Priority Missions
What we are working on *right now*.
- [x] **Data-correctness fix: the AI chat widget's local Bürgergeld quick-estimate applied a flat 20% income-Freibetrag instead of the legally correct tiered structure, understating the benefit shown to users for incomes between 603€ and 1000€/month (2026-08-06, dev-loop iteration 86)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the three already-parked ones). `src/components/chat-widget.jsx`'s `calcBuergergeld()` (the site-wide AI chat widget's own hand-rolled Bürgergeld estimator, called from two response-generation paths, lines ~315/~500) computed the Erwerbstätigenfreibetrag as a flat 20% across the whole 100€-1000€ income band (`frei = 100; if (income>100) frei += min(income-100,900)*0.2; if (income>1000) frei += min(income-1000,200)*0.1`) — legally wrong per §11b Abs. 3 SGB II, which tiers it 20% on 100€-603€ (Minijob-Grenze), 30% on 603€-1000€, then 10% on 1000€-1200€/1500€. The correct tiered structure was already implemented correctly in the canonical, separately-maintained `calculateBuergergeld()` in `src/logic/calculator-2026.js` (lines ~155-177) — notably, this same chat widget file already imports and delegates to that file's `calculateExactWohngeld()` for its Wohngeld estimate (with an explicit comment explaining why: "Delegiert an die kanonische, mit dem Backend synchron gehaltene Formel... statt eine eigene, unabhängig gepflegte Kopie... zu führen"), but the Bürgergeld side never got the same treatment and kept an independently-drifting copy — the same "hand-rolled formula drift" bug class this file has already had fixed twice before for its Wohngeld side (Mietstufe table and WoGG formula, both iteration 74). Concrete failure: a single adult with 800€/month income sees a Freibetrag of 240€ under the old buggy formula vs. the legally correct 259,70€ — understating their Freibetrag by ~20€ and, symmetrically, overstating anrechenbares Einkommen and understating the live Bürgergeld amount shown in the chat's "Monatl. Anspruch" card. **Fixed**: ported the same tier boundaries/percentages/caps (100 base; 20% on min(income-100,503); 30% on min(income-603,397); 10% on min(income-1000, topCap-1000) where topCap is 1200 or 1500 with children) from `calculator-2026.js` into the local function, deliberately *not* swapping in the full canonical `calculateBuergergeld()` call (which also folds in Kindergeld-as-countable-income, Mehrbedarf, disability/pensioner modes, and a netto-factor conversion) — those don't belong in this widget's intentionally simplified quick-estimate model and swapping them in would have changed behavior well beyond the identified bug. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed exact tier/boundary/percentage match against the canonical formula by reading both functions side-by-side, manually traced several income points (300/603/800/1000/5000€) confirming the new Freibetrag is always ≥ the old buggy one across all ranges so this is a pure fix with no regression direction, confirmed the `children` call-site argument correctly lands in the topCap 1200/1500 branch, confirmed diff scoped to the single function with no stray `package-lock.json` changes, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the auto-generated PDF cover page mislabeled cold rent as "Warmmiete" (warm rent), silently omitting heating cost, on a real, user-printed government-benefits application document (2026-08-06, dev-loop iteration 85)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the three already-parked ones). `src/components/smart-calculator.jsx`'s own form labels the `rent` field "Bruttokaltmiete" (cold rent, i.e. excluding heating) and captures heating separately in a distinct `heatingCost`/`profileInput.heating` field — but the `sessionStorage.setItem('sozialerNavigatorState', ...)` payload that feeds the `/pdf-paket` cover-page feature only ever persisted `rent`, never `heating`. `src/components/trust-pdf-generator.jsx`'s cover-page generator then printed that cold-rent-only value under the label `'Warmmiete:'`. Concrete failure: a user enters Kaltmiete 650€ + Heizkosten 110€, generates their application PDF, and the caseworker-facing cover page shows "Warmmiete: 650 Euro" — silently dropping the 110€ heating cost and mislabeling a cold-rent figure as the warm-rent total, on the exact document meant to give a fast, accurate summary. The correct pattern already existed elsewhere in the codebase (`src/components/authority-app.jsx:543`, which reads `heating` from a different data source, `window.lastCalculationResult`, that already carried the full `profileInput` and was never affected by this bug). **Fixed**: added `heating: profileInput.heating` to the sessionStorage payload in `smart-calculator.jsx`, and changed the PDF row in `trust-pdf-generator.jsx` to `` `${savedState.input.rent} € Kalt + ${savedState.input.heating ?? 0} € Heizung` `` under the same label, mirroring `authority-app.jsx:543`. The `?? 0` fallback keeps old, pre-fix `sessionStorage` entries (saved without a `heating` key) safe — renders "0 € Heizung" rather than "undefined". Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed the bug by reading the actual code rather than trusting the description, confirmed the old-data fallback is safe, confirmed no scope creep, ran its own independent `npm run build`) before committing — the implementer did not self-approve. **Follow-up flagged by the reviewer, not fixed this pass** (pre-existing, not introduced by this diff, and out of this narrow fix's scope): for `housingType === "Eigentum"` (homeowner) users, the rent/heating form inputs are never shown (they're conditionally rendered only for `housingType === "Miete"`), so `profileInput.rent`/`profileInput.heating` stay at `0` for owners — meaning the cover page will show "0 € Kalt + 0 € Heizung" for a homeowner instead of their actual `interest`/`operatingCosts`/`propertyTax` figures (which the real benefit-calculation logic at `smart-calculator.jsx:388-391` already branches on correctly via `isOwner`). Worth a future iteration: either map the cover page's housing-cost row to the correct fields per `housingType`, or omit/relabel the row when `housingType === "Eigentum"`.
- [x] **Data-correctness fix: the auto-generated PDF cover page mislabeled total household size as "N Erwachsene(r)" (adults), inflating the household composition shown on a real, user-printed government-benefits application document (2026-08-06, dev-loop iteration 84)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the three already-parked ones). `src/components/trust-pdf-generator.jsx`'s cover-page generator (the "TAXFIX-FEATURE" block prepended to the downloadable Bürgergeld/Wohngeld application PDF whenever `sessionStorage`-saved calculator state exists) rendered the household-size row as `` `${savedState.input.persons} Erwachsene(r), ${savedState.input.kids} Kind(er)` ``. `savedState.input.persons` is not an adult-only count — it's the wizard's total household headcount (`smart-calculator.jsx`'s "Personen im Haushalt" `<select>`, labeled "1 Person"/"2 Personen"/"3 Personen"/"4+ Personen"; confirmed by the payload logic at `smart-calculator.jsx:417`, `parsedPersons - parsedKids > 1`, which only makes sense if kids are already included in `persons`). So the cover page double-counted children: a single parent with 2 kids (persons=3, kids=2) got a PDF reading "3 Erwachsene(r), 2 Kind(er)" — implying a 5-person household with 3 adults, when the true household is 1 adult + 2 children (3 total). This is the site's trust-branded auto-fill feature, explicitly pitched as speeding up a real caseworker's preliminary assessment ("Diese Anlage dient zur schnelleren Vorab-Einschätzung durch die Behörde") — every household with kids > 0 got an internally-inconsistent, inflated composition on a document mailed to a real Jobcenter/Wohngeldbehörde. The same underlying data was already phrased correctly two other places in the same feature: `trust-pdf-generator.jsx`'s own on-screen preview panel (`{savedState.input.persons} Person(en)`) and `authority-app.jsx:541` (`` `${savedState.input.persons} Person(en) (${savedState.input.kids} Kind/er)` ``) — confirming this was a one-off copy/label bug, not a deliberate design choice. Fixed by changing the cover-page row to match `authority-app.jsx`'s exact existing phrasing: `` `${savedState.input.persons} Person(en) (${savedState.input.kids} Kind/er)` ``. Verified via `npm run build` (clean) + independent subagent review (PASS — independently traced `persons`/`kids` from the wizard input through to this usage rather than trusting the description, confirmed the fix wording matches `authority-app.jsx` verbatim, confirmed the diff is exactly one line via `git diff --stat`, grepped the repo for any other reference to the old "Erwachsene(r)" string and found none, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: live-backend calculator mislabeled uncertain "possible" Kinderzuschlag as confident "probable" and silently summed it into the headline total (2026-08-06, dev-loop iteration 83)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the three already-parked ones). `src/components/smart-calculator.jsx`'s live-backend success path (`POST /api/v4/analyze`) mapped each result's UI confidence tier purely from `r.amount > 0`, ignoring the backend's own explicit three-way `r.status` field (`"eligible" | "possible" | "ineligible"`, set in `backend/engine.py` and forwarded verbatim by `backend/main.py`). `calculate_kinderzuschlag()` (`backend/engine.py` ~437-445) has a branch that returns `status: "possible"` together with a **non-zero** `amount` (the full potential Kinderzuschlag) when the parents' income alone doesn't cover their SGB II need but income + Kinderzuschlag would — an explicitly uncertain estimate. Because the frontend only checked `amount > 0`, this got mislabeled `"probable"` (high confidence), and `results-section.astro` sums all `"probable"` amounts into the prominent headline total-benefits figure (excluding `"possible"` results) — so a genuinely uncertain Kinderzuschlag estimate was silently presented as a confident entitlement and its euro amount added to the total shown to the user, on the site's primary, most-visited calculation path. A real financial-trust/accuracy issue for a benefits-advice site, not cosmetic (Wohngeld's own "possible" branch happens to return `amount: 0.0`, so it was unaffected — but the root cause was generic and would silently mis-map any future backend branch pairing "possible" with a non-zero amount). Fixed by reading `r.status` directly instead of inferring from amount: `eligible: r.type === "ALERT" ? "probable" : r.status === "eligible" ? "probable" : r.status === "possible" ? "possible" : "none"` — preserves the pre-existing ALERT special-case (status `"possible"`/amount `0.0` per `main.py`, but always shown as a prominent alert card, unchanged). `results-section.astro` already had full, non-stub support for a `"possible"` group (separate section, own styling, full result cards, correctly excluded from `totalAmount`) — confirmed by the independent reviewer, so this was a drop-in fix needing no downstream changes. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the Kinderzuschlag "possible"+non-zero-amount claim in `engine.py`/`main.py`, confirmed `results-section.astro`'s "possible" group is fully functional not a stub, confirmed no other downstream consumer of `.eligible` assumes only "probable"/"none", confirmed the ALERT short-circuit is preserved, ran its own independent `npm run build`, confirmed diff is minimal/no scope creep) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Bugfix: `bildung-und-teilhabe.astro` lead form used a stale/wrong Supabase schema and bypassed the entire `/api/leads` pipeline (2026-08-06, dev-loop iteration 82)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the three already-parked ones). Every other lead form on the site (`lead-modal.astro`, `avgs.astro`) submits `POST /api/leads/` with the real Supabase `leads` columns (`first_name`, `last_name`, `email`, `interest`, `modal_type`, etc. — see `api/leads.ts`). `bildung-und-teilhabe.astro` instead did a **direct client-side Supabase insert**, bypassing `/api/leads` entirely, with a completely different German-named payload (`vorname`, `nachname`, `typ: 'but'`, `status: 'neu'`) that appears nowhere else in the codebase — leftover from this page's original commit (`9eb3e13`), never migrated when `leads.ts` was later hardened with validation/rate-limiting/webhook-forwarding (iterations 9-10). This page is not orphaned — it's linked live from the primary calculator result cards (`results-section.astro:456`, the "Details zu Bildung & Teilhabe" CTA). A real user filling out the "BuT-Hilfe kostenlos anfordern" form almost certainly hit a Supabase column-mismatch error on every submission (generic "Fehler beim Senden" shown to the user), and even in the unlikely case the wrong columns did exist, the lead would never reach the Make.com webhook and would skip all server-side email-format validation/rate-limiting the rest of the site's forms rely on. Fixed by replacing the direct Supabase insert with a `fetch('/api/leads/', ...)` call mirroring `lead-modal.astro`'s exact pattern, supplying all fields `api/leads.ts` requires (`first_name`, `last_name`, `email`, `interest: 'Bildung und Teilhabe'`, `modal_type: 'but'`, `source: 'bildung-und-teilhabe-page'`); removed the now-unused `@supabase/supabase-js` client import/instantiation from this file entirely. Success/error UI behavior (show success message vs. alert + re-enable button) preserved unchanged. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed bug description accurate via git diff, confirmed all four server-required fields present, confirmed no leftover Supabase references, confirmed no regressions, independently re-ran `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [ ] **PARKED — owner decision needed: `src/pages/api/chat.ts` (real-LLM chat relay) is orphaned from the UI — wire it up or delete it? (2026-08-06, dev-loop iteration 81)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two already-parked ones). `chat.ts` is a public, unauthenticated `POST` route that, if `OPENAI_API_KEY` or `GEMINI_API_KEY` is ever set, forwards the client-supplied `messages`/`context` straight into a real OpenAI/Gemini chat-completion call — its own header comment documents this as an intentional future activation switch ("Zukunft: einfach OPENAI_API_KEY... eintragen, dann wird automatisch echtes LLM genutzt"). Confirmed via repo-wide grep and `git log` (both files trace to the same origin commit `41d3f48`, never wired together since): the live "KI-Assistent" widget, `src/components/chat-widget.jsx` (mounted site-wide), has **zero** callers of `/api/chat` — it ships as a fully local hand-rolled NLU/calculator engine (the same one iterations 47/59/73 fixed for drift) and never fetches this endpoint. So today the route is dead/harmless (always falls through to `{reply: null, source: 'local'}` since no key is configured) — but the risk is latent: if an operator ever sets that key believing it activates the widget's AI mode, this endpoint instantly becomes a live, unauthenticated, real-money LLM relay reachable by anyone on the internet, with no legitimate traffic baseline to make abuse visible (unlike a real feature, where a spike would stand out against normal usage). This file has been reviewed 13+ times across iterations 8, 25, 30, 34, 36, 41, 42, 44, 46, 62, 71, 76 for rate-limiting/HTML-injection/correctness — always as an assumed-real feature — but never previously flagged as having zero frontend callers. **Business/product question, deliberately not resolved this pass** (same reasoning as the still-parked orphaned Stripe-checkout item above regarding not unilaterally making product-scope calls): is the chat widget meant to eventually call this route for cases beyond the local engine's coverage (in which case it needs real wiring plus a UI fetch call), or is "fully local assistant" the permanent intended behavior (in which case the correct fix is deleting `chat.ts` entirely, removing a live LLM-cost/abuse surface that serves no current function)? **Fixed this pass instead** (safe, narrow, no product-scope decision required, mirrors the checkout.ts precedent of defensively hardening rather than guessing at the business call): the route already had a 20 req/min per-IP rate limit (iteration 25) but no cap on message count or message size — added `MAX_MESSAGES = 20` (rejects >20 messages in the array, 400) and `MAX_MESSAGE_LENGTH = 4000` (rejects any message content over 4000 chars, 400), plus a shape check requiring each message to have `role` in `('user'|'assistant')` and non-empty string `content`; also tightened the pre-existing empty/non-array guard to also reject a present-but-empty `messages` array. Bounds the worst-case per-request payload forwarded to a paid LLM API if the route is ever activated, without touching the OpenAI branch, Gemini branch, no-key fallback branch, or error handling — all confirmed byte-identical in the diff. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-ran `npm run build`, confirmed no off-by-one/bypass in the new checks, confirmed no legitimate input would be wrongly rejected given the generous caps, confirmed the other three response branches untouched, noted as a non-blocking observation that whitespace-only content isn't trimmed before the non-empty check — immaterial since the endpoint is currently unreachable and this is explicitly scoped as defensive hardening, not full sanitization) before committing — the implementer did not self-approve.
- [x] **Reliability fix: the daily WBA-Schutz deadline-reminder cron job was a silent permanent no-op because it only exported a POST handler, while Vercel Cron always triggers via GET (2026-08-06, dev-loop iteration 80)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones). `vercel.json`'s `crons` block schedules `GET /api/cron/send-wba-reminders` daily (`0 8 * * *`), but `src/pages/api/cron/send-wba-reminders.ts` only exported `POST: APIRoute`. Astro's endpoint dispatcher (`astro/dist/runtime/server/endpoint.js`) looks up `mod[request.method] ?? mod['ALL']`; with neither `GET` nor `ALL` exported, every real Vercel cron invocation hit a bare 404 before the handler body ever ran — the route worked fine when manually POSTed to (e.g. curl), which is presumably why it survived four prior audits of this same file (iterations 9, 29, 37, 45 — auth, HTML-injection, past-date validation, dashboard stat; none touched the HTTP method). Net effect: real users who signed up for the "WBA-Schutz" feature (told "Service aktiviert!") never actually received their scheduled 6-week/2-week Weiterbewilligungsantrag deadline-reminder emails via the cron path — a silent product-level failure of a benefit-deadline safety feature. Fixed by renaming the handler body to an internal `const handler` and exporting it under both `GET` (the real fix — what Vercel actually calls) and `POST` (kept for manual/local invocation); no auth/query/email/response logic touched. Verified independently: confirmed Vercel's GET-only cron behavior against Vercel's own docs; read Astro's actual dispatcher source to confirm the 404 fallback path; wrote a standalone Node harness driving Astro's real `renderEndpoint()` against both the before/after module shapes (before: GET → 404; after: GET → 200); confirmed via `npx tsc --noEmit` no new type errors; confirmed the CRON_SECRET bearer-token auth check is method-agnostic so making the route GET-reachable adds no new exposure (never cookie/ambient-auth-based, so no CSRF concern); confirmed no other route in `vercel.json`'s crons block (only this one entry exists) has the same bug. `npm run build` clean, `npm run build` clean again on independent re-run, no `package-lock.json` diff generated. Independent subagent review (PASS — re-verified all claims against primary sources rather than trusting the description, confirmed handler logic byte-identical, confirmed no scope creep) before committing — the implementer did not self-approve.
- [ ] **PARKED — owner decision needed: the paid "Antragsservice" flow (`src/pages/api/checkout.ts` + `src/pages/api/webhooks/stripe.ts`, real 5,99€ Stripe charge + government-application email) is orphaned from the UI but still live, unauthenticated, and has an unfixed false-success bug (2026-08-04, dev-loop iteration 78)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two already-parked ones). `git log -S"api/checkout" --all` confirms commit `9854995` ("update erfolg page and application send endpoint for free workflow") deliberately removed `authority-app.jsx`'s only call site to `/api/checkout` when the product pivoted to the current free-only `send-application.ts` flow — but never deleted `checkout.ts`/`webhooks/stripe.ts` themselves. Repo-wide grep confirms zero remaining frontend callers. Despite being unreachable through the site's own UI, both files are still live, publicly POST-able routes on a real Stripe secret key, and the webhook has continued to be actively security-hardened in later iterations (7, 12) as if it were a real in-use path — i.e. the team's own mental model still treats it as live. Two distinct problems, deliberately **not resolved this pass**: **(1) Business/product question** — is this flow meant to stay retired (in which case the correct fix is deleting `checkout.ts`/`webhooks/stripe.ts`, the `stripe` npm dependency, `STRIPE_*` env vars, and the stale "Stripe — paid PDF package checkout" line in `README.md`), or is it meant to be reinstated later (in which case it needs a real UI entry point again plus the fix below)? This is a real-money, real-legal-document-submission decision with no clear signal either way in the repo, and permanently deleting a payment integration is a hard-to-reverse call a routine bugfix pass shouldn't make unilaterally — needs explicit owner input. **(2) Correctness bug, contingent on (1)** — even if kept, `webhooks/stripe.ts`'s `handleCheckoutCompleted()` can and does set `paid_applications.status = 'FAILED'` (PDF-gen failure, authority-send failure — lines ~163-174, ~225-255, ~292-306) *after* the browser has already landed on `checkout.ts`'s statically-baked `success_url`, and `erfolg.astro`'s client script (lines 123-172) only ever branches on a `?pending=1` query param that the *free* flow appends synchronously from its own fetch response (`authority-app.jsx:327`) — the paid flow has no equivalent mechanism (structurally can't at redirect time, since Stripe fixes `success_url` before the async webhook runs) and was never given one, so a customer whose payment succeeded but whose application actually failed to send would still see an unconditional "Antrag erfolgreich versendet" — the exact false-success bug class iteration 75 fixed for the free flow, still open here. Fixing this properly needs a new session-status lookup (e.g. a small server-side API route querying `paid_applications` by `stripe_session_id`, returning only a status enum — never raw PII to the client) wired into `erfolg.astro`, which is real new surface, not a one-line fix, and its value is contingent on resolving (1) first. **Fixed this pass instead** (safe, narrow, no product-scope decision required): `checkout.ts` had literally zero server-side input validation — `email`/`firstName`/`lastName`/`authority`/`authorityEmail`/`street`/`zipCity`/`benefitLabel` were used straight from client JSON with no required-field check, no email-format check, no control-character stripping, unlike every sibling public endpoint (`send-application.ts`, `leads.ts`, `wba-reminder.ts`, `webhooks/stripe.ts` itself on the read side). Added the same established `stripControlChars`/`EMAIL_REGEX`/required-field pattern (mirrors `send-application.ts` near-verbatim) before the Stripe session is created — defense-in-depth that's correct regardless of how (1) is eventually resolved, and doesn't touch the endpoint's reachability or behavior for any real caller (repo-wide grep confirmed there is none today). Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed all fields sanitized/validated at the right point before any Stripe/metadata use, confirmed no existing caller could be broken since none exists, confirmed no diff noise, ran its own independent `npm run build`) before committing. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Reliability fix: unrelated email failures in `/api/send-application` could erase the `authoritySent` result and trigger a duplicate real send to the authority (2026-08-06, dev-loop iteration 79)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two already-parked ones). The endpoint does three sequential `sendEmail()` calls: (1) to the authority, already wrapped in try/catch and tracked via an `authoritySent` boolean (fixed in iteration 75); (2) a confirmation copy to the applicant; (3) an internal audit-trail email to admin. Calls (2) and (3) were **not** wrapped in try/catch — if either threw (mail-provider outage, rate limit, etc.), it propagated to the outer catch block, discarding `authoritySent` and returning a generic 500 even when the authority-critical send in step (1) had already succeeded. The frontend (`authority-app.jsx`'s `handleConfirmSend`) treats any non-2xx response as a full failure, alerts the user, and re-enables the submit button — inviting a retry that would resubmit the same, real, deadline-relevant application to the government authority a second time, since there's no idempotency guard. Fixed by wrapping calls (2) and (3) each in their own try/catch (log via `console.error`, do not rethrow), so the response always reaches `{ success: true, authoritySent }` once the authority-critical send has been attempted — mirrors the pattern already used for call (1). Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed brace/paren nesting correct, frontend retry behavior as described, no new bug from swallowing the two non-critical failures since both remain `console.error`-logged and the more important `authoritySent` signal still reaches the client) before committing. `npm install`-generated cosmetic `package-lock.json` diff (`peer: true` flag churn, same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Correctness fix: live-backend result cards rendered "undefined" for every benefit's title/description on the primary calculator path (2026-08-04, dev-loop iteration 77)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; the scan was pointed at areas iteration 76 hadn't yet re-audited — accessibility/mobile-CSS beyond the two already-fixed modals, `src/pages/api/chat.ts`, other cron routes, remaining `src/components/**`, other content pages, remaining calculator-formula drift). `src/components/smart-calculator.jsx`'s live-backend success path (the primary `POST /api/v4/analyze` branch, only falling back to the local offline engine on API failure/timeout) built each result-card object with `{ title, description, ... }`, but `src/components/results-section.astro`'s card renderer (the `<h5>` title at line ~504, the explanatory `<p>` at line ~511, and the gauge-legend label at line ~631) reads `.name`/`.reasoning` instead — field names that only the *offline fallback* engine (`evaluateAllBenefits()` in `src/logic/benefit-engine.js`) natively produces. Net effect: on every normal, successful calculator run with the backend reachable — i.e. the site's entire intended, primary path — every result card's benefit name and explanation literally rendered as the string "undefined", while the amount/eligibility badge still worked (those fields use `.amount`/`.eligible`, present in both shapes) so the page never visibly crashed. This is exactly the kind of defect that survives testing done against a working backend without eyeballing the literal rendered card text, and only the (correctly-labeled) offline-fallback banner path was ever displaying real titles/descriptions. Fixed by adding `name`/`reasoning` aliases (mirroring the existing `title`/`description`, both kept — the `sperrzeit_alert` banner code still reads `.title`/`.description` directly and was left untouched/unbroken) at all three places `mappedResults` objects are built in `smart-calculator.jsx` (the main `.map()`, the `lastenzuschuss` push, the `grundsicherung_alter` push), plus a housingCard mutation site that appended an extra sentence only to `.description` (never `.reasoning`, so the appended note would never have actually rendered) — now updates both. Also fixed an adjacent, smaller defect in the same success-path `id`-mapping `if` chain: only `SGB2`/`WOHNGELD`/`ALERT` backend `type` values were mapped to internal ids; `KINDERGELD`/`KINDERZUSCHLAG`/`ELTERNGELD` (real values the backend returns per `backend/main.py`'s `benefit_titles` dict) fell through to `id: "unknown"`, degrading their dedicated CTA button/badge-color logic in `results-section.astro` to the generic fallback — added the three missing mappings. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the bug was real by tracing both field-name consumers and producers, confirmed all three `mappedResults` construction sites were covered with no site missed, confirmed the backend's real `type` values against `backend/main.py` to validate the new id mappings, confirmed `title`/`description` consumers (`sperrzeit_alert` banner) still work since both field-name pairs coexist on the same objects, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope). **Superseded entry, not re-flagging**: the below iteration-76 entry remains for history.
- [x] **Security fix: the Flask backend's `/api/v4/analyze` endpoint (`backend/main.py`) leaked raw Python exception text to any direct HTTP caller on unhandled errors (2026-08-04, dev-loop iteration 76)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; the scan was pointed at areas past iterations had flagged as not yet deeply audited — `backend/main.py`/`models.py` full audit, `admin/feedback.astro`/`admin/dashboard.astro`, `premium/*`, remaining `content/lexikon`/`content/ratgeber` pages, `src/utils/get-authority.ts`, `src/utils/districtUtils.ts` [file no longer exists — stale reference from an earlier note], `src/lib/pdf-generator.ts`). `backend/main.py` is the sole production backend (`POST /api/v4/analyze`, called directly by `src/components/smart-calculator.jsx`). Its generic catch-all handler (`except Exception as e:`) returned `jsonify({"error": "An unexpected error occurred", "details": str(e)}), 500` — any unhandled exception inside `engine.evaluate_all(req)` (e.g. an edge-case `KeyError`/`TypeError` the extensive `getattr`/dict-lookup logic in `engine.py` doesn't anticipate) serialized the raw Python exception message straight into the JSON response body. Unlike the frontend, this is a direct HTTP endpoint: Flask-CORS's `origins` restriction only stops browser JS from *reading* a cross-origin response, it does not stop a direct `curl`/Postman POST from receiving the full body, so this leaked to any caller unconditionally in production (no `debug=` gate on `app.run()`, confirmed no `FLASK_ENV`/`DEBUG` check anywhere in the file). Same bug class already fixed in iteration 30 for the TypeScript API routes (`checkout.ts`/`leads.ts`/`send-application.ts`), just not yet caught in the Python backend. Fixed by dropping the `"details": str(e)` field from the response — the `app.logger.error(f"...: {e}", exc_info=True)` call immediately above is untouched, so the full error is still captured server-side for debugging; this is not error-swallowing, only not re-exposing internals to the client. The adjacent `except ValidationError as e:` handler (still returns `{"error": "Invalid request data", "details": e.errors()}`, 400) was intentionally left alone — Pydantic's structured field-validation output is legitimate client-input feedback, not internal exception leakage, matching how the codebase already treats validation errors elsewhere. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed via its own `git diff` that only this one field was removed and nothing else touched; traced `smart-calculator.jsx`'s error path and confirmed it never reads `.error`/`.details` from the 500 body, so no caller depended on the removed field; sanity-checked the ValidationError-vs-Exception distinction holds; ran its own independent `npm run build` plus `python3 -m py_compile` on all four `backend/*.py` files; confirmed no log-forwarding-to-client mechanism exists and Werkzeug's interactive debugger isn't enabled; grepped all of `backend/*.py` and confirmed this was the only instance of the bug class in the Python backend, nothing else missed) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Areas checked and ruled out this pass** (worth recording so a future scan doesn't re-tread them): `backend/models.py` (no exploitable validation gaps beyond generic self-reported-input range checks); `backend/engine.py` (SGB II/Wohngeld formulas, rates, coefficients, rent-limit tables cross-checked line-by-line against `calculator-2026.js`/`backend/rules/*.json`, no drift found); `admin/feedback.astro`/`admin/dashboard.astro` (auth/cookie/service-role usage consistent; a non-constant-time password comparison exists in both but is a pre-existing low-severity pattern, not a new isolated defect); `src/components/premium/pro-input.tsx` (minor `aria-describedby` gap, but only used on the internal `design-system.astro` showcase page, not any real user flow — not worth a dedicated fix); `content/lexikon/*`/`content/ratgeber/*` (numeric claims spot-checked against `backend/rules/sgb2.json`, all consistent); `src/utils/get-authority.ts` (fallback chain traced end-to-end, no dead-end/wrong-authority path); `src/lib/pdf-generator.ts` (text-sanitization fallback is defensive, no crash/data-loss path). **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope). **Superseded entry, not re-flagging**: the below iteration-75 entry remains for history.
- [x] **Trust/data-integrity fix: the free "Antrag versenden" flow (`src/pages/api/send-application.ts`) always told the user their application had been sent to the authority, even when the outbound email to the authority genuinely failed (2026-08-04, dev-loop iteration 75)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones). This is the site's core "kostenloser Antragsversand" flow — forwarding a formless application by email to secure a Fristwahrung (deadline-preservation) date. The endpoint tracked send success in a local `authoritySent` flag (only `true` inside the try/catch around `sendEmail()` to the authority) but never acted on it: the applicant's confirmation email always claimed "wir... an die Behörde gesendet haben", and the API always returned `{success: true}`, so `authority-app.jsx` always redirected to `/erfolg/` — a page titled "Antrag erfolgreich versendet". A real Resend outage/rate-limit/rejected-recipient failure (a genuine `sendEmail()` throw path, not hypothetical) meant a user could believe their deadline-critical application was filed when it never reached the authority, with no reason to follow up before losing retroactive eligibility for that month. Fix: (1) API response now includes `authoritySent: boolean`; (2) the applicant confirmation email branches subject/body on it — honest "automatischer Versand fehlgeschlagen, Team übernimmt manuell" wording on failure instead of a false success claim (the admin audit email already correctly branched on this, so only the user-facing side was dishonest); (3) `authority-app.jsx` passes `&pending=1` to `/erfolg/` when `authoritySent === false`; (4) `erfolg.astro`'s client script swaps the hero heading/body to the honest "wird manuell nachbearbeitet" copy when `?pending=1` is present. Independent review agent caught two real issues before commit, both fixed: (a) the honest-messaging swap was gated behind a blocking top-level `import { supabase }` in the same script — if that import were ever slow/blocked, the safety-critical message would silently never render; moved to a dynamic `import()` inside the (unrelated) feedback-form submit handler so the pending-swap no longer depends on it; (b) the `name` URL param was interpolated into `innerHTML` via a `<>&`-stripping regex (safe today only because the sink was plain text, not an attribute) — replaced with `textContent`/`createElement` DOM construction, no string-in-HTML sink at all. Verified via `npm run build` (green) after both the initial implementation and the review-driven follow-up fixes. **Not addressed, left as a known gap**: the reviewer noted the whole `?pending=1` swap is still client-JS-only rather than rendered server-side in the (SSR-capable, `output: 'server'`) Astro page — a JS-disabled/very-degraded browser would still see the default "Antrag erfolgreich versendet" text. Judged acceptable for this pass (JS is required for the rest of the page's interactivity anyway, e.g. star rating/feedback form) but a full SSR-rendered version would be more robust if this page is revisited. found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; iteration 73 explicitly flagged that the site's accessibility/mobile-CSS beyond the two fixed modals and the PLZ autocomplete remained an under-swept candidate area — the agent checked that area first, found nothing new there this pass, and widened to other under-audited surfaces per its brief). `src/components/chat-widget.jsx` (the "KI-Assistent" live chat feature, presenting itself to users as a confident calculated result card, not a disclaimer-only answer) implemented its own, third, independently-maintained copy of the Wohngeld formula/constants — `WG_MAX_RENT` (a rent-cap table) and `WG_COEFF` (formula coefficients), consumed by a hand-rolled formula in `calcWohngeld` — completely separate from the canonical, backend-synced implementation in `src/logic/calculator-2026.js` (`RENT_LIMITS`/`WOGG_COEFFS`/`calculateExactWohngeld`, which the code's own comments say are deliberately kept byte-for-byte in sync with `backend/engine.py` "um Backend/Frontend-Drift ... zu vermeiden"). This third copy had drifted from both canonical sources in four independently-confirmed ways: (1) missing the § 19 WoGG `1.15` Bundesanteil multiplier entirely (canonical: `1.15 * (M - (a + b*M + c*Y) * Y)`; chat-widget: `M - (a + b*Y + c*M) * Y`, no `1.15` anywhere); (2) the `b`/`c` coefficients had their roles swapped (canonical multiplies `b` by rent `M` and `c` by income `Y`; chat-widget did the opposite); (3) the coefficient values themselves were stale (e.g. 1-person: chat-widget `{a:0.04, b:0.000321, c:0.000127}` vs. canonical/`backend/rules/wohngeld.json` `{a:0.040, b:0.0004797, c:0.0000408}`); (4) the `WG_MAX_RENT` rent-cap table was stale by ~80-130€ per Mietstufe (e.g. 1-person row `[392,426,469,512,562,619,711]` vs. canonical `RENT_LIMITS[1]` `[491,538,586,641,692,745,807]`). A fifth, compounding bug in the same function: `calcWohngeld` multiplied the caller's monthly income by 12 (`const Y = income * 12`) under the assumption the formula needs an annualized figure — but `calculateExactWohngeld` (and its real caller, `benefit-engine.js`, confirmed by reading both) treats `income` as already-monthly with no internal `*12`, so this was an independent units bug layered on top of the coefficient drift, not a compensating correction. Net effect: any user chatting with the AI assistant about Wohngeld got a materially wrong €-estimate, inconsistent with what the same inputs would produce in the site's actual Smart-Calculator/backend — exactly the "silently diverged duplicate calculation engine" bug class this project's iterations have repeatedly found and fixed elsewhere (Elterngeld iter 66/67, Kinderzuschlag iter 63, SGB2 Freibetrag iter 64, disability Mehrbedarf iter 70), just not yet caught in this specific component. `calcBuergergeld`'s own simplified rent-cap heuristic (a different, deliberately-approximate feature, independently cross-checked by the research agent against `backend/rules/sgb2.json`/`kinderzuschlag.json` and confirmed otherwise correct) also referenced the same stale `WG_MAX_RENT` table for its rent-cap estimate. Fixed by deleting the duplicated `WG_MAX_RENT`/`WG_COEFF` constants and the hand-rolled formula entirely, and importing/calling the canonical `calculateExactWohngeld`/`RENT_LIMITS` from `calculator-2026.js` directly instead — in `calcWohngeld` (the real fix; passes the caller's monthly income/rent straight through with no `*12`, matching how `benefit-engine.js` calls the same function) and in `calcBuergergeld`'s rent-cap lookup (index shifted from the old 0-indexed `WG_MAX_RENT[persons-1]` to `RENT_LIMITS`'s 1-indexed keys, `RENT_LIMITS[clamp(persons,1,5)]`). Verified via `npm run build` (clean) + a standalone Node harness directly exercising `calculateExactWohngeld` with representative chat-widget-style inputs (1 person/500€ income/400€ rent/Mietstufe 3 → 492€; 2 persons/1200€ income/700€ rent/Mietstufe 7 → 656€, both sane and consistent with the canonical engine, unlike what the old broken formula would have produced) + an independent subagent review (PASS on all 6 checks — independently confirmed the canonical exports/signatures match the new call sites, traced every `calcWohngeld`/`calcBuergergeld` call site to confirm the plain-number return contract is unchanged, independently confirmed `RENT_LIMITS` is keyed 1-5 so the `calcBuergergeld` index-base change is not an off-by-one, grepped the whole file for zero leftover references to the deleted `WG_MAX_RENT`/`WG_COEFF` identifiers, ran its own independent `npm run build`, and independently confirmed via `benefit-engine.js`'s real call sites that `income` is correctly monthly-not-annual so removing the old `*12` fixes a units bug rather than introducing one) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — the research agent's brief also covered general accessibility/mobile-CSS beyond the two previously-fixed modals per iteration 73's note; that area was not exhaustively re-swept this pass (the chat-widget finding was strong enough to report first) and remains a candidate for a future iteration. **Superseded entry, not re-flagging**: the below iteration-73 entry remains for history.
- [x] **Accessibility fix: the site-wide lead-capture modal (`lead-modal.astro`, used for AVGS/Elterngeld/Kinderzuschlag/PDF-checklist/legal-help/Pflege CTAs across the site) and the WBA-reminder modal (`wba-reminder-modal.astro`) were not usable as accessible dialogs (2026-08-04, dev-loop iteration 73)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; iteration 72 explicitly flagged that the rest of the site's accessibility/mobile-CSS beyond the smart-calculator PLZ autocomplete component was not exhaustively swept and remained a candidate area). Three issues in `lead-modal.astro`: (1) the dialog root's `aria-labelledby="modal-title"` pointed at an `id` that didn't exist anywhere in the file (confirmed via repo-wide grep) — every screen-reader user opening this modal in any of its 7 variants heard an unlabelled "dialog" with no accessible name; (2) `window.openLeadModal` toggled visibility via CSS classes only, never moved focus into the panel and never restored it to the trigger on close, so keyboard users could tab straight through into page content hidden behind the backdrop despite `aria-modal="true"`; (3) no `Escape`-to-close handler and no focus trap existed anywhere in the file. `wba-reminder-modal.astro` had the same three gaps, plus its backdrop had no `role="dialog"`/`aria-modal` at all. Fixed by giving each of the lead-modal's three step headings a real `id` (`avgs-step-1/2/3-title`) and updating the dialog's `aria-labelledby` at every step transition (all 7 `openLeadModal` type branches via a shared "which step is visible" check, the step1→step2 click handler, and the step2→step3 submit-success handler); adding `id="wba-modal-title"`/`role="dialog"`/`aria-modal="true"`/`aria-labelledby` to the WBA modal backdrop; capturing `document.activeElement` before opening and calling `.focus()` on the panel (`tabindex="-1"` added to both panels) shortly after opening, then restoring focus to the captured trigger element on close, in both files; and adding a `document`-level `keydown` listener in both files (guarded to no-op while the respective modal is hidden, so it can't interfere with other modals or normal page behavior) that closes on `Escape` and traps `Tab`/`Shift+Tab` within the panel's live-computed focusable-elements list. Verified via `npm run build` (clean) + an independent subagent review (PASS — independently traced every `aria-labelledby` set-point across all `openLeadModal` branches and both step-transition handlers back to a real matching `id`, confirmed via the built `dist/client/*.html` output that none of the four new ids collide anywhere, confirmed the two `keydown` listeners are registered once at page-load [not re-stacked per modal-open call] and correctly no-op while their modal is closed, confirmed `lastFocusedElement?.focus()` is a safe no-op rather than a throw if the element was since removed from the DOM, confirmed no regression to the existing step-transition/animation/fetch/success-state logic in either file, ran its own independent `npx astro build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — the rest of the site's accessibility/mobile-CSS beyond these two modals and the previously-fixed smart-calculator autocomplete was not exhaustively swept and remains a candidate area for a future pass. **Superseded entry, not re-flagging**: the below iteration-72 entry remains for history.
- [x] **Accessibility fix: the PLZ/city autocomplete in Step 1 of the main calculator wizard was keyboard-inoperable, permanently blocking keyboard-only users from the site's core conversion funnel (2026-08-04, dev-loop iteration 72)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; iteration 71 explicitly flagged that a from-scratch bug hunt was due and listed accessibility/mobile-CSS as an under-audited candidate area), pointed at `backend/main.py`/`models.py` full audit, `admin/feedback.astro`/`admin/dashboard.astro`, `premium/*`, remaining `content/lexikon`/`content/ratgeber` pages, and accessibility/mobile-CSS. `src/components/smart-calculator.jsx`'s PLZ input (Step 1, rendered on the homepage with no `defaultCity` — the site's primary entry point) filters and shows a suggestion dropdown as the user types, but the suggestion items (`suggestions.map(...)`) only had an `onClick` handler — no `onKeyDown`, no `tabIndex`, no ARIA roles anywhere in the file (confirmed via a repo-wide grep for `role="listbox"`/`aria-selected`/`ArrowDown` returning zero matches). The input's own `onKeyDown` was the generic shared `handleKeyDown`, whose only behavior is `Enter → handleNextStep()`; `isStepValid(1)` requires `selectedCity !== null`, and `selectedCity` is only ever set inside `handleCitySelect`, itself only ever called from that `onClick`. Net effect, confirmed by tracing the full call chain: a keyboard-only user (motor-impairment/switch-access users, some screen-reader users) who typed a valid PLZ and pressed Enter got nothing — no error, no advance — a hard WCAG 2.1.1 keyboard-operability block with zero workaround, on the mandatory first step of the entire product. Fixed by adding a dedicated `handleCityKeyDown` handler (new `highlightedSuggestion` state) that intercepts ArrowUp/ArrowDown (navigate, with correct wrap-around including from the initial "nothing highlighted" `-1` state — the first pass had an off-by-one here, caught and fixed after independent review), Enter (select the highlighted suggestion, or the first one if none highlighted yet), and Escape (close the dropdown) only while suggestions are open, falling back to the original shared `handleKeyDown` otherwise — so every other step/input in the wizard (all of which reuse the same shared handler) keeps its exact prior Enter-advances-the-wizard behavior, unchanged. Also added standard combobox/listbox/option ARIA wiring (`role="combobox"`/`aria-expanded`/`aria-controls`/`aria-activedescendant` on the input; `role="listbox"` on the dropdown; `role="option"`/`aria-selected` on each item, plus a visual highlight style and `onMouseEnter` sync so keyboard and mouse highlighting stay consistent) so screen readers can track the active suggestion too, not just sighted keyboard users. Verified via `npm run build` (clean) + an independent subagent review (PASS — independently traced `isStepValid`/`handleCitySelect`/the suggestion JSX from scratch to confirm the original block was real rather than trusting the description, independently traced the new handler's Enter/Escape/fallback-to-`handleKeyDown` logic and confirmed no other input in the file was affected, caught the ArrowUp wrap-around off-by-one from the `-1` initial state — fixed in a same-pass follow-up before commit, confirmed the pre-existing `hover:` classes on suggestion items weren't accidentally dropped, confirmed ARIA `id`/`aria-controls`/`aria-activedescendant` values actually match, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — this pass's research agent also swept `backend/main.py`/`models.py` in full, `admin/feedback.astro`/`admin/dashboard.astro` (no new bug beyond the already-documented, already-parked `item.url`-as-raw-`href` and plain-`===`-password-comparison nitpicks from iteration ~53), confirmed no `premium/` directory exists in this repo, and cross-checked all remaining `content/lexikon`/`content/ratgeber` pages numerically against `backend/rules/*.json`/`calculator-2026.js` with no drift found — so those areas are now considered covered; the rest of the site's accessibility/mobile-CSS beyond this one component was not exhaustively swept and remains a candidate area for a future pass. **Superseded entry, not re-flagging**: the below iteration-71 entry remains for history.
- [x] **UX/data-integrity fix: the "formloser Antrag" PDF download for Wohngeld/Lastenzuschuss users was generated from a broken template with fake baked-in content that collided with the real user's data (2026-08-04, dev-loop iteration 71)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; iteration 70 explicitly flagged that a from-scratch bug hunt was due), pointed away from the already-heavily-audited calculation engines/API routes/data files and toward less-audited surfaces (PDF generation, chat widget, accessibility, SEO metadata, mobile CSS, TS type errors). Both `src/components/trust-pdf-generator.jsx` (the `/pdf-paket` page, reached from the Wohngeld/Lastenzuschuss result cards) and `src/components/authority-app.jsx` (the "Amtly" assistant embedded in `results-section.astro`) built the Wohngeld/Lastenzuschuss download by loading `public/forms/TEST_Wohngeld.pdf` and filling 4 AcroForm text fields (`Vorname`/`Nachname`/`Strasse_Hausnummer`/`PLZ_Ort`) with the real user's name/address. That template also had **static, non-field text drawn directly onto the page**: a fake sender ("Anna Musterfrau, Beispielweg 7, 80331 München, E-Mail: anna@test.de", at x=395, outside any field) and a fake recipient ("Wohngeldbehörde München, Marienplatz 8, 80331 München", at x=70) — and the fake recipient text's y-coordinates (681.89/666.89/651.89) sit exactly inside the `Nachname` field's rect (`[59.5, 661.39, 260.5, 682.39]`) and the `Strasse_Hausnummer` field's rect (`[59.5, 631.39, 260.5, 652.39]`), confirmed by decompressing the PDF's content streams and cross-referencing against the AcroForm `/Rect` coordinates in the same file. So filling the form fields overwrote the fake recipient address with the applicant's own name/address, while the fake sender email and fake closing signature ("Anna Musterfrau") were left completely untouched — every real Wohngeld/Lastenzuschuss user who used this feature (in either of the two entry points) downloaded a broken, nonsensical letter: their own name/address duplicated in the recipient slot, addressed to nobody real, "signed" by a fictional person with a fake email, and always Munich regardless of the user's actual city. Fixed by removing the dependency on the broken static template entirely for the Wohngeld/Lastenzuschuss path in both files: each now builds a fresh one-page PDF from scratch with pdf-lib (proper DIN-letter layout — real sender, real recipient resolved from each component's own already-available city→authority data (`getAuthority()` from the previously-unused `src/utils/get-authority.ts` in `trust-pdf-generator.jsx`; the component's own existing `authority` state, already resolved elsewhere in the same file, in `authority-app.jsx`), date, subject, body, and a closing signed with the real applicant's name — no fabricated persona). The Bürgergeld path (the real, correct 16-page official BA `Hauptantrag_Buergergeld.pdf`) was left untouched in both files, just moved into an `else` branch; the two now-orphaned unconditional `form.flatten()` calls were correctly relocated so they only run for the Bürgergeld AcroForm path (the new Wohngeld path has no form to flatten). Deleted the now-unused `public/forms/TEST_Wohngeld.pdf`. Verified via `npm run build` (clean) + an independent subagent review (PASS — independently re-extracted and decompressed the template from git history to re-confirm the field/fake-text coordinate overlap byte-for-byte rather than trusting the description, verified `pdfDoc`/`form`/`y` variable scoping line-by-line in both diffs, confirmed the relocated `form.flatten()` calls are correctly branch-scoped, confirmed zero remaining references to `TEST_Wohngeld.pdf` anywhere in the repo, and reproduced the clean `npm run build` independently) before committing — the implementer did not self-approve. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — next iteration should run a fresh from-scratch bug hunt; candidate under-audited areas still include `backend/main.py`/`models.py` full audit, `admin/feedback.astro`/`admin/dashboard.astro`, `premium/*`, remaining `content/lexikon`/`content/ratgeber` pages not yet cross-checked against the rules JSON, and accessibility/mobile-CSS across the site (not deeply audited in iteration 71's scan either, just deprioritized in favor of the PDF bug).
- [x] **Data-correctness fix: the client-side JS fallback benefit engine silently dropped the disability Mehrbedarf (§21 Abs. 4 SGB II) for Bürgergeld/Grundsicherung im Alter, and its own constant was the wrong percentage anyway (2026-08-04, dev-loop iteration 70)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones; iteration 69 had explicitly flagged that a from-scratch bug hunt was due), pointed at areas not yet deeply audited (`backend/main.py`/`models.py` in full, `admin/*`, `premium/*`, layouts, content pages cross-checked against the rules JSON) and told to verify any candidate against a second in-repo source before reporting. `src/logic/benefit-engine.js`'s `BuergergeldModule.calculate()` and `GrundsicherungAlterModule.calculate()` both call `calculateBuergergeld()` (`src/logic/calculator-2026.js`) without ever passing `hasDisability` — even though the same file correctly threads `hasDisability`/`disabilityGdb` through for the sibling `WohngeldModule`/`LastenzuschussModule` calls to `calculateExactWohngeld()`. Since `calculateBuergergeld`'s `hasDisability` parameter defaults to `false`, this JS engine (the fallback used whenever the live `/api/v4/analyze` call fails/times out, and *always* for the Grundsicherung-im-Alter/pensioner estimate regardless of API success — confirmed via `smart-calculator.jsx`) never applied any disability Mehrbedarf at all for these two benefit types, despite the user having correctly checked "Behinderung" in the wizard. **A second, compounding bug was found in the same function while verifying the fix**: even where `calculateBuergergeld()` is correctly called with `hasDisability: true` (the pre-existing `calculateBestOption()` call site), the constant it added was `563 × 0.17` (17%), labeled "Schwerbehinderung Mehrbedarf" — but 17% is actually the *pregnancy* Mehrbedarf rate (`backend/rules/sgb2.json`'s `pregnancy_percent: 0.17`, confirmed live in `backend/engine.py`'s `is_pregnant` branch), not disability. The legally correct disability rate is 35% (§21 Abs. 4 SGB II), confirmed against `backend/engine.py`'s `is_disabled` branch (`total_need += rate * 0.35`) — the source-of-truth engine this JS fallback is meant to mirror. Real impact: a disabled user whose live-backend request fails, or any disabled pensioner (Grundsicherung im Alter always uses this JS path), got their Bürgergeld/Grundsicherung estimate understated by ~197€/month (35% vs. 0% of the 563€ Regelsatz) — even after the missing-flag fix alone, they'd still have been understated by ~102€/month (35% vs. the wrong 17%) had only the flag been wired up without also fixing the constant. Fixed with the minimal correct change: added `hasDisability: !!input.hasDisability` to both `calculateBuergergeld()` call sites in `benefit-engine.js` (matching the existing pattern used for Wohngeld/Lastenzuschuss two functions above), and changed `calculator-2026.js`'s constant from `563 * 0.17` to `563 * 0.35` with the comment corrected to cite §21 Abs. 4 SGB II. `calculateBestOption()` (the third, already-correct caller of `calculateBuergergeld`) and `calculateExactWohngeld()`'s own separate disability-allowance logic (unrelated code path, `gdbVal`/`isCareDependent`-based) were both confirmed untouched and unaffected. Verified via a standalone Node harness directly exercising `calculateBuergergeld()` (single person, 0€ income, 400€ rent, Mietstufe giving a 500€ rentLimit: no-disability case = 963€, with-disability case = 1160€, diff = 197€ ≈ 563×0.35 exactly) + `npm run build` (clean, only the pre-existing unrelated PDFButton chunk-size warning) + independent subagent review (PASS — independently re-confirmed the 35%/17% split by reading `backend/engine.py`/`backend/rules/sgb2.json` directly rather than trusting the description, independently re-derived 563×0.35=197.05€ and confirmed it matches the backend's `rate * 0.35`, independently grepped the whole repo for every `calculateBuergergeld(` call site and confirmed all three are now correctly wired with no fourth site missed, independently confirmed `calculateBestOption()` and `calculateExactWohngeld()`'s disability logic were untouched by the diff, flagged and had removed an incidental `npm install`-generated cosmetic `package-lock.json` diff before commit) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — next iteration should run a fresh from-scratch bug hunt in the areas this pass's research agent didn't have time to fully cover (`backend/main.py`/`models.py` full audit, `admin/feedback.astro`/`admin/dashboard.astro`, `premium/*`, remaining `content/lexikon`/`content/ratgeber` pages not yet cross-checked against the rules JSON).
- [x] **Repo hygiene / test-correctness pass (2026-08-04, dev-loop iteration 69)**: closed the two loose ends iteration 68 itself surfaced rather than mining for a new bug (both already precisely scoped, no fresh Explore scan needed). **(1) Stale test assertion, take 2**: iteration 68 fixed `verify_engine.py`'s Test 3 but left Test 2 (`check("Test 2: SGB2 amount", ..., 1718.2)`, low-income family of 4) failing with actual output `1711.7`, and *guessed* (without verifying) that the culprit was a Nachrangprinzip/§12a adjustment. Independently traced the real cause instead of trusting that guess: hand-computed `calculate_sgb2`'s full pipeline for this exact test case (RBS 506+506+390+390=1792 + Wohnkosten 1050 = total_need 2842.00; Erwerbstätigenfreibetrag for brutto=1800/has_child=True/cap=1500 → 100 + (603−100)×0.20 + (1000−603)×0.30 + (1500−1000)×0.10 = 369.70; anrechenbar = 1500−369.70 = 1130.30; raw_diff = 2842.00−1130.30 = **1711.70**, matching the actual output exactly) — and confirmed the *actual* culprit was iteration 64's `freibetrag_step1_limit` 538→603 change (re-deriving the old value under the pre-iteration-64 538€ boundary reproduces the old hardcoded 1718.2 exactly), not the Nachrangprinzip block, which was independently confirmed in `evaluate_all()` to only overwrite `sgb2["amount"]` when `kinderzuschlag["status"] == "eligible"` — Test 2's Kinderzuschlag status is `"possible"`, so that code path only appends Hinweis-text to the reason string and never touches the amount. Updated the assertion to `1711.7`. Verified via `python3 verify_engine.py` (all checks pass) + `python3 -m py_compile` on the touched file + `npm run build` (clean) + independent subagent review (PASS — independently re-hand-traced the same calculation from `engine.py`/`sgb2.json` from scratch, independently confirmed the Nachrang block's `"eligible"`-only gating via a live run showing the Hinweis-text-only branch fires, independently confirmed both RBS rates for the 8y/12y children resolve to `rbs_5`, confirmed via `git diff --stat`/`git status` that only this one line changed with no scope creep and no `package-lock.json` diff, ran its own independent `verify_engine.py`) before committing — the implementer did not self-approve. **(2) `result-roadmap.astro` follow-up, closed as moot**: iteration 68 flagged `src/components/result-roadmap.astro` as a likely-dead file worth confirming/removing in a future pass. Checked this pass: the file does not exist in the working tree (`find` for `result-roadmap.astro` returns nothing) and `git log --all -- src/components/result-roadmap.astro` shows it was already deleted in an earlier commit (`1d67d63`, pre-dating this dev-loop's numbered iterations) — the note in iteration 68 was itself stale by the time it was written (referring to a file that no longer existed), so no action was needed; recorded here only so the open nitpick isn't left dangling or re-flagged again. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — this iteration deliberately did not run a fresh broad Explore scan since it had two precisely-scoped, already-surfaced loose ends to close instead; a fresh from-scratch bug hunt is due next iteration.
- [x] **Repo hygiene / test-correctness pass (2026-08-04, dev-loop iteration 68)**: no new bug found via this pass's scan besides the two deliberately-parked open items, so the two smallest already-flagged-but-not-yet-fixed nitpicks from prior iterations were closed instead, run as two parallel worktree-isolated agents (no file overlap: one backend, one frontend), each independently reviewed by a fresh subagent before merge. **(1) Stale test assertion**: iteration 66's Elterngeld sliding-scale-rate fix left `backend/verify_engine.py`'s Test Case 3 (expectant mother, 450€ minijob income) asserting the old flat-rate outcome, `check("Test 3: Elterngeld amount", ..., 300.0)` — flagged as a non-blocking nitpick in iteration 66 and again in iteration 67, never closed. Independently hand-derived the correct value from `_get_elterngeld_rate()`/`calculate_elterngeld()` in `engine.py` (450€ falls in the sub-1.000€ tier: `rate = 0.67 + floor((1000-450)/2)×0.001 = 0.945`, `450×0.945 = 425.25€`, within the [300,1800] clamp) and updated the assertion to `425.25`, confirmed both by running the script and by an independent reviewer's from-scratch re-derivation (PASS). **(2) Confirmed-dead frontend files**: `src/utils/districtUtils.ts`, `src/data/services.ts`, and `src/components/authority-search.astro` were flagged as dead code (zero live call sites) back in iteration 55 but never removed. Re-verified independently this pass via repo-wide `git grep` for each filename and every exported symbol — all three confirmed to have zero references outside themselves; `authority-search.astro`'s only textual referrer, `result-roadmap.astro`, is itself unreachable (the live `authority-app.jsx` imports the separate `result-roadmap.jsx` React component instead, not the `.astro` file), confirmed empirically via a clean `npm run build` after deletion. All three deleted (212 lines). **Follow-up note, not part of this pass's approved scope**: `result-roadmap.astro` (the file left dangling an import to the now-deleted `authority-search.astro`) appears to be dead code itself, superseded the same way by `result-roadmap.jsx` — worth confirming and removing in a future hygiene pass. **Both worktree agents branched from a stale point ~99 commits behind the working branch tip** (a pre-existing quirk of how these worktree agents get created, not something either agent could see) — the orchestrating session caught this before merging (rebased the frontend-cleanup branch onto the tip; the backend branch had already self-corrected via its own fast-forward check), re-ran `npm run build` after each rebase/merge to confirm nothing broke, and merged both cleanly with no conflicts (different files) before committing. Both changes verified via `npm run build` (clean) + `python3 -m py_compile` on the touched backend files + independent subagent review (PASS on both) before committing — the implementer(s) did not self-approve. **New open nitpick surfaced while verifying the Test 3 fix, not fixed this pass** (out of the approved scope for this iteration — a different test case, a different underlying engine change): `verify_engine.py`'s Test 2 (`"Test 2: SGB2 amount"`, expected `1718.2`) now fails too, actual `1711.7` — caused by a separate, later change already on this branch (a Nachrangprinzip/§12a SGB II adjustment letting Wohngeld+Kinderzuschlag reduce the computed Bürgergeld amount), which made this assertion stale the same way iteration 66 made Test 3 stale. Needs the same treatment as this pass's Test 3 fix: verify the new `1711.7` (or whatever the current correct figure is) is actually correct before updating the assertion — noted here rather than guessed at. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the live Python backend's Elterngeld calculation silently used the household's current net income instead of the dedicated pre-birth income the wizard already collects, collapsing the estimate to the 300€ statutory floor for parents already on leave (2026-08-04, dev-loop iteration 67)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones), told to read iteration 66's Elterngeld entry in full first (to avoid re-flagging the sliding-scale-rate fix it already made) and to independently re-derive any candidate math rather than trust that the calculator is now fully correct. `src/components/smart-calculator.jsx`'s wizard collects a dedicated "Nettoeinkommen vor Geburt" field (state `netIncomeBeforeBirth`) plus a Basis/Plus toggle (`elterngeldOption`) specifically for Elterngeld — distinct from the general "Monatliches Netto-Einkommen (Haushalt)" field used for Bürgergeld/Wohngeld/Kinderzuschlag — and the JS fallback engine (`src/logic/benefit-engine.js`'s `ElterngeldModule.calculate`) already read `input.netIncomeBeforeBirth`/`input.elterngeldOption` correctly. But the `payload` object POSTed to the live backend (`/api/v4/analyze`) never included either field (confirmed via `HouseholdRequest` in `backend/models.py` having no such field and `backend/main.py` doing no remapping), so `backend/engine.py`'s `calculate_elterngeld()` fell back to summing `main_parent.incomes` — the household's *current* net income — as the calculation basis instead. Real impact: a parent already on parental leave (an extremely common case for this exact form section) typically enters €0 or a reduced figure as current income while entering their real, higher pre-birth income in the dedicated field — e.g. €0 current / €1.900 pre-birth: live backend collapsed straight to the 300€ statutory minimum (rate applied to 0 income = 0, clamped up to the floor) vs. the correct €1.235/month the JS engine would have shown, a €935/month (~76%) understatement, layered on top of and independent from iteration 66's rate-formula fix (which corrected the formula but not which income value feeds it — confirmed by re-reading that entry's own verification harness, which only exercised the rate helper on hand-picked income values and never checked which field sources it). A second, smaller gap was fixed in the same pass since it's the same missing-field cluster: the Basis/Plus toggle was also never sent, so the backend never applied the JS engine's "Elterngeld Plus halves the amount" rule at all. Fixed with the minimal correct change: added `net_income_before_birth: Optional[float] = None` and `elterngeld_option: str = "basis"` to `HouseholdRequest` (`backend/models.py`), added both keys to the frontend payload (`smart-calculator.jsx`), and in `calculate_elterngeld()` (`backend/engine.py`) used `request.net_income_before_birth` as the calculation basis when provided and >0, falling back to the previous current-income-sum behavior only when absent/zero — for backward compatibility with any other caller of the API that doesn't send the new field — then applied the sliding-scale rate exactly as before, clamped to [300, 1800], and halved the result if `elterngeld_option == "plus"`, ported to match the JS engine's clamp-then-halve order exactly (not halve-then-clamp, which would produce a different, wrong number). Verified via a standalone Python harness directly exercising `calculate_elterngeld()` (the €0-current/€1.900-pre-birth case reproducing the exact €1.235 figure instead of the old €300; the field-omitted-entirely and field-explicitly-zero cases both correctly falling back to reproduce the exact old current-income-sum behavior, confirming no breaking change for other callers; an Elterngeld-Plus case at €800 pre-birth confirming the €308 clamp-then-halve result) + `python3 -m py_compile backend/engine.py backend/main.py backend/models.py` + `npm run build` (clean, only the pre-existing unrelated PDFButton chunk-size warning) + independent subagent review (PASS — independently re-read `benefit-engine.js`'s `ElterngeldModule.calculate` line-by-line to confirm the Python port matches exactly including clamp/halve order, independently confirmed the new field names match `smart-calculator.jsx`'s existing state variables with no casing/typo mismatch, independently confirmed via `backend/main.py`'s `model_validate_json` usage that old/other API callers omitting the new fields safely default and hit the unchanged fallback path, wrote and ran its own independent Python test cases rather than trusting the harness output described here, ran its own independent `py_compile`/`npm run build`, confirmed via `git diff --stat`/`git status` that only the three intended files changed with no scope creep and no `package-lock.json` diff, checked for any other payload-builder in the codebase that might also need the new fields and found none) before committing — the implementer did not self-approve. **Non-blocking nitpick from the reviewer, not fixed this pass**: `backend/verify_engine.py` (a pre-existing manual verification script, not part of the build/CI) has a Test 3 assertion hardcoded to the old, now-superseded 300€ Elterngeld outcome — confirmed via `git stash` to already fail on the base branch before this change too (pre-existing, unrelated to this diff, not a regression), worth updating in a future pass to assert the new correct sliding-scale amount instead. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the live Python backend's Elterngeld calculation used a flat 65% income-replacement rate instead of the legally required §2 Abs. 2 BEEG sliding scale, understating Elterngeld for most low/middle-income households (2026-08-03, dev-loop iteration 66)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones), told to read this changelog in full first to avoid duplicating 65 prior iterations' coverage, then focus on under-audited areas and cross-verify any candidate against a second in-repo source of truth (or an external authoritative source) before reporting. `backend/engine.py`'s `calculate_elterngeld()` (backing the live, primary `/api/v4/analyze` path most real users hit — the JS engine only runs as a fallback on network failure) computed `estimated_amount = parent_income * self.elterngeld_rules["standard_percentage"]` with `standard_percentage` hardcoded to a flat `0.65` in `backend/rules/elterngeld.json`, applied uniformly regardless of income. Under §2 Abs. 2 BEEG, Elterngeld actually replaces up to 100% of pre-birth net income for incomes below 1.000€ (rising by 0.1 percentage points per full 2€ under 1.000€), a flat 67% between 1.000€-1.200€, tapers from 67% down to 65% between 1.200€-1.240€, and only flattens to 65% above 1.240€ — exactly what the site's own JS fallback engine, `src/logic/benefit-engine.js`'s `ElterngeldModule.calculate` (~lines 86-95), already correctly implements, confirming this was an isolated backend-only divergence (same bug class as iteration 61's Wohngeld table drift and iteration 63's Kinderzuschlag rate drift — live Python backend disagreeing with the already-correct JS fallback for identical households) rather than a site-wide misunderstanding of the law. Real impact: any household with pre-birth net income under 1.240€ (the majority of this site's low/middle-income target audience) got a systematically understated Elterngeld estimate on the live path — e.g. 800€ net income: correct = 800×0.77 = 616€/month vs. old (wrong) 800×0.65 = 520€/month, a 96€/month (~16%) understatement; at 500€ net income: correct = 500×0.92 = 460€/month vs. old 500×0.65 = 325€/month, a 135€/month (~35%) understatement — risking under-planned finances or distrust when users later got the correct, higher amount from the actual Familienkasse. Fixed with a new `_get_elterngeld_rate(net_income)` helper method in `engine.py` (added `import math` for the required `floor` step-function), ported line-for-line from the JS reference implementation, called from `calculate_elterngeld()` in place of the flat rate; `elterngeld_rules["standard_percentage"]` (0.65) is still live as the initial/base value and the effective rate for the >1.240€ case, not made dead. Verified via a standalone Python harness directly exercising `_get_elterngeld_rate` across all boundary values (0, 500, 800, 1000, 1100, 1200, 1220, 1240, 1500 — all matched hand-derived JS-equivalent output exactly, including the 616€/460€/975€ full end-to-end `calculate_elterngeld()` amounts for the 800€/500€/1500€ cases) + `python3 -m py_compile backend/engine.py backend/main.py backend/models.py` + `npm run build` (clean, frontend untouched) + independent subagent review (PASS — independently re-derived the sliding-scale formula straight from `benefit-engine.js` rather than trusting the description, cross-ran it in Node against the new Python method across 14 boundary values including off-by-one probes like 999/1201/1239/1241 and got byte-identical results, independently instantiated `SocialRuleEngine` itself and called both the new helper and the full `calculate_elterngeld()` rather than trusting reported output, confirmed `min_amount`/`max_amount` clamping and `expects_child`/`has_baby` eligibility gating still function correctly, confirmed via `git diff --stat`/`git status` that only `backend/engine.py` changed with no scope creep and no `package-lock.json` diff, ran its own independent `py_compile`, confirmed `standard_percentage` in the JSON rules file is still meaningfully read rather than dead) before committing — the implementer did not self-approve. Two non-blocking nitpicks from the reviewer, not fixed (pre-existing, not introduced by this change): (1) the JS engine has a distinct "Mindestelterngeld"-worded reasoning string for the `netIncome <= 0` case, while the Python backend reaches the same 300€ numeric outcome via the generic min-amount clamp with generic reasoning text — a message-parity cosmetic difference, not a numeric bug; (2) the JS engine hardcodes `0.65` directly rather than reading `elterngeld_rules.standard_percentage` from the shared JSON, so the two engines could silently re-diverge if that JSON value is ever changed without a matching JS edit — currently they agree, so no live bug, but worth a future refactor to have both engines read one shared source of truth. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the PDF cover-page "Berufl. Status" field mislabeled 8 of 9 occupational statuses as "Rentner/Pensionär" on official application documents (2026-08-03, dev-loop iteration 65)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones), pointed at areas not yet covered by iterations 1-64 — `feedback-widget.astro`/`admin/feedback.astro`/`admin/dashboard.astro`, `premium/index.ts`, `data/services.ts`/`city-insights.js`/`target-groups.js`/`faqs.js`, `utils/districtUtils.ts`/`get-authority.ts`/`slugify.ts`, all `content/lexikon/*.md` and `content/ratgeber/*.md` files re-cross-checked against `backend/rules/sgb2.json`/`benefit-engine.js`/`calculator-2026.js`, `authority-search.astro`/`authority-app.jsx`/`trust-pdf-generator.jsx`, and `api/chat.ts`/`checkout.ts` — explicitly told to verify any candidate against a second in-repo source of truth before reporting. `src/components/trust-pdf-generator.jsx` (the "TAXFIX-FEATURE" cover page pdf-lib inserts as page 0 of the real, downloadable government-application PDF, explicitly instructing the user to attach it and "send it to the responsible authority") computed the "Berufl. Status" row as a binary ternary: `savedState.input.status === 'employee' ? 'Angestellt/Arbeitssuchend' : 'Rentner/Pensionär'`. But `smart-calculator.jsx`'s "Beschäftigungsstatus" `<select>` (lines 713-721) offers 9 possible values (`employee`, `self_employed`, `unemployed_sgb2`, `unemployed_sgb3`, `seeking_work`, `student`, `trainee`, `pensioner`, `asylum_seeker`), persisted verbatim to `sessionStorage` and read back unmodified by the PDF generator — so every status except `employee` (self-employed, both flavors of unemployed, actively seeking work, student, trainee, asylum seeker) fell into the `else` branch and was printed as "Rentner/Pensionär" on an official-looking document meant to accompany a real application to a real authority, regardless of the applicant's actual, correctly-selected status — a real risk of confusing the caseworker or looking inconsistent with the rest of the (correctly filled) government form. Fixed with a `STATUS_LABELS` lookup object (module-level, in the same file) mapping all 9 status strings to the same German labels already used in `smart-calculator.jsx`'s own `<option>` list, with a `'Sonstiges'` fallback for any missing/unrecognized value (previously undefined behavior would have been a crash-free but still-wrong silent fallback into 'Rentner/Pensionär'; now it degrades to an honest "Other" instead of a specific wrong label). Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-derived the 9-value set directly from `smart-calculator.jsx`'s `<select>` rather than trusting the description and confirmed a 1:1 key match with no typos/missing/extra keys, independently traced `status` state → `profileInput.status` → `sessionStorage` persistence → `trust-pdf-generator.jsx`'s read-back to confirm the raw string is never transformed, independently confirmed the `'Sonstiges'` fallback doesn't crash on an undefined/missing status, independently checked the new labels' umlaut characters (ü, previously unproven in this file's `drawText` calls, only ä/ö/ß had been) against pdf-lib's WinAnsi-encoded `StandardFonts.Helvetica` and confirmed no real encoding risk since ü/Ü are standard Latin-1/WinAnsi code points in the same family as the already-proven ä/ö/ß, ran its own independent `npm run build`, confirmed via `git diff --stat`/`git status` that only this one file changed with no scope creep and no `package-lock.json` diff) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No further actionable open item found this pass** beyond the two deliberately-parked ones (fabricated testimonials needing owner decision; optional "User Accounts" feature, out of scope) — the research agent's scan of the areas above (feedback system, admin dashboard, utils, data files, all lexikon/ratgeber content re-verified against source-of-truth tables, authority search/PDF components, chat/checkout API routes) found no other confirmed bug; one speculative, unconfirmed CSS-cascade note on the feedback-widget star rating was explicitly not reported since it couldn't be verified without a live render.
- [x] **Data-correctness fix: the Bürgergeld Erwerbstätigenfreibetrag tier-1/tier-2 boundary was pegged to the stale 2025 Minijob-Grenze of 538€ instead of the current 2026 value of 603€, understating the earned-income allowance for anyone earning between 538€ and 603€ gross (2026-08-03, dev-loop iteration 64)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones), pointed at backend calculation functions not yet deeply audited, content pages under `src/content/lexikon`/`ratgeber` for stale legal thresholds, and told to verify any finding itself against another in-repo source of truth rather than trusting one location. The SGB II Erwerbstätigenfreibetrag (§11b Abs. 3 SGB II) has a 20%-tier that runs from 100€ up to the Minijob-Grenze (§8 Abs. 1a SGB IV), then a 30%-tier up to 1.000€. The Minijob-Grenze is itself indexed to the statutory minimum wage, which rose from 12,82€ to 13,90€/hour on 1.1.2026, raising the Minijob-Grenze from 538€ to 603€/month (officially confirmed by Deutsche Rentenversicherung/minijob-zentrale.de, Dec-2025 announcements) — but the codebase, which explicitly brands itself "2026", still hardcoded the old 538€ boundary in three independent places: `backend/rules/sgb2.json`'s `freibetrag_step1_limit` (feeding `backend/engine.py`'s `_calculate_freibetrag`, which backs the live `/api/v4/analyze` path), `src/logic/calculator-2026.js`'s JS fallback engine (both the tier-boundary logic itself and two separate `nettoFactor` income-estimation cutoffs reusing the same stale constant), and four content pages with worked examples (`src/content/lexikon/buergergeld-freibetrag.md`, `buergergeld-voraussetzungen.md`, `src/content/ratgeber/buergergeld-und-minijob.md`, `src/data/questions.json`). Real impact: for gross earned income between 538€ and 603€/month, the old code put that slice into the 30%-tier instead of the correct 20%-tier — e.g. at exactly 603€ gross, correct freibetrag = 100 + 503×0.20 = 200,60€ vs. the old (wrong) 100 + 438×0.20 + 65×0.30 = 207,10€, a 6,50€/month understatement of the *user's disposable freibetrag* (i.e. the old code was actually too generous in this narrow band — the opposite direction from most prior iterations' bugs — meaning the live calculator overstated how much a Bürgergeld recipient in this exact income slice gets to keep, and correspondingly understated how much is deducted from their Bürgergeld). Fixed with the minimal correct change: `538`→`603` in `sgb2.json`, with the JS engine's span constants recomputed (100-538/438 span → 100-603/503 span; 538-1000/462 span → 603-1000/397 span) and both `nettoFactor` cutoffs updated to match; the RENT_LIMITS Wohngeld rent-cap table in the same JS file, which coincidentally also contains the literal value 538 for an unrelated Mietstufe-2 rent ceiling, was deliberately left untouched (confirmed via an existing in-file comment plus independent review) since it has nothing to do with the Minijob-Grenze. Content pages' worked examples were recomputed from scratch for the new 603€ boundary rather than just find-replacing the number. **A second, unrelated pre-existing bug was found and fixed in the same pass** while independently recomputing `buergergeld-freibetrag.md`'s worked example (income 1.400€, no children stated): the example incorrectly applied the 10% step-3 freibetrag to the full 1.000€-1.400€ span (400€ → 40€), when the step-3 cap for a childless household is legally 1.200€ (only 1.500€ with minor children in the household per the file's own table two lines above) — so only 200€ of that span should receive the 10% freibetrag (20€), with the remaining 200€ (1.200€-1.400€) getting zero freibetrag, since it exceeds the cap entirely. This bug predates this iteration (present even under the old 538€ boundary — the example's stated total of 366,20€ was already wrong; the mathematically correct total under the old boundary would have been 346,20€) and was independent of the Minijob-Grenze fix, but was caught and corrected in the same edit since it was in the exact worked example already being touched. Verified via a standalone Python harness directly exercising `_calculate_freibetrag`'s logic against `sgb2.json` (603€ case reproducing the exact 200,60€ figure; 1.400€ case reproducing the corrected 339,70€ total with the step-3 cap correctly applied) + `python3 -m py_compile backend/engine.py backend/main.py backend/models.py` + `npm run build` (clean) + independent subagent review (PASS — independently recomputed every changed number from scratch rather than trusting the description, independently confirmed the RENT_LIMITS table's coincidental 538 was correctly left untouched, independently confirmed `engine.py` has no other hardcoded duplicate of the limit (reads only from the JSON rules), independently confirmed no scope creep via `git diff --stat`/`git status` and no `package-lock.json` diff, ran its own independent `py_compile`/`npm run build`, and did flag one non-blocking nitpick — two stale `# ... (100-538€)`/`# ... (538-1000€)` comments in `engine.py` that still said 538 despite the logic reading from the (now-fixed) JSON — fixed in a same-pass follow-up since it was a trivial one-line-each doc-only change in a file already part of this diff) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the live Python backend's Kinderzuschlag calculation deducted 50% of parents' excess income instead of the legally correct 45%, systematically understating the benefit (2026-08-03, dev-loop iteration 63)**: found via a fresh targeted research-agent scan (brain.md had no actionable open item besides the two deliberately-parked ones), pointed at re-checking recently-touched files for regressions, other hardcoded-constant drift like iteration 55's "168€" bug, stale legal thresholds in `src/content/lexikon`/`ratgeber`, and other client-trust gaps — explicitly told to verify any finding itself rather than trusting a single source, and to avoid re-flagging the two parked items or re-mining dead code. `backend/engine.py`'s `calculate_kinderzuschlag()` (line ~421) computed `amount = max(0.0, max_kiz - (excess * 0.5))`, deducting 50% of the parents' income exceeding their own SGB-II-style need (`excess = parent_earned_income - parent_need`) from the maximum Kinderzuschlag. Under §6a Abs. 3 Satz 3 BKGG the correct Anrechnungsquote is 45% (55% of the excess stays anrechnungsfrei) — confirmed against the statute and cross-checked against the site's own already-correct JS fallback engine, `src/logic/benefit-engine.js:143` (`const parentIncomeReduction = exceedingParentIncome * 0.45;`), which agrees with the backend on every other Kinderzuschlag constant (parent need, 297€/child max, min-income thresholds), making this an isolated single-constant divergence. Since `calculate_kinderzuschlag()` backs the live `/api/v4/analyze` path that `smart-calculator.jsx` calls first on every submit (the JS engine only runs on network failure), any working low-to-moderate-income family whose earned income exceeds their own need — a normal, non-edge-case outcome for Kinderzuschlag's actual target group — got a real amount understated by 5 percentage points of their excess income, never overstated (50% always deducts more than 45%). Example: single parent, 1 child, rent 500€, net income 1200€ → old (wrong): 153.50€/month; correct: 167.85€/month, a ~14€ (~9%) monthly underpayment estimate for this case, growing with the excess. Fixed with the minimal correct change: `excess * 0.5` → `excess * 0.45`, comment updated to cite §6a Abs. 3 Satz 3 BKGG; no other constant in the function touched. Verified via a standalone Python harness directly exercising `SocialRuleEngine.calculate_kinderzuschlag` (single-parent/1-child/1200€-income case reproducing the exact 167.85€ figure) + `python3 -m py_compile backend/engine.py backend/main.py backend/models.py` + `npm run build` (clean, frontend untouched) + independent subagent review (PASS — independently confirmed the 45% statutory rate, independently re-read `benefit-engine.js` to confirm the JS engine already used 0.45, independently read the full `calculate_kinderzuschlag` function to confirm no other logic was affected, independently confirmed via `git diff --stat`/`git status` that only this one file/2 lines changed with no scope creep and no `package-lock.json` diff, ran its own independent `py_compile`, independently sanity-checked the arithmetic) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Routing fix: two live client-side `fetch()` calls to internal API routes were missing the trailing slash required by this site's Astro config, silently 404ing every real submission (2026-08-03, dev-loop iteration 62)**: found via a fresh targeted Explore scan (since brain.md had no actionable open item besides the two deliberately-parked ones), this time specifically instructed to grep prior iteration entries for "not yet audited" hints and to sweep `src/pages/api`, `backend/*.py`, `vercel.json`, `src/data/*`, `src/lib/*`, admin pages, and `chat.ts`/`chat-widget.jsx` rather than re-scanning the ~55 areas already covered by name across iterations 1-61. `astro.config.mjs` sets `output: 'server'` (Vercel adapter) together with `trailingSlash: 'always'`, with no `src/middleware.ts` anywhere in the repo to normalize paths — under this exact combination, a server API route defined at `src/pages/api/foo.ts` only matches a request to `/api/foo/` (trailing slash); a request to `/api/foo` (no slash) 404s before ever reaching the route handler (a documented upstream Astro/Vercel behavior, withastro/astro#9206). Most `fetch()` calls in this codebase already correctly include the trailing slash (`src/components/lead-modal.astro:348` → `/api/leads/`, `src/components/authority-app.jsx:305` → `` `/api/send-application/` ``), but two callers didn't: `src/pages/avgs.astro:377` called `fetch('/api/leads')` (no slash) — the lead-capture form on the live `/avgs/` landing page, meaning every real submission 404'd and the user's name/email/phone/interest was silently never written to Supabase or forwarded to the Make.com webhook, just a generic client-side failure shown instead; and `src/components/wba-reminder-modal.astro:175` called `fetch('/api/wba-reminder')` (no slash) — this modal is embedded on 3 live pages (`index.astro`, `kindergeld/index.astro`, `grundsicherung-im-alter/index.astro`) and is the *sole* caller of that endpoint, so every real WBA-reminder submission 404'd before ever reaching `src/pages/api/wba-reminder.ts`'s validation/rate-limit logic (hardened for correct error-message surfacing as recently as iteration 58) — the reminder was never actually scheduled, regardless of how correct that downstream logic is. Confirmed via `git log` that both regressions are long-standing, present since each file/feature's original commit, not a recent regression — meaning these two forms have likely been silently broken in production the whole time; a plain `npm run build` never would have caught this since it's a routing-path string mismatch, not a compile error. Fixed with the minimal correct change: added the trailing slash to each of the two `fetch()` URL string literals, matching the convention already used correctly by every other internal-API caller in the codebase (confirmed via a repo-wide grep of every `fetch('/api/...')`/`` fetch(`/api/...`) `` call that these were the only two missing it; `smart-calculator.jsx`'s `fetch(apiUrl, ...)` targets the separate external Render-hosted Python backend, not an Astro route, so it's correctly out of scope and unaffected by this Astro-specific behavior). Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed via `git diff` the change is exactly these two one-line edits with no scope creep, independently re-confirmed `astro.config.mjs`'s `output`/`trailingSlash` settings and the absence of any `middleware.ts`, independently confirmed both target routes (`api/leads.ts`, `api/wba-reminder.ts`) exist, independently re-read both sibling callers to confirm the trailing-slash convention claim rather than trusting the description, independently checked `vercel.json` for any rewrite/redirect that might have already made the old no-slash URLs work anyway — confirmed none exists, so this fix is not a no-op, ran its own independent `npm run build`, independently grepped the whole `src/` tree for any other missed `/api/*` fetch call and found none) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the live Python backend's Wohngeld formula (§19 WoGG Rasterformel) used an outdated/wrong Anlage 1 (rent-limit) and Anlage 2 (a/b/c coefficient) table, disagreeing sharply with the site's own JS fallback engine for identical households (2026-08-03, dev-loop iteration 61)**: found via a fresh targeted Explore scan (since brain.md had no actionable open item besides the two deliberately-parked ones), pointed at `backend/engine.py`/`main.py` in full plus areas not yet named in this changelog. `backend/rules/wohngeld.json`'s `rent_caps`/`coefficients` and `backend/engine.py`'s `calculate_wohngeld` implemented the correct-looking formula shape (`WG = 1.15 × (M − (a + b·M + c·Y)·Y)`, the `1.15` factor confirming this was at least nominally updated for the 2023 Wohngeld-Plus reform) but with numerically wrong table values — e.g. household size 2's coefficients were `a=0.057` in the backend vs. the legally correct `a=0.030`. **Externally verified against the actual law** (web search against Anlage 2 WoGG text, not just trusting either in-repo table): household size 2's official coefficients are exactly `a=0.030, b=0.0003571, c=0.0000304` — matching `src/logic/calculator-2026.js`'s `WOGG_COEFFS[2]` byte-for-byte, confirming the frontend's JS fallback engine (used by `smart-calculator.jsx` whenever the live `/api/v4/analyze` call times out/fails — a real, regularly-exercised path per its own 35s-timeout/cold-start-warning handling, not hypothetical) already had the correct, current table, while the backend's was stale. The backend's rent-limit table was also missing the entire Heizkosten-/Klimakomponente the 2023 reform folded into the official Mietobergrenze (Anlage 1) — `calculate_wohngeld` never referenced `request.rent_heating` at all (unlike `calculate_sgb2`/the SGB2-Bedarf-für-Eigentümer path, which do use it correctly), and its old rent-cap numbers (e.g. 359€ for size 1/Mietstufe 1) were far below the frontend's reform-era, heating-inclusive limit (491€ for the same cell) — reproducing a live case (single person, Mietstufe 1, net income 800€, rent 400€) showed the backend returning ≈137€ Wohngeld vs. the JS engine's ≈399€ for the identical household, a ~3x gap purely from stale tables. Fixed with the architecturally correct change (not a data patch that would drift again): replaced `wohngeld.json`'s `rent_caps`/`coefficients` with `rent_limits`/`extra_person_limit`/`heating_pauschale`/`heating_pauschale_extra_person`/`coefficients`, transcribed to match `calculator-2026.js`'s already-in-production, now-externally-spot-verified `RENT_LIMITS`/`EXTRA_PERSON_LIMIT`/`WOGG_COEFFS` tables exactly (coefficients extended from household sizes 1-5 to the full official 1-12 range); added three helper methods to `engine.py` (`_get_wohngeld_rent_limit`/`_get_wohngeld_heating_pauschale`/`_get_wohngeld_coefficients`) mirroring the frontend's `getRentLimit`/`getHeizkostenPauschale` logic exactly (base value for household size ≤5, `+ (hh_size − 5) × extra_value_at_that_mietstufe` for larger households); reworked `calculate_wohngeld`'s rent-capping so the official Heizkostenpauschale is correctly split out before capping the user's actual cold+utility rent against the reform-era limit and then added back into the final `M` used in the formula (identical structure to the frontend's `M_limit_kalt`/`M_kalt`/`M` derivation) — this also incidentally hardens a pre-existing latent bug where an empty `members` list could have caused a `KeyError` on the old `coefficients["0"]` lookup, via a `safe_hh_size = max(1, hh_size)` guard now threaded through all three new helpers. Verified via a standalone Python harness directly exercising `SocialRuleEngine` (single-person/Mietstufe-1 case reproducing the ~137€→281€ improvement, the household-size-5/Mietstufe-7 table boundary, the household-size>5 extra-person-extension path, and an explicit empty-`members` edge case — all ran without exceptions) + `python3 -m py_compile backend/engine.py backend/main.py backend/models.py` + `npm run build` (clean, frontend untouched) + independent subagent review (PASS — independently re-derived the extra-person rent-limit indexing against `calculator-2026.js`'s `getRentLimit` line-by-line rather than trusting the description, independently confirmed the `safe_hh_size` guard actually reaches all three new helpers by testing an explicit empty-members request end-to-end, independently confirmed every JSON key the code looks up actually exists, independently verified the `estimated_amount` cap against the now heating-inclusive `M` is the intended correction and not an accidental loosening by checking `amount <= M` held in every eligible test case, confirmed via `git diff --stat`/`git status` that only the 2 intended files changed with no scope creep and no `package-lock.json` diff, ran its own independent `py_compile`) before committing — the implementer did not self-approve. One minor non-blocking observation from the reviewer, not fixed (true parity was never the claim): the frontend rounds its final `M` to whole euros while the backend now rounds to 2 decimal places — same structure, sub-€1 rounding-granularity difference only, not a defect. **Important scope boundary, deliberately NOT fixed this pass — the single largest remaining driver of backend-vs-frontend Wohngeld divergence**: this fix closes the rent-limit/coefficient *table* drift, but a separate, deeper methodology gap remains and was NOT touched: `calculate_wohngeld` uses each household member's raw, self-reported `amount_net` directly as `Y` in the formula, with zero WoGG-specific deductions — no Alleinerziehenden-Entlastungsbetrag (110€), no Kinderabzugsbetrag (100€/child), no Schwerbehinderung-Zuschlag (150€), no Werbungskostenpauschale (102.50€ for employees), and no employment-status-based Pauschale (0.7/0.8/0.9 for employee/self-employed/pensioner) — all of which `calculator-2026.js`'s `calculateExactWohngeld` already correctly applies before deriving its `Y`. Confirmed via reproducing the same nominal 800€-income case with tables now aligned: backend still returns 259€ vs. the frontend's 371€ for the identical nominal input, entirely attributable to this income-side gap, not tables. This is a materially different, larger undertaking than this iteration's fix — it isn't a matter of copying more constants, it requires a product/architecture decision (should the API contract change to accept gross income + let the backend compute the reform's WoGG-specific deductions server-side, mirroring how `calculate_sgb2` already derives its own SGB2-specific `_calculate_freibetrag` from brutto rather than trusting a client-supplied net figure — the same backend file already has two inconsistent income-netting philosophies for two different benefit types) before it can be safely attempted; worth a dedicated future iteration, not a quick follow-up. **Also noted in passing, not itself a bug**: the frontend's `getHeizkostenPauschale`/heating-pauschale mechanic is a legally-fixed flat amount by household size, not the user's actual reported heating cost — so `request.rent_heating` correctly has no role in the Wohngeld formula (unlike its correct, existing use in the SGB2 accommodation-cost calc); this was double-checked before concluding it wasn't an oversight, since the field's total absence from `calculate_wohngeld` initially looked like a missed-input bug.
- [x] **Content fix: literal Markdown `**...**` bold syntax was rendering as visible stray asterisks on the live, public `/grundsicherung-im-alter/` SEO landing page (2026-08-03, dev-loop iteration 60)**: found via a fresh targeted Explore scan (since brain.md had no actionable open item besides the two deliberately-parked ones), pointed at areas not yet heavily audited across 59 prior iterations: `authority-app.jsx`, `trust-pdf-generator.jsx`, `pdf-paket.astro`, `admin/dashboard.astro`, `erfolg.astro`/`danke.astro`/`abbruch.astro`/`agb.astro`/`avgs.astro`/`bildung-und-teilhabe.astro`/`ueber-uns.astro`/`design-system.astro`, `lib/rate-limit.ts`/`email.ts`/`supabase.ts`/`utils.ts`/`pdf-generator.ts`/`html-escape.ts`, `data/city-insights.js`/`target-groups.js`/`authorities.json`/`benefits.json`/`cities_2026.json`/`city_districts.json`/`faqs.js`/`questions.json`, `utils/get-authority.ts`/`slugify.ts`, `content/lexikon/*.md`/`content/ratgeber/*.md`, remaining `backend/*.py`. `src/pages/grundsicherung-im-alter/index.astro` (a plain Astro/HTML page with no Markdown renderer or `set:html` anywhere in the file — confirmed via direct read) had a copy/paste slip in its "Müssen Kinder für die Grundsicherung aufkommen?" FAQ paragraph: `**100.000 Euro**` using Markdown bold syntax instead of the `<strong>` tag the same file already uses consistently elsewhere (e.g. `<strong>Schonvermögen von 10.000 Euro</strong>` two paragraphs above). Since nothing in this file's render path processes Markdown, real visitors to this live, indexed landing page saw the literal characters `**100.000 Euro**` instead of bold text — confirmed by inspecting the built `dist/client/grundsicherung-im-alter/index.html` output both before and after the fix. Confirmed via a repo-wide grep for `\*\*[^*]+\*\*` across all `.astro` files that this was the only occurrence of the bug, not a systemic template issue. Fixed with the minimal one-line change: replaced `**100.000 Euro**` with `<strong>100.000 Euro</strong>`, matching the file's existing pattern exactly; no other text in the paragraph (including the "34,31 Euro" pauschalierter-Unterhaltsbeitrag figure) was touched. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the file has no Markdown-processing step so the bug was real and user-visible, independently confirmed `<strong>` is the file's existing pattern via its own grep, independently ran its own repo-wide grep confirming no other `.astro` file has the same stray-asterisks bug, independently confirmed only the two `**` markers changed with all surrounding German text/figures byte-identical and tags properly paired, ran its own independent `npm run build`, confirmed via `git status`/`git diff --stat` that only this one file/2 lines changed) before committing — the implementer did not self-approve. **Dead-data note surfaced during this scan, not acted on**: `src/data/benefits.json` mislabels "Grundsicherung" with `legal_basis: "SGB II / Reform 2026"` and `agency_type: "Jobcenter"` (should be SGB XII / Sozialamt) — factually wrong, but the file has zero imports anywhere in the codebase (confirmed via grep), so it's inert dead data with no live effect; worth deleting or correcting in a future repo-hygiene pass, not urgent since nothing reads it. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: the site-wide AI chat widget's hardcoded Wohngeld-Mietstufe city table had drifted out of sync with the site's canonical per-city data, giving visibly wrong Wohngeld estimates for ~20 of ~44 cities (2026-08-03, dev-loop iteration 59)**: found via a fresh targeted scan (delegated to an Explore subagent, pointed at `chat-widget.jsx`, `faq.astro`, `footer.astro`, `partner-cta.astro`, `result-roadmap.jsx`, `success-roadmap.astro`, `seo/*.astro`, `ui/*.jsx`, `content/config.ts`, `data/*.js`, `layouts/datenschutz.astro`/`impressum.astro`, several standalone pages, `utils/get-authority.ts`/`slugify.ts`, `backend/verify_engine.py`, `lib/supabase.ts`/`utils.ts`/`pdf-generator.ts`/`rate-limit.ts`/`email.ts`, `api/chat.ts`, `api/checkout.ts` — areas not yet deeply audited across 58 prior iterations — since brain.md had no other actionable open item besides the two deliberately-parked ones). `src/components/chat-widget.jsx` (mounted site-wide via `client:load` in `layout.astro`, live on every page) maintained its own hardcoded `CITY_MIETSTUFE` city→Mietstufe (Wohngeld rent-level tier, 1-7) lookup table, used by its `calcWohngeld()` function to estimate a user's Wohngeld entitlement from free-text chat input (e.g. "Ich verdiene 1500€, zahle 600€ Miete und wohne in Berlin"). This table had drifted independently from `src/data/wohngeld-data.js` — the site's canonical, per-city dataset (paired with real PLZ/authority contact info) that drives the real `/wohngeld/<stadt>` landing pages and is already imported the same way by `smart-calculator.jsx`. Confirmed via direct comparison: Berlin was 6 in the chat widget vs. 4 in the canonical data; Köln 5 vs. 6; Stuttgart 6 vs. 5; Bonn 4 vs. 6; Heidelberg/Freiburg 5 vs. 6; and roughly 20 of the ~44 hardcoded cities disagreed, several by 2 full tiers. Since Mietstufe directly indexes `WG_MAX_RENT` (e.g. 1 person: Stufe 4 = 469€ vs. Stufe 6 = 619€ rent ceiling, a 150€/month swing), any visitor asking the chatbot about Wohngeld in one of these cities got a materially different euro estimate than the site's own dedicated city page or the main calculator would produce for the identical inputs — understating entitlement for some cities (risk of discouraging a legitimate applicant) and overstating it for others (risk of a false expectation later corrected by the real calculator, undermining trust). Fixed with the architecturally correct change rather than a manual data patch (which would just reintroduce the same drift risk next time either table is updated): `CITY_MIETSTUFE` is now derived from `wohngeldData` (`Object.fromEntries(wohngeldData.map(c => [c.stadt.toLowerCase(), c.mietstufe]))`), so the two data sources can no longer disagree — this also expanded chat-widget city recognition from ~44 to ~101 cities for free, since the canonical dataset covers far more cities than the old hardcoded list. Kept 3 small manual additions: `'munich'`/`'cologne'` English aliases (resolved via `.find()` against the canonical München/Köln entries, not re-hardcoded numbers) and `'halle'` (wohngeldData's entry is literally named `"Halle (Saale)"`, so a plain `.toLowerCase()` match wouldn't hit it — resolved the same way via `.find()`). Kept `'potsdam': 4, 'bochum': 3, 'leverkusen': 3` as genuine manual fallbacks, confirmed via grep that these 3 cities have no entry in `wohngeldData` at all (no canonical landing page exists for them), so there's nothing to derive from yet. Verified via `npm run build` (clean) + a standalone Node script computing the full derived table and confirming all previously-mismatched cities now match `wohngeld-data.js` exactly, no `.find()` call returns `undefined` (which would throw a module-load-time `TypeError` and break the widget — and possibly the page — for every visitor), and all 101 resulting Mietstufe values fall within `WG_MAX_RENT`'s valid 1-7 index range + independent subagent review (PASS — independently re-read `wohngeld-data.js` and confirmed JS `.toLowerCase()` correctly handles German umlauts, independently confirmed both `.find()` targets exist exactly once in the data file so neither throws, independently confirmed Potsdam/Bochum/Leverkusen are genuinely absent from the canonical dataset via its own grep, independently traced every other `CITY_MIETSTUFE` read site in the file to confirm none assumes a fixed table size/contents, independently confirmed `smart-calculator.jsx` already imports `wohngeld-data.js` the identical way with no issue, confirmed via `git diff --stat`/`git status` that only this one file changed with no scope creep, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **UX fix: the WBA-reminder modal discarded the backend's specific validation error and always showed a hardcoded generic "transmission error" alert (2026-08-03, dev-loop iteration 58)**: found via a fresh targeted scan (delegated to an Explore subagent, after a separate scoping pass determined the previously-flagged "Pflegegrad/Bestattungsvorsorge" runner-up — see correction note on iteration 56/57's entries below — was too ambiguous for an unattended pass and had to be skipped; the scanner was pointed at `src/pages/api/chat.ts`, `checkout.ts`, `backend/main.py` beyond already-fixed spots, `src/components/ui/*`, remaining `src/data/*`, and any component not yet named in this changelog). `src/components/wba-reminder-modal.astro`'s submit handler only checked `response.ok`; on any non-ok response it threw a generic `Error('API Error')` and the catch block always showed the same hardcoded alert ("Es gab einen Fehler bei der Übertragung. Bitte versuchen Sie es später erneut."), discarding the response body entirely. But `src/pages/api/wba-reminder.ts` (hardened in iteration 37) returns specific, correctly-localized JSON error messages for real, reachable cases: a past Bescheid end-date ("Das gewählte Enddatum liegt in der Vergangenheit."), invalid email/month/year, and a rate-limit hit (`src/lib/rate-limit.ts`'s `rateLimitResponse()`, "Zu viele Anfragen..."). Since this modal's month `<select>` always lists all 12 months regardless of the selected year (only the year dropdown is date-aware), a user filling the form partway through a year — e.g. picking "März 2026" in August 2026 — reliably hits the past-date rejection and was told to "try again later," advice that can never help since the real problem is the date they picked, not a transmission fluke. This modal is live and reachable from 3 real pages (`index.astro`, `kindergeld/index.astro`, `grundsicherung-im-alter/index.astro`). Fixed with the minimal correct change confined to this one file: the non-ok branch now attempts `await response.json()` and uses `data.error` as the alert message if present, keeping the original generic message as a fallback only if the body isn't valid JSON (e.g. a genuine infra-level error); genuine network/fetch exceptions (the outer `catch`) are untouched and still show the generic message, which remains correct for that case. Mirrors the existing `alert("Fehler beim Versenden des Antrags: " + (data.error || 'Unbekannt'))` pattern already used in `authority-app.jsx`. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-read every non-ok path in `wba-reminder.ts` and `rate-limit.ts` and confirmed all of them return valid JSON with a populated `error` string so `data.error` is always set in realistic cases, independently confirmed the inner try/catch never rethrows so there's no double-alert, independently confirmed the unchanged `finally` block still re-enables the submit button regardless of which alert path fires, independently checked whether a raw Supabase error could ever leak into the client-visible `error` field and confirmed it can't (`supabaseError` is only `console.error`'d server-side; the route still returns 200 in that case), confirmed via `git diff --stat` that only this one file changed with no scope creep, ran its own independent `npm run build`) before committing — the implementer did not self-approve. One real, low-severity nitpick the reviewer flagged was fixed in the same pass: the non-ok branch no longer left any console trace (only the outer network-exception catch still logged), unlike before the fix where every non-ok response threw and got logged; added a `console.error('WBA reminder submission rejected:', response.status, message)` call alongside the alert so non-ok responses (e.g. rate-limit hits, validation rejections) remain visible in the browser console for debugging, re-verified via a second `npm run build` (clean) after this follow-up edit. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Security/data-integrity fix: `src/pages/api/leads.ts` trusted client-submitted `partner_vertical`/`consent_partner_transfer` verbatim with no server-side derivation from `modal_type`, allowing a forged partner-consent record (2026-08-03, dev-loop iteration 57)**: this exact gap was first flagged by iteration 51's independent reviewer and repeated as a "runner-up candidate, not pursued" note in iterations 55/56, but never itself closed — picked up this pass since brain.md had no other actionable open item besides the two deliberately-parked ones. `src/components/lead-modal.astro`'s shared modal (`window.openLeadModal(type)`) only ever sets `#avgs-partner-vertical` to `'legal'` and shows the `#partner-consent-container` GDPR-style checkbox for the `legal_help` branch — every other modal type (`avgs`, `application`, `kiz`, `elterngeld`, `pdf-checklist`, `pflege`) resets both to blank/unchecked at the top of the function. But the public, unauthenticated `/api/leads` endpoint read `data.partner_vertical`/`data.consent_partner_transfer` straight from the client JSON body with zero cross-check against `modal_type` — so a direct API caller bypassing the UI entirely could POST e.g. `modal_type: "pdf-checklist"` with `partner_vertical: "legal"`/`consent_partner_transfer: true` and get a fabricated "user consented to have their data forwarded to an external legal partner" record inserted into Supabase and forwarded to the external Make.com webhook, for a request that never showed the user any such consent UI. Fixed with the minimal correct change confined to `leads.ts`: both fields are now derived purely server-side from `modal_type === 'legal_help'` instead of being read from client JSON at all; the Make.com webhook forward body (which spreads `...data` first, then applies explicit override keys) now explicitly includes both derived values after the spread so a raw forged client field can't leak through that path either (previously `consent_partner_transfer` had no override key there at all, only `partner_vertical` did). Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-read `lead-modal.astro`'s `openLeadModal` to confirm exactly which branch sets/shows the consent fields and that all others reset them, independently confirmed the client already coerces the checkbox to a real JSON boolean (`data.consent_partner_transfer === 'on'`) before the fetch so the server's `=== true` comparison is valid rather than silently always-false, independently confirmed JS object-literal override semantics mean the webhook body's late explicit keys win over the early `...data` spread for both fields, confirmed via `git diff --stat`/`git status` that only this one file/13 lines changed with no scope creep and no `package-lock.json` diff, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Runner-up candidate still open, not pursued this pass**: the backend-parity gap for Pflegegrad (`hasCareDependent`/`careDependentGrad`) and Bestattungsvorsorge (`isBereaved`) — collected in the frontend wizard and used by the local JS fallback engine's Wohngeld-Mehrbedarf/care-benefit modules, but never sent in the payload to or ported into `backend/engine.py`'s live calculation path — first noted in iteration 56's runner-up list, still unaddressed; larger/riskier scope than this iteration's fix (mirrors the multi-benefit-type porting done in iterations 34/48/56, not a single-file change), worth a dedicated future iteration. **Correction from iteration 58, this framing was wrong — do not re-attempt as a simple port without a product decision first**: a dedicated scoping pass (iteration 58) found this description conflates two unrelated things and isn't the clean mechanical port iterations 34/48/56 were. (1) `isBereaved` is mislabeled here as "Bestattungsvorsorge" — it actually has nothing to do with funeral-cost provision (which doesn't exist as a concept anywhere in this repo, confirmed via a repo-wide grep for "Bestattung" returning zero hits); its only real use in `benefit-engine.js` is a standalone Hinterbliebenenrente (Witwen-/Waisenrente, survivor's pension) suggestion card, entirely unrelated to Wohngeld/SGB2/Kinderzuschlag Mehrbedarf, and porting it would mean building a brand-new backend benefit-type/endpoint, not adding a field to an existing calculation. (2) `hasCareDependent`/`careDependentGrad`'s only Wohngeld/SGB2-relevant effect in the JS engine is the §17 WoGG 150€/month income allowance inside `calculateExactWohngeld` — but `backend/engine.py`'s `calculate_wohngeld` is already missing several *other* JS-engine allowances (single-parent 110€, per-child 100€, Werbungskosten 102.50€) unrelated to care-dependency, so porting *only* the care allowance would produce a partial, still-inconsistent mirror rather than the full 1:1 parity the earlier iterations' pattern implies — a real scope judgment call, not a mechanical fix. (3) The JS engine's other 6 standalone Pflege-related modules (Pflegegeld, Pflegesachleistung, Entlastungsbetrag, Landespflegegeld, Wohnumbau, Pflegeunterstützungsgeld) have no backend home at all; including them means new calculation functions and a bigger API response shape, a product decision, not a bugfix. Also noted in passing: `backend/models.py`'s `Person` already has an unused, unwired `is_cared_for: bool` field (never populated by the frontend, never read in `engine.py`) — worth knowing before assuming this needs a brand-new field. **Before attempting this again**: get an explicit decision on (a) whether Hinterbliebenenrente/`isBereaved` belongs in the backend at all, and (b) whether the care-allowance fix should be scoped to just the §17 WoGG allowance (and if so, whether to fix `calculate_wohngeld`'s other missing allowances in the same pass to avoid a worse partial-parity state) or the full 7-module port.
- [x] **Data-correctness/eligibility fix: the live Python backend (`/api/v4/analyze`) had zero Vermögensprüfung (asset/Schonvermögen check) for Bürgergeld, Wohngeld and Kinderzuschlag, silently granting a real €/month eligibility to households legally excluded by their assets (2026-08-03, dev-loop iteration 56)**: found via a fresh targeted scan (delegated to an Explore subagent pointed at recent "Runner-up candidates" noted-but-not-pursued in iterations 47-55, areas of the codebase not yet mentioned in any prior iteration entry, a repo-wide TODO/FIXME sweep, and a numeric cross-check of legal thresholds in content vs. the calculation engines — since brain.md had no actionable open item left besides the two deliberately-parked ones). `backend/models.py`'s `HouseholdRequest` had no `assets`/`vermoegen` field of any kind (confirmed via a case-insensitive grep for `asset|vermög|vermoeg|schonverm` across `backend/*.py` → zero matches), and `backend/engine.py`'s `calculate_sgb2()`/`calculate_wohngeld()`/`calculate_kinderzuschlag()` contained no asset check whatsoever. Meanwhile `src/components/smart-calculator.jsx`'s Step 4 wizard has a live UI field ("Vermögen / Ersparnisse (Haushalt)") with a tooltip explicitly warning *"...oberhalb kann der jeweilige Anspruch entfallen"* — the value was collected into React state (`assets`/`hasHighAssets`) and folded into `profileInput` for the local JS engine, but the `payload` object actually POSTed to the live backend never included it, and there was no backend field to receive it even if it had been sent. The already-existing local JS fallback engine (`src/logic/benefit-engine.js`'s `exceedsAssetLimit`/`getAssetLimit`, used by `BuergergeldModule`/`WohngeldModule`/`LastenzuschussModule`/`KinderzuschlagModule`) already implements this correctly — but that path only ever runs on network failure/timeout, never on a normal successful live-backend response. Real-world impact: any user with savings above the legal Schonvermögen threshold (e.g. 50.000€) who honestly filled in the asset field — which the UI itself warns can void their claim — got a normal `"eligible"` Bürgergeld/Wohngeld/Kinderzuschlag amount from the live calculator anyway, with no warning; this is more severe than a typical amount-miscalculation bug in this repo's history since it silently bypasses an entire legal eligibility gate rather than just understating a figure. This same class of gap (live Python backend missing something the JS fallback engine already does correctly) was previously found and fixed for other dimensions in iterations 33/34/38/40/48, and had been explicitly *mentioned but not itself fixed* as far back as iteration 22's changelog. Fixed with the architecturally correct change (not a client-side workaround, since this is a true backend data-model gap): added `assets: float = 0.0`/`has_high_assets: bool = False` to `backend/models.py`'s `HouseholdRequest` (both default to backward-compatible no-op values so old/other API clients that never send these fields see unchanged behavior); added a new `_exceeds_asset_limit(request, base, per_additional_member)` helper to `backend/engine.py` mirroring the JS engine's exact threshold/scaling formula (limit = base + per_additional_member × (household_size − 1), strict `>`, also true if `has_high_assets` is set); added an early-return "ineligible" gate using this helper to `calculate_sgb2`/`calculate_kinderzuschlag` (40.000€ + 15.000€/member) and `calculate_wohngeld` (60.000€ + 30.000€/member), placed right after each function's existing `_is_asylum_seeker` early-return; and wired `smart-calculator.jsx`'s already-collected `assets`/`hasHighAssets` state into the `payload` object POSTed to `/api/v4/analyze` (previously silently dropped before the network request). Deliberately did not add an asset check to `calculate_kindergeld`/`calculate_elterngeld` — confirmed both the JS engine and actual German law correctly have no Vermögensprüfung for these two (not means-tested), so that's a correct scope boundary, not a gap. Verified via a standalone Python harness directly exercising `SocialRuleEngine` (boundary cases exactly at/one-cent-above/below the scaled limit for both 1-person and multi-member households, the `has_high_assets` flag alone forcing exclusion even with `assets=0`, and an old-client-compat case with no asset fields sent at all still returning normal eligible results) + `python3 -m py_compile backend/main.py backend/engine.py backend/models.py` + `npm run build` (clean) + independent subagent review (PASS — independently re-read `benefit-engine.js`'s `getAssetLimit`/`exceedsAssetLimit` and independently re-verified the Python port's constants/formula match rather than trusting the description, independently ran its own Python REPL boundary/backward-compat tests reaching the same PASS conclusions, independently confirmed Pydantic v2's default field-handling behavior, independently confirmed the frontend's snake_case `has_high_assets`/`assets` payload keys correctly match the new Pydantic field names, independently confirmed `calculate_kindergeld`/`calculate_elterngeld` correctly remain untouched as a real scope boundary rather than an oversight, confirmed via `git diff --stat`/`git status` that only the 3 intended files changed with no scope creep and no `package-lock.json` diff, ran its own independent `py_compile`/`npm run build`) before committing — the implementer did not self-approve. One pre-existing cosmetic nitpick the reviewer flagged (missing trailing newline at EOF in `backend/models.py`, not introduced by this diff) was fixed in the same pass since the file was already being touched. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Runner-up candidates noted by the scanning agent, not pursued this pass**: `src/pages/api/leads.ts` still trusts client-submitted `partner_vertical`/`consent_partner_transfer` verbatim with no server-side derivation from `modal_type` — a malicious client could forge fake partner-consent records (flagged by iteration 51's reviewer, never closed); the same backend-parity gap pattern exists for Pflegegrad (`hasCareDependent`/`careDependentGrad`) and Bestattungsvorsorge (`isBereaved`) — never ported to `backend/engine.py`, understating Bürgergeld Mehrbedarf/Wohngeld allowances on the live path; lower severity than the assets gap since these affect amount, not the eligibility gate itself (mentioned as deliberately-not-touched as far back as iteration 22).
- [x] **Data-correctness fix: `backend/main.py`'s Sperrzeit "opportunities" card showed a hardcoded literal "168 € Verlust verhindern" instead of the household's real computed sanction amount, wrong for any household with a partner (2026-08-03, dev-loop iteration 55)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed at `backend/models.py`/`requirements.txt`, remaining pages/components/utils/data files, `package.json`, a repo-wide TODO/FIXME sweep, and broken-link checks — since brain.md had no actionable open item left besides the two deliberately-parked ones). `/api/v4/analyze` builds an `ALERT` card using the real computed value (`loss = sgb2_res["sanction_applied"]`), but a few lines later appends a second, unrelated "opportunities" card for the same Sperrzeit event using the hardcoded literal `"168 € Verlust verhindern ⚖️"` instead of reusing `loss`. `backend/engine.py`'s `sanction_amount = round(main_regelsatz * 0.30, 2)`, where `main_regelsatz` is `rbs_1` (563€, single, per `backend/rules/sgb2.json`) or `rbs_2` (506€, partner present) — i.e. **168.90€ single / 151.80€ partner**, never exactly "168". Both cards render on the same results screen (`results-section.astro`), so a partner household saw two conflicting euro figures (151.80€ in the alert text vs. "168€" in the card title) for the identical sanction. Fixed by reusing the already-in-scope `loss` variable (Python has function-level scoping; both `if sgb2_res.get("sanction_applied", 0) > 0:` guards check the identical condition on the identical dict, so `loss` is always bound by the second use). Independent review (PASS) additionally flagged a real, closable inconsistency: the pre-existing ALERT text interpolated `loss` with no format spec (`"168.9 €"` for a single person) while the fix's new opportunities-card title used `.0f` (`"169 €"`) — same number, two different displayed strings in the same response. Closed in the same iteration: both sites now use `.2f`, matching the precision `engine.py`'s own `reason` string already uses for this figure. Verified via `python3 -m py_compile backend/main.py` + `npm run build` (clean, frontend unaffected) + independent subagent review (PASS — independently re-derived 563×0.30=168.9/506×0.30=151.8 from the actual rules file rather than trusting the description, independently confirmed `sanction_applied` in the returned dict is genuinely `sanction_amount` not a different figure, independently confirmed `loss`'s scoping guarantee across both `if` blocks, independently grepped repo-wide for any other hardcoded "168" and found none, confirmed via `git diff --stat`/`git status` that only `backend/main.py` changed with no scope creep, ran its own independent `py_compile`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Runner-up candidates noted by the scanning agent, not pursued this pass**: `src/utils/districtUtils.ts` and `src/data/services.ts` are both dead code with zero call sites (superseded by `slugify.ts`/`authority-app.jsx` respectively) — cosmetic dead-code cleanup, not a live bug; `src/components/authority-search.astro` is a fully-built but entirely unreferenced component (superseded by `authority-app.jsx`).
- [x] **Routing/SEO fix: `vercel.json`'s legacy citySlug catch-all redirect was silently 301-redirecting the real, live `/kindergeld/` and `/grundsicherung-im-alter/` landing pages to the homepage, and also caught the unlinked `/design-system/` page (2026-08-03, dev-loop iteration 54)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from the ~45 previously-heavily-audited areas — backend calc logic/rules JSON, JS fallback engine, smart-calculator wizard, chat-widget, results-section, lead-modal/leads.ts, trust-pdf-generator/pdf-paket, pdf-generator.ts, checkout/send-application/stripe-webhook, cron/send-wba-reminders, wba-reminder-modal, admin feedback/dashboard, authority-app/get-authority/districtUtils/authorities data, nav.astro/layout.astro, SEO schema files, the already-fixed lexikon/ratgeber content and questions.json, ratgeber/lexikon dead-link fixes, kindergeld/grundsicherung-im-alter page content itself, feedback-widget, supabase migrations — this pass explicitly pointed at `src/pages/premium/**`, `src/components/ui/*`, the various standalone legal/info pages, `backend/main.py` route handlers, `src/pages/api/chat.ts`, remaining `src/data/*`/`src/utils/*` files, and config files `astro.config.mjs`/`vercel.json`). `vercel.json`'s catch-all redirect rule — a negative-lookahead regex on `/:citySlug((?!...)[^/]+)/:path*` intended to 301 legacy single-segment "city slug" URLs (e.g. `/berlin/wohngeld`) back to `/` — works by excluding every legitimate top-level static route name from its exclusion group; confirmed via `git log` that the regex was authored on 2026-06-23 (commits `62a639b`/`161b9ef`), four days *after* `src/pages/kindergeld/index.astro` and `src/pages/grundsicherung-im-alter/index.astro` were added (2026-06-19, commit `41d3f48`) without either new route ever being added to the exclusion list — a straightforward oversight, not a deliberate exclusion. Since the site runs with `trailingSlash: 'always'` (`astro.config.mjs`), every real request to these pages arrives as `/kindergeld/`/`/grundsicherung-im-alter/`, which matches the catch-all (citySlug = "kindergeld"/"grundsicherung-im-alter", `:path*` matching zero segments) and gets 301-redirected straight to the homepage — the actual page is never reached. Confirmed via a standalone regex reproduction (using `path-to-regexp`, the actual library Vercel evaluates this syntax with) that the pre-fix regex matched `/kindergeld/`, `/kindergeld`, `/grundsicherung-im-alter/`, `/design-system`, and `/design-system/` (also unexcluded, same bug, lower-stakes since it's unlinked) — all wrongly caught — while still correctly matching genuine legacy paths like `/berlin/wohngeld/`/`/muenchen/`. Confirmed via reviewing the full `redirects` array in evaluation order that no earlier, more specific rule already handled these two routes before the catch-all reached them. Real-world impact: `/kindergeld/` is a full, non-trivial 275-line SEO-optimized landing page (FAQ schema, calculator wiring, OG meta pointing at its own canonical URL) that was completely unreachable in production — any visitor from a search result or bookmark got silently bounced to the homepage, and the page could never rank since Vercel returned a 301 away from it on every crawl; confirmed both pages are genuinely indexed/crawlable (present in the built `dist/client/sitemap-0.xml` with full canonical URLs), so this was actively harmful for SEO (crawl-budget waste, redirect chains), not just a moot fix for an orphaned page. `/grundsicherung-im-alter/` is worse — it's an active internal CTA target: `src/pages/lexikon/[...slug].astro`'s sidebar "Kontext" widget (rendered on every SGB-XII/Grundsicherung-im-Alter/Renten-topic lexikon article) links to it, so real users clicking that in-page CTA were dead-ended at the homepage instead of the dedicated page the CTA promised. Fixed with the minimal correct change: added `kindergeld`, `grundsicherung-im-alter`, and `design-system` to the regex's negative-lookahead exclusion group in `vercel.json`, touching only that one line. Verified via `npm run build` (clean, both pages correctly prerendered to `/kindergeld/index.html`/`/grundsicherung-im-alter/index.html`) + a standalone `path-to-regexp` reproduction script confirming the post-fix regex no longer matches any of the 3 previously-wrongly-caught paths while still correctly matching genuine legacy city-slug paths + independent subagent review (PASS — independently re-verified both pages are real non-trivial content via direct read, independently reproduced the pre-/post-fix regex behavior using the actual `path-to-regexp` library rather than trusting the description, independently confirmed no earlier `redirects` array entry intercepts these routes first, independently confirmed both pages are genuinely sitemap-indexed/crawlable via the built output, confirmed via `git diff --stat`/`git status` that only this one file/1 line changed with no scope creep and no `package-lock.json` diff, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Runner-up candidates noted by the scanning agent, not pursued this pass**: `src/pages/kindergeld/index.astro` has a leftover `<!-- TODO: Link aktivieren, sobald /kinderzuschlag/ Seite existiert -->` comment with no corresponding link rendered — cosmetic/unfinished cross-link, not a live bug; `backend/main.py`'s `benefit_titles` dict has no entry for a hypothetical future `grundsicherung_alter` key (would fall back to an ugly `.capitalize()` string) — currently moot since the Python engine's `evaluate_all()` never returns that key (client-side-only per iteration 48), purely latent/hypothetical, not an active bug today.
- [x] **UX fix: the global header's mobile hamburger menu button was pure decoration with zero wiring, making the nav links and primary CTA completely unreachable from the header on every page for every mobile visitor (2026-08-03, dev-loop iteration 53)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from the ~40 previously-heavily-audited areas — all `src/pages/api/**` handlers, admin dashboards, `backend/engine.py`/rules JSON, `benefit-engine.js`/`calculator-2026.js`, chat-widget/authority-search/smart-calculator/results-section/lead-modal/trust-pdf-generator, SEO schema files, the 4 already-checked lexikon articles and `questions.json`, index/kindergeld/grundsicherung-im-alter/404 pages — this pass explicitly pointed at other `lexikon`/`ratgeber` markdown not yet named, `backend/main.py`/`models.py`, and `src/components/*` files not yet audited). `src/components/nav.astro` — rendered unconditionally on every page via `layout.astro`'s `<Nav />` — hides its desktop `<ul>` nav links ("Startseite", "Über Uns") and its "Anspruch prüfen" CTA behind `hidden md:flex` below Tailwind's 768px breakpoint, and shows only a hamburger `<button>` (`md:hidden`) in their place with hover states that make it look interactive. That button had no `id`, no `aria-*` attribute, no click handler, and no menu panel existed anywhere in the codebase for it to open — confirmed via repo-wide grep (`menu-open|toggleMenu|mobileMenu|hamburger|nav-toggle` → zero matches) and via `git log -p` showing it was introduced with an explicit `<!-- Placeholder for future implementation if needed -->` comment that survived unimplemented through the most recent rebrand. Real-world impact: every mobile visitor (the majority of traffic on a consumer benefits site) saw a header with only the logo and a tappable-looking icon that silently did nothing on tap — no menu opened, no console error — making the header's two nav links and its primary calculator CTA completely unreachable on mobile; the footer duplicates "Über uns"/`/#calculator` but only after scrolling to the page bottom, not a substitute for header nav. Fixed with the minimal correct change confined to this one file: added `id`/`aria-expanded`/`aria-controls`/`aria-label` to the button; added a new `<div id="mobile-menu">` panel (initially `hidden`, rendered inside `<header>` after `</nav>` so it appears as an in-flow dropdown below the header bar, not a full-screen overlay) containing the same 3 links/CTA styled for mobile; added a small inline `<script>` toggling the panel's `hidden` class on click and syncing `aria-expanded`/`aria-label` in both directions. Verified via `npm run build` (clean) + direct inspection of `dist/client/index.html` (button/panel ids and attributes present, compiled `type="module"` script matches source logic) + independent subagent review (PASS — independently traced the `classList.toggle('hidden') === false` derivation of `isOpen` through both click directions from the correct initial state and confirmed it's correct despite reading unusually, confirmed Astro compiles this to a `type="module"` script executed once per page load with no duplicate-listener risk since `<Nav/>` renders exactly once per page, confirmed via repo-wide grep that no other file already manages a `#mobile-menu`/`#mobile-menu-button`-conflicting mobile menu, confirmed the native `<button>` remains keyboard-operable by default and that the `hidden` attribute already removes the closed panel from the accessibility tree with no extra `aria-hidden` needed, confirmed via `git diff --stat`/`git status` that only this one file/34 lines changed with no scope creep and no `package-lock.json` diff, ran its own independent `npm run build`) before committing — the implementer did not self-approve. Deliberately out of scope as a minor, non-blocking nitpick raised by the reviewer: no `Escape`-to-close or focus management when the panel opens — acceptable for this minimal-scope dropdown, not a regression, worth a future polish pass if desired. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Crash fix: admin feedback dashboard threw a `TypeError` and 500'd on any real feedback submission because the `user_feedback` table was missing a `resolved` column that the admin page reads/writes (2026-08-03, dev-loop iteration 52)**: found via a fresh targeted scan (delegated to an Explore subagent, this time explicitly pointed at files never mentioned across any of the 51 prior iterations, rather than re-scanning the ~35 already-heavily-audited areas: `src/components/feedback-widget.astro`, `src/pages/admin/feedback.astro`, `src/pages/api/chat.ts`'s own request handling, `bildung-und-teilhabe.astro`/`avgs.astro`/`abbruch.astro`/`danke.astro`/`agb.astro`/`datenschutz.astro`/`impressum.astro`/`ueber-uns.astro`/`design-system.astro`, `lexikon/index.astro`, `ratgeber/[slug].astro`, `success-roadmap.astro`, `trust-badges.astro`, `premium/*`, `ui/*`, the live `result-roadmap.jsx`, `astro.config.mjs`/`vercel.json`/`package.json`). `supabase/migrations/20260217_create_feedback_table.sql` — the only migration creating `public.user_feedback` — defines exactly 7 columns (`id`, `created_at`, `type`, `message`, `rating`, `contact`, `url`); confirmed via reading all 3 files in `supabase/migrations/` that no later migration ever added a `resolved` column. But `src/pages/admin/feedback.astro` (the operator-facing dashboard for the globally-mounted `feedback-widget.astro`, live on every page via `layout.astro`) reads and writes `resolved` as if it already existed: the "toggle" action does `update({ resolved: !currentStatus })` (silently no-ops if the column is absent, since its Supabase error isn't checked — a pre-existing, non-regressed gap, not fixed this pass), and — the actual crash — the render loop's toggle-button `<input type="hidden">` did `value={item.resolved.toString()}`. Since the column doesn't exist, `item.resolved` is `undefined` on every real fetched row, and `undefined.toString()` throws a `TypeError` during Astro SSR, uncaught, inside the `.map()` over `feedbackData` — meaning the entire dashboard 500s and becomes completely unusable the moment at least one real visitor has ever submitted feedback (an ordinary, expected, non-error condition, not an edge case), locking the operator out of the one tool built to triage it. The other 3 non-`.toString()` usages of `item.resolved` (row shading, button style, button label) are all in truthy/ternary contexts where `undefined` is merely falsy, so they don't throw and needed no change. Fixed with the minimal correct change at both the schema and code layers: added `supabase/migrations/20260803_add_resolved_to_feedback.sql` (`alter table ... add column if not exists resolved boolean not null default false`, idempotent, styled consistently with the two existing migration files) as the actual root-cause fix; and changed the one crashing call site to `(item.resolved ?? false).toString()` as a defensive guard so the page renders correctly (all rows shown as "Offen"/unresolved) even in the window before that migration is actually applied to a live database, rather than crashing. Verified via `npm run build` (clean) + independent subagent review (PASS-WITH-NITPICK — independently re-read the creation migration and confirmed the column is genuinely absent, independently traced `feedbackData`'s real Supabase origin and confirmed the pre-fix `.toString()` call was the sole crash site while the other 3 usages are non-throwing truthy checks, grepped the repo for any other `resolved` reference and found none needing updates, confirmed the new migration's syntax/style matches its siblings, confirmed via `git diff --stat` that only these 2 files/1 new file changed with no scope creep and no `package-lock.json` diff, ran its own independent `npm run build`) before committing — the implementer did not self-approve. **Operational gap flagged by the reviewer, deliberately not resolved this pass**: this repo has no CI/automation anywhere that applies `supabase/migrations/*.sql` to the live database (confirmed via search — no `.github/workflows`, no migration-runner script) — adding the migration file is necessary but not sufficient; whoever operates the live Supabase project needs to manually run this migration (e.g. via the Supabase SQL editor or CLI) for the toggle-resolved feature to actually persist, otherwise the dashboard will render correctly (thanks to the defensive guard) but toggling will keep silently no-op-ing forever. This unattended session has no Supabase credentials/access to apply it directly — **flagging here for the project owner to run the new migration against production**. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness/consent fix: the shared lead-capture modal leaked a stale `partner_vertical`/consent-checkbox state from one CTA context into the next unrelated one it was reopened for (2026-08-03, dev-loop iteration 51)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from all previously-heavily-audited areas — backend calc logic, JS fallback engine, PLZ/authority data, lexikon/ratgeber content already checked for the 538€ threshold, chat-widget NLP, smart-calculator payload/sessionStorage hand-off, results-section CTA wiring, SEO JSON-LD, admin dashboard, wba-reminder flow, index.astro hero, backend models/routes — since brain.md had no actionable open item left besides the two deliberately-parked ones; this pass was pointed at `checkout.ts`/Stripe webhook, `send-application.ts`, `leads.ts`, `lead-modal.astro`, `erfolg.astro`, `partner-cta.astro`, other landing pages, `trust-pdf-generator.jsx`/`pdf-generator.ts` internals, `authority-app.jsx` beyond PLZ data, `src/utils/*.ts`, dead code/TODOs). `src/components/lead-modal.astro`'s single shared modal (`window.openLeadModal(type)`) is reused across 7 different CTA contexts (`avgs`, `application`, `kiz`, `elterngeld`, `pdf-checklist`, `legal_help`, `pflege`) wired up from multiple pages, most prominently `results-section.astro` which fires `legal_help`, `pdf-checklist`, `kiz`, and `pflege` all on the same results screen. Only the `legal_help` branch ever set the hidden field `#avgs-partner-vertical` to `'legal'` and un-hid the `#partner-consent-container` div (a GDPR-style "forward my data to an external Anwalt/Coach partner" consent checkbox, `#partner-consent-check`) — but since the modal's DOM nodes persist across calls (not re-mounted) and no branch, including the top-of-function reset block, ever reset these three elements back to a clean baseline, any user who opened the `legal_help` modal even once (even just to look, without submitting) and then later reopened the *same* modal for an unrelated CTA (e.g. "PDF-Paket kostenlos anfordern") would see the legal-partner consent checkbox incorrectly still visible/still checked, and — if they submitted — the lead would be POSTed to `/api/leads` with a stale `partner_vertical: 'legal'` and `consent_partner_transfer: true` despite being an unrelated PDF/KiZ/coaching/Elterngeld request. Confirmed via `src/pages/api/leads.ts` that both fields are read verbatim from client `FormData` with no server-side derivation/cross-check against the actual modal type, then persisted to Supabase and forwarded as-is to the external Make.com partner webhook — so this wasn't just a UI cosmetic issue but a real data-correctness/consent-integrity bug: a household's lead could get misrouted to the legal-partner vertical, and a false "consent to forward to an external partner" record could be created for a user who never saw or intended that consent on the request they actually submitted. Fixed with the minimal correct change: added 3 reset lines at the very top of `openLeadModal`, before the type-specific `if/else if` chain (so every branch, including the default `avgs` branch, starts from a clean baseline) — reset `#avgs-partner-vertical`'s value to `''`, re-hide `#partner-consent-container`, and uncheck `#partner-consent-check`; the pre-existing `legal_help` branch's own set-to-`'legal'`/show-consent code runs afterward in the same call and is unaffected, so that flow still works correctly. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-traced the full bug via its own read of `lead-modal.astro`'s `openLeadModal` function and `leads.ts`'s handling of `partner_vertical`/`consent_partner_transfer` rather than trusting the description, confirmed the 3 reset lines run before the branch chain so `legal_help` itself is unaffected by ordering, confirmed the defensive null-guards on `getElementById` are harmless/consistent with existing code style since all 3 elements are always statically present in the DOM, confirmed via `git diff --stat`/`git status` that only this one file/6 net lines changed with no scope creep, ran its own independent `npm run build`; noted one pre-existing, out-of-scope observation not part of this fix — `leads.ts` still trusts client-submitted `partner_vertical`/`consent_partner_transfer` with no server-side derivation from `modal_type`, so a malicious client could still forge these fields directly against the API, a separate server-side hardening question for a future pass) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: educational content across 4 files taught a stale 520€ Bürgergeld-Freibetrag threshold that contradicts the live 538€ value used by all three calculation engines (2026-08-03, dev-loop iteration 50)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from the ~35 previously-heavily-audited areas — `backend/engine.py`/`backend/rules/*.json` core calc logic, `benefit-engine.js`/`calculator-2026.js`, PLZ/authorities data, `smart-calculator.jsx` payload fields, `chat-widget.jsx`, `results-section.astro`, PDF generation, all `src/pages/api/**` handlers, admin dashboard, wba-reminder flow, SEO schema files (just fixed in iteration 49), `grundsicherung-im-alter`/`kindergeld` landing pages — since brain.md had no actionable open item left besides the two deliberately-parked ones; this pass explicitly pointed the scanner at `src/content/lexikon/*.md` articles beyond the one already fixed in iteration 43, other landing/legal pages, and numeric content-vs-engine cross-checks not yet specifically done). `backend/rules/sgb2.json`'s `freibetrag_step1_limit` (538), `backend/engine.py`'s step1/step2 boundary math, and `src/logic/calculator-2026.js` (lines 165-172, which even carries an explicit comment "Updated 2024/2026 limit (was 520)") all consistently use **538 €** as the boundary between the 20%-band and 30%-band of the Bürgergeld Erwerbstätigenfreibetrag staffelung — but `src/content/lexikon/buergergeld-freibetrag.md`'s table and worked example (1400€ income case) still taught the old **520 €** figure, as did two inline mentions in `src/content/lexikon/buergergeld-voraussetzungen.md`. Worse, `src/content/ratgeber/buergergeld-und-minijob.md` was internally self-contradictory: it correctly stated the 538€ Minijob-Grenze in its intro/tip sections but then computed its own worked example (someone earning exactly 538€) using the old 520€ boundary, adding a spurious extra "Schritt 3" 30%-band calculation (30% of the 18€ between 520-538) that doesn't exist under the real 538€ threshold — since 538€ income never exceeds the 538€ step1 limit (`if (inc > 538)` in `calculator-2026.js` is strictly false at exactly 538€), the true answer has no step-2 contribution at all. `src/data/questions.json`'s matching FAQ entry (`buergergeld-und-minijob` slug) had already-correct 187.60€ arithmetic in its `content` field but a stale, inconsistent `shortAnswer` field ("ca. 184 €") and one stray "520 €" mention in its prose. Real-world impact: a user reading any of these articles before or after running the site's own calculator would see a different, wrong income-band boundary and, in the minijob article's case, a wrong total Freibetrag (189,40€ claimed vs. the true 187,60€) and a wrong anrechenbares-Einkommen figure — a factual inconsistency on a legal/financial-benefits site between its own educational content and its own calculation engine. Fixed with the minimal correct change per file: `buergergeld-freibetrag.md`'s table/example recomputed (100 + 87,60 + 138,60 + 40 = 366,20€, was 368€) — also fixed an unrelated stray English word "With Kind im Haushalt" → "Bei Kind im Haushalt" in the same paragraph, incidental to the numeric fix; `buergergeld-voraussetzungen.md`'s two inline mentions swapped to 538€ (prose only, no arithmetic to recompute); `buergergeld-und-minijob.md`'s worked example rewritten to remove the now-nonexistent step-2 contribution and recompute the total as 187,60€ (was 189,40€) and anrechenbares Einkommen as 350,40€ (was 348,60€); `questions.json`'s `shortAnswer` corrected to "ca. 187,60 €" and its one stray "520 €" mention fixed to "538 €", bringing it in line with the field's own already-correct `content` arithmetic. Repo-wide grep confirmed no other file references this same Freibetrag-staffelung 520€ figure (remaining "520" hits elsewhere in `src/` are unrelated — Wohngeld rent caps, PLZ postal codes, an unrelated Grundsicherung pension-income example, and the JS engine's own explanatory "(was 520)" comment). Verified via `npm run build` (clean, both after the initial single-file fix and after the follow-up 3-file consistency fix) + `node`/`python3` JSON-validity check on `questions.json` + two rounds of independent subagent review (round 1 PASS on the single-file `buergergeld-freibetrag.md` fix, but flagged the same stale 520€ figure still present uncorrected in `buergergeld-voraussetzungen.md` and `buergergeld-und-minijob.md` — closed in the same iteration rather than deferred; round 2 PASS on the full 4-file fix — independently re-derived the correct 538€-income Freibetrag math from `calculator-2026.js` line-by-line including the exact `inc > 538` strict-inequality boundary condition, independently re-verified the 1400€ example's arithmetic, independently cross-checked `backend/engine.py`/`backend/rules/sgb2.json` agree with the JS engine, grepped repo-wide for any remaining stale "520 €" Freibetrag references and found none, confirmed via `git diff --stat` that only these 4 files changed with no scope creep, confirmed `questions.json` is still valid JSON, ran its own independent `npm run build`) before committing — the implementer did not self-approve, and the reviewer-found gap in round 1 was fixed and re-verified within the same iteration rather than deferred. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **SEO/structured-data fix: homepage shipped two conflicting Organization objects and two conflicting WebSite objects in JSON-LD, one of them advertising a `/suche` search route that doesn't exist (2026-08-03, dev-loop iteration 49)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from all previously-audited areas — backend calc logic, chat-widget NLP, authority/PLZ data, admin dashboard, lexikon content, pdf-paket handoff, index.astro hero numbers, wba-reminder flow, email/webhook validation, smart-calculator payload fields — since brain.md had no actionable open item left besides the two deliberately-parked ones; this pass explicitly pointed the scanner at `src/components/seo/*`, structured-data/schema wiring, and other landing pages, none of which any of the 48 prior iterations had touched). `src/layouts/layout.astro` renders `<Schema>` (`src/components/seo/schema.astro`) unconditionally in every page's `<head>`, whose `@graph` unconditionally includes its own `organizationSchema`/`websiteSchema` regardless of the `type` prop. `src/pages/index.astro` — the site's single most SEO-critical page — additionally imported and rendered a second, independently-defined schema component, `SchemaOrg` (`src/components/seo/seo-schema.astro`), with `type="Organization"` and `type="Website"` on top of that. Confirmed via `npm run build` + direct inspection of the built `dist/client/index.html` that the pre-fix homepage shipped 4 separate `<script type="application/ld+json">` blocks: two structurally-different top-level `Organization` objects (one with `logo` as an `ImageObject` + `@id`, the other with `logo` as a bare string, no `@id`) and two structurally-different top-level `WebSite` objects — duplicate/conflicting markup for the same entities on one page, which per Google's structured-data guidelines risks being flagged/ignored in Search Console, undermining the GEO/rich-results strategy brain.md Phase 2 explicitly prioritizes. Additionally, `seo-schema.astro`'s `websiteSchema` carried a `potentialAction`/`SearchAction` pointing at `${siteUrl}/suche?q={search_term_string}` — confirmed via repo-wide grep that no `/suche` route exists anywhere in the project, so if Google ever trusted this markup enough to render a Sitelinks Search Box, real visitors clicking it would hit a live 404. Confirmed via grep across `src/pages/**` that `SchemaOrg type="Organization"`/`type="Website"` (the `seo-schema.astro` import specifically) were used at exactly these two lines and nowhere else in the codebase — no other page relies on this specific schema shape. Fixed with the minimal correct change: removed just the two redundant lines from `index.astro`, keeping `<SchemaOrg type="SoftwareApplication" />` (not duplicated by `layout.astro`'s `Schema`, which has no SoftwareApplication branch) — `layout.astro`'s always-rendered `<Schema>` already fully covers Organization/WebSite for every page including the homepage, so nothing is lost, only de-duplicated; and since `type="Website"` had exactly one call site (the one just removed), the broken `/suche` SearchAction can no longer render anywhere on the live site without needing to touch `seo-schema.astro` itself or build a new route. Verified via `npm run build` (clean) + direct post-fix inspection of `dist/client/index.html` (exactly one top-level Organization, one top-level WebSite, zero `/suche` occurrences; the one incidental second "Organization" match is `SoftwareApplication`'s nested `author` property value, not a duplicate top-level JSON-LD document) + independent subagent review (PASS — independently re-confirmed via its own grep that no other page/call site uses this schema shape, independently read `layout.astro`/`schema.astro` to confirm Organization/WebSite still render unconditionally post-fix, independently rebuilt and diffed `dist/client/index.html` before/after by temporarily restoring the two removed lines to see the 4-script duplication firsthand then reverting, confirmed via `git diff --stat` that only these 2 lines in 1 file changed with no scope creep, grepped for any client-side JSON-LD parsing that might depend on the removed schema's exact shape and found none, ran its own independent `npm run build`) before committing — the implementer did not self-approve. **Deliberately left for a future pass, not blocking**: `schema.astro` and `seo-schema.astro` remain two near-duplicate, independently-maintained JSON-LD implementations (still both actively used — `seo-schema.astro` for `TechnicalArticle`/`GovernmentService`/`FAQPage`/`BreadcrumbList` on other pages, `schema.astro` for the base graph everywhere) that could silently stack a duplicate again on some other page in the future if a `type` is ever added to both; consolidating into one source of truth is a larger refactor than this iteration's minimal-diff scope. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Bugfix: live/primary calculation path never computed Grundsicherung im Alter (SGB XII) for pensioners, silently showing "Kein Anspruch" instead of the calculation the site's own dedicated landing page explicitly advertises (2026-08-03, dev-loop iteration 48)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from the ~30 previously-heavily-audited areas — `src/pages/api/**`, `src/pages/admin/**`, `backend/engine.py`'s core calc logic, `benefit-engine.js`/`calculator-2026.js`, the PLZ/authorities data files, `smart-calculator.jsx`'s payload/wizard fields, `chat-widget.jsx`, `pdf-generator.ts`, lexikon sidebar links — since brain.md had no actionable open item left besides the two deliberately-parked ones). `backend/engine.py`'s `evaluate_all()` (the function backing `/api/v4/analyze`, the primary calculation path `smart-calculator.jsx` calls first on every submit, only falling back to the client-side engine on network failure/timeout) only ever computes 5 benefit types — SGB2 (Bürgergeld), Wohngeld, Kindergeld, Kinderzuschlag, Elterngeld — confirmed via a full-file grep that no `calculate_grundsicherung_alter()` (or any "grundsicherung" reference at all) exists anywhere in `backend/*.py`. `calculate_sgb2()` already correctly excludes retirees (`is_retired`/age ≥ `REGELALTERSGRENZE`) from Bürgergeld with an ineligible/€0 result whose *text* points them toward "Grundsicherung im Alter (SGB XII) - ein eigenes Verfahren" — but that's purely informational copy, never an actual computed amount, so a low-income pensioner using the live path landed on `primaryType = "Kein Anspruch"` (per `smart-calculator.jsx`'s success-path logic, which only ever consults `SGB2`/`WOHNGELD` result types for the headline). Meanwhile the client-side fallback engine (`src/logic/benefit-engine.js`'s `GrundsicherungAlterModule`, id `grundsicherung_alter`) already implements this correctly and complete — real asset-limit check, real Regelsatz-based Bedarf calculation reusing the audited `calculateBuergergeld()` helper — but was only ever reached via the `catch` block's local-fallback path on network failure/timeout, never on a normal successful live-backend response. `src/components/results-section.astro` even has a fully-built, ready-to-render CTA card for this exact id (`case 'grundsicherung_alter': ... /pdf-paket?type=Grundsicherung`) that normally never fired because the live backend never returned that id. This directly contradicts the site's own dedicated marketing page, `src/pages/grundsicherung-im-alter/index.astro` ("Wir berechnen Ihre Grundsicherung im Alter in 2 Minuten... Kostenlos, 100% anonym"), and the wizard's own dedicated pensioner UI branch (`smart-calculator.jsx`, collects `grundrenteYears` but never even sends it to the backend) — a "fully marketed, fully wired-on-the-client, dead-on-the-live-path" feature gap for an entire vulnerable user segment (low-income retirees) under completely normal, successful backend operation, not an edge case or an error state. Fixed with the minimal correct change without needing a backend redeploy: in `smart-calculator.jsx`'s live-backend success path, added a `status === "pensioner"` branch that runs the already-imported, already-audited `evaluateAllBenefits(profileInput)` and extracts just the `grundsicherung_alter` entry, pushing it into `mappedResults` (same shape as the existing `buergergeld`/`wohngeld`/`lastenzuschuss` entries) and promoting it to the headline `primaryType`/`primaryAmount` if it exceeds whatever the backend already returned. **Independent review caught a real, non-blocking-but-worth-closing gap before commit**: since `backend/engine.py`'s `calculate_wohngeld()` has no retiree exclusion at all, a pensioner can legitimately get a nonzero live-backend Wohngeld amount *and* a nonzero local Grundsicherung amount simultaneously — but the two benefits are legally mutually exclusive in practice (§7 Abs. 1 WoGG, Grundsicherung already covers Kosten der Unterkunft), so showing both as independent firm "probable" claims with two separate PDF-Antrag CTAs would itself be a new, reviewer-flagged misleading-UI bug for this same vulnerable group. Closed in the same iteration before commit: added a comparison so only the higher of the two (`Wohngeld`/`Lastenzuschuss` vs. `Grundsicherung im Alter`) stays `"probable"`, while the lower is demoted to `"possible"` with an explanatory Wahlrecht ("meist kommt nur eine der beiden Leistungen infrage") note appended to its existing description — verified by hand-tracing all three cases (Grundsicherung higher, Wohngeld higher, no housing card present) via a standalone Node re-implementation of the merge logic, confirming exactly one card stays "probable" in each direction. Deliberately did not port this into `backend/engine.py` as a real Python `calculate_grundsicherung_alter()` — that's a larger, separate piece of work (mirroring how iteration 34 ported the asylum-seeker exclusion into the backend) than this iteration's scope; flagged as a natural future follow-up if the client-side reuse pattern used here is ever considered insufficient. Verified via `npm run build` (clean, both before and after the review-driven Wohngeld/Grundsicherung demotion fix) + a standalone Node script re-implementing and exercising both the `GrundsicherungAlterModule` calculation directly (low-income pensioner gets a real, positive entitlement; a high-income-pensioner control and a young-employee control both correctly get none/not-relevant) and the win/lose merge logic in isolation + independent subagent review (PASS-WITH-ONE-REAL-GAP-FOUND — independently re-confirmed via its own `git diff --stat` that only this one file/final diff changed with no scope creep, independently re-traced that `evaluateAllBenefits()` is a pure function with no shared/mutated state and that its `grundsicherung_alter` result genuinely carries `.type`/`.amount`/`.eligible`/`.reasoning`, independently confirmed every field `GrundsicherungAlterModule.calculate` reads is actually present in `profileInput`, independently confirmed the `results-section.astro` CTA id match, independently confirmed via `backend/engine.py:97` that a pensioner can never get a nonzero live Bürgergeld amount alongside the new entry (no Bürgergeld double-count possible) — but then found and precisely specified the real Wohngeld/Grundsicherung double-display gap described above by reading `calculate_wohngeld()` itself rather than assuming retiree exclusion applied everywhere, ran its own independent `npm run build`) before committing — the implementer did not self-approve, and the reviewer-found gap was fixed and re-verified within the same iteration rather than deferred. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Bugfix: chat widget's NLP city-detection silently misread ordinary German words as a city name, corrupting the conversation's Wohngeld estimate (2026-08-03, dev-loop iteration 47)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed at previously-under-audited areas — other `lexikon/*.md` content, `kindergeld/index.astro`, legal pages, `chat-widget.jsx`/`chat.ts`, SEO schema files, TODO/FIXME sweeps, broken internal links, `backend/main.py` route handlers — since brain.md had no actionable open item left besides the two deliberately-parked ones). `src/components/chat-widget.jsx`'s `extractEntities()` — the NLP layer behind the chat assistant mounted globally on every page via `src/layouts/layout.astro` — detected a user's city with a plain unanchored substring match (`lower.includes(city)`) against every key in the ~40-entry `CITY_MIETSTUFE` dictionary. Two dictionary entries, `'essen'` and `'halle'`, are also ordinary standalone German words (food/to-eat; hall/warehouse) — so a message like "Ich habe kaum noch Geld für Essen" (barely any money for food) or "Ich wohne in einer alten Fabrikhalle" got misdetected as the city Essen/Halle purely from substring containment (the plain `.includes()` also matched the city name as a substring of unrelated words, e.g. "vergessen" contains "essen"). Confirmed via tracing the full data flow: the detected `ent.city` merges into `context.city` via `setContext`/`dispatchMessage` and is never reset for the rest of the conversation, then feeds a real `calcWohngeld()` call (using that city's Mietstufe rent-cap/coefficient tables) at 3 separate call sites, with the wrong city name echoed back to the user in both a UI `Tag` and the result text — so a routine, on-topic message about not affording food could produce a fabricated location and a materially wrong benefit estimate presented as a concrete number, silently. Fixed with the minimal correct change: switched matching to a word-boundary regex (`\bcity\b`) for all cities (closes the "vergessen"/"Fabrikhalle" substring-within-a-word false positives generically, confirmed safe against JS's umlaut/`\b` interaction since none of the ~40 dictionary entries start/end with a non-word umlaut character); for the specific ordinary-word collisions (`essen`, `halle`, and `kiel` — "Kiel" is also a standalone German noun, ship's keel, added after independent review flagged it as the same risk class, lower likelihood but zero-cost to close), added a requirement that the match only counts with a location-anchoring preposition (`in`/`aus`/`bei`/`nähe(von)`/`stadt`) or when the entire trimmed/punctuation-stripped message is just that one word — preserving the legitimate bare-one-word-reply flow the bot itself prompts for ("In welcher Stadt wohnst du?", confirmed via grep at 3 call sites) while blocking the false-positive food/building/keel readings. All other ~37 cities are unaffected beyond the word-boundary tightening (none collide with a common German word). Verified via a standalone Node script re-implementing the exact matching logic against 13+ hand-picked cases (false-positive food/forget/warehouse sentences correctly return no city; bare "Essen"/"Halle" replies and "in Essen"/"aus Halle"/"Stadt Essen" phrasings correctly match; unrelated cities like "Berlin" unaffected) + `npm run build` (clean) + independent subagent review (PASS-WITH-NITPICK — independently re-traced the full `ent.city` → `context.city` → `calcWohngeld()`/UI-echo data flow by reading the whole 1000+-line file rather than trusting the description, independently confirmed the "In welcher Stadt wohnst du?" bare-reply UX is real via its own grep, ran its own independent 27-case Node test including specifically verifying the JS `\b`/umlaut interaction across every umlaut city name individually, confirmed regex-injection is a non-issue since all dictionary keys are static hardcoded alphabetic strings, confirmed via `git diff --stat` that only this one file changed with no scope creep, ran its own independent `npm run build`; sole nitpick — 'kiel' not yet in the ambiguous set — was closed in a follow-up edit within the same iteration before commit) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Bugfix: Behördensuche returned the wrong city's authority contact for at least 9 real postal codes, because overly broad PLZ prefixes in the curated `authorities.json` swallowed neighboring cities' exact codes (2026-08-03, dev-loop iteration 46)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed at previously-unaudited areas — `src/pages/api/chat.ts`, `checkout.ts`, `cron/send-wba-reminders.ts` auth, `authority-search.astro`/`authority-app.jsx`, other lexikon articles, `backend/models.py`/`main.py` — since brain.md had no actionable open item left besides the two deliberately-parked ones). `src/components/authority-app.jsx`'s `findAuthorityInDB()` — the core matching logic behind the "Behördensuche" feature — checks `src/data/authorities.json` (curated, richer data for ~20 major cities, matched by city-name substring or PLZ-prefix) *before* falling back to `src/data/cities_2026.json` (109 cities, one exact representative PLZ each) for an exact match. Several `authorities.json` entries used overly broad 2-digit prefixes — Berlin `"14"`, Köln `"50"`/`"51"`, Essen `"45"`, Nürnberg `"90"` — while every other city in the same file already used 3+ digit prefixes precisely to avoid this. Confirmed via a cross-reference script (every `authorities.json` prefix checked against every `cities_2026.json` entry's exact PLZ) that these 4 broad prefixes collide with 9 real, different, already-correctly-catalogued cities: Essen/`"45"` swallows Gelsenkirchen(45881), Recklinghausen(45657), Mülheim(45468), Marl(45768), Herten(45699); Köln/`"50"` swallows Brühl(50321); Köln/`"51"` swallows Leverkusen(51373); Berlin/`"14"` swallows Potsdam(14467); Nürnberg/`"90"` swallows Fürth(90762) — verified each city's own real PLZ range doesn't actually extend into the colliding neighbor's code (e.g. Berlin's real range tops out well below Potsdam's 144xx). Real-world impact: since `authority.email` from this function flows directly into `/api/send-application` and the Stripe webhook's `authorityEmail` metadata, a Gelsenkirchen or Potsdam (etc.) resident using the site's free or paid Antrag-generation flow would have their real, formal Wohngeld/Bürgergeld application emailed to the *wrong municipality's* Jobcenter/Sozialamt — undermining the product's core promise, with no error or indication anything was wrong. Considered and rejected a fix that hardcodes narrower numeric PLZ boundaries per city (risk of getting real-world postal-code geography subtly wrong from memory without an authoritative source). Instead fixed the root cause generically: added a guard so a bare PLZ-prefix match (not a city-name match — those are unaffected) is only honored if the search term isn't a full 5-digit PLZ that exactly belongs to a *different* city already correctly catalogued in `cities_2026.json`; if it does, control falls through to that exact-match lookup instead, which has the right data for every affected city. This generalizes to any other broad-prefix collision that might exist or get introduced later, not just the 4 found this pass. Verified via a standalone Node script re-implementing both the pre- and post-fix matching logic and running it against all 9 collision PLZs (all now correctly resolve to the true neighboring city) plus 4 control cases — a genuine Essen PLZ, a genuine Berlin district PLZ (Spandau), a city-name search ("berlin"), and a genuine Köln PLZ — confirming zero regression to legitimate matches. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed at least 3 of the collisions by reading the actual `authorities.json`/`cities_2026.json` data itself rather than trusting the description, independently traced that Berlin's district-level `plz` arrays for Spandau/Mitte never overlap Potsdam's 144xx range so the separate district-matching code path is unaffected, confirmed the guard can't throw on any of the 109 `cities_2026.json` entries (`stadt` always a non-empty string), confirmed city-name searches are unaffected since `isCityMatch` is OR'd independently of the new guard, confirmed via `git diff --stat` that only this one file/14 lines changed, ran its own independent `npm install && npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Bugfix: admin partner dashboard's "Fristwahrungen Heute" stat tile was a hardcoded literal `2026`, not a real computed value (2026-08-03, dev-loop iteration 45)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed away from the many already-heavily-audited areas — email/webhook handlers, backend rules JSON, PLZ data, dead-file sweeps, smart-calculator.jsx payload fields — since brain.md had no actionable open item left besides the two deliberately-parked ones). `src/pages/admin/dashboard.astro` — the partner/business dashboard shown to whoever tracks lead and WBA-Schutz activity — renders three stat tiles side by side: `statTotal` (real, `leads.length`) and `statAvgs` (real, a filter over `leads`), but the third tile, labelled "Fristwahrungen Heute" (deadline protections today), rendered the bare literal string `2026` with no backing variable at all — confirmed via `git show HEAD:...` showing the pre-fix line was a static `<div>2026</div>`, not an expression. Since 2026 happens to be the current year, the fake number was indistinguishable from a real, coincidentally-matching KPI at a glance — it would have kept showing "2026" unchanged forever, becoming obviously broken only once the calendar moved on. Real-world impact: every day, regardless of how many WBA-reminder deadlines were actually due, the dashboard silently told whoever relies on it for operational tracking a made-up number. Fixed with the minimal correct change: added a `statFristwahrungen` variable, populated via a `supabaseAdmin` count-only query (`select('id', { count: 'exact', head: true })`) against the `wba_reminders` table, filtered with `.or('reminder_date_1.eq.<today>,reminder_date_2.eq.<today>')` — mirroring the exact same-day match logic the cron job (`src/pages/api/cron/send-wba-reminders.ts`) already uses to decide who gets emailed, using the same `YYYY-MM-DD` `todayStr` derivation. Deliberately counts both sent and not-yet-sent reminders due today (reads as "deadline events happening today," not "still-pending reminders" — the latter would misleadingly show 0 right after the daily cron fires each morning); flagged by the reviewer as a reasonable judgment call, not re-litigated here. Query failures leave `statFristwahrungen` at its safe default of `0` rather than throwing. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed via its own `git show`/`git diff` that the pre-fix literal was real and unbacked by any variable, independently re-read both `wba-reminder.ts` and `send-wba-reminders.ts` to confirm the table schema and `.or()` filter syntax/semantics correctly mirror "due today," confirmed `supabaseAdmin` is correctly in scope at the new query's call site, confirmed the safe-default-to-0 error handling doesn't crash the page on query failure, confirmed via `git diff --stat`/`git status` that only this one file/14 lines changed with no scope creep, confirmed the rendered value is a plain count number with no XSS/injection surface, ran its own independent `npm install && npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. `src/components/smart-calculator.jsx`'s `handleSubmit` shows a loading message after 3s explicitly warning "Beim ersten Aufruf kann der Serverstart (Render.com Cold Start) bis zu 30 Sekunden dauern" (the live backend, `sozialer-navigator-api.onrender.com`, is on a Render tier that spins down when idle) — but the `AbortController` gating the actual `fetch()` (`signal: controller.signal`) was set to abort after only `15000`ms, half of the window the app itself just told the user to expect. Real-world impact: any user who is first to hit the calculator after a period of inactivity — precisely the scenario the app's own message anticipates — routinely got their live request aborted mid-cold-start, well before the promised 30s grace period elapsed. On abort, the `catch` block silently falls back to `evaluateAllBenefits()` (`src/logic/benefit-engine.js` → `src/logic/calculator-2026.js`), a second, independent implementation of the WoGG §19 Wohngeld formula with its own rent-cap table (`RENT_LIMITS`) and coefficients (`WOGG_COEFFS`) — confirmed via direct comparison that these do **not** match `backend/rules/wohngeld.json`'s `rent_caps`/`coefficients` (e.g. 1 person/Mietstufe 1: backend cap `359` vs. client-fallback cap `491`; backend `a` coefficient `0.063` vs. client `a` `0.040`), the audited table `backend/engine.py` uses. So a routine, non-error cold-start timeout silently served a divergent, unaudited Wohngeld estimate on the site's core value proposition — compounded by the fallback banner's only recovery action being a full `location.reload()`, discarding the user's already-filled wizard state (confirmed via grep: only a post-submit `sessionStorage` write exists per iteration 42's fix, no read-back of in-progress wizard state). Fixed with the minimal correct change: raised the abort timeout from `15000` to `35000`ms, so a real cold start now has a fair chance to complete before the code gives up and falls back — bringing enforcement in line with (slightly exceeding) the 30s already promised to users. Deliberately left unfixed as a separate, larger follow-up (flagged by both the finding agent and the reviewer): `calculator-2026.js`'s `RENT_LIMITS`/`WOGG_COEFFS` tables still materially diverge from `backend/rules/wohngeld.json`'s audited values with no shared source of truth, so *if* the fallback still triggers (rate-limiting, real outage, cold start >35s), it can still produce a different number than the live backend — reconciling two independently-built Wohngeld constant tables needs a dedicated data-verification pass against the official 2026 WoGG tables, not a single-line timeout fix, and picking the wrong one without that verification risks introducing a new, less-audited error. Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the diff is exactly this one line via its own `git diff HEAD`, independently re-verified the 30s UI message text and the `signal: controller.signal` wiring by reading the file directly rather than trusting the description, checked for and ruled out any other timer/UI state that would regress from a longer wait (no competing timeout, no spinner assuming fast resolution, `clearTimeout` correctly still fires on both the success and catch paths), ran its own independent `npm install && npm run build`, confirmed no scope creep via `git diff --stat`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Reviewer's advisory note, not acted on**: 35s leaves only ~5s margin over the stated 30s worst case; if cold-start overruns beyond 35s are observed in the field, a larger buffer (e.g. 40-45s) may be worth a future follow-up.
- [x] **Hygiene: closed both low-priority follow-ups iteration 42 flagged — 2 dead files removed, 1 stale year label fixed (2026-08-02, dev-loop iteration 43)**: no other actionable open item existed besides the two long-standing deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts, out of scope), so this pass first verified and closed iteration 42's own two flagged follow-ups, then did a quick fresh scan for one more candidate. (1) `backend/rules/2025.json` — confirmed genuinely dead: `backend/engine.py` (the only rules-file loader) opens exactly 5 hardcoded filenames (`sgb2.json`/`wohngeld.json`/`kindergeld.json`/`kinderzuschlag.json`/`elterngeld.json`), no glob/dynamic loading exists anywhere in `backend/`, and a full-repo grep for "2025" in any `.py` file returned zero hits. Content-wise it structurally duplicated `sgb2.json`'s Regelbedarfsstufen and `wohngeld.json`'s WoGG rent-cap coefficients with nothing unique — deleted. (2) `src/components/result-roadmap.astro` — confirmed genuinely dead: `authority-app.jsx`'s `import ResultRoadmap from './result-roadmap'` resolves to the sibling `result-roadmap.jsx` (a React file can't import a `.astro` file as a JSX component), and a repo-wide grep found zero other references to the `.astro` file; it was an orphaned earlier Astro-native version superseded by the `.jsx` rewrite, same "confusing dead duplicate" shape as iteration 31's `getAllAuthoritiesForCity()` finding — deleted. (3) `src/content/lexikon/buergergeld.md:19` cited its Regelsatz example as "563 € für Alleinstehende in 2024" — cross-checked 563€ against `backend/rules/sgb2.json`'s `rbs_1` and `src/components/chat-widget.jsx`'s explicitly-labeled "Regelsätze 2026" copy, confirmed the figure itself is still correct for 2026, only the trailing year label was stale — fixed to "in 2026". A quick fresh scan for one more candidate bug (checked `chat.ts`, `lead-modal.astro`, `leads.ts`, `erfolg.astro`, `partner-cta.astro`) turned up nothing new and actionable this pass. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-ran the same greps rather than trusting the description, independently diffed the deleted `2025.json`'s content against `sgb2.json`/`wohngeld.json` to confirm zero unique data was lost, independently confirmed `result-roadmap.jsx` is a real, complete, actually-rendered component and that the deleted `.astro` file's only imports (`AuthoritySearch`/`PartnerCTA`) are separate untouched files with no shared dependency now missing, independently cross-checked the 563€/2026 figure, confirmed via `git diff --stat`/`git status` that only the 3 intended changes were made with no scope creep, confirmed `package-lock.json` was left clean/untouched, ran its own independent `npm run build`) before committing — the implementer did not self-approve. This closes both of iteration 42's flagged follow-ups.
- [x] **UX/functional fix: `/pdf-paket` page's advertised auto-fill/cover-page feature never fired for any real user — the `sessionStorage` key it reads was never written anywhere (2026-08-02, dev-loop iteration 42)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones — this pass was explicitly pointed at areas not yet deeply audited in 41 prior iterations (non-`api/**` pages/components, lexikon content accuracy, backend calculation branches not yet covered). `src/components/trust-pdf-generator.jsx` — the entire body of the live `/pdf-paket` route (`src/pages/pdf-paket.astro`) — reads `sessionStorage.getItem('sozialerNavigatorState')` on mount and, if present, uses it to auto-fill the PLZ/city field, show a "Daten sicher übernommen!" trust panel (Miete/Einkommen/Haushalt summary), and insert a pre-filled cover page into the generated official PDF (Brutto-Einkommen, Warmmiete, Haushaltsgröße, Berufl. Status) — explicitly advertised in the UI copy as "Wir heften diese Werte automatisch als Deckblatt an deinen Antrag, damit du sie nicht erneut ausfüllen musst." Confirmed via repo-wide grep that `sessionStorage.getItem('sozialerNavigatorState')` (2 call sites, both reads, both in this one file) was the *only* reference to that key anywhere in `src/` — nothing ever called `.setItem` for it. `src/components/results-section.astro`'s "Formlosen Antrag generieren ➔" CTA buttons (the live results screen, the main free-value-prop hand-off point, same flow iteration 31 called "the core free value proposition of the entire site") link to `/pdf-paket?type=...` via a plain `<a href>` full-page navigation — and neither `results-section.astro` nor `smart-calculator.jsx` (the wizard that already computes all the needed data into its `profileInput` object) ever persisted it before that navigation. Real-world impact: every real user completing the calculator and clicking through to generate their free Antrag-PDF got `savedState === null` unconditionally — the city/PLZ field was never auto-filled, the reassuring "your data carried over" panel never showed (silently replaced by generic fallback copy), and the promised pre-filled Behörde cover page silently never got attached, contradicting the page's own on-screen promise for 100% of real traffic through this path. Same "advertised feature is structurally dead because its data hand-off was never wired" bug shape as iteration 18's 7 dead wizard fields, but at a different hand-off point (calculator → `/pdf-paket`) not previously audited. Fixed with the minimal correct change: in `smart-calculator.jsx`'s `handleSubmit`, right after `profileInput` is fully constructed (so both the live-API-success and local-fallback result paths that follow both see it), added `sessionStorage.setItem('sozialerNavigatorState', JSON.stringify({ input: { city: selectedCity, rent: profileInput.rent, income: profileInput.income, persons: profileInput.persons, kids: profileInput.kids, status: profileInput.status } }))` wrapped in try/catch (mirrors the existing defensive try/catch already on the read side in `trust-pdf-generator.jsx`, guards against Safari-private-mode/quota-exceeded `setItem` throws). Field names/shapes were verified field-for-field against exactly what `trust-pdf-generator.jsx` reads (`savedState.input.city.plz`/`.city.stadt`/`.rent`/`.income`/`.persons`/`.kids`/`.status`), and `selectedCity` was confirmed (via `wohngeld-data.js`/`cities_2026.json`) to already carry `.plz`/`.stadt`/`.mietstufe` in the exact shape expected. `setItem` fully overwrites on every submit, so re-running the calculator with different inputs in the same tab correctly refreshes the persisted data — no stale-merge risk. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed via its own repo-wide grep that the bug was real and this was the only pre-existing reference to the key, independently re-verified every field name/shape match by reading the actual current file contents rather than trusting the description, confirmed the write's placement is unconditionally before both the live-API and local-fallback branches so both see it, confirmed no other code reads/writes this same sessionStorage key that could now conflict, confirmed the try/catch is an appropriate (not over/under-engineered) defensive choice matching the existing read-side pattern, confirmed no XSS/security concern (same-origin, user's own data, only ever consumed as PDF text-field values or plain-text JSX interpolation — never `dangerouslySetInnerHTML`), confirmed via `git diff --stat` that only this one file/16 lines were touched with no scope creep, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New lower-priority items noted by the scanning agent, not pursued this pass**: `backend/rules/2025.json` appears to be a dead/orphaned duplicate of `sgb2.json`+`wohngeld.json`'s values with no loader referencing it anywhere in `backend/` (worth a dedicated hygiene look); `src/content/lexikon/buergergeld.md:19` cites its Regelsatz example as "in 2024" even though the figure itself (563€) is still the correct current value used everywhere else — a stale year label, not a wrong number, purely cosmetic.
- [x] **Cosmetic fix: closed iteration 40's own flagged follow-up — homepage hero mockup still showed the pre-fix stale 292€ Kinderzuschlag figure (2026-08-02, dev-loop iteration 41)**: iteration 40 fixed the live backend's real Kinderzuschlag max-amount-per-child rate (250€/292€ → 259€/297€) but flagged, as a separate non-blocking cosmetic follow-up, that `src/pages/index.astro`'s static homepage hero — a purely decorative "engine.py: check_eligibility()"-styled mockup card, not wired to any real calculation, confirmed via direct inspection to be hardcoded text nodes with no `{}` expressions — still displayed the old `"292,00 €"` Kinderzuschlag sample figure alongside `"380,00 €"` Wohngeld summing to `"672,00 €"`, which could read as inconsistent to a careful visitor comparing it against the real (now-correct) 297€ figure used consistently everywhere else in the repo (`backend/rules/kinderzuschlag.json`, `src/logic/benefit-engine.js`, public FAQ copy). This iteration closed that exact follow-up: updated the mockup's Kinderzuschlag figure to `"297,00 €"` and recomputed the summed total to `"677,00 €"` (380 + 297), touching only these two hardcoded strings (plus one incidental trailing-whitespace cleanup on an adjacent blank line) in `src/pages/index.astro`. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-verified the diff via its own `git diff`, independently confirmed the arithmetic (380,00 + 297,00 = 677,00 €, no rounding issue), independently confirmed via direct file inspection that these are static hardcoded text nodes with zero JS/Astro logic computing them (not a real calculation path), independently cross-checked `backend/rules/kinderzuschlag.json` and `src/logic/benefit-engine.js` to confirm 297€ is indeed the correct, consistent current value used everywhere else in the repo and this edit removes the last remaining stale-292€ reference rather than introducing a new inconsistency, confirmed via `git diff --stat` that only `src/pages/index.astro` was touched with no scope creep and no `package-lock.json` diff left behind, ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This closes iteration 40's flagged follow-up completely — no other actionable open item found this pass besides the two long-standing deliberately-parked ones below (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope).
- [x] **Data-correctness fix: live-backend Kindergeld/Kinderzuschlag amounts were stale by one legislative update cycle, understating both benefits on the primary calculation path (2026-08-02, dev-loop iteration 40)**: found via a fresh targeted scan (delegated to a research subagent, since brain.md had no actionable open item left besides the two deliberately-parked ones), specifically cross-checking `backend/rules/*.json` against the JS fallback engine and the site's own public copy for numeric inconsistencies. `backend/rules/kindergeld.json`'s `amount_per_child` was `250.00` and `backend/rules/kinderzuschlag.json`'s `max_amount_per_child` was `292.00` — both read directly into the live eligibility/amount math in `backend/engine.py` (`calculate_kindergeld` line ~283, `calculate_kinderzuschlag` line ~345, the primary `/api/v4/analyze` calculation path). But every other source of truth in the repo already agreed on higher, current 2026 values: `src/logic/benefit-engine.js` (`259 * kidsCount` for Kindergeld with an explicit "Stand 2026" comment; `297 * kidsCount` for Kinderzuschlag max), `src/logic/calculator-2026.js` (259 for Kindergeld-as-child-income), and the site's own public-facing FAQ/marketing copy on `src/pages/kindergeld/index.astro`, `src/components/results-section.astro`, `src/components/chat-widget.jsx`, and `src/data/target-groups.js` — all consistently stating "259 € pro Kind" (Kindergeld) and "bis zu 297 € pro Kind" (Kinderzuschlag). Kindergeld rose 250€→255€ (Jan 2025)→259€ (Jan 2026) and Kinderzuschlag's max rose 250€→292€ (2024)→297€ (2025, unchanged into 2026) — so the backend JSON was stuck at old rates from before both of those updates. Real-world impact: every household with children calculated via the live backend (the primary path — the local JS fallback only runs on network failure/timeout) got Kindergeld understated by 9€/child/month and, for households whose Kinderzuschlag amount is capped by the per-child maximum (the common case in the eligible income band), Kinderzuschlag understated by 5€/child/month — a live calculator result a user could directly notice contradicts the site's own FAQ text on the same page. Same "live Python backend stale/wrong, everything else already correct" bug shape as iterations 33/34/38, just not previously found in these two rules files' amount fields (iteration 38 only touched this same `kinderzuschlag.json` file's *threshold* fields, not its amount field). Fixed with the minimal correct change: updated `kindergeld.json`'s `amount_per_child` to `259.00` and `kinderzuschlag.json`'s `max_amount_per_child` to `297.00` — no `engine.py` logic changes needed, matching the pattern of prior data-only fixes in this series. Also updated the three `backend/verify_engine.py` expected values that depended on the old constants (Test 1 Kindergeld 250.0→259.0 for 1 child; Test 2 Kindergeld 500.0→518.0 for 2 children; Test 2 Kinderzuschlag 584.0→594.0, hand-verified against the test household's actual calculation path — gross income clears the couple minimum-income gate, net income falls short of parent_need, so the `"possible"` branch returns `max_kiz` directly = 2×297). Verified via `python3 -m py_compile` (clean, fresh venv since system Python lacks pydantic) + the updated `backend/verify_engine.py` assertion suite (all 12 checks pass, including the 3 updated ones, zero regression elsewhere) + `npm run build` (clean, Python-only change with no frontend impact) + independent subagent review (PASS — independently re-confirmed via its own repo-wide grep that 259€/297€ are the consistent values everywhere else and only these two backend JSON files were the outlier, independently confirmed both JSON keys are read into live (not dead) calculation code, independently hand-verified the arithmetic behind all three updated `verify_engine.py` expectations by tracing the actual branch taken in `calculate_kinderzuschlag()` for Test 2's household composition, ran its own independent venv install + `py_compile` + `verify_engine.py` (exit 0) + `npm run build`, confirmed via `git diff --stat` that only the 3 intended files/10 lines changed with no scope creep, confirmed no `package-lock.json` diff was left behind) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New open item found by the reviewer, not fixed this pass (cosmetic, non-blocking)**: `src/pages/index.astro:274` has a static homepage hero mockup (styled as an "engine.py: check_eligibility()" illustrative example, not wired to any real calculation) displaying `"292,00 €"` as a sample Kinderzuschlag figure alongside a `"380,00 €"` Wohngeld figure summing to `"672,00 €"` — this decorative example happens to still show the old stale 292€ rate. Harmless (not read by any code, purely illustrative marketing copy) but could read as inconsistent with the real 2026 figures and the fresh fix above if a careful visitor compares them; worth a follow-up cosmetic pass to bump the mockup number to 297€ (and adjust the summed total accordingly) for internal consistency, not urgent enough to bundle into this data-correctness fix.
- [x] **Data-correctness/legal fix: Kinderzuschlag minimum-income gate compared net income against a legally gross ("brutto") threshold, closing iteration 38's own flagged follow-up (2026-08-02, dev-loop iteration 39)**: iteration 38 fixed the swapped 600€/900€ single-vs-couple threshold values but flagged, as a separate, unfixed, informational open item, that `calculate_kinderzuschlag()`'s minimum-income gate (`backend/engine.py`) summed `inc.amount_net` (net) to compare against those thresholds — but §6a BKGG's Mindesteinkommensgrenze, and the site's own public-facing text (`src/pages/kindergeld/index.astro:241`, "Mindesteinkommen von 900 Euro **brutto** für Paare bzw. 600 Euro **brutto** für Alleinerziehende"), define it as a gross threshold, meaning the net-based check made the real-world bar higher than the law requires — a user could have gross income clearing the legal gate but still get wrongly blocked because their net income (necessarily lower) fell short of the same number. Fixed with the minimal correct change iteration 38 already sketched: added a second accumulator, `parent_earned_income_gross` (summed from `inc.amount_brutto`), used only for the minimum-income gate check and its reason string (now explicitly says "brutto"); left the pre-existing `parent_earned_income` (net) accumulator completely untouched for the downstream parent-need/50%-taper Bedarf calculation a few lines below, which is a legally distinct net-income-based test that must not be conflated with the gross gate — exactly the care iteration 38 called out as needed. Confirmed the fix's precondition actually holds before relying on it: `src/components/smart-calculator.jsx` already collects and sends **both** `amount_brutto` and `amount_net` as two independent, separately-required wizard fields ("Monatliches Brutto-Einkommen" / "Monatliches Netto-Einkommen"), so real gross data was already reaching the backend and simply going unused for this specific check — no frontend change was needed. Verified via `python3 -m py_compile` (clean, fresh venv since system Python lacks pydantic) + the existing `backend/verify_engine.py` assertion suite (all checks still pass, zero regression to the 3 existing household test cases) + a targeted repro script constructing a single parent with gross ≥ 600€/net < 600€ (confirmed no longer wrongly blocked at the gate) and a couple with net ≥ 900€/gross < 900€ (confirmed now correctly blocked at the gate — this is the actual real-world bug the old net-based check missed) + `npm run build` (clean, Python-only change with no frontend impact) + independent subagent review (PASS — independently re-read the diff and confirmed the gate now uses gross while the downstream taper calculation still correctly uses only net with no variable conflation, independently confirmed `IncomeSource.amount_brutto` is a real always-present non-Optional field so the sum can't silently break, independently traced the frontend to confirm gross income is real independently-collected user data and not a duplicate/faked value, ran its own independent regression suite + a freshly-written independent repro script reproducing both boundary cases, confirmed via `git diff --stat` that only this one file/10 lines changed with no scope creep) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This closes iteration 38's flagged follow-up completely — no other actionable open item found this pass besides the two long-standing deliberately-parked ones below.
- [x] **Data-correctness/legal fix: Kinderzuschlag minimum-parental-income thresholds were transposed in the live backend's rules data, silently applying each household type's threshold to the wrong household type (2026-08-02, dev-loop iteration 38)**: found via a fresh targeted scan (delegated to a research subagent explicitly pointed at `backend/rules/*.json`, since brain.md had no actionable open item left besides the two deliberately-parked ones and that area had never been audited in any of the 37 prior iterations). `backend/rules/kinderzuschlag.json` had `"min_income_single": 900.00` and `"min_income_couple": 600.00` — but under §6a BKGG, the real Mindesteinkommensgrenze is **600€ for single parents (Alleinerziehende)** and **900€ for couples**, i.e. the two values were exactly swapped. `backend/engine.py:327`'s lookup logic itself (`min_income_couple if has_couple else min_income_single`) was already correct — only the underlying data was wrong — so the live Kinderzuschlag eligibility gate (`/api/v4/analyze`, the primary calculation path called from `smart-calculator.jsx`) was actually enforcing 600€ for couples and 900€ for single parents, backwards. Real-world impact: a single parent with parental income in the 600-899€ band was wrongly told they don't meet the minimum-income requirement and pushed toward Bürgergeld instead of being evaluated for Kinderzuschlag (worth up to 292€/child/month per the same rules file) they were legally entitled to have checked; a couple with income in that same band incorrectly cleared a gate the law says should block them, and could be shown a Kinderzuschlag "possible"/"eligible" result they don't legally qualify for. Same "live Python backend wrong, JS fallback engine already correct" bug shape as the Mietstufe fix (iteration 33) and the asylum-seeker exclusion (iteration 34) — confirmed via independent corroboration already present in the repo: `src/logic/benefit-engine.js:118` (`const minGross = parentsCount === 1 ? 600 : 900;`) and the site's own public-facing text at `src/pages/kindergeld/index.astro:241` ("Mindesteinkommen von 900 Euro brutto für Paare bzw. 600 Euro brutto für Alleinerziehende") both already had the correct 600/single, 900/couple values — only the live-backend JSON was wrong, since the original scaffold commit. Fixed with the minimal correct change: swapped the two values back (`min_income_single: 600.00`, `min_income_couple: 900.00`), touching only this one JSON file — `engine.py`'s lookup code needed no change. Verified via `python3 -m py_compile` (clean) + the existing `backend/verify_engine.py` assertion suite (all checks still pass, zero regression to the 3 existing household test cases, none of which happen to sit in the 600-899€ boundary band) + a targeted repro script constructing single-parent and couple households with net parental income in that exact boundary band, confirming the gate now resolves correctly for both (single parent clears the gate at 700€; couple is correctly blocked at 700€ — both inverted from the pre-fix behavior) + `npm run build` (clean, Python-only change with no frontend impact expected or found) + independent subagent review (PASS — independently re-confirmed the lookup logic and post-fix resolution by reading the actual current file contents, independently found the same two corroborating sources (JS engine + kindergeld page text) plus its own knowledge of §6a BKGG, confirmed via `git diff --stat` that only this one file/2 lines changed, ran its own independent `py_compile` + `verify_engine.py` + a fresh independently-written repro script including a before/after sanity check by temporarily reverting to the pre-fix JSON and confirming the bug reproduces exactly as described, then restoring the fix) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New open item found this pass, not fixed (flagged by both the implementer and the independent reviewer, informational/orthogonal to the swap fix)**: `calculate_kinderzuschlag()`'s minimum-income gate (`engine.py:325`, `parent_earned_income += inc.amount_net`) compares **net** income against the threshold, but §6a BKGG's Mindesteinkommensgrenze — and the site's own text quoted above — explicitly define it as a **brutto** (gross) threshold. Reusing the net-income accumulator for this specific legal test likely makes the real-world bar higher than the law intends (a user needs more gross income than the law requires to clear a net-income-based check against a brutto-denominated threshold), a plausible additional pre-existing bug — but distinct from and independent of today's swap fix, not verified/quantified this pass, and left unfixed to keep this iteration's change minimal and cleanly reviewable. Worth a dedicated future iteration: likely fix is accumulating `amount_brutto` instead of `amount_net` for this specific gate check (the later `parent_need`/excess-income calculation a few lines down may still legitimately want net income — needs care to not conflate the two legally-distinct income concepts).
- [x] **Bugfix: WBA-reminder signup accepted already-past Bescheid end dates, silently guaranteeing the user never gets reminded (2026-08-02, dev-loop iteration 37)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. `src/components/wba-reminder-modal.astro`'s year `<select>` had hardcoded options `2024`/`2025`/`2026`/`2027` — with today being 2026-08-02, two of those four options (2024, 2025) were already fully in the past. `src/pages/api/wba-reminder.ts` only validated `month` is 1-12 and `year` is 2000-2100 — no check that the resulting Bescheid end date is actually in the future. The cron job that sends the reminder emails (`src/pages/api/cron/send-wba-reminders.ts`) matches `reminder_date_1`/`reminder_date_2` against today's date via an exact-equality `.eq()` query — so any reminder date already in the past can never equal "today" again and will never fire, silently, forever. Real-world impact: a user who mis-clicked a past year (or any past month within the current year — the month dropdown has no date-awareness either) saw the "Service aktiviert!" success message and a Supabase row got written, but they would never receive either the 6-week or 2-week reminder email — the worst failure mode for a service whose entire promise is "we'll remind you before your benefits lapse." Fixed at both layers: (1) the modal's year select is now populated dynamically at runtime via script (`currentYear` through `currentYear+3`) instead of hardcoded values, so stale past years stop appearing as the years roll forward; (2) the API now rejects (400, German error message) if the computed `endDate.getTime() <= Date.now()`, checked before the reminder dates are calculated and before the Supabase insert, closing the root cause regardless of what the dropdown offers (defense in depth — also covers picking a past month within the current year). Verified via `npm run build` (clean) + independent subagent review (PASS — independently confirmed the diff via its own `git diff`, verified the date math (`new Date(yearNum, monthNum, 0)` last-day-of-month logic) is untouched and correct, confirmed the future-date check is correctly placed before both the reminder-date calculation and the Supabase insert so no past-dated row can leak through, confirmed the dynamically-generated year list preserves the `required` attribute and empty-placeholder default-selection behavior, confirmed the month dropdown and existing 1-12/2000-2100 range validation are untouched, noted one edge case as expected-not-a-bug — submitting on the last day of the current month for that same month is correctly rejected since both reminder windows would already be in the past too — ran its own independent `npm run build`) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: resolved iteration 35's own flagged follow-up — audited the ~27-33 per-file `plz`/`amt_adresse` mismatches, found and fixed 6 more genuine bugs (2026-08-02, dev-loop iteration 36)**: iteration 35 found 5 copy-pasted-`plz` bugs via cross-city PLZ duplicate detection, but its second reviewer also flagged a broader, unverified pattern — dozens of entries per file where `plz` differs from the postal code embedded in that same record's own `amt_adresse` field, hypothesized as intentional (many big/mid cities genuinely have multiple real postal codes: a representative city-wide PLZ vs. the specific street address of the Sozialamt/Jobcenter office) rather than confirmed bugs, since a same-shape mismatch with no cross-city collision can't be told apart from a real error just by pattern-matching. This iteration did the "dedicated pass" iteration 35 recommended: extracted all mismatches in both files (27 in `wohngeld-data.js`, 34 in `cities_2026.json` — via a small script matching each object's `plz` against the 5-digit PLZ embedded in its own `amt_adresse`), and checked each one individually rather than assuming the whole set was safe. The overwhelming majority (Berlin, Hamburg, München, Köln, Düsseldorf, Dortmund, Essen, Hannover, Wiesbaden, etc.) confirmed genuinely intentional — these are real multi-PLZ cities, left untouched. Found 6 that were not: (1) **4 missing-leading-zero PLZ in `cities_2026.json` only** — Leipzig (`"4103"`), Dresden (`"1067"`), Chemnitz (`"9111"`), Jena (`"7743"`) were each missing their leading zero (real PLZ: `04103`/`01067`/`09111`/`07743`), confirmed via that record's own `amt_adresse` field AND the sibling `wohngeld-data.js` entry for the same city, which already had the correct zero-padded value in every case — meaning this was a `cities_2026.json`-only regression, not present in the sibling file. Since `src/utils/get-authority.ts`'s Behördensuche lookup does an *exact* string match (`c.plz === plz`), any user typing their real, correctly-zero-padded postal code for these 4 cities could not find their city at all. (2) **1 corrupted non-numeric `plz` in `cities_2026.json`** — a second, duplicate Ludwigshafen record (`slug: "ludwigshafen-am-rhein"`, distinct from the primary `slug: "ludwigshafen"` record) had `plz: " Ludwigshafen"` — literally the city name as a string, not a postal code — fixed to `"67059"` per its own `amt_adresse` (and matching the primary Ludwigshafen record's real PLZ; the two rows being a same-city duplicate, like the pre-existing Bayreuth/Pforzheim duplicates, is expected and out of scope to deduplicate here). (3) **1 cross-city PLZ error in `wohngeld-data.js` only** — Recklinghausen had `plz: "45879"` while its own `amt_adresse` said `"Rathausplatz 3, 45657 Recklinghausen"`; the equivalent `cities_2026.json` Recklinghausen entry already correctly had `"45657"`. This one didn't get caught by iteration 35's cross-city-duplicate detector because no other city's `plz` field in the dataset happened to also be exactly `"45879"` — but the review agent found independent in-repo corroboration that `45879` genuinely belongs to Gelsenkirchen, not Recklinghausen: `src/components/smart-calculator.jsx`'s own hardcoded `MAJOR_CITY_PLZ_RANGES` table has `{ stadt: "Gelsenkirchen", min: 45879, max: 45899 }`, and traced the bug to its origin in the original source file, `src/data/master_daten_2026_improved.csv.txt:85`, which already had this exact self-contradiction (`45879,Recklinghausen,...,"Rathausplatz 3, 45657 Recklinghausen"`) — a pre-existing source-data bug, not introduced by any prior dev-loop iteration. Fixed to `"45657"`. Verified via `npm run build` (clean) + JSON-validity check (`cities_2026.json` still parses, still 109 entries) + a post-fix collision re-scan (confirmed none of the 6 fixes introduced a new cross-city PLZ collision; the two same-value cases that exist post-fix, Ludwigshafen's two rows and the pre-existing Bayreuth/Pforzheim ones, are same-city and expected) + independent subagent review (PASS on all 6 — independently re-verified each fix against its own `amt_adresse` field and the sibling file by reading the actual current file contents rather than trusting the description, specifically scrutinized the highest-risk claim (Recklinghausen/Gelsenkirchen, the one relying on real-world PLZ geography rather than an in-repo duplicate) and found the independent `smart-calculator.jsx` corroboration plus the source-CSV origin described above, confirmed via `git diff --stat` that only these two files/6 lines were touched, ran its own independent `npm run build` and JSON-parse check) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This closes iteration 35's flagged open item completely — every mismatch in both files has now been individually checked, not just pattern-matched.
- [x] **Data-correctness fix: 5 cities had a `plz` (postal code) field copy-pasted from a different city's record, breaking PLZ-based city lookup (2026-08-02, dev-loop iteration 35)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. Both city datasets — `src/data/wohngeld-data.js` (95 entries, used by the homepage calculator's `smart-calculator.jsx` "PLZ ONLY" search, `c.plz.startsWith(inputStr)`) and `src/data/cities_2026.json` (109 entries, used by the Behördensuche feature via `src/utils/get-authority.ts`/`src/components/authority-search.astro`/`authority-app.jsx`, several call sites doing an *exact* `c.plz === plz` match) — had 5 records where the `plz` field was an exact copy of a *different* city's real postal code, contradicted by that same record's own `amt_adresse` (office street address) field a few lines below, which contains the city's actual correct postal code: Wolfsburg (`"38100"`, Braunschweig's real PLZ → fixed to `"38440"`), Witten (`"58313"`, Herdecke's → fixed to `"58452"`), Marburg (`"35576"`, Wetzlar's → fixed to `"35037"`), Marl (`"45657"`, Recklinghausen's → fixed to `"45768"`), Gelsenkirchen (`"48143"`, Münster's → fixed to `"45881"`). Real-world impact: users typing their real, correct postal code for Wolfsburg/Witten/Marburg/Marl/Gelsenkirchen into either the homepage calculator's city search or the Behördensuche could not find their city at all (no fallback path existed either — none of these 5 are in `smart-calculator.jsx`'s `MAJOR_CITY_PLZ_RANGES` list), while users typing the *donor* city's real PLZ (Braunschweig/Herdecke/Wetzlar/Recklinghausen/Münster) saw a bogus, factually-wrong duplicate suggestion for the unrelated victim city. Not every city needed fixing in both files — Wolfsburg's `cities_2026.json` entry and Gelsenkirchen's `wohngeld-data.js` entry were already correct, only their sibling file had the bug; all 5 were wrong in at least one of the two files. **First review pass caught a real gap before commit**: the initial fix touched Marl only in `cities_2026.json` and missed the identical bug in the equivalent `wohngeld-data.js` record (still `plz: "45657"` there) — found and fixed in a follow-up edit within the same iteration, then re-verified by a second independent review pass. Verified via `npm run build` (clean, both before and after the Marl follow-up fix) + two sequential independent subagent reviews (first PASS-with-one-gap-found — independently re-derived each city's real PLZ from its own `amt_adresse` field rather than trusting the description, confirmed the pre-fix values genuinely matched a different city's real PLZ in both files, checked for and correctly distinguished 2 unrelated legitimate same-city duplicate-PLZ pairs — Bayreuth's two offices and a duplicate Pforzheim row — from the actual bug class, confirmed via its own `git diff --stat` that only the 2 data files were touched, confirmed no new PLZ collision was introduced, ran its own build; second pass re-confirmed the Marl fix specifically, re-scanned both full files — 95 and 109 entries respectively — for any remaining cross-city `plz` duplicate and found none, confirmed the other 4 fixes were untouched/still present, ran its own clean build) before committing — the implementer did not self-approve at any point. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **New open item found by the second review pass, not fixed this pass**: both files have ~27-33 entries (out of ~95-109) where `plz` differs from the postal code embedded in that same record's own `amt_adresse` field, but is *not* duplicated with another city's `plz` (e.g. Berlin's `plz: "10115"` vs. its `amt_adresse`'s `10178`) — this looks like an intentional pattern (a representative/central PLZ for the city's Mietstufe-zone vs. the specific street address of its Sozialamt/Jobcenter office, common for multi-PLZ big cities) rather than a bug, since it's the same shape as this iteration's own fix target only where the "mismatch" has no cross-city collision to prove it's wrong data rather than a deliberate choice — but it wasn't verified city-by-city this pass, since doing so exhaustively is a bigger data-audit task than a single dev-loop iteration and the two reviewers explicitly flagged it as "out of scope per the task's exact definition" rather than confirmed-safe. Worth a dedicated pass to spot-check a sample of these for correctness before assuming they're all intentional.
- [x] **Data-correctness/legal fix: live-backend calculation had zero awareness of asylum-seeker status — AsylbLG-eligible users were told they qualify for Bürgergeld/Wohngeld/Lastenzuschuss/Kinderzuschlag instead (2026-08-02, dev-loop iteration 34)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. The wizard's `status` select offers `asylum_seeker` (`src/components/smart-calculator.jsx`), and the local JS fallback engine (`src/logic/benefit-engine.js`) already correctly excludes `status === "asylum_seeker"` from `KinderzuschlagModule`/`WohngeldModule`/`LastenzuschussModule`/`BuergergeldModule` (an "JS-Engine-Audit" fix from an earlier, undated iteration) — but that fix only ever touched the local fallback engine. The live Render backend (`backend/engine.py`, the primary calculation path; the local engine only runs on network failure/timeout) had **zero** references to asylum/Asyl anywhere (confirmed via full-file grep) and no field in `backend/models.py`'s `Person` model to even carry the status. Real-world impact: under German law, asylum seekers during their asylum procedure receive AsylbLG (Asylbewerberleistungsgesetz) instead of Bürgergeld/Wohngeld/Kinderzuschlag — the systems are mutually exclusive — so any user selecting "Asylbewerber(in)" and completing the wizard (an unblocked, unvalidated flow — `status` isn't part of `isStepValid()`) got a normal, legally-incorrect SGB II/Wohngeld/Kinderzuschlag calculation from the primary path with no error or indicator anything was wrong, for one of the calculator's more vulnerable user groups. Fixed with the same scope as the JS engine's own exclusion: added `is_asylum_seeker: bool = False` to `Person` (`backend/models.py`), a `_is_asylum_seeker()` helper plus early-return `ineligible` guards (pointing users to the separate AsylbLG/Sozialamt process) added to exactly `calculate_sgb2()`, `calculate_wohngeld()` (also covers the frontend's derived Lastenzuschuss display, which reads directly off the Wohngeld result when `isOwner`), and `calculate_kinderzuschlag()` in `backend/engine.py` — deliberately leaving `calculate_kindergeld()`/`calculate_elterngeld()` untouched, matching the JS engine's own scope (Kindergeld/Elterngeld are not part of the AsylbLG/SGB-II mutual-exclusion system). Wired `is_asylum_seeker: status === "asylum_seeker"` into the live-backend payload's `main` member object in `smart-calculator.jsx`, alongside the existing `is_retired`/`is_student` fields. Verified via `python3 -m py_compile` (clean), the existing `backend/verify_engine.py` assertion suite (all checks still pass, zero regression to the 3 existing household test cases), `npm run build` (clean) + independent subagent review (PASS — independently re-traced the field-name/value matching frontend-to-Pydantic end-to-end rather than trusting the description, independently confirmed every one of the three guarded functions' existing return paths share the same `status`/`amount`/`reason`/`application_link` key shape as the new early-return dicts — relevant because `backend/main.py`'s `/api/v4/analyze` endpoint does `res["application_link"]` unconditionally per result, a KeyError/500 risk if a key were missing, confirmed none is, independently confirmed via its own grep of `benefit-engine.js` that the 3 guarded Python functions exactly match the JS engine's own AsylbLG-exclusion scope and that Kindergeld/Elterngeld are correctly left untouched, ran its own independent venv install + `verify_engine.py` + a fresh repro script proving both the asylum-seeker exclusion and a same-shape control household's unaffected normal eligibility, confirmed via `git diff --stat` that only the 3 intended files were touched) before committing — the implementer did not self-approve. **One non-blocking nitpick from the review, not changed**: the new `ASYLBLG_APPLICATION_LINK` points to the bare BAMF homepage (`https://www.bamf.de/`) while the accompanying reason text correctly says the actual venue is "beim Sozialamt" (BAMF runs the asylum status procedure, not the AsylbLG benefit payments, which are administered locally) — a slight authority/link mismatch, but the same style already exists elsewhere in this file (the pre-existing retiree→Grundsicherung-im-Alter redirect links to a `deutsche-rentenversicherung.de` page for a benefit that's also actually administered by the local Sozialamt), so left as consistent with established precedent rather than a new problem introduced here; worth a dedicated look if this file's application-link accuracy gets a future pass. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Data-correctness fix: live-backend Wohngeld/Lastenzuschuss calculation silently used Mietstufe 1 for every user, regardless of their real city (2026-08-02, dev-loop iteration 33)**: found via a fresh targeted scan (delegated to a research subagent), since brain.md had no actionable open item left besides the two deliberately-parked ones. `src/components/smart-calculator.jsx`'s `handleSubmit` builds a `payload` object (lines ~387-395) POSTed to the live Render backend (`/api/v4/analyze`, the primary calculation path — the local fallback engine only runs on network failure/timeout) but never included `city_tier` (the Mietstufe, 1-7), even though the component already holds it on `selectedCity.mietstufe` (displayed to the user at line 861, sourced from `src/data/wohngeld-data.js`). `backend/models.py:43` declares `city_tier: Optional[int] = None` — its own inline comment (`# HIER WAR DER FEHLER: Dieses Feld muss existieren!`) shows a prior pass added the field to the schema but never wired the frontend to populate it. `backend/engine.py:203`: `mietstufe = request.city_tier if request.city_tier in range(1, 8) else 1` — since the frontend always sent `city_tier` as absent/undefined, this always evaluated to the `else` branch, silently defaulting every live-API user to Mietstufe 1 (the lowest rent-cap tier) regardless of their actual selected city. Per iteration 25's own persona-testing note, Mietstufe varies Wohngeld by "17€-230€" for identical income across tiers 1-7 — so any user in a city with mietstufe 2-7 (the majority of the dataset) got a systematically understated, silently-wrong Wohngeld/Lastenzuschuss estimate from the primary calculation path, with no error and no visual indicator anything was off. Fixed with the minimal correct change: added `city_tier: selectedCity ? selectedCity.mietstufe : null` to the payload object. The `null` fallback is defensive/effectively unreachable in the real flow — confirmed `isStepValid(1)` requires `selectedCity !== null` before the wizard lets the user advance past step 1, and there's no UI path to null it back out afterward. Confirmed the local fallback engine (`src/logic/benefit-engine.js`/`calculator-2026.js`) was never affected by this bug — it already reads `input.selectedCity.mietstufe` correctly (with its own sensible default of 4 when no city is set), so this fix is scoped purely to the live-backend payload path. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed `selectedCity`/`.mietstufe` is the correct in-scope variable/property by tracing the file itself rather than trusting the description, independently re-traced `backend/engine.py`'s fallback-to-1 behavior in Python semantics (`None`/out-of-range values in `range(1,8)` evaluate `False`), confirmed the `null`-fallback path is safe and can't be reached in normal use via the step-validation gate, confirmed via `git diff` that this is exactly the one added line with nothing else touched, confirmed its own clean `npm run build`, and confirmed the already-correct local fallback engine was correctly left untouched) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **UX/SEO fix: dead-end internal links to retired `/wohngeld/`, `/buergergeld/`, `/buergergeld-grundsicherung/` routes on `/ratgeber/` and the sitewide lexikon sidebar (2026-08-02, dev-loop iteration 32)**: found via a fresh targeted scan (delegated to a research subagent) of areas not yet deeply audited in prior iterations — non-admin/non-api pages, `src/data/**`, `src/utils/**` beyond the two files already checked, and config files (`vercel.json`, `astro.config.mjs`). `src/pages/ratgeber/index.astro`'s two headline "guide" cards ("Wohngeld 2026", "Bürgergeld & Regelsätze") linked to `/wohngeld/` and `/buergergeld/`, and `src/pages/lexikon/[...slug].astro`'s "Kontext" sidebar widget — rendered on every one of the 18 lexikon article pages — picked one of `/wohngeld/`, `/buergergeld/`, or `/buergergeld-grundsicherung/` as its "Zum {Thema}-Ratgeber" CTA. None of these three paths are real Astro routes: confirmed via `vercel.json`, which has explicit permanent (301) redirects sending all three straight to the bare homepage (`/`) — these were retired pages, and `astro.config.mjs`'s sitemap `serialize()` still has stale priority rules referencing them too (left untouched, cosmetic/out of scope). Real-world impact: any user clicking these links — on the ratgeber hub or on any lexikon article's sidebar — was silently bounced to the homepage with zero context, a bait-and-switch broken-link bug (same class as the `/#wohngeldrechner` anchor bug fixed in iteration 3). Fixed by repointing all three to real, live, on-topic destinations instead of a bare homepage bounce: `/wohngeld/` → `/lexikon/wohngeld/` (a real, existing lexikon deep-dive article, `src/content/lexikon/wohngeld.md`), `/buergergeld/` → `/lexikon/buergergeld/` (`src/content/lexikon/buergergeld.md`), `/buergergeld-grundsicherung/` → `/grundsicherung-im-alter/` (a real, separately-live landing page, already independently linked from `src/pages/kindergeld/index.astro:250`, confirming it's a genuine route and not itself orphaned). **Regression caught by independent review and fixed before commit**: the lexikon sidebar widget's title/slug matching logic meant that viewing `/lexikon/wohngeld/` or `/lexikon/buergergeld/` directly would now show the widget linking to itself (a self-referential CTA) — strictly better than the prior dead-homepage-bounce for those same two pages, but still worth closing cleanly. Fixed with a one-line guard: the widget doesn't render at all when its computed `targetLink` equals the current article's own `/lexikon/{slug}/` URL. Verified via `npm run build` (clean, both before and after the self-link guard) + independent subagent review (PASS — independently confirmed both lexikon target articles exist and resolve to the correct URLs by cross-checking `getStaticPaths`/content-collection slug derivation and `astro.config.mjs`'s `trailingSlash: 'always'` setting, confirmed `/grundsicherung-im-alter/` is genuinely live, confirmed via its own repo-wide grep that no other reference to the three dead URLs remains anywhere in `src/`, confirmed `vercel.json`'s redirects were correctly left untouched since they still serve legitimate legacy/external inbound links, confirmed no scope creep, ran its own independent `npm install && npm run build`, and directly inspected the built HTML output to confirm the self-link guard actually suppresses the widget on the two affected pages while leaving it intact and correctly pointed everywhere else) before committing — the implementer did not self-approve. **Noted, not fixed this pass (non-blocking, flagged by the reviewer)**: the sidebar widget's `isSgbXii` branch (matching on `t.includes('sgb xii') || t.includes('grundsicherung im alter') || t.includes('renten') || s.includes('sgb-xii')`) currently matches zero of the 18 existing lexikon entries' titles/slugs, so the new `/grundsicherung-im-alter/` target is technically unreachable dead code today — harmless (no live effect, no broken link since it's never rendered), just future-proofing for lexikon entries that don't exist yet; not worth a dedicated pass on its own. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Critical bugfix: `src/lib/pdf-generator.ts` was missing its `pdf-lib` import, so `generateApplicationPdf()` threw `ReferenceError: PDFDocument is not defined` on every single call — breaking both live PDF-generation flows (2026-08-02, dev-loop iteration 31)**: found via a fresh targeted scan of the three candidates iteration 30 flagged as not-yet-deeply-audited (`src/utils/get-authority.ts`, `src/utils/districtUtils.ts`, `src/lib/pdf-generator.ts`). `src/lib/pdf-generator.ts`'s `generateApplicationPdf()` function body uses `PDFDocument`, `StandardFonts`, and `rgb` (all from the `pdf-lib` package, already in `package.json`) but the file had **no import statement at all** — a plain repo read confirmed zero `import` lines anywhere in the file. Traced via `git log --follow` / `git show`: the import was present and correct in the file's earlier history (under its pre-rename name `src/lib/pdfGenerator.ts`) but got silently deleted in commit `e7b5fae` ("feat: final launch version...") while the file was being rewritten — a later pure rename (`e193ecb`, 100% content match) carried the already-broken file forward to its current path/name unchanged, so the bug has been live since `e7b5fae`. This function is called by **both** of this site's real PDF-producing flows: `src/pages/api/send-application.ts` (the free "formloser Antrag" flow — the core free value proposition of the entire site) and `src/pages/api/webhooks/stripe.ts`'s `handleCheckoutCompleted()` (the paid Stripe checkout flow, generating the PDF sent after a successful payment) — meaning every real user request to either flow has been silently throwing and failing since that commit, not just a latent/unreachable defect. Confirmed live and not just a hypothesis by writing and running an actual reproduction script (`npx tsx`, calling `generateApplicationPdf` with a minimal valid payload) against the unfixed code first — confirmed it threw `ReferenceError: PDFDocument is not defined` — before making any change. Fixed with the minimal correct change: restored the single dropped import line, `import { PDFDocument, rgb, StandardFonts } from 'pdf-lib';`, at the top of the file. Nothing else in the file was touched. Re-ran the same reproduction script against the fixed code and confirmed it now resolves successfully and returns a valid PDF `Buffer`. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed the diff is exactly this 2-line addition via its own `git diff`, independently re-traced the `git show`/rename history rather than trusting the description, confirmed `PDFDocument`/`StandardFonts`/`rgb` are pdf-lib's correct named exports by checking `node_modules/pdf-lib`'s own type declarations, ran its own independent `npm run build`, wrote and ran its own separate repro script proving the exact before/after behavior — including temporarily `git stash`ing the fix to reconfirm the old code throws, then `git stash pop`-restoring the fix, leaving the repo in the fixed state — and confirmed both call sites (`send-application.ts`, `webhooks/stripe.ts`) pass data shapes matching the function's signature) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. This was the single highest-value finding available this pass — a total, unconditional break of both live PDF-generation paths is far more severe than any of the info-disclosure/hygiene items fixed in recent iterations — so no other candidate area was scanned further once this was found and confirmed. **Noted for a future pass, not fixed here (low priority, no live impact)**: while scanning `src/utils/get-authority.ts` on the way to this finding, spotted that its exported `getAllAuthoritiesForCity()` has zero callers anywhere in the repo (confirmed via repo-wide `git grep`) — genuinely dead code — and its own internal logic has a latent bug even if it were called (`c.plz === cityName`, comparing a PLZ field against the function's `cityName` argument, which the function doesn't even take a separate `plz` parameter for). Since it's unreachable, this has no live effect; a future hygiene pass should either remove it or fix-and-wire it, not both fixed and left dead. `src/utils/districtUtils.ts` was also checked this pass and found clean, no issue.
- [x] **Security/hygiene fix: raw internal `error.message` leaked to the client on 3 public API routes (2026-08-02, dev-loop iteration 30)**: found via a fresh targeted scan across `src/components/**`, `src/utils/**`, and a repo-wide grep for `error.message` in `src/pages/api/**`, prompted by there being no actionable open item left in brain.md besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). Also checked several other candidate areas this pass that turned out to be non-issues, worth recording so a future scan doesn't re-tread them: `chat-widget.jsx`'s `dangerouslySetInnerHTML={{ __html: renderText(msg.text) }}` is safe — `renderText()` HTML-escapes `&`/`<`/`>` before applying its own limited markdown (`**bold**`, newlines); the several `innerHTML` template-literal sites in `authority-search.astro` and `results-section.astro` only interpolate static, non-user-controlled data (city names from the static `cities_2026.json` dataset selected via dropdown, not free text; benefit/opportunity titles hardcoded in `backend/main.py`) — confirmed by tracing `input.city` back to `selectedCity`, which is only ever set via `handleCitySelect(city)` from the static autocomplete list, never from the raw free-text search field; `src/pages/api/webhooks/stripe.ts`'s `STRIPE_WEBHOOK_SECRET` check already fails closed (early-returns if unset), so it's not another instance of the fail-open bug class fixed in iterations 27-29. The actual finding: `src/pages/api/checkout.ts`, `src/pages/api/leads.ts`, and `src/pages/api/send-application.ts` (three public, unauthenticated POST routes — Stripe checkout creation, Supabase lead insert, real email+PDF send respectively) each caught their internal error and echoed the raw `error.message` string straight into the client-facing JSON 500 response body (`{ error: error.message }` / `{ error: error.message || '...' }` / `{ error: 'Interner Server Fehler: ' + error.message }`). That leaks internal implementation details to any anonymous caller — Stripe SDK internals/config hints, Supabase error text (which can include table/column/constraint names), or Resend/pdf-lib internals — a minor information-disclosure smell, not a severe vuln on its own, but unnecessary and inconsistent with how the rest of the codebase already treats untrusted-facing surfaces carefully. Fixed by replacing each of the three response bodies with a fixed, generic German-language error string, while leaving every `console.error(...)` call immediately above untouched so full error detail is still captured server-side for debugging — this is not error-swallowing, only not re-exposing internals to the client. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed via its own `git diff` that exactly these 3 single-line swaps were made with no other logic touched, confirmed the `console.error` calls still log the complete original error object, grepped the frontend callers in `src/components/**` that hit `/api/leads` and `/api/send-application` and confirmed neither does string-matching control flow on `.error` content — both just display it generically in an `alert()` with a fallback, confirmed no frontend caller of `/api/checkout` exists in `src/components` to depend on its error text either, confirmed its own clean `npm run build`, confirmed no unrelated files touched) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **No new actionable open item found this pass** besides the two long-standing deliberately-parked ones below — a future iteration should do another fresh targeted scan (candidates not yet deeply audited: `src/utils/get-authority.ts`'s district/fallback lookup logic, `src/utils/districtUtils.ts`, `src/lib/pdf-generator.ts`).
- [x] **Security fix: fail-open `CRON_SECRET` check in `src/pages/api/cron/send-wba-reminders.ts` (2026-08-02, dev-loop iteration 29)**: found via a fresh targeted scan of `src/pages/api/**`, prompted by there being no actionable open item left in brain.md besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). This is a public POST route (`/api/cron/send-wba-reminders`, registered in `vercel.json`'s `crons` block, invoked daily by Vercel Cron) with no access control besides its own `Authorization: Bearer <CRON_SECRET>` header check — but that check was `if (CRON_SECRET && authHeader !== ...)`, which **fails open**: if `CRON_SECRET` is unset in the deployment environment, the condition is false and the handler runs with zero authentication, letting anyone on the internet POST to this URL and trigger a real batch send of WBA reminder emails (via Resend, from the site's verified domain, to real leads pulled from Supabase) on demand, repeatable at will with no rate limiting. Exact same anti-pattern already found and fixed for `ADMIN_PASSWORD` in `admin/feedback.astro` (iteration 27) and its precedent, `dashboard.astro` (iteration 28) — a secret-gated check that silently degrades to "no gate at all" when the secret is unconfigured, instead of failing closed. Fixed by inverting the condition to `if (!CRON_SECRET || authHeader !== ...)`, so an unconfigured `CRON_SECRET` now safely disables the endpoint (401 on every request) rather than leaving it wide open — deploying/using this feature requires an operator to set `CRON_SECRET` in Vercel's env config (same operational requirement iteration 27 already established for `ADMIN_PASSWORD`), matching how Vercel's native Cron Jobs feature auto-injects that header when the env var is configured, so a correctly configured deployment sees no behavior change at all. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-verified the diff is exactly this 2-line change via its own `git diff`, hand-checked all 4 truth-table cases (secret unset/set × header matching/mismatched) produce the intended pass/401 outcome, confirmed via repo-wide grep that no other code calls this endpoint expecting the old unauthenticated-fallback behavior, confirmed via its own independent `npm install && npm run build` run, confirmed no `.env.example`/docs conflict) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed by either the implementer or the reviewer. **Not addressed this pass, out of scope**: this endpoint's Supabase client uses `PUBLIC_SUPABASE_ANON_KEY` (not the service-role key used elsewhere for admin operations) to read/update the `wba_reminders` table, meaning the security of reads/writes to that table also depends on Supabase Row-Level-Security policies configured outside this repo — pre-existing since the file's original commit, not a regression introduced here, and out of scope for a single-file auth-gate fix; worth a dedicated look if the RLS policy for that table is ever in question.
- [x] **Security fix: `src/pages/admin/dashboard.astro` had zero authentication, leaking real lead PII to any site visitor (2026-08-02, dev-loop iteration 28)**: found via a fresh targeted scan of `src/pages/admin/**`, prompted by there being no actionable open item left in brain.md besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). Unlike `src/pages/admin/feedback.astro` (password-gated as of iteration 27), `dashboard.astro` had **no auth check at all** — only a code comment saying "In einer Produktionsumgebung würdest du hier eine Authentifizierung vorschalten" that was never acted on. Any visitor loading `/admin/dashboard` got a page that ran client-side JS querying the Supabase table `master_claims_2026` (real leads: `full_name`, `city`, `income`, `total_claim_value`, `avgs_interest`) via a Supabase `anon` key hardcoded in the page source, with the results rendered into the DOM via raw `innerHTML` string concatenation — a stored-XSS vector on top of the PII leak if `full_name`/`city` ever contained HTML (no code in this repo currently writes to that table, so the write path is external/unverified, but the read side was live and exploitable regardless of who populates it). Fixed by porting the exact server-side auth pattern already shipped in `feedback.astro` in the same directory: `ADMIN_PASSWORD`-gated login form, httpOnly/`sameSite: 'strict'`/`secure`-in-prod `admin_auth` cookie, and the data fetch moved server-side using `SUPABASE_SERVICE_ROLE_KEY` (never shipped to the client) instead of the client-side anon-key query. Rendering now goes through Astro's normal auto-escaping JSX templating instead of `innerHTML`, closing the XSS angle as a side effect. Also dropped the dead "Aktion"/"Details" column (a button with no `onclick` handler that never did anything) and changed the "Aktualisieren" control from a client JS call to a plain page-reload link, both now-unnecessary once the page is server-rendered per request. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed the auth gate has no bypass, confirmed zero leftover client-side Supabase/anon-key code via its own grep, confirmed all lead fields render via auto-escaping `{}` JSX with no `set:html`/innerHTML anywhere, confirmed the dropped dead button and reload-link change are harmless given nothing else in the repo referenced `loadLeads`/`window.loadLeads`/`master_claims_2026`, confirmed cookie name/path/flags match `feedback.astro` exactly so logging into either admin page authenticates both — consistent with existing behavior, not a new gap) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Not addressed this pass, worth a future look**: the shared `admin_auth` cookie means both admin pages trust the same single shared password with no per-user accounts/audit trail — pre-existing since `feedback.astro`'s iteration-27 fix, not a regression introduced here, but worth revisiting if the admin surface grows.
- [x] **Security fix: hardcoded admin password in `src/pages/admin/feedback.astro`, plus a formData double-read bug in the same file (2026-08-01, dev-loop iteration 27)**: found via a fresh targeted scan of `src/pages/**` for new bugs/TODOs, prompted by there being no actionable open item left in this file besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional User Accounts feature, out of scope). `src/pages/admin/feedback.astro` — the admin panel that reads/deletes real user feedback (PII: contact info, free-text messages, submitted URLs) from Supabase via the `SUPABASE_SERVICE_ROLE_KEY` — had `const ADMIN_PASSWORD = "navigator-secret"` hardcoded and committed in plaintext in this **public** repo: anyone reading the source on GitHub could read the password and log in as admin to view or delete any user's feedback data. Fixed by moving it to `import.meta.env.ADMIN_PASSWORD` (an env var, never committed — `.env`/`.env.production` are already gitignored), matching how `SUPABASE_SERVICE_ROLE_KEY`/`PUBLIC_SUPABASE_URL` are already read in the same file. Failed closed rather than falling back to any default: if `ADMIN_PASSWORD` is unset, every auth check is gated on `ADMIN_PASSWORD &&` so login is fully disabled with a clear inline error ("ADMIN_PASSWORD fehlt in .env — Login ist deaktiviert."), not silently bypassable via an empty-string comparison. Also hardened the `admin_auth` cookie (previously had none of these): added `httpOnly: true` (blocks reading the cookie via JS/XSS), `sameSite: 'strict'` (blocks the cookie being sent on cross-site requests, closing a latent CSRF gap on the delete/toggle POST actions, which had no CSRF token), and `secure: isProd` (only sent over HTTPS in production, `isProd` derived the same way `webhooks/stripe.ts`/`send-application.ts` already detect prod via `process.env.VERCEL_ENV`). **Second, unrelated bug found and fixed in the same file while reviewing the auth flow**: the original code called `Astro.request.formData()` up to 3 times per POST request across 3 separate if-blocks (login-check, a near-duplicate login block, and the delete/toggle action handler) — a `Request` body stream can only be consumed once per the Fetch spec, so the second call would throw ("Body is unusable" under Node/undici, which Astro's Vercel Node serverless functions use) on essentially every real POST to this page, meaning the login flow was likely already broken in production independent of the password issue. Fixed by parsing `formData` exactly once at the top of the frontmatter (`null` for non-POST requests) and reusing that single object in both the login check and the action handler, removing the now-redundant duplicate login block entirely. Verified via `npm run build` (clean) + independent subagent review (PASS — independently re-confirmed the password string is gone repo-wide via its own grep, traced all 6 auth-flow scenarios by hand including the empty-string-`ADMIN_PASSWORD` bypass case, confirmed the formData single-read fix is correct and complete, confirmed no leftover references to the removed `data` variable, confirmed the env-var read pattern matches the file's own existing style, confirmed no scope creep) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed. **Not fixed this pass, out of scope**: `item.url` (a user-submitted URL) is rendered as a raw `href` in the admin table without scheme validation — a `javascript:`-scheme URL could theoretically execute in the admin's browser context if clicked, but only after the admin (not an anonymous attacker) is already authenticated and manually clicks a suspicious link in their own feedback table; lower severity than the credential leak, worth a dedicated look in a future pass. No `.env.example`/README env-var docs exist in this repo to update (checked, none found) — deploying this fix requires the operator to set `ADMIN_PASSWORD` in Vercel's env config, otherwise the admin panel is (safely) locked out.
- [x] **Security fix: added rate limiting to `/api/checkout.ts`, the last unrate-limited cost-vector flagged in iteration 25 (2026-08-01, dev-loop iteration 26)**: `src/pages/api/checkout.ts` creates a real, unauthenticated Stripe Checkout session via the Stripe API on every request, with zero rate limiting — the "reasonable next candidate" iteration 25 explicitly flagged but left unfixed after covering `send-application.ts`/`leads.ts`/`wba-reminder.ts`/`chat.ts`. Wired the same `checkRateLimit`/`rateLimitResponse` helper from `src/lib/rate-limit.ts` in as the first statement of the `POST` handler, before both the `STRIPE_SECRET_KEY` env check and the `stripe.checkout.sessions.create()` call, using a distinct `scope: 'checkout'` bucket (5 requests/60s per IP — checked against no collision with the four existing scopes). Chose 5/min as proportional to this route's actual risk shape: unlike `send-application.ts` (real email + PDF generation per request, tightest limit) it only creates a Stripe API session object (no real payment or email side effect until a customer completes checkout), so it's closer in cost-shape to `chat.ts`/`leads.ts` than to the email-sending routes, while still meaningfully bounding Stripe-API-call-volume abuse and fake-session creation. Did not add additional input validation (email format, required fields) in this pass — out of scope, and the injection-relevant half of this concern (unescaped Stripe metadata reaching outbound HTML emails) was already closed in iteration 12's fix to the downstream `webhooks/stripe.ts` handler, confirmed still in place (`stripControlChars`/`EMAIL_REGEX` applied when metadata is read back before use). Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed rate-limit check is the literal first statement with a real early return, confirmed scope string doesn't collide with the other 4 in-use scopes, confirmed the 5/min limit is a defensible proportional choice, confirmed diff is minimal with no regression to the existing Stripe session logic) before committing.
- [x] **Security fix: added IP-based rate limiting to public unauthenticated endpoints (2026-08-01, dev-loop iteration 25)**: resolved a residual risk flagged repeatedly (iterations 8, 9, 10) but never fixed — `src/pages/api/leads.ts`, `send-application.ts`, and `wba-reminder.ts` are all public, unauthenticated POST routes with zero rate limiting, each capable of triggering a real Supabase write and/or a real outbound email per request. `send-application.ts` is the highest-value target: it sends a real email (with a PDF attachment) to a real government authority on the user's behalf at no cost and no barrier — an unrate-limited spam/abuse vector against third parties riding the site's domain reputation. Added `src/lib/rate-limit.ts`: a small in-memory fixed-window counter keyed by `scope:clientIP` (IP from `x-forwarded-for`/`x-real-ip`), with periodic sweeping of expired buckets to bound memory. Wired into all three flagged routes, plus a **new finding** from this pass: `src/pages/api/chat.ts` forwards to OpenAI/Gemini using a real API key when one is configured (`OPENAI_API_KEY`/`GEMINI_API_KEY`) and had zero request limiting — an unbounded real-money-cost vector the moment either key is set, even though no key is currently configured in this repo's tracked env. Limits chosen proportional to cost/sensitivity: `send-application.ts` 3/10min (real email + PDF gen), `leads.ts`/`wba-reminder.ts` 10/min (DB write only), `chat.ts` 20/min (interactive UX, cost risk only if a key is later added). **Known limitation, documented in the file's own comment, not fixed this pass**: state is a module-level `Map`, so it only limits requests handled by the same warm Vercel serverless instance — it does not coordinate across concurrent instances under real distributed-traffic abuse. Confirmed via `astro.config.mjs` (`adapter: vercel()`, no `edge` flag) that this project uses Node.js Serverless Functions, which do keep warm module state across invocations in the same container, so the approach works as intended for its stated (partial) scope; a shared store (e.g. Upstash Redis) would close the multi-instance gap but needs a new dependency + env var, out of scope here. IP-derivation trusts Vercel to set (not let a client override) `x-forwarded-for`/`x-real-ip` for direct connections — documented inline, reasonably well-supported per available evidence but not independently verified against a live deployment in this pass. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed algorithm has no off-by-one and never blocks a legitimate first request, confirmed the check is the literal first statement in all 4 `POST` handlers with a real `return` on block (no fall-through to the expensive work below), confirmed import paths, confirmed Vercel serverless compatibility, confirmed no scope creep) before committing; the reviewer's one requested polish (documenting the IP-trust assumption inline) was applied before commit. **New open item from this pass, not fixed**: `src/pages/api/checkout.ts` (creates real unauthenticated Stripe Checkout sessions) has the same unrate-limited-cost-vector shape as `chat.ts` but wasn't in this pass's scope — reasonable next candidate.
- [x] **Security fix: `/api/leads` forwarded raw, unsanitized data to the external Make.com webhook despite sanitizing the same fields for Supabase (2026-08-01, dev-loop iteration 24)**: found via a fresh targeted scan of `src/pages/api/**`, prompted by there being no other actionable open item in this file besides the two deliberately-parked ones. Iteration 10 added `stripControlChars`/`EMAIL_REGEX` sanitization to `src/pages/api/leads.ts` for the Supabase insert and its own brain.md note claimed this sanitization applied "before they reach Supabase **or the Make.com webhook forward**" — but re-reading the actual code (confirmed via `git log`/`git show` on the original commit, `cf772e5`, which never touched the webhook block) showed that claim was inaccurate: the webhook-forward call at the bottom of the file spread the raw `data` object (`...data`) verbatim into the JSON body POSTed to the hardcoded external `MAKE_WEBHOOK_URL`, completely bypassing the sanitization done two lines above for the Supabase insert. Same "sanitize for one sink, forward raw to another" bug class already found and fixed multiple times elsewhere in this repo (Stripe webhook HTML-injection in iteration 12, WBA-reminder emails in iteration 9) — here an attacker's CR/LF or other control characters in `first_name`/`email`/`interest`/`city`/`modal_type`/`partner_vertical` would reach the external Make.com automation completely unsanitized even though the Supabase copy was clean, and Make.com scenarios like this typically fan out into downstream emails/CRM actions where such injection matters. Also noticed and fixed a smaller latent gap in the same file while there: `phone` was never run through `stripControlChars` for the Supabase insert either (unlike every sibling field) — added the same sanitization. Fixed by reusing the already-sanitized local variables (`first_name`, `last_name`, `email`, `phone`, `interest`, `city`, `modal_type`, `partner_vertical`) as overrides spread after `...data` in the webhook body, so the sanitized values win while other pass-through fields (`consent_partner_transfer`, etc.) are preserved unchanged. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed override order correct so sanitized values win, confirmed `lead-modal.astro` (the sole caller) has no dependency on the raw phone value being echoed, confirmed no other unsanitized free-text field still reaches the webhook, confirmed diff is minimal/focused) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Security fix: removed two dead, unauthenticated, live-reachable Flask endpoints from `backend/main.py` (2026-08-01, dev-loop iteration 23)**: found via a fresh targeted scan of `backend/**` prompted by iteration 22's JS-engine-audit note about a missing `source_type` on child/partner income — that specific lead turned out to be a non-issue on closer inspection (the wizard's "Einkommen der Kinder (z.B. Unterhalt, Waisenrente)" field is legally non-Erwerbseinkommen, so `source_type` defaulting to Pydantic's `IncomeType.NONE` already produces the legally-correct Versicherungspauschale treatment — identical to what `IncomeType.MAINTENANCE` would get, since neither is in `ERWERBSEINKOMMEN_TYPES`; and the wizard collects no partner income at all, so that half of the note was moot too — no code change needed there). Continuing the scan turned up a real, actionable finding instead: `backend/main.py` (the Flask app deployed on Render via `Procfile`'s `gunicorn ... main:app` — confirmed live, since `smart-calculator.jsx` calls this same service's `/api/v4/analyze`) exposed two more routes with zero auth, zero rate limiting, and zero use by any current frontend code (confirmed via repo-wide grep for `create-checkout-session`/`v4/leads`/`LeadRequest`/`leads.jsonl` — zero hits outside `backend/main.py`/`models.py` themselves): (1) `POST /api/v4/create-checkout-session` created a real Stripe Checkout session from fully attacker-controlled `email`/`firstName`/`lastName`/`authority` request-body fields with no validation at all (not even the `.includes('@')` check other now-fixed endpoints originally had) — an unauthenticated Stripe-session-creation oracle; (2) `POST /api/v4/leads` appended unvalidated request JSON verbatim to a local `leads.jsonl` file with no field checks. Both were dead duplicates of the real, actively-hardened live paths (`src/pages/api/checkout.ts` for Stripe, `src/pages/api/leads.ts` for Supabase leads — both untouched, both already have proper validation from iterations 8-12) — same "orphaned but publicly reachable" bug class already found and fixed for `send-pdf.ts` in iterations 10-11. `git log --oneline -- backend/main.py` shows these routes are untouched placeholder/test code from the original single scaffold commit (`sk_test_Platzhalter_Bitte_Ersetzen` fallback key, an explicit `# TODO: Setze deinen ECHTEN Secret Key` comment) — never wired to real production Stripe credentials, never called by the frontend. Removed both routes plus the now-unused `stripe` import/config, the now-unused `LeadRequest` Pydantic model (`backend/models.py`), the now-unused `json`/`datetime` imports, and the `stripe` line from `backend/requirements.txt`. Verified via: `python3 -m py_compile main.py models.py` (clean), a full backend test-client smoke test in a fresh venv confirming `/` and `/api/v4/analyze` still work exactly as before and the two removed routes now correctly 404, the existing `backend/verify_engine.py` assertion suite (all 12 checks still pass, zero regression), `npm run build` (clean, this diff is backend-Python-only so no frontend impact expected or found), and an independent subagent review (PASS — independently re-confirmed zero live references to the removed routes/model anywhere in the repo, confirmed `/api/v4/analyze` and its Pydantic models (`HouseholdRequest`/`Person`/`IncomeSource`/`IncomeType`/`TerminationReason`) are byte-for-byte untouched, confirmed no dangling imports, confirmed via git history that these routes were never the production-intended payment path) before committing — the implementer did not self-approve. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diff (same npm-version artifact as every prior iteration) was reverted, not committed.
- [x] **Vermögensfreibetrag wizard field: built the missing UI + fixed the flat-threshold bug, closing out the seven dead fields flagged in iteration 18 (2026-08-01, dev-loop iteration 22)**: resolved `hasHighAssets` (Step 4), the last of the seven fields — deliberately deferred in iteration 21 because a naive boolean checkbox couldn't represent that Kinderzuschlag/Wohngeld/Lastenzuschuss/Bürgergeld/Sozialhilfe/Grundsicherung-im-Alter each apply a *different* real Schonvermögen threshold. Added a numeric "Vermögen / Ersparnisse (Haushalt)" input to Step 4 (state `assets`, wired into `profileInput.assets`), then in `src/logic/benefit-engine.js` replaced each module's blanket `if (input.hasHighAssets)` gate with a real numeric comparison against that module's own already-documented threshold (Kinderzuschlag/Bürgergeld: 40k; Wohngeld/Lastenzuschuss: 60k; Sozialhilfe: 10k; Grundsicherung im Alter already had a numeric `>10000` check, now finally fed real data since `input.assets` had no UI before). **First review pass caught a real correctness bug before commit**: all five thresholds were flat household totals, but German Schonvermögen law scales with household size — SGB II Karenzzeit (Kinderzuschlag/Bürgergeld) is 40k for the first person **+15k per additional household member**; WoGG Wohngeld-Plus (Wohngeld/Lastenzuschuss) is 60k **+30k per additional member**; and the SGB XII modules' *own pre-existing reasoning text* already said "10.000 € pro Person" while the code compared a flat total, contradicting itself. Since this diff is what makes these previously-100%-dead gates start actually firing for real users, shipping the flat version would have wrongly told multi-person households (e.g. a family of 4 with 70k savings — legally fine under the real 85k SGB-II-Karenzzeit limit) that they don't qualify for benefits they actually do. Fixed by adding two small helpers (`getAssetLimit`/`exceedsAssetLimit`: base + per-additional-member increment, used for the 4 SGB-II/WoGG-style modules) and a flat per-person multiplication for the 2 SGB-XII modules (matching their own stated "pro Person" text) — verified by hand for 1-person (reduces to the original single-person figures) and 4-person households (85k/150k/40k respectively, no double-counting). Updated the Wohngeld/Lastenzuschuss/Bürgergeld reasoning strings and the wizard's `InfoTooltip` copy to reflect the per-household-size scaling. **Deliberately did not touch the live backend** (`backend/engine.py` has no assets/Vermögen logic at all, confirmed via grep — same reasoning as Pflegegrad/Bestattungsvorsorge in iterations 20-21). Verified via `npm run build` (clean) + **two sequential independent subagent reviews** (first found the flat-threshold bug described above and specified the exact fix; a second, fresh review pass re-verified the corrected math by hand for 1- and 4-person households, confirmed no variable-shadowing regression in the Grundsicherung-im-Alter module, confirmed the `hasHighAssets` fallback survived at all 6 call sites, and confirmed the tooltip copy still matches) before committing — the implementer did not self-approve at any point. This closes iteration 18's original list of 7 dead wizard fields to fully resolved (7/7), each built one at a time across iterations 19-22 with independent review, rather than batched through in one unattended pass as iteration 18 explicitly warned against.
- [x] **JS-Engine-Audit + 4 Korrekturen (2026-08-01)**: Follow-up zum Python-Backend-Audit — auf Nutzerwunsch die separate, im Frontend genutzte JS-Engine (`src/logic/calculator-2026.js` + `src/logic/benefit-engine.js`, importiert von `smart-calculator.jsx` als Fallback, wenn die Live-API nicht erreichbar ist) mit einem Node-Testharness (diverse Personas + gezielte A/B-Tests) geprüft. Die JS-Engine implementierte die 5 Python-Fixes strukturell bereits (Mietstufe, Rentner/Studenten-Ausschluss, Freibetrag nach Einkommensart, Kinderzuschlag/Bürgergeld-Vorrang) — aber vier konkrete Bugs verhinderten, dass das korrekt griff:
  1. **Live-Payload sendete `is_retired`/`is_student`/Einkommens-`source_type` nie** (`smart-calculator.jsx`) — dadurch waren die frisch gepushten Python-Backend-Fixes über den echten API-Pfad nur teilweise wirksam, und mit dem neuen Freibetrag-Fix hätten alle echten Nutzer (auch normale Angestellte) fälschlich nur die 30€-Pauschale statt des korrekten gestaffelten Freibetrags bekommen — ein durch den Python-Fix neu entstehendes Regressionsrisiko, hier verhindert.
  2. **String-Mismatch**: `calculator-2026.js` prüfte 3x `status === 'self-employed'` (Bindestrich), das Formular liefert aber `'self_employed'` (Unterstrich) — Selbstständige liefen immer durch die generische Angestellten-Logik statt der dedizierten Selbstständigen-Behandlung.
  3. **Asylbewerber wurden nicht von Bürgergeld/Wohngeld/Lastenzuschuss/Kinderzuschlag ausgeschlossen** (`benefit-engine.js`) — konnten gleichzeitig mit AsylbLG angezeigt werden, obwohl beide Systeme sich während des Asylverfahrens ausschließen. Fix ergänzt in allen vier betroffenen Modulen (der erste Fix erzeugte laut Review-Pass eine Lücke bei den übrigen drei, sofort mitbehoben).
  Verifiziert über: Node-Testharness mit 19 diversen Personas (0 Fehler/Auffälligkeiten), gezielte A/B-Tests (Mietstufe 1-7 zeigt echte Varianz; self_employed vs. employee zeigt jetzt unterschiedliche, korrekte Beträge; Asylbewerber zeigt nur noch AsylbLG statt AsylbLG+Bürgergeld), `npm run build` (grün), unabhängiger Review-Subagent (fand die unvollständige Asylbewerber-Ausschluss-Lücke, sofort behoben) vor dem finalen Commit. **Nicht behoben, nur notiert**: Partner- und Kindes-Einkommen im Live-Payload haben ebenfalls kein `source_type` (fällt auf Pydantic-Default `"none"` zurück, also Pauschale statt Freibetrag) — laut Review vorbestehend, nicht durch diesen Pass verursacht, aber gleiche Fehlerklasse; Kandidat für eine künftige Iteration.
- [x] **100-Personas-Audit + 5 Engine-Korrekturen (2026-08-01)**: Auf Nutzerwunsch `backend/engine.py` direkt (nicht über die Live-API) mit 100 diversen Haushalts-Personas durchgerechnet (Singles/Paare/Alleinerziehende mit 1-3 Kindern, alle 7 Mietstufen, Einkommen 0-6000€, Schwangerschaft, Behinderung, Rentner, Studierende, Eigentümer, Sanktionsfälle). Fünf systematische Lücken gefunden und behoben, in dieser Reihenfolge:
  1. **Wohngeld nutzte `city_tier` (Mietstufe) gar nicht** — A/B-Test bewies identischen Betrag über alle 7 Mietstufen hinweg. `calculate_wohngeld()` implementiert jetzt die amtliche WoGG-Rasterformel (§19 WoGG: `WG = 1,15 * (M - (a+b*M+c*Y)*Y)`) mit den bereits vorhandenen, aber ungenutzten Mietobergrenzen/Koeffizienten aus `rules/wohngeld.json`, inkl. Bagatellgrenze (<10€ → ineligible statt auf 10€ hochgerundet).
  2. **Rentner wurden als Bürgergeld-berechtigt angezeigt** — `is_retired`/Alter wurden nie geprüft. Neue Vorprüfung in `calculate_sgb2()`: Hauptperson ab `REGELALTERSGRENZE=67` oder `is_retired=True` → Redirect auf Grundsicherung im Alter (SGB XII) statt Bürgergeld-Betrag.
  3. **Erwerbstätigenfreibetrag wurde auf alle Einkommensarten angewendet** (auch Rente/Unterhalt) statt nur auf Erwerbseinkommen. Neue Konstante `ERWERBSEINKOMMEN_TYPES` (employment/minijob/self_employed) erhält den gestaffelten Freibetrag; alle anderen Einkommensarten nur die neue `versicherungspauschale` (30€, in `rules/sgb2.json`).
  4. **Studierende wurden ohne BAföG-Ausschlussprüfung als Bürgergeld-berechtigt angezeigt** (§27 SGB II). Neue Vorprüfung: Hauptperson `is_student=True` und nicht gleichzeitig `is_single_parent`/`is_pregnant` (bekannte Ausnahmen) → ineligible mit Erklärung.
  5. **Kein Vorrang zwischen Kinderzuschlag und Bürgergeld** — beide konnten gleichzeitig als unabhängige Ansprüche angezeigt werden. `evaluate_all()` prüft jetzt das Nachrangprinzip (§12a SGB II): nur ein **bestätigtes** (`status=="eligible"`) Kinderzuschlag-Ergebnis kann Bürgergeld auf ineligible setzen, wenn Wohngeld+Kinderzuschlag die Lücke decken; ein unsicheres `"possible"`-Ergebnis hebt den Anspruch nicht auf, sondern ergänzt nur einen Hinweistext (Korrektur aus dem Review-Pass, siehe unten).
  Verifiziert über: erneuter 100-Personas-Lauf (0 Fehler, alte Widersprüche aufgelöst oder transparent erklärt), gezielte A/B-Tests (Mietstufe 1-7 bei identischem Einkommen zeigt jetzt echte Varianz 17€-230€ statt konstant 36€; Erwerbseinkommen vs. Rente bei identischem Netto zeigt jetzt unterschiedliche Bürgergeld-Beträge), bestehende `verify_engine.py`-Suite (weiterhin grün, keine Regression), `npm run build` (grün). Unabhängiger Review-Subagent fand einen echten Fix-Bedarf (Nachrang-Umschaltung reagierte auf unsicheren "possible"-Status statt nur auf "eligible") — behoben und erneut vollständig verifiziert vor dem Commit. Hinweis: `is_cared_for` (Pflegegrad) bleibt weiterhin unbenutzt in `backend/engine.py` — deckt sich mit dem unabhängigen Fund aus Iteration 18 unten; wurde in diesem Pass bewusst nicht behoben (kein Backend-Modul für Pflege existiert bislang, siehe dortige Begründung).
- [x] **Bestattungsvorsorge/Hinterbliebenenrente wizard field: built the missing UI, third of the seven dead fields flagged in iteration 18 (2026-08-01, dev-loop iteration 21)**: resolved `isBereaved` (Step 4, declared in `smart-calculator.jsx`, default `false`) — already read into `profileInput` (the local-fallback-engine payload) and already gates one module in `src/logic/benefit-engine.js` (~line 869, `Hinterbliebenenrente`: `isRelevant: (input) => !!input.isBereaved`, fixed 450€ average estimate, softest `eligible: "possible"` tier) but had zero rendered UI, same dead-state pattern as disability/Pflegegrad before iterations 19-20. Added a single checkbox ("Ehepartner oder Elternteil verstorben?") to Step 4, styled identically to the sibling GdB checkbox block directly above it (same card/checkbox/label/`InfoTooltip` pattern) — no conditional sub-fields needed since the engine module itself is a flat yes/no gate with a fixed estimate, unlike GdB/Pflegegrad which have officially-scaled sub-values. Kept the `InfoTooltip` copy explicitly hedged ("kann... auslösen, sofern die verstorbene Person rentenversichert war") to match the engine's own soft "possible" framing and avoid implying guaranteed money for a sensitive, high-stakes life event. **Deliberately did not touch the live backend** (`backend/engine.py`): confirmed via case-insensitive grep for bereave/witwe/hinterbliebe/waise — zero matches, so (same reasoning as Pflegegrad in iteration 20) there is no backend-side logic to wire this checkbox to yet; the local fallback engine, which most users hit, now fully works for this field. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed JSX/state wiring, copy accuracy/hedging appropriate for a bereavement question, no stale dependents on the old always-false behavior, backend genuinely has nothing to wire, no scope creep) before committing. **Remaining open, not touched this pass** (1 of the original 7 state variables): Vermögensfreibetrag (`hasHighAssets`, Step 4) still has zero rendered UI — **deliberately deferred, not just skipped for time**: unlike the other 6 fields, this one is NOT a clean single-purpose gate. `hasHighAssets` is checked as a blanket boolean across *six* different benefit modules in `benefit-engine.js` (Kinderzuschlag ~120, Wohngeld ~165, Lastenzuschuss ~201, Bürgergeld ~301, Sozialhilfe ~378) each with **legally different real asset-freibetrag thresholds** (SGB XII Grundsicherung: 10.000€ per person; Bürgergeld: 40.000€ for the first household member; Wohngeld/Lastenzuschuss: 60.000€) that the engine has already collapsed into one undifferentiated flag — plus a separate, currently-dead `input.assets` numeric field only consulted by the Grundsicherung-im-Alter module (`parseFloat(input.assets) > 10000 || input.hasHighAssets`, line ~339) that has no UI either and no wizard input feeds it. A single "Verfügen Sie über Vermögen oberhalb der Freibeträge?" checkbox would be actively misleading: a household with e.g. 15.000€ savings is fine for Bürgergeld (40k) and Wohngeld (60k) but excluded from Sozialhilfe/Grundsicherung (10k) — ticking one box can't represent that correctly, and a wrong answer here means telling someone they qualify for zero of five major benefits (or wrongly telling them they qualify for none when they'd actually clear some thresholds). Doing this properly needs either (a) a numeric "Vermögen/Ersparnisse" input plus per-module threshold comparisons replacing each module's blanket `input.hasHighAssets` check with `input.assets > <module-specific threshold>`, or (b) a deliberate product decision on how to phrase a single simplified question if per-module numeric precision is out of scope for the wizard's UX — either way this is calculation-logic work across 5 files, not a one-field UI-only fix like the other 6, so it needs a dedicated iteration (or human product/legal sign-off on the simplification), not a rushed pass. This closes out iteration 18's list of 7 dead fields to "6 resolved, 1 deliberately deferred with a documented reason," not "7 done."
- [x] **Pflegegrad wizard field: built the missing UI, second of the seven dead fields flagged in iteration 18 (2026-08-01, dev-loop iteration 20)**: resolved the highest-value remaining candidate recommended at the end of iteration 19 — `hasCareDependent`/`careDependentGrad`/`careOrganization` (declared under the "Step 2 States" comment in `smart-calculator.jsx`, default `false`/`"PG 2"`/`"private"`) were already read into `profileInput` (the local-fallback-engine payload, lines ~312-314) and already consumed by real, tested calculation logic in `src/logic/benefit-engine.js` (six Pflege modules: Pflegegeld, Pflegesachleistung, Pflege-Entlastungsbetrag, Landespflegegeld, Zuschuss barrierefreier Umbau, Pflegeunterstützungsgeld — lines ~696-824) and `src/logic/calculator-2026.js` (a Wohngeld GdB/Pflegegrad-adjacent allowance, lines ~273-292) — but had zero rendered UI anywhere in the component, exactly like `hasDisability` before iteration 19. Added a "Pflegegrad amtlich festgestellt?" checkbox to Step 2 (Haushalt & Wohnen — matches where the state is declared), with two conditional selects when checked: Pflegegrad (PG 1–5, string values verified to exactly match what both engine files check via `.includes()`/array-membership) and "Wer übernimmt die Pflege überwiegend?" (private/Angehörige vs. service/Pflegedienst, matching the `careOrganization` fork between the Pflegegeld and Pflegesachleistung modules). Followed the same card/checkbox/`InfoTooltip`/conditional-reveal pattern as the sibling disability block. **Deliberately did not wire the live-backend payload this time** (unlike the disability field's `is_disabled`): `backend/models.py` has an `is_cared_for` field on `Person`, but `backend/engine.py` never reads it anywhere (confirmed via case-insensitive grep for "care"/"pflege" — zero matches) — wiring it would be the same kind of inert, cosmetic-only fix iteration 18 correctly discarded for disability before the UI existed; here the backend-side logic itself doesn't exist yet, so there's nothing to wire it *to*. Worth a future backend pass if the live API should also apply Pflege-related surcharges/allowances, tracked as a new implicit gap (not a regression — the local fallback engine, which is what most users hit, now fully works). Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed state wiring, exact string-value matches against both engine files' expectations, design-system consistency, step-2 placement correctness matching where state is declared, and copy/legal accuracy of the Pflegegrad 1 "no cash benefit" case) before committing.
- [x] **Disability (GdB) wizard field: built the missing UI, one field of the seven flagged in iteration 18 (2026-08-01, dev-loop iteration 19)**: resolved the highest-value candidate from iteration 18's finding (below) — `hasDisability`/`disabilityGdb` had real, already-tested calc logic on both the local fallback engine (`calculator-2026.js`: 17% Bürgergeld Mehrbedarf, Wohngeld GdB-threshold allowance) and the live backend (`backend/engine.py:90`, 35% SGB2 Mehrbedarf) but no rendered control. Added a "Grad der Behinderung (GdB) amtlich festgestellt?" checkbox to Step 4 (Einkommen & Vermögen), with a GdB `<select>` (20/30/.../100 in steps of 10 — matches the official §152 SGB IX scale — default "50 (Schwerbehinderung)") shown when checked, following the existing checkbox+conditional-select pattern already used for "Alleinerziehend" (Step 3) and the pregnancy-status select (also Step 3): same `labelClass`/`inputClass`/`inputDarkClass` via `cn()`, same `InfoTooltip`, same `animate-in slide-in-from-top-2` reveal. Also wired `is_disabled: hasDisability` into the live-backend payload's `members[0]` object (previously never sent — `backend/engine.py:90`/`backend/models.py:34` already read/accept it; this is the one-line fix iteration 18 correctly discarded as inert until the UI existed, now legitimate). Confirmed the local Wohngeld path (`benefit-engine.js:168-181`/`218-231`) already forwards `disabilityGdb` correctly to `calculateExactWohngeld` — the review agent flagged that the separate, unused `calculateBestOption` export in `calculator-2026.js` does *not* forward `disabilityGdb`, but `grep -rn "calculateBestOption" src/` confirms it's dead code (zero callers), so this doesn't affect the live feature; left alone as out-of-scope repo hygiene, not a live bug. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed JSX structure/pattern match, state wiring, backend field match, and GdB copy/scale accuracy; no scope creep) before committing. **Remaining open, not touched this pass** (6 of the original 7 fields, see iteration 18 below for full detail): Pflegegrad/care-dependent (`hasCareDependent`/`careDependentGrad`/`careOrganization`, Step 2), Vermögensfreibetrag (`hasHighAssets`, Step 4), Bestattungsvorsorge (`isBereaved`, Step 4) still have zero rendered UI — each needs the same one-field-at-a-time treatment (build UI, verify calc wiring, careful legal copy) in a future iteration; recommend Pflegegrad next since it also gates the Wohngeld GdB-adjacent allowance path (`calculateExactWohngeld`'s `isCareDependent`/`careDependentGrad` branches) and has legal specificity (Pflegegrad 1-5) similar to what was just handled for disability.
- [x] **Dead wizard state: 7 collected fields never settable by any UI control (found 2026-08-01, dev-loop iteration 18)**: started this iteration to fix what looked like the same bug class as iteration 13 (`expects_child`/`owns_property` missing from the live-backend payload) — `src/components/smart-calculator.jsx` builds a `members[0].is_disabled` field for the live backend payload sourced from `hasDisability` state, and `backend/engine.py`'s `calculate_sgb2()` genuinely applies a 35% Mehrbedarf-Behinderung surcharge when `is_disabled` is true (verified: rbs_1 563€ → 760.05€ for a single disabled adult). Drafted the one-line payload fix (`is_disabled: hasDisability`), but an independent review subagent caught that the premise was wrong: `hasDisability` (and `disabilityGdb`, plus `hasCareDependent`/`careDependentGrad`/`careOrganization` from Step 2, and `hasHighAssets`/`isBereaved` from Step 4) are declared `useState` variables that get read into **both** the live-backend payload/`profileInput` (used by the local fallback engine, `src/logic/benefit-engine.js`/`calculator-2026.js`) **and** are consumed downstream by real calculation logic in both engines (e.g. `calculator-2026.js:279-281` uses `hasDisability`+`disabilityGdb` for a Wohngeld room-size/GdB rule; `benefit-engine.js:885-887` similarly) — but there is **no checkbox, select, radio, or any other rendered input anywhere in the 1298-line component** that ever calls `setHasDisability`, `setDisabilityGdb`, `setHasCareDependent`, `setCareDependentGrad`, `setCareOrganization`, `setHasHighAssets`, or `setIsBereaved`. Confirmed independently (not just trusting the reviewer) via `grep -n "setHasDisability\|disabilityGdb\|hasDisability"` and a scan of Step 2/Step 4 JSX render blocks — zero matches outside the `useState` declarations and the payload-building code. These 7 fields are permanently stuck at their defaults (`false`/`"50"`/`"PG 2"`/`"private"`) for every real user, on **both** the live and offline calculation paths — meaning the disability Mehrbedarf, Pflegegrad/care-dependent Wohngeld adjustment, Vermögensfreibetrag (assets), and Bestattungsvorsorge (bereaved) logic are all currently unreachable dead code from a user's perspective, regardless of backend correctness. **Discarded the inert one-line payload fix rather than committing it** — wiring a field that can never be non-default would be a cosmetic, misleading "fix" with zero live effect; nothing to build on until the UI exists. **Deliberately not fixed this pass**: building 7 legally-nuanced form fields (Schwerbehinderung/GdB, Pflegegrad, Vermögensfreibetrag threshold, Bestattungsvorsorge) that must (a) fit the existing "Law Firm Premium" design system, (b) get the legal framing/wording right per benefit type, and (c) be QA'd end-to-end per field, is feature-scale work — not a bounded bug fix suitable for a single unattended pass. Also a real risk if rushed: presenting a legally-loaded question (e.g. Grad der Behinderung) with imprecise wording could itself misinform users, so this needs either a deliberate product/design pass or at minimum a slower, one-field-at-a-time iteration with careful legal-copy review, not five-plus fields batched through in one autonomous cycle. Recommend a future iteration (or the human owner) pick ONE of these fields (disability is the highest-value candidate — it's the only one with a real, verified, non-trivial live-backend financial impact already coded and tested) and build just its UI end-to-end before attempting the rest. **Resolved for the disability field in iteration 19 above; Pflegegrad/Vermögensfreibetrag/Bestattungsvorsorge still open.**
- [x] **Jobcenter Search Integration**: Parsing `master_daten_2026_improved.csv.txt` (used `cities_2026.json`) and building the lookup UI.
- [x] **Wohngeldbehörde Search Integration**: Implemented via `AuthoritySearch.astro` (handles both Jobcenter & Wohngeld).
- [x] **Partner Interface / API**: Implemented schema & integration (PartnerCTA).
- [x] **Lead Nurturing Webhook**: Implemented secure API (`/api/leads`) to send user data to the automation workflow.
- [x] **AVGS Monetization**: Refined funnel with "Recht statt Almosen" messaging & integrated into Results.
- [x] **Test hardening: `backend/verify_engine.py` now actually asserts instead of just printing (2026-08-01, dev-loop iteration 17)**: resolved the follow-up flagged at the end of iteration 16 — the script's 3 household test cases previously only printed computed amounts/statuses with no pass/fail check, so a real regression (like the RBS1/RBS2 Regelsatz mixup iteration 16 had to find via a fresh manual scan) could silently print a wrong number while the script still declared "[SUCCESS] Verification Tests Completed Successfully!" and exited 0. Added a `check(label, actual, expected, tolerance)` helper (numeric comparison with a small tolerance, exact match for strings/status fields) called after each test case against the current, verified-correct engine output — 12 assertions total covering SGB II amount/status, Kindergeld, Wohngeld, Kinderzuschlag, and Elterngeld across the 3 cases. Failures accumulate in a `FAILURES` list (so one bad case doesn't hide the rest) and the script now `sys.exit(1)`s with a `[FAILURE]` summary if any check fails, `[SUCCESS]`/exit 0 otherwise. Sanity-checked the regression guard actually works by re-introducing the exact iteration-16 bug pattern (RBS keyed off member count instead of partner presence) in a throwaway copy of `engine.py` and confirming the new Test 1 SGB2-amount check correctly fails (1845.68 vs expected 1902.68) and the script exits 1 — then discarded the throwaway file. Still no CI wired to run this script automatically (noted again as a possible future item, not addressed here — out of scope, this pass only fixes the script's own signal quality for whoever/whatever does run it). Verified via `npm run build` (clean; change is Python-only, no frontend impact) + independent subagent review (PASS — confirmed assertions correct, expected values match a live run, no scope creep beyond this one file, exit-code behavior verified) before committing.
- [x] **Data-correctness fix: single-parent households got the wrong SGB II Regelsatz (2026-08-01, dev-loop iteration 16)**: found via a fresh targeted scan of `backend/**` (brain.md had no other actionable open item besides the two deliberately-parked ones — iteration 15 explicitly flagged that the next iteration should scan for new bugs). `backend/engine.py`'s `calculate_sgb2()` determined the "main" household member's base Regelsatz via `rate = rbs_1 if len(request.members) == 1 else rbs_2` — keying off raw member count instead of partner presence. Per SGB II Regelbedarfsstufen, RBS 1 (563€) applies to a single adult **or a single parent with only minor children** (no partner); RBS 2 (506€, the couple rate) applies only when a partner is present. Any single-parent household (main + child(ren), no partner — `len(members) > 1` but no partner role) was silently downgraded to the couple rate, a 57€/month underpayment on one of the calculator's most common real-world cases. The exact same buggy pattern existed a second time, independently, in the Sperrzeit-sanction calculation a few lines below (`main_regelsatz = rbs_1 if len(request.members) == 1 else rbs_2`), understating the 30% self-termination sanction base the same way (151.80€ instead of the correct 168.90€ for a single parent). Fixed both sites by introducing `has_partner = any(m.role == "partner" for m in request.members)` (mirroring the pattern already used correctly elsewhere in the same file for Kinderzuschlag, e.g. `has_couple` at the existing line ~220) and using it instead of the member-count check; solo-adult and couple households are unaffected (verified numerically: single-parent trace went from 1065.68€ → 1122.68€ total Bürgergeld, exactly the 57€ RBS delta; couple-household sanction unchanged at 151.80€). This is reachable from production — `src/components/smart-calculator.jsx` calls the live Render backend (`/api/v4/analyze`) that runs this code. Found and fixed via two sequential independent subagent passes (initial targeted scan found the first instance; a first review pass caught that the identical bug pattern was still present unfixed at the sanction-calculation call site a few lines down; a second, final review pass confirmed both fixes correct with live numeric runs and no regressions) before committing — the implementer did not self-approve at any point. No `npm run build` impact (Python-only backend file, no frontend import). **Follow-up noted, not fixed this pass**: `backend/verify_engine.py`'s existing single-parent test case only prints its result rather than asserting against an expected value, which is why this bug went uncaught — worth adding real assertions in a future pass, low priority since there's no CI wired to run this script anyway.
- [x] **Content Hygiene Fix (2026-07-29)**: Found & fixed a leaked `// TODO: Faktencheck...` comment that was embedded inside a live, user-facing FAQ answer string on `/kindergeld/` (rendered on-page AND in FAQPage JSON-LD schema). Trimmed to the fact-checked, deadline/portal-name-free part of the sentence. Also removed a stale `// TODO: Verify logo path` comment in `src/components/seo/schema.astro` after confirming `public/logo.png` exists and the schema URL is correct. Verified via `npm run build` + independent subagent review.

## 2. 🗺️ Strategic Roadmap
### Phase 1: Core Foundation (Current)
- [x] **Smart Calculator**: Logic for Wohngeld & Bürgergeld 2026 implemented.
- [x] **Premium UI**: "Pro-Card" design system, Apple-inspired aesthetics.
- [x] **Local SEO Architecture**: `[citySlug]` structure ready.
- [x] **Search Data**: Robust Jobcenter & Wohngeldbehörde database integration (via `AuthoritySearch`).

### Phase 2: Authority & Trust (GEO Focused)
- [x] **GEO-First Content**: Rewrite `/lexikon` intros as direct Q&A for AI.
- [x] **Schema.org Optimization**: [CRITICAL] Implement `GovernmentService` and `FAQPage` schema.
- [x] **Social Proof (Trust Badges)**: `<TrustBadges />` (factual claims: kostenlos, DSGVO, Made in Germany, SSL) is now rendered on `index.astro` between the mission section and FAQ.
- [ ] **Social Proof (Testimonials)**: `src/components/testimonials.astro` exists but is intentionally **not** wired in — it contains fabricated named customer quotes (Thomas W., Sabine K., Michael R.) with no real source. Owner decision (2026-07-29): do not publish as-is; needs either real customer testimonials or anonymized/generic placeholders before going live (risk: irreführende Werbung / UWG).
- [x] **Repo hygiene follow-up (2026-07-31)**: removed the remaining stray root-level dev-scratch scripts flagged on 2026-07-29 — `test_bat.bat`, `test_lead.js`, `verify_all_cities.js`, `verify_fix.js` — plus two more of the identical category found during this pass, `test_stripe.js` and `test_fictitious_person.js` (manual localhost-only smoke scripts, unreferenced by app code/build/docs). Added `*.bat`, `/test_*.js`, `/verify_*.js` to `.gitignore` to prevent recurrence (root-anchored, doesn't touch `src/`). Verified via `git grep` (no references anywhere outside brain.md's own note) + `npm run build` + independent subagent review. Not in scope for this pass (deliberately left alone, different risk profile): `backend/` (Python service + `__pycache__`), root `.cjs`/PDF-extraction utility scripts, and generated `*.pdf`/`*.json` sample files — these look like they could still be in active use and warrant a dedicated look rather than a drive-by deletion.
- [x] **Repo hygiene follow-up 2 (2026-07-31, dev-loop iteration 2)**: did the deferred "dedicated look" at the root-level `.cjs`/PDF-extraction scripts and generated sample files. Confirmed dead (zero references in `package.json`, `src/`, `public/`, `vercel.json`, `backend/`, or any config — checked via repo-wide `git grep`) and removed: `add_fields.js`, `add_missing_cities.cjs`, `audit_cities.cjs`, `download_real_pdfs.js`, `extract_missing_cities.cjs`, `generate_plz_lookup.cjs`, `generate_test_pdf.js`, `get-pdf-fields.ts`, `get_real_fields.js`, `read_pdf_fields.cjs` (one-off PDF-field/city-data utilities, several hardcoding the original developer's personal Windows path `c:/Users/Jan-r/OneDrive/...`; `generate_plz_lookup.cjs`'s intended output `src/data/plz-lookup.js` was never even committed, confirming it was never adopted). Also removed the empty (0-byte) duplicate root `engine.py` — **not** the real `backend/engine.py` (15KB, live Render service), which was left untouched and verified present after the change. Also removed `full_status.json` / `temp_nlm_status.json` (stale status blobs from an unrelated external notebook/AI tool) and the generated sample PDFs `Erika_Mustermann_Bürgergeld_Antrag.pdf`, `Erika_Mustermann_Wohngeld_Antrag.pdf`, `Filled_TEST_Buergergeld.pdf` (test output, not the live `public/forms/*.pdf` templates, which were confirmed untouched and still referenced by `authority-app.jsx`/`trust-pdf-generator.jsx`). Added `/*_status.json` to `.gitignore` (root-anchored). Verified via `npm run build` (clean, dist output complete) + independent subagent review (PASS, no issues) before committing. Backend and `public/forms/` explicitly out of scope and confirmed untouched.
- [x] **Repo hygiene follow-up 3 (2026-07-31, dev-loop iteration 5)**: deleted the two dead calculator files flagged as a follow-up in iteration 4 — `src/logic/client-calculator.js` (585-line unused alternate calculator/UI-state module, same "log request URL before fetch" pattern noted in the iteration-4 privacy fix, but never imported anywhere) and `src/logic/calculator.ts` (0-byte empty file). Re-verified independently via repo-wide `git grep` (not just `src/`) before deleting: zero imports/references in `src/`, `backend/`, `package.json`, or any config — the only other place either filename appeared was `PROJECT_CONTEXT_FOR_NOTEBOOKLM.md`, which described `client-calculator.js` as "UI Input" in the architecture docs; updated that doc's file-tree listing and workflow section to drop the reference, and removed the equivalent stale "UI Input" line from this file's Tech Stack section. `src/logic/` now contains only the two live files: `benefit-engine.js` and `calculator-2026.js`. Verified via `npm run build` (clean) + independent subagent review (PASS, no issues, confirmed dead via its own repo-wide grep) before committing. (`npm install` in this session's fresh container regenerated `package-lock.json` with cosmetic `"peer": true` diffs from a newer local npm version — unrelated to this change, reverted rather than committed.)
- [x] **Repo hygiene follow-up 4 (2026-07-31, dev-loop iteration 6)**: removed 17 orphaned dead files under `src/` found via a fresh sweep (`brain.md` had no other open, actionable `- [ ]` items left besides the two deliberately-parked ones below). Confirmed zero references via repo-wide `git grep` for each: `capsule-nav.astro` (unused "kelvin-*" themed nav from an earlier design iteration, superseded by the current brand-navy/gold system — not the same component as the live design), `application-pdf-template.jsx` (superseded by live `trust-pdf-generator.jsx`), `benefit-recommendations.astro`, `bestandskunden-section.astro`, `city-map.jsx` (superseded by `authority-search.astro`/`authority-app.jsx`), `city-schemas.astro` (distinct from the live `seo/schema.astro` & `seo/seo-schema.astro`), `lead-magnet.astro` (distinct from the live `lead-modal.astro`), `notice-check-quiz.tsx`, `risk-teaser.astro`, `seo/local-business-schema.astro`, `step-calculator.jsx` (superseded by the live `smart-calculator.jsx`), `step-card.astro`, `welcome.astro`+`welcome.jsx` (unmodified Astro-starter-template pair, never wired into any page — the two files only reference each other), `layouts/dashboard-layout.astro` (the live `admin/dashboard.astro` actually uses `layouts/layout.astro`), and `assets/astro.svg`+`background.svg` (only ever used by the deleted `welcome.astro`). Verified via `npm run build` (clean, no missing-module errors) + independent subagent review (PASS) before committing. Not touched (deliberately out of scope, generic boilerplate not specific to this project): the root `README.md` still describes the default Astro-starter folder structure (`Welcome.astro`, `astro.svg`) rather than the real project layout — cosmetic doc staleness, not a functional issue, worth a dedicated pass if the team wants a project-specific README. (`npm install` in this session's fresh container again regenerated `package-lock.json` with cosmetic npm-version diffs, same as iteration 5 — reverted rather than committed.)
- [x] **Security fix: unauthenticated HTML-injection / open mail-relay in `/api/send-application` (2026-07-31, dev-loop iteration 8)**: this is the public, unauthenticated, free "formloser Antrag" endpoint (`src/pages/api/send-application.ts`) — no payment/auth barrier at all. It accepted fully client-controlled JSON (`email`, `firstName`, `lastName`, `street`, `zipCity`, `benefitLabel`, `authority`, `authorityEmail`) and (a) validated the recipient address only via `.includes('@')`, and (b) interpolated every one of those fields directly into three real HTML emails (to the authority, to the applicant, and to the admin audit inbox) sent via Resend from the site's verified domain, with zero escaping anywhere in the call chain. An attacker could set `authorityEmail`/`email` to any third-party address and inject arbitrary HTML/links (e.g. a fake "Bescheid bestätigen" phishing link) into a legitimate-looking, real email sent from `info@sozialer-navigator.de` — turning the endpoint into an open relay riding on the site's mail reputation, at no cost and with no rate limit. Fixed: added `src/lib/html-escape.ts` (`escapeHtml`) and wrapped every interpolated field in all three HTML email bodies; replaced the `.includes('@')` check with a real `EMAIL_REGEX` (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`) applied to both `email` and `authorityEmail`, which also blocks whitespace/CRLF in those two fields; added a `stripControlChars` pass on all free-text fields (`firstName`, `lastName`, `street`, `zipCity`, `benefitLabel`, `authority`, plus the two email fields) right after parsing the request body, closing a residual gap the independent reviewer flagged — `firstName`/`lastName`/`benefitLabel` are interpolated raw into plain-text email *subject* lines (correctly left un-HTML-escaped there, since HTML-escaping a subject would show literal `&amp;` to a human), so without this they could still carry a `\r\n` into the subject with unverified consequences depending on Resend's internal header handling. Left the PDF generator untouched (it has its own separate `sanitizeForPdf`, a different context — WinAnsi font encoding, not HTML/header injection). Verified via `npm run build` (clean) + independent subagent review (PASS on the HTML-injection fix; flagged the subject-line control-character gap, which was then fixed in a follow-up edit within the same iteration) before committing. Found via a targeted scan of `src/pages/api/**` email-sending handlers, prompted by there being no other small, actionable open `- [ ]` item in this file besides the two deliberately-parked ones below. **Backup candidates noted, not fixed this pass** (lower severity, out of scope): `src/pages/api/leads.ts` and `src/pages/api/wba-reminder.ts` insert client JSON into Supabase with only presence checks on a few fields, no email-format validation, no rate limiting (DB-spam risk, not outbound-email/HTML-injection risk); `src/pages/api/chat.ts` forwards user-supplied calculator context into an LLM prompt with no range validation, but doesn't cross a trust boundary in a harmful way (the user's own data going to the user's own request).
- [x] **Validation fix: unvalidated public leads/PDF-relay endpoints (2026-08-01, dev-loop iteration 10)**: fixed the two "backup candidate" endpoints flagged (but deliberately left) in iterations 8-9. Ran as two parallel worktree-isolated agents (no file overlap), each independently reviewed by a fresh subagent before merge — the review caught a real regression, described below. (1) `src/pages/api/leads.ts`: the public, unauthenticated Supabase-insert endpoint accepted any JSON with zero validation (no email-format check, no required-field check) — explicitly flagged twice in brain.md as "worth a validation pass." Added the same `EMAIL_REGEX`/`stripControlChars` pattern from `send-application.ts`: required-field check on `first_name`/`email`/`interest`/`modal_type`, email-format validation, control-char stripping on all string fields before they reach Supabase or the Make.com webhook forward. **Regression caught by independent review and fixed before merge**: the initial implementation also required `last_name`, but `src/components/lead-modal.astro`'s single free-text "Ihr Name" field splits on whitespace and produces `lastName = ''` for any single-word name — that would have silently 400'd every mononym/nickname submission across all `openLeadModal(...)` flows (PDF checklist, AVGS modal, Kinderzuschlag, Elterngeld, etc.), surfaced to the user as an indistinguishable generic "Fehler beim Senden" alert. Fixed by leaving `last_name` optional (mirrors how `phone`/`city` are already handled), matching its actual pre-existing behavior. (2) `src/pages/api/send-pdf.ts`: a public, unauthenticated endpoint that sends a real branded email with a PDF attachment via Resend to `formData.get('email')`, previously validated only via truthiness — any string would trigger a real send, making it a free, unrate-limited relay ridable for spam/harassment against arbitrary third parties using the site's domain reputation. Added the same `EMAIL_REGEX` check plus CRLF-stripping and trimming. Note: this endpoint has no live frontend caller found (the site's "PDF-Checklist" CTAs all route through `/api/leads` instead, per the pattern above) — only referenced by `debug_infrastructure.js` — so it may be dead/orphaned; fixed defensively rather than removed since it's still a reachable public route, but worth a dedicated look in a future repo-hygiene pass to confirm dead and decide removal vs. keep. Both verified via `npm run build` (clean) + the independent subagent review described above before committing/merging. Not touched (out of scope): rate limiting on either endpoint — still a residual DB-spam/abuse-relay risk lower than the fixed issues, worth a future pass if abuse is observed.
- [x] **Security fix: HTML-injection in WBA-reminder emails (2026-08-01, dev-loop iteration 9)**: the line above (iteration 8) assessed `wba-reminder.ts` as "DB-spam risk, not outbound-email/HTML-injection risk" — that assessment was incomplete: it missed that the public, unauthenticated `wba-reminder.ts` endpoint (only presence-checked `name`/`email`/`month`/`year`, no email-format validation) feeds a Supabase table (`wba_reminders`) that a separate cron job, `src/pages/api/cron/send-wba-reminders.ts`, later reads to send two real HTML reminder emails per lead via Resend from the site's verified domain (`service@sozialer-navigator.de`) — and that cron job interpolated `lead.name` directly into both HTML bodies with **zero escaping**. Same bug class as the `send-application.ts` open-relay/HTML-injection fix from iteration 8: an attacker could POST an arbitrary `name` (e.g. containing an `<a href="...">` phishing link) and any target `email`, wait for the scheduled reminder date, and have that HTML blasted out in a real email from the company's domain to an arbitrary recipient. Fixed: `wba-reminder.ts` now validates `email` against the same `EMAIL_REGEX` used in `send-application.ts`, strips control characters (`\r\n`) from `name`/`email` via `stripControlChars`, and validates `month`/`year` are sane integers (1-12 / 2000-2100) before constructing the `Date` (previously a non-numeric month/year would silently produce an `Invalid Date` and later throw inside `toISOString()`); `send-wba-reminders.ts` now imports and applies `escapeHtml()` (from `src/lib/html-escape.ts`, added in iteration 8) to `lead.name` at both interpolation sites (reminder 1 and reminder 2 templates). `lead.email` is only used in the `to:` array (not interpolated into HTML), so no other field needed escaping. Verified via `npm run build` (clean) + independent subagent review (PASS, confirmed both templates fixed, no other unescaped fields, no regressions to month/year date-math or the existing required-field validation) before committing. **Backup candidate noted, still not fixed** (lower severity, distinct risk, out of scope for this pass): `src/pages/api/leads.ts` still has no email-format validation and no rate limiting on its public Supabase insert endpoint — but unlike `wba-reminder.ts`, nothing currently reads the `leads` table back into an HTML email template, so its residual risk is DB-spam/data-quality, not HTML-injection. Worth a validation pass in a future iteration regardless.
- [x] **Data-correctness fix: live-backend calculator silently dropped `expects_child`/`owns_property` (2026-08-01, dev-loop iteration 13)**: found via a targeted scan of `src/components/**` and `backend/**` for logic not covered by iterations 1-12 (which focused on `src/pages/api/**` email-injection bugs). `src/components/smart-calculator.jsx`'s `handleSubmit` builds a `payload` POSTed to the live Python backend (`/api/v4/analyze`) but never included two fields the frontend already computes: (1) `isPregnantOrNewborn` (the wizard checkbox, tooltip explicitly "für Elterngeld") was never sent as `expects_child` — `backend/engine.py`'s `calculate_elterngeld()` gates eligibility on `has_baby OR expects_child`, so any user who checked "Schwangerschaft" always got told they're ineligible for Elterngeld from the live API (no error thrown, so the client's offline fallback engine never kicked in to correct it — silently wrong, not a crash). (2) `isOwner` (`housingType === "Eigentum"`, already computed locally to build `rentCold`/`rentUtility`) was never sent as `owns_property` — `backend/engine.py`'s `calculate_sgb2()` uses this flag to apply a different, owner-specific accommodation-cost formula instead of the default renter formula, so property-owning users always got the renter calculation regardless of what they selected in the wizard. Fixed by adding both keys to the `payload` object. **Deliberately left out of scope**: the backend also has a separate per-member `is_pregnant` field (a different SGB2 "Mehrbedarf Schwangerschaft" surcharge) — the frontend's single checkbox conflates "still pregnant" and "already has a newborn under 14 months", and the surcharge should legally only apply while still pregnant, so wiring it up risks over-crediting need for households with an already-born child; needs a UI split into two distinct states first, tracked as a new open item below rather than guessed at. Ran as two parallel worktree-isolated agents (no file overlap) alongside the pdf_generator.py removal below, each independently reviewed by a fresh subagent (both CONFIRMED) before merge. Verified via `npm run build` (clean) + both independent reviews before committing.
- [x] **Repo hygiene: removed dead `backend/pdf_generator.py` (2026-08-01, dev-loop iteration 13)**: a `class PDFService` using `reportlab`, completely orphaned — zero references anywhere in `backend/` (confirmed via repo-wide grep by both the implementer and independent reviewer), and `reportlab` isn't even in `backend/requirements.txt` (only flask/flask-cors/gunicorn/pydantic/stripe), so it would have raised `ModuleNotFoundError` if ever invoked. The live PDF path is the unrelated TS module `src/lib/pdf-generator.ts`. Also removed the now-orphaned compiled bytecode cache `backend/__pycache__/pdf_generator.cpython-312.pyc` (was tracked in git) and added `__pycache__/`/`*.pyc` to `.gitignore` to stop Python bytecode artifacts from being committed going forward. Verified via `npm run build` (clean) + independent subagent review (CONFIRMED) before committing. `npm install`-generated cosmetic `package-lock.json` `"peer": true` diffs (same npm-version artifact as every prior iteration) were reverted, not committed.
- [x] **Repo hygiene follow-up 6 (2026-08-01, dev-loop iteration 15)**: resolved the "noted but not fixed" item from iteration 13. Deleted `backend/Antrag_V4.pdf` and `backend/Antrag_Vorbereitung.pdf` — leftover reportlab-generated sample output from when the now-removed `pdf_generator.py` was still manually invoked, first added in the original `049a07a` backend commit and untouched since. Confirmed dead via repo-wide grep (both filenames appeared nowhere except this file's own prior note) by both the implementer and an independent subagent reviewer, which also spot-checked that the actually-live PDF templates (`public/forms/Hauptantrag_Buergergeld.pdf`, `public/forms/TEST_Buergergeld.pdf`, `public/forms/TEST_Wohngeld.pdf`, `public/downloads/wohngeld-notfallplan.pdf`) are unrelated and untouched. Verified via `npm run build` (clean) + independent subagent review (CONFIRMED safe) before committing. `npm install`-generated cosmetic `package-lock.json` diff (same npm-version artifact as every prior iteration) was reverted, not committed. No other small, actionable open item remains from prior "noted but not fixed" flags besides the two deliberately-parked ones (fabricated testimonials, needs owner decision; optional "User Accounts" feature, out of scope for a hygiene/bugfix pass) — next iteration should look for new bugs/TODOs via a fresh targeted scan.
- [x] **Elterngeld "Mehrbedarf Schwangerschaft" (SGB2 surcharge) wired to the frontend (2026-08-01, dev-loop iteration 14)**: resolved the item flagged in iteration 13. `src/components/smart-calculator.jsx`'s single boolean checkbox ("Schwangerschaft oder Kind unter 14 Monate alt?") conflated two distinct legal states — still pregnant vs. already has a newborn — and the SGB2 `is_pregnant` Mehrbedarf surcharge should legally only apply while still pregnant. Replaced the checkbox with a three-way `<select>` (`pregnancyStatus`: "none" | "pregnant" | "newborn"). `isPregnantOrNewborn` is now derived (`pregnancyStatus !== "none"`) and keeps its existing behavior unchanged for both "pregnant" and "newborn" — `expects_child` in the backend payload and the local-fallback engine's Elterngeld-relevance flag (`src/logic/benefit-engine.js`) still fire for either state, so Elterngeld eligibility gating is untouched. The new `is_pregnant: pregnancyStatus === "pregnant"` is sent on the "main" household member only, so the SGB2 surcharge now applies exclusively to the still-pregnant case, closing the over-crediting risk noted in iteration 13. **Known limitation, not fixed this pass**: `is_pregnant` is only ever set on the "main" applicant member — if the pregnant person in the household is the partner rather than the main applicant, the surcharge still won't apply; the wizard has no UI concept of "who" is pregnant beyond the single household-level flag, so fixing this would need a separate product decision (e.g. a person-picker) and is out of scope here. Verified via `npm run build` (clean) + independent subagent review (PASS — confirmed correctness of both derived flags, no regressions via repo-wide grep, no leftover references to the old checkbox/setter, JSX matches existing `<select>` patterns in the file) before committing.
- [x] **Security fix: HTML-injection in Stripe webhook emails (2026-08-01, dev-loop iteration 12)**: found via a targeted scan of `src/pages/api/**` for the same bug class already fixed in iterations 8/9 elsewhere (unescaped user-controlled fields interpolated into outbound HTML emails). `src/pages/api/webhooks/stripe.ts`'s `handleCheckoutCompleted()` sends 3 real HTML emails (customer confirmation, authority application, admin audit) built from `session.metadata` — set verbatim from client-controlled JSON in `checkout.ts` with zero server-side validation (`firstName`, `lastName`, `authority`, `authorityEmail`, `street`, `zipCity`, `benefitLabel`). None of these were HTML-escaped: an attacker completing a real (or cent-level) Stripe checkout with e.g. `firstName: <a href="https://evil.example">Bescheid bestätigen</a>` could get that HTML rendered in legitimate emails sent from the site's verified domain — to the paying customer, to an attacker-chosen "authority" address (also attacker-controlled), and to the internal admin inbox. Iteration 7 had already fixed this file's separate signature-verification bypass but missed this HTML-injection issue. Fixed by wrapping every interpolation site across all three templates (plus the error-path `sendAdminAlert()` email, which `JSON.stringify()`'d the raw session/metadata into HTML unescaped — flagged by the independent reviewer as the same bug class, outside the initial diff, fixed in the same pass) in the existing `escapeHtml()` helper from `src/lib/html-escape.ts`. Also added `stripControlChars` at the point metadata is extracted (closing a CR/LF header-injection gap in the two email subject lines that use `firstName`/`lastName`/`benefitLabel`, same residual-gap pattern as iteration 8's `send-application.ts` fix) and tightened the authority-email send gate from a bare `.includes('@')` check to the same `EMAIL_REGEX` used in the sibling files. Verified via `npm run build` (clean) + independent subagent review (PASS; confirmed all interpolation sites covered, subject-line CR/LF protection intact, `EMAIL_REGEX` tightening doesn't break the `TEST_AUTHORITY_EMAIL`/local-dev-redirect override paths, control-char stripping harmless for the non-HTML uses of the same fields) before committing. `npm install` in this session again regenerated cosmetic `package-lock.json` `"peer": true` diffs (same npm-version artifact as prior iterations) — reverted, not committed.
- [x] **Repo hygiene follow-up 5 + docs (2026-08-01, dev-loop iteration 11)**: resolved two independent open items, run as parallel worktree-isolated agents (no file overlap), each independently reviewed by a fresh subagent before merge. (1) `src/pages/api/send-pdf.ts`: the "dedicated look to confirm dead and decide removal vs. keep" flagged in iteration 10. The first worktree agent's own check produced a false "still referenced" alarm — its worktree had branched from a commit *before* `558ff49` (the earlier hygiene commit that removed `debug_infrastructure.js`, `send-pdf.ts`'s only historical caller), so its stale checkout still had that file tracked. Re-verified directly on the actual branch tip in the orchestrating session: repo-wide `git grep` for `send-pdf`/`sendpdf`/`send_pdf`/`debug_infrastructure` found zero live references anywhere (confirmed again independently by the review subagent). Deleted the file; `src/lib/html-escape.ts` and `src/lib/email.ts` (shared helpers it used) were confirmed still present and still used by `send-application.ts`/`webhooks/stripe.ts`/`cron/send-wba-reminders.ts`, so nothing else broke. This removes a previously-hardened but ultimately unreachable public mail-relay attack surface entirely, rather than just defending it. (2) `README.md`: replaced the unmodified default Astro-starter-kit boilerplate (talked about `Welcome.astro`, generic `astro.svg`) with a project-accurate README — description, real tech stack (Astro 5/Vercel, React islands, Tailwind v4, Supabase, Stripe, Resend, pdf-lib, Python `backend/`), real project structure (spot-checked against the actual tree), commands matching `package.json` scripts, and a pointer to `brain.md` for deeper context. Review caught one stale leftover (`src/assets/` listed but doesn't exist — from copying structure notes before the `assets/` folder was removed in an earlier hygiene pass) and it was fixed before commit. Both changes verified via `npm run build` (clean) + independent subagent review (PASS on both, one minor README fix applied) before committing. `npm install`-generated cosmetic `package-lock.json` diffs (same npm-version `"peer": true` pattern as prior iterations) were reverted, not committed.
- [x] **Security fix: Stripe webhook signature bypass (2026-07-31, dev-loop iteration 7)**: `src/pages/api/webhooks/stripe.ts` decided whether to skip Stripe signature verification via `request.url.includes('localhost')` — a substring match against the *entire* incoming URL, including the query string. In production, a request like `POST /api/webhooks/stripe?x=localhost` would satisfy that check, causing the handler to `JSON.parse` the raw body directly as a trusted `Stripe.Event` instead of verifying it via `stripe.webhooks.constructEvent`. Since `handleCheckoutCompleted` trusts `session.metadata` (firstName/lastName/authority/authorityEmail/street/zipCity) verbatim, an unauthenticated attacker could forge a `checkout.session.completed` event and trigger real side effects: PDF generation plus an email sent to a real government authority (with an attacker-chosen address in CC), all without ever paying. Fixed to check the actual parsed hostname (`new URL(request.url).hostname`), which is immune to query-string manipulation, and to accept both `localhost` and `127.0.0.1` for local-dev robustness (the latter is what some `stripe listen --forward-to` setups use by default). Verified via `npm run build` (clean) + independent subagent review (confirmed exploit was real and the fix closes it; suggested the `127.0.0.1` addition, which was incorporated) before committing. Found via a targeted security-focused scan of `src/pages/api/**` webhook/payment handlers (brain.md had no other open, actionable `- [ ]` items besides the two deliberately-parked ones below). Not touched (out of scope): the separate `!process.env.VERCEL_ENV` dev/prod check further down the same file (used to redirect the authority email to admin) — looked plausible for this project's actual Vercel deployment (confirmed via `npm run build` using `@astrojs/vercel`), no evidence of a bug there.
- [x] **Bugfix pass 3 (2026-07-31, dev-loop iteration 3)**: fixed two small independent bugs found via a targeted scan (broken internal links + stray TODOs). (1) `src/pages/404.astro`: the "Zum Rechner" button linked to `/#wohngeldrechner`, an anchor id that doesn't exist anywhere in the codebase — it now scrolled to nothing. Fixed to `/#calculator`, the actual id used on the homepage's calculator section. (2) `brain.md` Quick Links: removed a dead `- [ ]` link pointing to `../.gemini/antigravity/brain/.../geo_strategy.md`, a path that doesn't exist anywhere on disk — a stale local-session artifact from a different AI tool, not a repo file, so there was nothing to point it to instead. Left the `/kinderzuschlag/` TODO in `src/pages/kindergeld/index.astro:251` alone — it's a placeholder comment for a page that hasn't been built yet, not a bug; building that page would be a new feature, out of scope for a bugfix pass. Verified via `npm run build` (clean) + independent subagent review (PASS, no issues) before committing.
- [x] **Privacy fix: debug console.log leaking user PII (2026-07-31, dev-loop iteration 4)**: found via a targeted repo scan that `src/components/smart-calculator.jsx` (the homepage calculator, mounted `client:load`) and `src/components/results-section.astro` were logging live user data to the browser console in production. Removed: the fetch payload/response logs in `smart-calculator.jsx` (income, rent, household composition sent to the backend, plus the full API response), and three logs in `results-section.astro` (script init, `showView()` calls, and the `benefit-calculation-completed` event handler logging the full result object incl. calculated benefit amounts). Purely diagnostic — no variables were declared only for the log calls, no side effects removed, confirmed no runtime/behavior change. Verified via `npm run build` (clean) + independent subagent review (PASS, no issues) before committing. **Follow-up noted, not fixed this pass**: `src/logic/client-calculator.js` has the same "log request URL before fetch" pattern at line ~436, but a repo-wide `git grep` found zero imports/references to that file anywhere in `src/` — it appears to be dead code (an unused alternate calculator path), so the log isn't reaching production; left alone rather than editing dead code, but worth deleting the whole file in a future repo-hygiene pass alongside the also-confirmed-empty `src/logic/calculator.ts` (0 references, empty file).

### Phase 3: Monetization Ecosystem
- [x] **Partner API**: Secure lead transmission to legal/coaching partners.
- [ ] **User Accounts**: (Optional) Save calculations for later.

## 3. 🏗️ Tech Stack & Architecture
- **Framework**: [Astro 5](https://astro.build) (Hybrid Rendering)
  - *Why*: SEO performance + React Islands for interactivity.
- **Styling**: [Tailwind CSS v4](https://tailwindcss.com)
  - *Config*: `tailwind.config.mjs`
  - *Key Colors*: `brand-blue` (#0071e3), `brand-slate` (#1d1d1f)
- **State Management**: React State (Local) + URL Params (Shareability).
- **Backend Logic**:
  - Calculation: Client-side (`src/logic/calculator-2026.js`).
  - Data: CSV/JSON imports (`src/data/`).

## 4. 🧠 Key Knowledge & Rules
### Design Philosophy
- **"Law Firm Premium"**: Authoritative, trustworthy, and sophisticated.
- **Visuals**: Deep Navy (`#0a1628`) & Gold (`#c5a67c`). Marble & Glass textures.
- **AntiGravity System**: Centralized design tokens for consistency.
- **Narrative**: **"Recht statt Almosen"** (Right, not Charity). Frame benefits as an investment in the user's future.

### Data & Logic
- **Wohngeld 2026**: Strict adherence to the official WoGG formula.
- **Mietstufen**: 1-7 scale determining rent caps. Crucial for accuracy.
- **Files**:
  - Logic: `src/logic/calculator-2026.js`
  - Data: `src/data/wohngeldData.js` (Agencies & Rent Levels)
  - Content: `src/pages/*.astro`

## 5. 📂 Quick Links
- **[Context for AI](PROJECT_CONTEXT_FOR_NOTEBOOKLM.md)**: Deep dive into the project's purpose.
- **[Schema Strategy](SCHEMA_STRATEGY.md)**: How we talk to Google.
- **[Verification](VERIFICATION_ARTIFACT.md)**: Testing protocols.

---
*This file is the central nervous system of the project. Keep it updated.*
